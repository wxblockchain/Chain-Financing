# 资产平台（Asset Platform）

资产平台包含 **资产端**（面向资产参与方的 Web）与 **资产管理端**（平台运营使用的 Web）两个端。
本目录收纳资产平台的全部 PRD 与原型；设计系统 `docs/design-system/` 为两个平台共用，仍留在仓库顶层。

## 目录结构

```text
asset-platform/
├── prd/          # 资产平台的产品需求文档，按模块建子目录
└── prototypes/   # 资产平台的原型（HTML、Figma、交互稿等）——待原型侧迁移后落位
```

> 当前 `prototypes/` 仍在仓库顶层，将由原型侧单独迁入本目录（含 `_shared/`），迁移完成前请仍按顶层路径查找原型。

## PRD 模块清单与端归属

「端归属」不是推断出来的，取自每份 PRD 主文档「文档信息」表里的 **覆盖端** 字段，改动 PRD 时请一并核对本表。

| 模块 | 路径 | 对应需求 | 端归属 | 依据（文档内的覆盖端口径） |
| --- | --- | --- | --- | --- |
| 账户与登录（含全局国际化基线） | [`prd/v1.0-账户与登录/`](prd/v1.0-账户与登录/v1.0-账户与登录-PRD.md) | WS-302 | **两端都有** | 覆盖端：资产端 Web、资产管理端 Web |
| 实名认证与审核 | [`prd/v1.0-实名认证与审核/`](prd/v1.0-实名认证与审核/v1.0-实名认证与审核-PRD.md) | WS-303 | **两端都有** | 覆盖端：资产端 Web（认证填写与状态）、资产管理端 Web（认证审核） |
| 消息通知 | [`prd/v1.0-消息通知/`](prd/v1.0-消息通知/v1.0-消息通知-PRD.md) | WS-304 | **两端都有** | 覆盖端：资产端 Web、资产管理端 Web |
| 协议管理 | [`prd/v1.0-协议管理/`](prd/v1.0-协议管理/v1.0-协议管理-PRD.md) | WS-301 | **资产管理端**（资产端只读取口径，无界面） | 覆盖端：资产管理端 Web（功能主体）；资产端仅涉及「当前生效版本」的读取口径，不涉及界面 |

### 尚未入库的模块

| 模块 | 对应需求 | 现状 |
| --- | --- | --- |
| 应收账款录入与确权 | WS-305 | 原型已入库（顶层 `prototypes/应收账款录入与确权/`），**PRD 尚未入库**。该原型的 PRD 基线是 WS-305 issue 上的附件 `chain-financing-ar-prd.md`（v1.1），仓库里没有对应模块目录。 |

## 端的归属只做标注，不做目录拆分

本目录**不再按端拆二级目录**（不建 `资产端/`、`资产管理端/`）。现有模块基本都跨端写在同一份文档里——例如实名认证同一目录内既有 `01-个人认证.md`（资产端）又有 `04-管理端审核.md`（资产管理端）——按端物理拆分会变成改写文档内容而非移动文件。端的归属统一由上表标注。

## 维护规则

- 新增模块时，同一次提交里补上表的一行，并在 [`prd/README.md`](prd/README.md) 的文档索引里登记路径与入口文件。
- 端归属以 PRD 主文档的「覆盖端」字段为准；PRD 改了覆盖端，本表同步改。
- 金融服务平台（金融服务端 / 运营端）的产物不放本目录，见 [`financial-service-platform/`](../financial-service-platform/README.md)。
