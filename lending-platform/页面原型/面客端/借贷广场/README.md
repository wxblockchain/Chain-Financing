# 借贷广场 · 融资项目、代币质押、授信报价与放款确认

> 2026-09-21 最新返回约定：项目详情和创建页移除页首重复的返回借贷广场入口；保留取消、异常恢复以及跨模块来源返回。

## 2026-09-21 · 只读详情与居中办理弹窗

按用户评论 `f5abc73f-ab4c-46dd-b2c3-19ebd2a41b9d`，在 `13086d8` 上继续增量调整。融资申请、放款、还款记录进入只读抽屉，不再提供办理或跨业务跳转按钮；预计计划、授信资料、历史和还款记录就地展开。协议及凭证仍从文件名称读取下载，关闭用于退出查看。

办理入口统一留在项目右侧操作区，发布、质押、授信报价、接受拒绝、放款登记/到账确认、还款/确认采用居中弹窗。多笔放款按所选业务直接办理，不经过只读详情；有效办理深链同样进入对应弹窗，失效动作显示原业务状态。确认、预览、放弃修改替换当前弹窗内容，不在详情之上叠加第二层。成功结果在原弹窗反馈，质押发起成功关闭并定位逐币清单。

沿用已有角色权限、业务规则、共享壳层及四类纵向清单。以下历次单抽屉办理描述由本节更新；不修改 PRD 业务定义。

## 2026-09-21 · L5～L8 纵向记录与单抽屉修订

在 WS-351 统一交付，不新增子 issue。输入为 `c25aa69` 的 L5/L6 V1.2、L7 V1.1、L8 V2.1；沿用 existing-system adaptation / console、现有 portal、tokens、组件和模块源码。以下为当前实现，后面的日期章节保留历次交付背景，旧版叠层描述不再适用。

- 左侧质押代币、融资申请、融资放款、还款信息纵向排列、自然滚动，各清单每页 5 条；右侧仍为质押 / 融资 / 还款。项目详情不显示操作记录，不常驻可融额度。
- L5～L8 查看、办理、必要最终核对与文件预览共用单抽屉。返回保留草稿和位置，关闭退出业务；有修改时在原抽屉确认放弃。成功留在本笔结果并显式选择下一步，质押发起成功则关闭并定位逐币结果。
- 申请详情区分当前报价和原位折叠历史，授信只常驻总额及已用额；协议文件直接下载。放款和还款各自成表并关联同笔业务，保留生成中、未开窗、超期、结清、无权及缺原件状态。
- 控制台查看和办理深链精确定位 business / instalment；跨模块关联查看可返回来源。完整附件继续组合资产广场、借贷广场、控制台、账户、消息，默认资产广场。

内部使用 Chromium 实测质押核对/发起、申请报价/接受拒绝、登记放款/到账确认、还款提交/确认、失败重试、草稿返回、放弃、文件预览下载、角色隐私、多笔分页及失效入口。源页与独立导出均验证关键路径，并检查桌面与窄屏实际呈现；截图仅内部使用。真实上游资料、SSO、钱包签名、链上执行、服务端消息及未裁定的跨期、时区、精度等边界未联调，不代表生产验收。

## 2026-09-21 · 固定操作页签与入池结果返回

按用户评论 `8eb82999-4c92-4de9-9143-9b052fcd52d4`，在已联通的 `7fe0fdc` 上增量修订。项目操作固定为质押 / 融资 / 还款，暂无业务时保留页签与空态，权限继续控制具体操作。

质押审核与确认列表新增协议名称下载，保留代币标识、提交时间倒序与右侧筛选；无协议显示“—”。入池确认页只核对选币、截止时间及费用，不再重复下载协议。成功发起即关闭弹窗、聚焦质押代币清单；本人项目清单优先显示最近执行的代币，处理中及失败代币仍可查看且不计入质押额度，逐张成功才更新池值。资金方及游客不显示这些私有执行条目。

实际 Chromium 在源码和独立 HTML 完成 76 项检查：三类身份的固定页签，协议实际下载、批量入池关闭与焦点、成功/失败回写及额度隔离；发起不可用、费用余额不足、费用变化、结论到期均保留确认弹窗且未发起执行，费用重新确认后可发起。中英文 × 375/768/1440px 复核列表布局与下载可达，桌面/窄屏截图仅内部检查，浏览器无脚本异常。共享面客入口继续沿用；真实签署协议、链上服务及已有未决业务口径不在本轮验证范围内。

## 2026-09-21 · 全部面客模块联通

按用户 `fe0e1e7b-8bfe-4c14-96f6-2d3f69b46546` 修订；基于当前 `cly2.0.0 @ 7a8e89b`，属于 existing-system adaptation / console。五个 HTML 原先加载不同子集：借贷入口缺少账户、控制台与消息；消息入口使用样板页，其他入口也未完整装入 L6–L8。

本轮统一使用共享 `portal-entry.js` 加载清单，复用已有模块源码与挂载结构；单一启动避免丢失渲染钩子和重复事件绑定。默认资产广场，导航进入真实借贷列表与控制台；账户菜单进入相应资产方/资金方账户，铃铛及消息列表共用消息池。控制台对象→项目办理→原对象，消息→项目→原消息，登录→原站内页面均可连续操作。原费用确认、角色权限、报价、放款和还款表单不重写，不改 PRD。

实际 Chromium 检查：五个源码入口及独立导出件的联通、角色菜单、登录回跳与页面呈现共 249 项通过，无脚本异常；连续核对控制台对象/放款信息与项目往返、消息与项目往返、质押弹窗、放款抽屉、还款计划。补查质押双币勾选→一次费用确认→两笔独立执行，以及已有资金方钱包连接/签名模拟→原消息页。主页面中英文 × 375/768/1280/1440/1920 检查无横向溢出，桌面和窄屏内部视觉复核。未重验全部业务异常及真实外部服务。

交付仍为本地演示：外部 SSO、钱包与签名、服务端会话、链上执行和真实消息事件均未联调。跨期执行与健康度等已有业务未决项保持；本轮不新增后台页面链接或跨部署权限。

## 2026-09-20 · WS-354：还款计划与还款确认接入

本轮为 existing-system adaptation / console；沿用一次完成及入库授权，交付为保留未决业务边界的 delta prototype。输入为 WS-354 评论 `1f5fdd86-7681-4182-9156-208bd9833908` 的 L8 V2.0 正文与流程图、只读 `ea56882` 的还款原型及当前 `cly2.0.0 @ d653cba`。母版 `sample-pages.js` 最近提交 `7b21230`，已读取并在 Chromium 打开借贷列表。无阶段重审、无 PRD 修改；旧版原型不复制搬动。

唯一入口仍为本目录 `借贷广场.html`，空地址默认资产广场，无首页。新增本目录 `还款计划与确认.js/css`，组合既有放款、报价与项目模型；源码引用共享层，附件由 `_shared/export.py` 内联生成，不维护第二份 HTML 源码。视觉取现有 `_shared/tokens.css`、`base.css` 与面客 portal：主色 `#1D4ED8`、底色 `#F7F8FB`、44px 控件、15px 正文、22px 卡片内边距、12px 卡片圆角；保留 760px 抽屉、560px 二次确认。

- 还款页签承载独立还款区，放款未确认前隐藏。单笔显示编号，多笔使用一个可检索选择器，需求/业务/放款编号匹配同一选项。公开计划与私有记录按主体区分；资金方不能提交还款，资产方不能确认收款。
- 初始计划留在报价详情和接受流程，接受时明示日期重算并留演示时间；放款提交日期作为起息日、原到账确认时点作为定稿时间。定稿六列表单笔查看，本息及计息区间行内展开；计划生成中保留放款事实，恢复生成不重复形成期次。
- 法币凭证、数币哈希、只读账户、选期、开窗、还款时间和备注校验接入同笔业务。还款与确认各有明确勾选的二次确认；取消、计划查看、记录和预览返回原期次及草稿。
- 提交冻结本期逾期且不减少本金占用；资金方确认按本期本金同时减少项目余额与授信占用，利息期不减本金。全部期次确认才结清业务；本金先确认但仍有未还利息时，不按零余额关闭项目。
- 项目终态与可信释放事实分开；没有释放结果不启用提取，不显示已释放。仅独立演示工具可注入可信已释放事实，重复操作反馈“已释放”，不重复扣减或自动提取。未新增跨平台协议或 PS 枚举。
- 深链格式 `#/project/{project}?business={business}&instalment={序号或计划编号}&repayment={plan|pay|confirm|record}`。目标重新校验；刷新默认回游客，只读公开计划可达，身份模拟不会由链接授予私有权限。控制台及消息模块的完整接入留给后续 issue；本轮提供同源事实及独立工具中的通知事件预览。

| 角色 × 功能 | 覆盖 |
| --- | --- |
| 资产方 / 还款与计划 | covered：正常、未开窗、逾期、待确认、已结清、资料校验、上传失败与提交失败 |
| 资金方 / 核对与确认 | covered：正常、确认临近与超期、失败重试、本金与利息差异、结清、幂等反馈 |
| 游客与非当事方 / 公开计划、深链 | covered：公开履约信息与私有资料隔离、目标无效、身份变化、无权写入 |
| 计划与关联 | covered：加载、失败重试、生成等待、多笔搜索无结果、同层返回、通知预览；L5/L6/L7 其他有效内容 preserved from existing |
| 真实服务与其他模块 | out of scope：真实转账/链上执行、后台通知投递、控制台聚合、文件持久化、撤回/修改/申诉/自动确认、部分及超额还款、提前结清、罚息 |
| 待裁定 | unresolved：X-05 结果未知/失败/部分成功的正式反馈、PS 编号、末期窗口冲突、业务逾期标记清除时点、业务时区、数币精度、账户展示、客服邮箱与 LN-01 命名 |

实际 Chromium 核验：源码 52 项主流程 + 17 项异常/上游衔接检查通过；中英文 × 375/414/768/769/900/901/1280/1440/1920 × 项目/计划/确认/二次确认，共 72 组布局检查通过。内部查看桌面计划、还款与窄屏确认。最终单文件按同组交互及布局复核；无脚本错误或外部网络请求。未验证其他浏览器、屏幕阅读器、真实鉴权或后端原子事务。所有金额、公司、地址及文件为本地演示；文件内容仅当前会话保存。演示日期按 UTC、结算金额按两位小数计算，不作为未决口径的裁定。

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/借贷广场/借贷广场.html \
  --output ../deliverables/WS-354-还款计划与还款确认.html
```

## 2026-09-20 · WS-353 串行增量：放款与确认关联优化

本轮为 existing-system adaptation / console 的既有原型修订，承接 `cly2.0.0 @ acde785`、本项 `b88ee15` 原型及 `e02884a` PRD 入库版。沿用一次完成及入库授权，仅调整 L7 JS/CSS、本说明和面客组件规范；保留默认进入资产广场、无首页、融资清单与四阶段进度，不修改 PRD、不启动下一项。

- 从融资详情进入放款、重传或到账确认，关闭／暂存回原记录，恢复滚动和触发按钮焦点；直接从操作栏打开则回原项目动作。成功后来源详情显示当前结果。
- 二次确认取消回原抽屉，保留输入与位置；凭证预览的关闭、Escape 和返回按钮均回原确认／记录抽屉，保留实收金额。
- 当前页面会话的草稿按业务、身份和动作隔离。必填及原因错误定位具体控件；空文件单独反馈。输入修正清除对应旧提示；窄屏返回与主操作并列，次操作另行。
- 提交、暂缓／重传要求、重传及文件上传的迟到回调不会作用于已离开的页面或另一笔业务；权限失效清理工作浮层。旧确认入口、未知业务和项目不匹配链接给出反馈，不落到错误记录继续办理。
- 保留 760px 抽屉、560px 二次确认与合同／金额并排核验。组件规范 §5.7 由本项已确认组合补齐，未替换共享母版或其他模块样式。

覆盖：双方的放款、到账确认、重传、暂缓、普通终止及嵌套预览／确认；非当事机构、游客与其他身份的操作隔离。L5/L6 既有内容 preserved from existing，本轮回归其三币种报价→接受→进入放款的关联，不声明全部相邻模块验收。特殊项目终止、SPV、客服邮箱与其他 PRD 未决项仍 unresolved；真实资金、通知投递、还款服务与文件持久化 out of scope。

实际验证：Chromium 源码 39 项检查通过；最终自包含 HTML 通过同组 39 项及额外 29 项三币种／历史／旧入口检查，无脚本错误或外部网络请求。两语言 × 九宽度（375/414/768/769/900/901/1280/1440/1920）× 放款／确认／重传，共 54 组布局与返回检查通过，底部动作可达。内部复核桌面合同与金额同屏、移动动作分组；不交截图。未验证真实服务、全部相邻模块、其他浏览器或屏幕阅读器。

交付仍为带既有未决边界的 delta prototype；所有企业、金额、文件和事件为本地演示，刷新不保留上传文件内容。源文件继续共享引用 `_shared` 的 token、壳层和组件，单文件附件由 `export.py` 内联生成，不作为第二份源码入库。

## 2026-09-20 · WS-352 串行增量：授信报价与接受／拒绝关联优化

本轮为 existing-system adaptation / console，基线 `cly2.0.0 @ d492551`，承接本项原型 `b188362` 与 PRD V1.1 `365a8ca`。沿用一次完成、入库授权；只改本模块 JS、CSS、README 和面客组件规范，不修改 PRD、不启动下一项。视觉继续取 `_shared/tokens.css`、`base.css`、portal 壳层和既有 `.cq-*`，不替换已确认布局。

- 确认流程首部紧凑展示机构与金额／利率，保留倒计时、三步进度和完整条款入口。收款人、收款银行分组，中转行保持折叠选填；主按钮明确当前动作。窄屏多按钮分行，避免中文末字被挤成一行。
- 字段错误就地关联并聚焦，确认声明和文件缺项有明确定位；步骤切换定位标题，同步骤的选择保留位置。从融资详情进入办理，可暂存回原需求和选中报价，再次进入保留账户与进度；最终关闭恢复实际触发行。
- 剪贴板失败时在商务摘要步骤内展开完整只读文本，收起后继续原步骤，不再跳到无法正确返回的独立复制弹窗；手动复制区不加入打印正文。
- 需求在确认报价期间失效时有可返回反馈；离开原流程后不执行迟到的本地模拟提交回调。角色变化后不显示接受表单，浮层期间保持背景不可操作。
- 保留默认资产广场、无首页、项目融资四阶段、授信成功后手动继续报价、三条报价示例及历史列表、接受后同一业务进入既有 L7。组件规范新增 §5.6，由已确认内容归纳，不以母版覆盖业务流程。

| 角色 / 流程 | 本轮覆盖 |
| --- | --- |
| 资金方 / 首次、追加、重新核定与报价 | covered：表单、上传、错误定位、失败重试、成功返回、手动重入、同层报价确认；原有效期与金额边界 preserved from existing |
| 资产方 / 账户、摘要、盖章件、接受与拒绝 | covered：分组、必填、声明、续办、复制回退、拒绝原因、提交结果、失效及离开后的旧回调 |
| 游客与无关主体 / 融资详情与历史 | covered：公开历史、隐藏私有原因、无接受入口、身份变更反馈；既有文件权限 preserved from existing |
| 相邻资产、项目与放款模块 | preserved from existing：默认入口、项目主结构和数据；放款入口冒烟通过，未重验全部 L5/L7 流程 |
| 正式主体资料、消息投递、接口和链上执行 | out of scope；企业名称补登、追加有效期基点、账号地区格式及结算精度等仍 unresolved |

实际 Chromium 核验：源码和独立 HTML 的主流程各 159 项通过，独立 HTML 另 18 项失败重试、五个进度起点、到期、权限和 L7 入口检查通过。中英文 × 授信／确认／历史 × 375/414/768/1280/1440/1920 共 36 组布局无页面或弹窗横向溢出，底部操作可达；内部查看桌面与窄屏。未发起外部网络请求。文件、企业、机构及金额均为演示；不代表真实鉴权、资料、签署、放款或后端异步处理验收。`Q-CR-07/08/09`、`DEP-26/27` 与消息适配等既有未决项保持，本轮无新增交付阻塞。

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/借贷广场/借贷广场.html \
  --output /tmp/WS-352-授信报价与接受拒绝-关联优化.html
```

## 2026-09-20 · WS-351 串行增量：入口、列表返回与组件契约

本轮为 existing-system adaptation / console。同步 `cly2.0.0 @ 4cc6911` 后，承接本项 `448c344`、L5 PRD V1.1 `d58d2e7`、WS-350 交接及当前分支已经合入的 L6/L7。只改原型与组件规范，不修改 PRD，不启动下一项。

- 默认空地址、旧根地址与品牌入口进入现有资产广场；共享导航进入借贷项目列表。直接打开项目详情仍定位该项目。源码直接组合资产广场的 JS/CSS/演示接入文件，独立 HTML 从相同来源内联，未另建资产页或复制共享壳层。
- 项目列表到详情携带本地返回快照；页面返回、父级面包屑和浏览器后退恢复条件、排序、加载范围、内外滚动与原行焦点。刷新详情、在新会话打开详情仍可返回；非法项目编码与未知编号进入可返回的空态，外部返回地址不接受。
- 保留七列项目清单、整行入口、左侧自然滚动、右侧内嵌操作、逐币审核弹窗和一次费用确认；保留当前报价与放款链路。从普通项目列表进入时清理过期的“返回代币详情”入口。
- 桌面项目状态列适度加宽，避免英文状态末尾拆字；未增加字段或操作列。
- 组件规范新增 §5.5，记录已确认模块布局、返回契约、逐币弹窗和关联容器；详情基线明确项目主页面不加操作记录。规范据模块确认结果补齐，不用通用母版重排。

实际 Chromium 核验源页与独立附件：入口与关联、列表筛选/追加/空态、键盘整行、三类返回、刷新与新会话恢复、非法编码和外部返回拦截；质押弹窗 105 项回归通过，覆盖五列与代币标识、倒序、跨页多选、单次确认、撤回/重申、权限及窄屏。另核对现有放款待确认操作抽屉入口。项目详情检查三角色 × 中英文 × 九宽度（375/414/768/769/900/901/1280/1440/1920），左侧保持自然滚动，无页面横向溢出；桌面及窄屏视觉内部查看，不交截图。

本轮无新增交付阻塞。综合健康度评分仍无确认口径，只展示质押覆盖；原有执行长期未知、跨七天/终态结果归属与统一业务时区等未决项仍保留（Q-LS-07/08/09）。持有地址默认不可用，关联核验显式启用既有虚构样例；真实链上执行、文件持久化、消息投递、其他浏览器及真实设备不属于本次核验。未重验全部 L6/L7 业务流程。

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/借贷广场/借贷广场.html \
  --output /tmp/借贷广场-入口与关联优化.html
```

## 2026-09-20 · WS-353 放款链路并入借贷广场

本节覆盖以下旧交付记录中“放款另模块承接”的说明。按用户 `bd79c045-0e58-42f9-8fdf-523a0cee8cea` 修订，属于已确认方向内的 existing-system adaptation / console 增量。输入为 WS-353 L7 PRD V1.0 及流程图、WS-352 最新原型 `b188362`；沿用当前共享 token、壳层、路由、项目清单、融资详情弹窗与操作栏。旧 `cly-V1.0.0 @ ea56882` 仅作只读参考。

唯一入口为本目录 `借贷广场.html`。L7 的 JS/CSS 移到本目录组合加载，移除原独立 `面客端/放款与放款确认/` 源页及三笔另造项目的演示初始化。接受报价直接形成同一业务的放款记录；保留项目其他需求、质押和授信余额，不重新生成金额、账户或汇率。旧独立附件仍是历史快照，请使用本次统一附件。

- 融资清单分列展示需求状态与放款进度，点击打开 WS-352 的融资详情；详情增加放款进度、记录清单、暂缓/重传原因、时限、操作历史和到账结果。
- 右侧保留四阶段进度，并在放款阶段区分资金方登记与资产方确认。接受、待放款、待确认、已到账和终止各自展示当前动作；报价历史保留每笔业务的结果。
- 接受时的收款信息、盖章件、利率及汇率快照直接供放款核验。盖章件重传保留版本；接受时文件与后续版本分开标识。
- 登记提交和到账确认均有二次确认；法币凭证与五项还款账户、数币哈希、时间和原因执行已定校验。到账确认同步项目和授信金额；普通终止释放本笔报价占用，后续报价使用新编号。
- 默认关闭的流程工具统一承载“5 · 放款与确认场景”，切换场景设置下一步身份；完整选择币种从“3 · 提交融资报价”开始。原有 L5/L6 入口继续保留。

| 角色 / 功能 | 本轮覆盖 |
| --- | --- |
| 双方 / 需求、报价、签署、放款、确认 | covered：在同一项目内连续办理；USD、USDT、USDC；融资详情与记录清单 |
| 资金方 / 暂缓、重传、普通终止、提交 | covered：权限、原因、合同版本、凭证/账户/哈希、提交前覆盖与状态校验 |
| 资产方 / 重传、到账确认、临近与超期 | covered：时限与状态、全额本金、未到账保持未确认、还款计划生成中 |
| 非当事机构、其他身份、游客 | covered：私有合同/账户/记录隔离，游客登录入口与操作拒绝 |
| L5/L6 / 质押、授信、报价及拒绝 | preserved from existing；本轮回归真实报价、接受、终止后新报价；不替代所有旧验收 |
| 通知、L8/L9、实际文件与业务服务 | 通知事件在独立工具预览；真实消息投递、控制台聚合、还款计划服务、文件持久化与资金执行 out of scope |
| 特殊项目终止、SPV、客服邮箱、其他 PRD 未决项 | unresolved；不新增权限、自动裁定或真实邮箱。到期及仍有融资余额的组合不演示终止放行 |

本地浏览器使用 Chromium；内部复核合同与金额同屏、桌面/窄屏抽屉、融资操作栏和流程工具。两语言 × 九宽度（375/414/768/769/900/901/1280/1440/1920）× 三状态，共 54 组，每组检查项目、融资详情及放款抽屉；无页面/弹层横向溢出，底部动作可达。端到端与异常用例覆盖实际报价→接受→放款→到账、两类额度转移、终止重报、重传版本、刷新、失败保留、覆盖恢复、机构权限、超期及旧入口。截图仅内部使用，不交付。

原型仍为带未决边界的 delta prototype。演示企业、账户、合同、汇率及链上地址不代表真实业务；快速跳到放款使用明确标识的样例合同，正常连续流程使用实际上传文件。业务事实随现有本地存储保存，文件内容仅当前打开期间有效，刷新后标记不可用并禁用下载。未验证真实接口、真实收付款、其他浏览器、真实设备或屏幕阅读器。无 PRD 或设计规范新增。

单文件由共享导出器生成（生成件仅作附件，不作为第二份源码入库）：

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/借贷广场/借贷广场.html \
  --output /tmp/借贷平台-融资报价至放款确认-原型.html
```

## 2026-09-20 · 融资进度、授信完成返回与报价列表

按用户 `dc6be973-5de7-468c-a565-ba33307ed5d2` 修订，属于既有 console 原型增量；沿用 portal、共享 token、操作栏及响应式表格。本节覆盖下方历史记录中“去除流程条”和保存授信后自动进入报价的描述。

- 项目操作的融资页签恢复“发布需求 → 机构报价 → 确认签署 → 融资放款”进度条，区分已完成、当前和后续阶段；需求结束后不标记进行中环节，已放款显示全部完成。
- 首次核定、追加和到期重新核定成功后关闭表单，提示“已完成授信，请继续报价”，由用户再次点击“立即报价”。再次进入时重算可用授信与覆盖；追加后仍不足也先返回，失败时保留表单和文件。
- 报价历史以列表展示机构、业务编号、融资及结算金额、年化利率、报价时间、处理结果与时间；可选择任一历史记录联动查看详情，拒绝原因仍仅相关主体可见。窄屏使用共享表格的逐项布局。默认关闭的演示工具在“确认与失效场景”增加“多次报价记录”，包含两家机构的三次顺序报价，不改变同需求报价锁定规则。

实际 Chromium 检查：源码 60 项、独立 HTML 64 项通过，覆盖保存返回、指定 toast、手动再次报价、仍不足与失败、融资各阶段及终态、三条报价切换与原因权限；中英文 375/414/768/1280/1440/1920 宽度无页面或弹窗横向溢出，内部复核桌面及窄屏视觉。未接真实接口、签署或放款服务，原有 `Q-CR-07`、`DEP-27` 等未决边界保持。

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/借贷广场/借贷广场.html \
  --output /tmp/借贷平台-融资进度与报价记录-原型.html
```

## 2026-09-20 · 融资详情、操作栏与流程演示修订

按用户 `6b678b7e-12ec-48f2-8fa4-22533397d3bc` 的三项页面反馈及 `81c55db2-e22c-4030-95ec-a7bb9972999d` 的演示工具反馈修订，属于 `existing-system adaptation` / `console`。复用 `448c344` 的 portal 壳层、共享 token/base、现有项目详情、表格、弹层及逐币质押逻辑。

- 项目详情主页面移除“授信与协议”“融资报价”两块。融资清单整行点击或键盘 Enter/空格打开同一融资详情弹窗，按需求、机构报价、协议签署、报价历史组织；切换历史报价不叠加弹层。企业授信协议收在融资详情内，仅相关主体可展开。
- 融资操作栏保留当前状态、金额、报价机构、必要倒计时及下一步动作，去除重复流程条；资金方默认进入融资页签，“全局”改名为“质押”。权限、上传边界、168 小时及授信有效期沿用已有规则。
- 默认关闭的演示工具展示当前环节、当前身份、下一步操作方。五个起点按发布→授信→报价→确认签署→放款组织，切换会重置示例并设置身份；原有质押、授信、报价、失效和权限控件按环节折叠分组。当前示例的放款入口仍由后续模块承接，不声称本附件实现 L7。

Chromium 实际核验源码和内联附件：清单鼠标/键盘入口与返回焦点、同需求多次报价及历史切换、拒绝原因/签署资料权限、五个演示起点与办理入口、已有评审控件保留；中英文 × 游客/资产方/资金方 × 375/768/1280/1920 × 主页面/融资详情/演示工具，共 72 个组合无横向溢出。内部视觉核对桌面及窄屏，不交付截图。未连接真实资料、文件或放款服务；已有 `Q-CR-07`、`DEP-27` 等边界保留。

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/借贷广场/借贷广场.html \
  --output /tmp/借贷平台-融资详情与流程演示-原型.html
```

## 2026-09-20 · 审核弹窗列与筛选布局

按用户评论 `a4fa8bc6-5133-45db-8a3f-368e272dce91` 修订，同步基线 `bc056bd`，保留其他模块最新改动。

筛选、重置置于筛选项右侧，窄屏换行后仍靠右。除批量勾选外，业务列依次为代币、数量、金额、状态、时间；状态合并呈现当前审核/入池进度，时间沿用提交时间倒序。代币编号前复用共享 `.tok-mark`，演示数据没有真实 Logo，沿资产广场规范使用资产方字母与编号尾位的默认标识块。详情入口、协议、撤回/重提和批量确认继续保留。

实际 Chromium 源码及独立 HTML 共 105 项检查通过：列顺序与值、图标位置、筛选按钮右对齐、排序筛选、历史协议、重提、跨页勾选和批量执行、中英文 375/768/1024/1440 四种宽度；完成内部桌面与窄屏视觉复核。未验证真实接口、钱包或链上交易。

## 2026-09-20 · 精简审核清单与项目详情

按用户评论 `06226862-dea2-423b-8cc1-b7e981233540` 及两张截图修订，基线同步至 `cly2.0.0@4b07c5e`，保留 WS-364 的逐币审核、批量确认及 V1.1 业务逻辑。此次反馈覆盖原清单字段铺陈与可选排序的展示要求。

- 审核弹窗保留勾选、代币/金额、审核状态、提交时间、后续状态五列；删除操作列和排序选择器，按提交时间倒序，翻页和筛选后仍维持该顺序。买方、账期、数量、审核人、截止及原因在代币编号打开的详情中查看；当前单笔的撤回与重新申请入口移至该详情，历史与协议保留。
- 项目详情不再显示操作记录区；不删除业务事件数据及默认关闭的通知演示能力。私有申请仍只对本企业资产方开放。
- 实际 Chromium 源码与独立 HTML 共 75 项检查通过：精简字段、倒序与重新申请、历史/协议、撤回和重提、跨页勾选与批量执行、筛选重置、键盘进入、角色隔离、详情移除记录，以及中英文 375/768/1024/1440 四种屏宽。内部视觉复核桌面/窄屏弹窗；未验证真实接口、钱包或链上交易。

## 2026-09-20 · WS-364 代币清单、48 小时审核失败与批量确认

既有原型修订，`existing-system adaptation` / `console`。业务基线为 `cly2.0.0@d58d2e7` 的 WS-351 V1.1 四册与 WS-359 V1.2 两册；保留 `5a61e67` 的一次入池确认。只修改本目录视图 JS、模块 CSS、演示数据及本说明；沿用面客 portal、共享 token/base/shell、原生分页与响应式表格，不改 PRD、样板或其他模块。

- 审核面板一行一个代币，跨提交批次勾选；筛选、按提交或截止排序、翻页保留选择。关闭清单、变更筛选或切换项目清空勾选。每币详情保留历次独立申请、审核人、原因全文、协议、截止与执行事实；审核通过与审后情况分开展示。
- 连续 48 小时只终结仍待审的单笔，释放其申请占用，保留零费用与重新申请入口。截止时审核无效；已通过、驳回或撤回的笔不回退。演示为本地定时检查及重新打开后的期限补算，不代表实现了无人在线的后端调度。
- 同一弹窗核对项目、选中代币、各自协议及截止、逐币费用和按币种合计。资格、期限或费用变化时指出具体代币，保留有效选择，等待再次确认更新内容；尚未实际发起不产生执行或费用。按张保留独立成功、失败、等待与实际费用。
- 执行等待不再套两天失败；普通晚到结果正常归属，冲突只标记差异并限制相关张的新动作。已发起跨七天或项目终态的演示只保留已知结果、费用及待核实归属，不代定终态。无异常释放入口，正常撤回、业务释放及提取保留。
- 质押事件预览置于默认关闭的演示工具；每事件关联代币和具体申请，重复同笔同类事件去重，旧事件仍定位旧笔，不自动勾选。材料、原因及费用仅在本企业视图显示；共享消息中心的正式投递未接入。

| 角色 / 范围 | 本次状态 |
| --- | --- |
| 本企业资产方 / 清单、筛选、历史、批量确认、超时重提、协议、结果 | covered：默认、空、筛选无结果、加载、错误重试、禁用、确认、处理中、成功、失败 |
| 游客、资金方、其他企业 / 私有申请、协议、原因和费用 | covered：视图隔离、直接入口与本地动作拒绝；真实鉴权不在原型中 |
| 公开广场、项目、融资报价及正常解除质押 | preserved from existing；邻接入口与正常提取回归 |
| 运营审核页面、真实钱包、链上执行与后端调度 | out of scope；仅以独立演示工具模拟逐笔输入 |
| 异常释放操作、队列、处理进度与恢复 | out of scope |
| 长期执行未知终结与差异关闭责任、跨七天/项目终态归属 | unresolved；仅显示等待、已知事实与保护限制 |
| 七天统一业务时区、签署承载及企业主体映射 | unresolved；示例 UTC 截止不构成业务时区裁定，演示协议不是签署文件 |

实际 Chromium 核验：源码与独立附件共 252 项主流程/响应式断言，另 30 项补充检查通过。覆盖跨批次选择、同币重提历史、48 小时前后与竞态、逐币期限、资格/费用变化后重确认、取消/费用不足/入口不可用、未知与晚到/冲突/跨期结果、协议查看下载和失败重试、通知逐笔定位及去重、筛选分页、角色权限、正常提取、邻接资产广场与放款入口。中英文 × 375/414/768/1280/1440/1920 的清单、确认和历史无页面或弹窗横向溢出，按钮可达；内部查看桌面与窄屏截图。补验最终附件的首笔/追加申请分类及历史保留。无脚本错误与 HTTP 依赖请求；未验证真实钱包、接口、后端定时任务、跨平台联验或 Safari/Firefox。

源码继续引用 `_shared/`，用现有 `export.py` 内联生成可离线打开的单文件附件；生成件不入库。演示数据与协议均已标注。默认存储键升级为 `hc-ws351-demo-v2`，避免沿用旧执行两天失败的演示结果；旧本地演示存档不删除。

以下为历史交付记录；本节覆盖旧记录中按申请铺卡和执行两天失败的描述。


## 2026-09-20 · L5 合并入池确认

按用户评论 `ea10ef09-3104-47ed-b26f-5c1e6b0fba55` 合并审核通过后的「核对操作与费用」和「确认链上费用」。基于 `b68472b`，同步分支至 `5039b1c`，保留共享底座和其他模块增量；沿用直接交付及入库授权。

点击申请的「确认入池」后，只需在一个弹窗核对项目、代币张数/金额、逐张预估费用和合计，点击「确认费用并发起入池」直接进入执行结果。保留本企业承担费用、失败已产生费用不退的披露，以及打开时和提交时的资格复核。取消/Escape 返回申请记录、不发起执行；解除质押流程仍沿用其既有步骤。窄屏正文可滚动，确认按钮可换行。

实际 Chromium 源码与独立附件共 73 项检查通过：一次确认发起、取消无变更、重复入口收敛、费用不足/入口不可用与重试、资格变化、结论过期、部分失败及逐张费用、执行超时、解除质押回归，中英文 375/768/1024/1440 四种屏宽。完成内部截图视觉复核；未验证真实钱包签名、接口或链上交易。本轮仅修改本目录 JS/CSS/README。

## 2026-09-20 · L5 融资需求与公开覆盖状态补充

在 `fa1e3d8` 上处理用户评论 `9f9db99a-9a4e-4a22-9ebd-b6f23d861f29`，沿用已确认方向和直接交付、入库授权。列表恢复「融资需求」金额、币种和当前状态，无在途需求时明确显示「暂无融资需求」；已有融资但无新需求的项目仍可浏览，未发布草稿继续隔离。需求金额可按现有排序规则降序排列。

新增「质押覆盖」列，直接消费与详情一致的三档覆盖计算，不足时显示实际缺口。依据 PRD 主册 §5.6、功能册 `FP-20`/`FP-21`；未把覆盖状态扩展成综合风险评级或健康分。综合健康度是否还包含其他维度、相应阈值和数据来源尚无定义，需需求方明确后再扩展。表格列宽适配中等屏幕，保留整行进入、返回恢复、20 条加载和原有操作面板。

实际 Chromium 检查源码与独立 HTML：融资需求金额/状态/无需求、草稿隔离、三角色公开展示、覆盖有余/持平/不足和恢复、缺口金额、筛选排序、键盘进入与返回、滚动加载及详情自然滚动；中英文与 375/768/1024/1440/1920 五种屏宽。截图仅内部视觉检查，未验证真实服务或链上数据。本次只改本目录视图 JS、CSS 和本文档。

## 2026-09-20 · L5 列表取舍与操作面板修订

Stage 4 · `existing-system adaptation` / `console`。以用户评论 `22894d71-8d2a-4729-8650-7ff7ffa60ad7` 为本轮反馈输入，按 `a65cc849-ab68-4ed7-9294-778e2888ed2e` 自主设计，沿用直接交付与入库授权。初读基线 `0565fea`，提交前同步 `cly2.0.0@75a5dc8`，保留已合入的 L6 授信报价、L7 跳转、资产广场项目返回入口和共享运行时增量。以下新安排覆盖旧交付记录中的同名布局描述。

- 列表收敛为融资项目、资产方企业、质押额度、当前融资需求状态、项目状态、更新时间。质押额度仍取有效质押价值 `D.numbers(p).value`，不改为 80% 可融额度。项目编号、代币构成、需求金额及编号、期限/利率、报价锁定及倒计时在详情查看。整行点击/键盘进入、筛选与每批 20 条保留。
- 左侧恢复自然页面滚动，不设固定高度或内滚区域。右侧操作面板吸附在共享顶栏下方，标签与主操作在面板内容滚动时保留；面板按可用视口计算最大高度。窄屏默认收起操作正文，保留三标签和主操作，展开后在面板自身滚动，避免挤占整页。
- 全局操作按额度概览、质押进度、管理动作分组，底部突出当前主操作；关闭项目降为次要入口。多次质押在审核弹窗按申请显示金额、提交时间和状态，独立进入入池/撤回动作，逐张代币与费用结果按需展开；进行中和历史分区，解除质押结果独立保留。
- 角标表示待办而非申请总数：全局统计当前所有者仍可确认入池的有效审核通过申请；融资统计当前所有者待处理的报价。审核中、链上执行中、历史记录、可选择报价的机会不计入；0 时隐藏。还款没有到期事实，不从融资余额推导角标。条件变化的审核通过记录仍可重新核验，沿用原复核及作废流程。
- 修改仅限本目录 `借贷广场.js`、`借贷广场.css` 和本文档；不覆盖 L6 模块代码或共享底座。原型仍是标注的离线演示，未新增正式业务规则。

本轮实际浏览器核验：Chromium 打开源码和独立 HTML，覆盖列表取舍、整行进入/返回、自然页面滚动、右侧吸附及内部滚动、短屏主按钮、移动展开/收起；通过实际提交形成三笔并存申请，核对逐笔金额、展开、审核通过、执行、失败、超时与失效后的角标变化。验证所有者/其他企业、资金方/游客私有信息隔离，以及现有 L6 报价与接受入口。中英文、三种角色、375/414/768/1024/1280/1440/1920 七种宽度；截图仅内部复核。未验证真实接口、真实交易、Safari/Firefox、真实设备或屏幕阅读器。

## 2026-09-20 · L6 授信报价与接受/拒绝

Stage 4 交付；`existing-system adaptation` / `console`，待定细项保留为 `delta prototype` 边界。用户评论 `1b95fef2-abec-4388-b26a-408b7c75b740` 明确授权“直接输出原型，无需等我确认”，本轮合并四阶段。业务输入为 L6 四册 PRD V1.0（`e572630`）和裁定评论 `9614435b-da52-4b77-a2bb-c8e710db6022`；初读分支 `0dd8c7e`，提交前同步 `737d1b9`（含 `0a8c4ff` 消息组件及共享运行时兼容增量）；共享样板源版本 `65a22f2`。

继续使用本目录 `借贷广场.html`，进入列表后打开项目，右侧“融资”标签承载当前动作。`授信报价.js` 接入现有 L5 页面与演示模型；HTML 仅增补模块布局和脚本引用，`借贷广场.js` 增加窄范围调用点。复用 `_shared/tokens.css`、`base.css`、`shell.js`、`registry.portal.js` 和设计系统，未另建导航、壳层或额度底座。P-LS-04/05/06 登记为详情内弹窗承载，不增加独立授信菜单。

实际视觉取值：主色 `#1D4ED8`、页底 `#F7F8FB`、白色卡片、16/12px 展示及普通圆角均来自 `_shared/tokens.css`；顶栏、1560px 宽容器、44px 面客控件及弹层来自 `_shared/base.css`。列表、五统计、信息区独立滚动和右侧操作标签继承 L5 当前实现，默认英文，支持中文，时间使用共享时区格式。

| 角色 / 页面或功能 | 本次覆盖 | 状态 |
| --- | --- | --- |
| 游客 / 列表与项目详情 | 公开报价条款、机构、锁定起始、已锁定时长、倒计时、历史快照；收款信息、盖章件、授信及拒绝私有原因隔离 | covered |
| 资金方 / P-LS-04 | 无授信、不足、到期、充足四分支；遮罩意图确认、取消无变更、首次/重新核定、追加金额、1～5年校验、三数差额、授信历史 | covered；追加改期 unresolved |
| 资金方 / P-LS-05 | 全额报价、三种结算币种、年化利率、只读还款类型及日期、汇率四要素、二次确认、成功锁定及金额联动 | covered；结算精度 unresolved |
| 资产方 / P-LS-06 | 法币空表与最近成功账户预填、数币统一地址、逐笔确认、户名/SWIFT校验、摘要复制/打印、盖章件数量/大小/格式、条款声明、中断续做、接受/拒绝 | covered；地区账号格式与主体服务 unresolved |
| 双方 / 异常与终态 | 汇率/覆盖不可用、汇率变化重确认、竞争报价、提交失败、文件失败、拒绝、168小时失效、需求失效、原快照历史与旧入口失效反馈 | covered（本地演示） |
| 列表与详情 / 通用状态 | 加载、空数据、筛选无结果、失败重试、无权限、游客、提交中、提交失败 | covered / preserved from existing |
| 双方 / 五类通知 | 相关身份可见的事件预览、24小时临期事件与去重、落业务详情；评审工具默认关闭 | 正式登记、模板、消息中心投递 unresolved |
| L5 / 列表、建池与质押 | 原有布局、查询、分页清单、审核与执行入口保持 | preserved from existing |
| 放款、还款、计息、链上执行、运营处理、控制台 | 既有交接边界；未实现生产接口与跨模块闭环 | out of scope |
| 审批、调低/冻结授信、独立授信管理、多家同时报价、部分融资、修改/撤回/展期报价、合同生成/审核、账户簿 | 不提供对应动作；PRD“本轮不做原型”已由本次用户授权覆盖 | out of scope |

实际验证：Chromium 149.0.7827.55 打开源码与生成附件；核心流程及异常断言通过。中英文 × 游客/资产方/资金方 × 375/414/768/769/900/901/1280/1440/1920 的列表和详情，以及两语言九宽度的账户弹窗，共126个布局组合无页面横向溢出。核对列表内滚、筛选位置、弹层键盘与 Escape、私有信息、提交状态、语言切换保留输入；附件启动无 HTTP 请求。内部查看桌面详情与移动账户表单截图，不交付截图。未验证 Safari、Firefox、真实设备、屏幕阅读器及生产服务时效。

保留边界：Q-CR-07 未定追加改期的五年基点，当前追加仅沿用原有效期；Q-CR-08 不臆造各地区账号/IBAN长度与字符集合，当前仅演示必填与已明确校验；Q-CR-09 仅使用能够精确呈现的演示换算，不定义币种舍入规则。企业登记名称/地址服务未接入，对应缺失仍阻断接受。通知为默认关闭工具中的事件预览，未代改共享登记。盖章件仅在本机内存保留内容，刷新保留填写进度与文件名但无法重新下载原件。所有企业、金额、汇率、资质及地址均为演示数据，页面不调用真实业务服务。

生成最终单文件附件（生成件不入库）：

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/借贷广场/借贷广场.html \
  --output /tmp/借贷平台-授信报价与接受拒绝-原型.html
```

以下为 L5 既有交付记录。

Stage 4 · `existing-system adaptation` / `console`。用户“直接输出页面原型”授权合并设计阶段、直接交付并提交既有模块目录。需求基线为 WS-351 四册 `c7ef580`；本轮以用户评论 `b03c51dd-2dfd-48e6-98b6-3973d06ca6a0` 的五项反馈覆盖先前列表与详情布局要求，业务规则与未确认事项保持原边界。

已同步 `cly2.0.0@04d96d3`。复用 `_shared/design-system/面客工作区组件规范.md`、`tokens.css`、`base.css`、`shell.js` 和共享路由。仅调整本模块 HTML、JS、README，不改公共组件、相邻页面或演示业务模型。

入口：[借贷广场.html](借贷广场.html)。默认英文、游客态；页头切换语言，默认关闭的演示工具切换资产方/资金方、审核结果、逐张执行结果、时钟和失败情形。演示数据保存在本地，工具内可重置；不发生真实链上操作。

## 本轮调整

| 页面 / 功能 | 最终表现 |
| --- | --- |
| `/marketplace` · P-LS-01 | 每行一个融资项目，突出项目名称、编号和所有者；项目池展示有效质押价值、总张数、计入/不计入覆盖张数与覆盖状态；当前需求单列展示金额、编号与状态，已放款/结束的历史需求不冒充当前需求 |
| 列表交互 | 整行点击、Enter 或空格进入详情；无操作列。币种/期限/关键词、更多筛选、排序、桌面定高内滚、吸顶表头、每批 20 条沿用现有约定；返回保留查询及加载位置 |
| `/project/{id}` · P-LS-02 | 标题、项目状态之下分为左侧信息与右侧操作；左侧五统计、质押清单、融资清单和操作记录独立滚动，右侧操作不跟随滚动 |
| 操作区 | 全局 / 融资 / 还款为内嵌标签页；支持方向键、Home、End。窄屏操作区置于信息上方，信息仍独立滚动；切换标签保留左侧位置 |
| 质押审核 | 仅所有者的全局标签显示“质押审核与结果”按钮及在途申请数；弹窗承载申请、审核历史、逐张执行结果。首次打开从顶部阅读；费用确认取消返回审核，关闭审核回到入口焦点 |
| 质押代币清单 | 编号下展示交易 Hash，点击复制完整值；相邻外链图标独立在新窗口打开区块链浏览器。移除“查看凭证”按钮；复制被拒时给出可选择的完整 Hash |
| `/project/new` · P-LS-03 | 保留建池、首笔申请、零费用提交和直接进入草稿详情的既有流程 |

公开清单与所有者私有审核/执行信息保持隔离。期限和利率仅展示当前需求已有数据，缺失值不推算；项目有效期不作为融资期限。示例 Hash 沿用已标注的演示数据，外链不代表存在真实交易。

## 继承的布局参考与边界

旧版实际分支名为 `cly-V1.0.0`，参考头 `ea56882a2d5c3680ed7220b9e1feb2a0d8fcb652`，目录 `financial-service-platform/prototypes/借贷广场-融资需求与代币质押/`，包含 `ls-module.js`。前轮已在浏览器打开两例详情，核对页头、五统计、信息清单和三段操作。此次按最新用户反馈将三段改为标签、审核移入操作弹窗，不再沿用“左侧审核首位”的旧安排，不复制旧版代码或 DOM 错位。

原型仍覆盖资产方建池/追加、审核撤回/驳回/失效、两次费用确认、逐张成功/失败、两自然日审核及执行超时、迟到回写冻结、发布/改额/撤下/关闭、解除质押预扣及失败回滚、终态提取、覆盖变化和主体权限。功能范围对应四册 F-LS-01～21（15 作废）；本轮未修改 PRD。

签名承载、主体映射、正式编号登记、对外审核承诺和消息分类保持未确认边界。授信报价现由下述 L6 增量接入。运营审核、真实接口/链上签名与费用、放款/还款、控制台和通知投递仍只提供演示事件或交接入口，不声称跨模块联调或生产验收完成。

## 源码与单文件附件

- `借贷广场.html`：共享壳层挂载点、模块布局与样式。
- `借贷广场.js`：页面、交互、双语文案、路由适配及评审工具。
- `演示数据.js`：独立演示业务模型，本轮未修改。

导出附件内联共享依赖；生成件不作为第二份源码入库：

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/借贷广场/借贷广场.html \
  --output /tmp/借贷广场-项目与操作修订.html
```

## 实际浏览器核验

2026-09-20 使用 Chromium 149.0.7827.55 实际打开模块源码和最终导出附件。核对默认无 hash、`#/`、共享导航进入列表，非编号单元格点击、整行键盘操作、返回与浏览器后退、20→24 条加载、当前需求筛选和历史需求隔离。

验证左右滚动独立、标签语义与键盘切换、审核弹窗位置及往返焦点、复制完整 Hash、复制失败回退、外链新窗口目标（拦截外站响应，不验证演示交易真实性）。回归两次费用确认、部分成功与失败费用、改额校验、审核超时零费用、执行超时逐张费用、迟到回写冻结、解除质押失败回滚和终态限制。

中英文 × 游客/资产方/资金方 × 375/414/768/1024/1280/1440/1920 宽度，共 84 个列表/详情组合，检查横向溢出、私有信息隔离与操作可见性。内部截图复核桌面列表、桌面/移动详情和审核弹窗；本轮不交截图附件。未做 Safari/Firefox、真实移动设备或屏幕阅读器验证。


## WS-350 持有表项目接入

资产广场 V2.1 组合复用本模块项目详情及 CSS；只增加可选 `CF.LSStorageKey` 隔离演示存储、组合时的路由监听边界及可选返回代币详情链接。独立借贷源页不设置这些上下文，保持原流程。此次只回归独立借贷列表/详情入口与资产到公开项目的往返，不替代 WS-351 全流程验收。

## WS-352 · 授信原型 V1.1

依据四册 PRD V1.1（`365a8ca`）与用户授信反馈，继续复用共享壳层、tokens、路由和 L5 页面。仅修订 `授信报价.js` 与本模块 CSS：三种授信模式采用企业基本信息→授信条件→授信协议；补齐资料完整/缺项/加载/失败、协议上传进度与取消/重试、就地校验、放弃修改确认及授信双方只读历史。独立评审工具默认关闭，可选企业资料状态与一次上传失败。

本次授信协议为每次 1～10 份、单份 ≤10 MB；与接受报价时 1～5 份融资合同盖章件分开。存量授信不补录历史协议，不影响已有额度直接报价。追加仍可编辑日期；保持原日期可提交，提前日期被拒绝，延期因 `Q-CR-07` 上限基点未确认展示暂不可校验反馈，不把任意基点演示为已定规则。企业真实资料供给及可见范围仍待 `DEP-27`；资料均为明确标注的本地示例，无企业接口或实际附件服务。

使用 Chromium 149 核验源码与导出附件：46 项授信交互检查（包含数量 0/1/10/11、8+3 整批拒收、大小等于/超过 10 MB、空文件、格式、部分失败、取消、重试、三种核定、日期边界、权限及历史）；另回归语言保留、键盘焦点、报价确认往返、授信后报价、资产方接受与合同独立上限、刷新后的文件不可用状态。中英文 × 三种授信模式 × 375/414/768/1280/1440/1920，共 36 个布局组合无页面/弹窗横向溢出，操作区可达；实际检查桌面及窄屏截图，仅内部使用。未验证其他浏览器或真实业务服务，不替代 L5/L7 的全流程验收。

本机文件内容仅在本次打开期间保留；刷新后历史仍可追溯名称/大小，但显示“本地文件不可用”并禁用预览下载。历史协议不提供删除动作，备注仅本资金机构可见。其他既有未决项（账户/IBAN、结算精度、通知及身份资料依赖）沿用 PRD 边界。

更新附件由共享导出器生成，不另存第二套源码：

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/借贷广场/借贷广场.html \
  --output /tmp/借贷平台-授信报价与接受拒绝-V1.1-原型.html
```
