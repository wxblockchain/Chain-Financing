# 借贷广场 · 融资项目、代币质押与授信报价

## 2026-09-20 · L6 授信报价与接受/拒绝

Stage 4 交付；`existing-system adaptation` / `console`，待定细项保留为 `delta prototype` 边界。用户评论 `1b95fef2-abec-4388-b26a-408b7c75b740` 明确授权“直接输出原型，无需等我确认”，本轮合并四阶段。业务输入为 L6 四册 PRD V1.0（`e572630`）和裁定评论 `9614435b-da52-4b77-a2bb-c8e710db6022`；初读分支 `0dd8c7e`，提交前同步 `737d1b9`（含 `0a8c4ff` 消息组件及共享运行时兼容增量）；共享样板源版本 `65a22f2`。

继续使用本目录 `借贷广场.html`，进入列表后打开项目，右侧“融资”标签承载当前动作。`授信报价.js` 接入现有 L5 页面与演示模型；HTML 仅增补模块布局和脚本引用，`借贷广场.js` 增加窄范围调用点。复用 `_shared/tokens.css`、`base.css`、`shell.js`、`registry.portal.js` 和设计系统，未另建导航、壳层或额度底座。P-LS-04/05/06 登记为详情内弹窗承载，不增加独立授信菜单。

实际视觉取值：主色 `#1D4ED8`、页底 `#F7F8FB`、白色卡片、16/12px 展示及普通圆角均来自 `_shared/tokens.css`；顶栏、1560px 宽容器、44px 面客控件及弹层来自 `_shared/base.css`。列表、五统计、信息区独立滚动和右侧操作标签继承 L5 当前实现，默认英文，支持中文，时间使用共享时区格式。

| 角色 / 页面或功能 | 本次覆盖 | 状态 |
| --- | --- | --- |
| 游客 / 列表与项目详情 | 公开报价条款、机构、锁定起始、已锁定时长、倒计时、历史快照；收款信息、盖章件、授信及拒绝私有原因隔离 | covered |
| 资金方 / P-LS-04 | 无授信、不足、到期、充足四分支；遮罩意图确认、取消无变更、首次/重新核定、追加金额、1～5年校验、三数差额、授信历史 | covered；追加改期 unresolved |
| 资金方 / P-LS-05 | 全额报价、三种结算币种、年化利率、只读还款类型及日期、汇率四要素、二次确认、成功锁定及金额联动 | covered；结算精度 unresolved |
| 资产方 / P-LS-06 | 法币空表与最近成功账户预填、数币统一地址、逐笔确认、户名/SWIFT校验、摘要复制/打印、盖章件数量/大小/格式、条款声明、中断续做、接受/拒绝 | covered；地区账号格式与主体服务 unresolved |
| 双方 / 异常与终态 | 汇率/覆盖不可用、汇率变化重确认、竞争报价、提交失败、文件失败、拒绝、168小时失效、需求失效、原快照历史与旧入口失效反馈 | covered（本地演示） |
| 列表与详情 / 通用状态 | 加载、空数据、筛选无结果、失败重试、无权限、游客、提交中、提交失败 | covered / preserved from existing |
| 双方 / 五类通知 | 相关身份可见的事件预览、24小时临期事件与去重、落业务详情；评审工具默认关闭 | 正式登记、模板、消息中心投递 unresolved |
| L5 / 列表、建池与质押 | 原有布局、查询、分页清单、审核与执行入口保持 | preserved from existing |
| 放款、还款、计息、链上执行、运营处理、控制台 | 既有交接边界；未实现生产接口与跨模块闭环 | out of scope |
| 审批、调低/冻结授信、独立授信管理、多家同时报价、部分融资、修改/撤回/展期报价、合同生成/审核、账户簿 | 不提供对应动作；PRD“本轮不做原型”已由本次用户授权覆盖 | out of scope |

实际验证：Chromium 149.0.7827.55 打开源码与生成附件；核心流程及异常断言通过。中英文 × 游客/资产方/资金方 × 375/414/768/769/900/901/1280/1440/1920 的列表和详情，以及两语言九宽度的账户弹窗，共126个布局组合无页面横向溢出。核对列表内滚、筛选位置、弹层键盘与 Escape、私有信息、提交状态、语言切换保留输入；附件启动无 HTTP 请求。内部查看桌面详情与移动账户表单截图，不交付截图。未验证 Safari、Firefox、真实设备、屏幕阅读器及生产服务时效。

保留边界：Q-CR-07 未定追加改期的五年基点，当前追加仅沿用原有效期；Q-CR-08 不臆造各地区账号/IBAN长度与字符集合，当前仅演示必填与已明确校验；Q-CR-09 仅使用能够精确呈现的演示换算，不定义币种舍入规则。企业登记名称/地址服务未接入，对应缺失仍阻断接受。通知为默认关闭工具中的事件预览，未代改共享登记。盖章件仅在本机内存保留内容，刷新保留填写进度与文件名但无法重新下载原件。所有企业、金额、汇率、资质及地址均为演示数据，页面不调用真实业务服务。

生成最终单文件附件（生成件不入库）：

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/借贷广场/借贷广场.html \
  --output /tmp/借贷平台-授信报价与接受拒绝-原型.html
```

以下为 L5 既有交付记录。

Stage 4 · `existing-system adaptation` / `console`。用户“直接输出页面原型”授权合并设计阶段、直接交付并提交既有模块目录。需求基线为 WS-351 四册 `c7ef580`；本轮以用户评论 `b03c51dd-2dfd-48e6-98b6-3973d06ca6a0` 的五项反馈覆盖先前列表与详情布局要求，业务规则与未确认事项保持原边界。

已同步 `cly2.0.0@04d96d3`。复用 `_shared/design-system/面客工作区组件规范.md`、`tokens.css`、`base.css`、`shell.js` 和共享路由。仅调整本模块 HTML、JS、README，不改公共组件、相邻页面或演示业务模型。

入口：[借贷广场.html](借贷广场.html)。默认英文、游客态；页头切换语言，默认关闭的演示工具切换资产方/资金方、审核结果、逐张执行结果、时钟和失败情形。演示数据保存在本地，工具内可重置；不发生真实链上操作。

## 本轮调整

| 页面 / 功能 | 最终表现 |
| --- | --- |
| `/marketplace` · P-LS-01 | 每行一个融资项目，突出项目名称、编号和所有者；项目池展示有效质押价值、总张数、计入/不计入覆盖张数与覆盖状态；当前需求单列展示金额、编号与状态，已放款/结束的历史需求不冒充当前需求 |
| 列表交互 | 整行点击、Enter 或空格进入详情；无操作列。币种/期限/关键词、更多筛选、排序、桌面定高内滚、吸顶表头、每批 20 条沿用现有约定；返回保留查询及加载位置 |
| `/project/{id}` · P-LS-02 | 标题、项目状态之下分为左侧信息与右侧操作；左侧五统计、质押清单、融资清单和操作记录独立滚动，右侧操作不跟随滚动 |
| 操作区 | 全局 / 融资 / 还款为内嵌标签页；支持方向键、Home、End。窄屏操作区置于信息上方，信息仍独立滚动；切换标签保留左侧位置 |
| 质押审核 | 仅所有者的全局标签显示“质押审核与结果”按钮及在途申请数；弹窗承载申请、审核历史、逐张执行结果。首次打开从顶部阅读；费用确认取消返回审核，关闭审核回到入口焦点 |
| 质押代币清单 | 编号下展示交易 Hash，点击复制完整值；相邻外链图标独立在新窗口打开区块链浏览器。移除“查看凭证”按钮；复制被拒时给出可选择的完整 Hash |
| `/project/new` · P-LS-03 | 保留建池、首笔申请、零费用提交和直接进入草稿详情的既有流程 |

公开清单与所有者私有审核/执行信息保持隔离。期限和利率仅展示当前需求已有数据，缺失值不推算；项目有效期不作为融资期限。示例 Hash 沿用已标注的演示数据，外链不代表存在真实交易。

## 继承的布局参考与边界

旧版实际分支名为 `cly-V1.0.0`，参考头 `ea56882a2d5c3680ed7220b9e1feb2a0d8fcb652`，目录 `financial-service-platform/prototypes/借贷广场-融资需求与代币质押/`，包含 `ls-module.js`。前轮已在浏览器打开两例详情，核对页头、五统计、信息清单和三段操作。此次按最新用户反馈将三段改为标签、审核移入操作弹窗，不再沿用“左侧审核首位”的旧安排，不复制旧版代码或 DOM 错位。

原型仍覆盖资产方建池/追加、审核撤回/驳回/失效、两次费用确认、逐张成功/失败、两自然日审核及执行超时、迟到回写冻结、发布/改额/撤下/关闭、解除质押预扣及失败回滚、终态提取、覆盖变化和主体权限。功能范围对应四册 F-LS-01～21（15 作废）；本轮未修改 PRD。

签名承载、主体映射、正式编号登记、对外审核承诺和消息分类保持未确认边界。授信报价现由下述 L6 增量接入。运营审核、真实接口/链上签名与费用、放款/还款、控制台和通知投递仍只提供演示事件或交接入口，不声称跨模块联调或生产验收完成。

## 源码与单文件附件

- `借贷广场.html`：共享壳层挂载点、模块布局与样式。
- `借贷广场.js`：页面、交互、双语文案、路由适配及评审工具。
- `演示数据.js`：独立演示业务模型，本轮未修改。

导出附件内联共享依赖；生成件不作为第二份源码入库：

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/借贷广场/借贷广场.html \
  --output /tmp/借贷广场-项目与操作修订.html
```

## 实际浏览器核验

2026-09-20 使用 Chromium 149.0.7827.55 实际打开模块源码和最终导出附件。核对默认无 hash、`#/`、共享导航进入列表，非编号单元格点击、整行键盘操作、返回与浏览器后退、20→24 条加载、当前需求筛选和历史需求隔离。

验证左右滚动独立、标签语义与键盘切换、审核弹窗位置及往返焦点、复制完整 Hash、复制失败回退、外链新窗口目标（拦截外站响应，不验证演示交易真实性）。回归两次费用确认、部分成功与失败费用、改额校验、审核超时零费用、执行超时逐张费用、迟到回写冻结、解除质押失败回滚和终态限制。

中英文 × 游客/资产方/资金方 × 375/414/768/1024/1280/1440/1920 宽度，共 84 个列表/详情组合，检查横向溢出、私有信息隔离与操作可见性。内部截图复核桌面列表、桌面/移动详情和审核弹窗；本轮不交截图附件。未做 Safari/Firefox、真实移动设备或屏幕阅读器验证。
