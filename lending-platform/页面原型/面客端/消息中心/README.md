# 面客消息中心原型

后续原型完善以面客消息中心当前三册为依据：[主PRD](../../../PRD/v1.0-面客消息中心/01-面客消息中心-PRD.md)、[流程图与状态机](../../../PRD/v1.0-面客消息中心/02-面客消息中心-流程图与状态机.md)、[消息通知](../../../PRD/v1.0-面客消息中心/03-面客消息中心-消息通知.md)。三册只定义信息需求与业务行为，页面布局与组件由设计决定（见[统一条款](../../../PRD/v1.0-面客消息中心/01-面客消息中心-PRD.md#design-boundary)）。以下历史记录是各轮原型的实现与演示边界，不代表已符合最新需求。

> 2026-09-21 最新返回约定（覆盖下方历史交付中的旧入口描述）：消息详情移除页首和文末重复的返回列表入口，由面包屑返回原列表；无关联操作时不保留空操作区。

## 2026-09-21 · 详情信息与操作层次修订

依据用户截图反馈 `f4020f3f-d08b-4820-ae82-c0a6548f1366`，起始基线 `5e29cd3`，交付前同步 `bc94bd0` 的面包屑返回约定；existing-system adaptation / console，沿用修订入库授权。继承统一 portal 组合、token、消息数据和路由，本轮仅改消息详情与对应组件约定。

- 阅读顺序为标题/消息元信息、正文、业务信息、主要操作。业务区去掉内层灰卡；业务名称与状态成组，编号旁为轻量复制图标（44px 热区及可访问名称），当前环节与步数采用有标签的字段排布。
- 主操作独立收尾，窄屏占满可用宽度；统一通过父级面包屑返回，不恢复页首和底部重复返回。没有关联操作时不留空操作栏。不可跳转原因紧邻禁用按钮；已读失败及重试留在消息元信息下方，避免混入业务操作。
- 本轮 CSS 中操作栏变化限定 `.nc-detail`，管理端复用的 `.nc-actions` 保留原值；无壳层、导航、token、PRD 或其他模块布局改动。所有面客入口及重新导出的附件复用同一消息详情。

Chromium 149 实测：导出附件 94 项断言通过、零脚本异常、零 HTTP 请求；内部查看 375 / 414 / 768 / 769 / 900 / 901 / 1280 / 1440 / 1920 九档详情，均无横向溢出，复制热区满足 44px。覆盖中英文长名称/编号/节点、四种业务状态与未知状态、缺编号/步数、无关联操作、过期消息、内容转义、复制成功/失败反馈、已读失败重试、业务预判禁用/超时、真实项目原型往返，以及筛选列表追加后位置/焦点和浏览器历史恢复。已读及关联权限仍为本地演示，未做真实服务端或 SSO 集成验收。

当前使用统一 `portal-entry.js` 加载，默认游客进入资产广场；查看演示消息可先在默认关闭的演示工具中切为资产方或资金方，再通过铃铛进入。关联跳转使用已组合的项目页并提供返回消息详情入口。下文是历次记录；历史的资产方默认身份、样板中转与独立模块说明以本节为准。

## 2026-09-21 · 默认入口与共享关联修订

沿用用户已确认的 `0a8c4ff` 阅读优化、一次完成及入库授权；本轮基线 `ca4c04b`，母版 HTML 最近变更 `364349d`、内容脚本 `7b21230`。平台 checkout 本轮成功，已核对最新远端。分类仍为 existing-system adaptation / console；不改 PRD，不以通用详情组件替换消息的单列阅读卡。

- 无 hash 打开原型默认资产广场；品牌链接明确指向资产广场，旧根路径与未知深链沿用壳层兜底。显式消息列表/详情深链保持原目标。从铃铛「查看全部」或账号菜单进入消息，顶栏保留三项主导航。
- 消息详情父级面包屑接入模块原列表快照，与既有返回链接共同恢复筛选、已加载范围、滚动锚点和焦点。旧消息对象深链进入关联项目时替换中转历史，修复后退再次被转回项目的问题。
- 保留白底分隔、已读/未读标签、整行打开、全部已读确认、每批 20 条和自然整页滚动。按此组合反向补齐共享组件规范；本轮未修改 CSS、token、登记表、已读数据或其他业务模块。

Chromium 131 项断言通过、零脚本异常：375 / 414 / 768 / 769 / 900 / 901 / 1280 / 1440 / 1920 九档核验入口、列表/详情无横向溢出、面包屑筛选/位置/焦点恢复及浏览器往返；额外检查 100 条返回、全部已读、筛选刷新、旧首页 ID/未知深链兜底、关联项目一次后退、母版与资产广场消息路由。已内部查看九档列表及手机详情。语法及自包含导出通过；历史异常场景未全部重测，真实服务端/SSO 不在本轮验证范围。

共享影响：`sample-pages.js` 仅调整旧 `panel=detail&object=…` 的兼容跳转历史处理，实际项目展示不变；组件规范记录既有组合及例外。消息源码与说明其余变动限定本模块。以下章节为历史交付记录，当前入口及样板关联以本节为准。

## 2026-09-20 · 阅读层次与已读入口修订

依据用户评论 `5931b43e-65fa-4c66-8cd8-6c98c2744492`，在独立整页版本上继续修订；沿用直接交付与入库授权。当前源码基线 `0dd8c7e`，已核对共享壳层的新账户菜单与游客入口，完整保留。类型仍为 existing-system adaptation / console。

- 列表统一白底、明确行分隔，以标题为首要信息，摘要/业务进度为次级文字，分类和含时区时间置于行尾。已读/未读仅用固定位置标签表达；取消逐条「标为已读」按钮和整片未读底色。整条消息是原生链接，支持点击、键盘 Enter 和新标签页打开；详情加载成功后自动已读。
- 状态改为「全部消息 / 未读 / 已读」直接切换，与分类继续叠加；保留「全部已读」及当前筛选范围的二次确认、失败重试。列表首屏加载改为与消息行匹配的骨架；铃铛预览同步标题优先及状态标签，详情统一阅读层次。
- 独立整页滚动、20 条追加、URL 筛选、已读一致性、详情返回位置和焦点保留。共享改动只增加 `base.css` 消息组件样式，并更新组件规范；未改 token、共享路由/壳层运行时和其他业务模块。

本轮实际验证：Chromium 136 项断言通过、无脚本异常。九档宽度 375 / 414 / 768 / 769 / 900 / 901 / 1280 / 1440 / 1920 检查无横向溢出、整页滚动、状态标签、整行进入、详情回顶及返回定位/焦点；内部查看列表、详情、中英文与铃铛预览。专项覆盖 Enter 打开即读、未读筛选下已读行原位保留、叠加筛选刷新、五批 100 条返回、全部已读取消/失败/重试、读取失败重试、跨标签同步、追加失败重试、加载/空/无结果/权限状态、资产方与资金方，以及母版和资产广场共享消息路由。真实后端、SSO、事件时效和全部历史业务预判异常未做本轮集成验收。

**PRD 待同步差异（用户已裁定，无需再选方案）：** 分册 01 §11.5 的列表行内显式已读入口、列表线框图与文案表，以及分册 02 `AC-NC-54`，应移除该入口；单条通过成功打开详情转已读，集中处理使用「全部已读」。幂等、跨标签同步、当前筛选全集范围与读后原位规则继续有效。本轮不代写四册 PRD；以下旧版验证中的「单条已读」指历史覆盖，以本节新入口为准。

基线：`PRD/v1.0-面客消息中心/` V1.0 四册（`b0284c3`）；从消息原型 `65a22f2` 修订，共享层核对至 `5c6ad85`，入库前核对 `f8cf33b`（新增借贷广场局部调整，无共享改动）。按用户「直接输出页面原型」合并剩余设计阶段，并依据「消息中心不是内嵌页面，你自己判断怎么展示用户体验最好」改为独立整页。分类为 existing-system adaptation / console，继承面客端 asset · portal 画布，默认英文，可切中文。

## 使用与维护

打开 `消息中心.html` 默认进入资产广场，默认资产方演示账号。通过铃铛「查看全部」或账号菜单进入消息列表；直接打开 `#/notifications` 或 `#/notification?id=…` 仍进入对应消息页。右下角「演示工具」默认关闭，可切换资金方、游客、受限会话、页面状态及异常场景。所有消息、类型、分类和办理节点都是虚构演示数据，不是业务触发登记表。

页面登记：`P-F-MC-01` → `#/notifications`，`P-F-MC-02` → `#/notification?id=…`。不新增顶栏主导航。筛选写在 hash 查询串；所有宽度均随整页滚动，不设列表内滚。桌面筛选栏吸顶于平台顶栏下，≤900px 随页面流动以节省阅读空间。列表末尾可返回筛选。详情采用独立阅读页，打开时回顶，返回恢复筛选、批次、原消息位置及焦点；追加消息或切换语言按可见消息锚点保持位置。

加载规则仍为每批 20 条、点击「加载更多」、不自动触底加载。PRD 加载规则无需修改；应撤销 Stage 1 与首次交付说明中「桌面定高内置滚动区」的旧默认方案。此变化是页面呈现设计，不是新增业务规则。共享组件规范已同步该例外，资产广场等其他页面的内置列表约定不变。

源文件：模块 HTML（骨架与局部组合）、`消息中心.js`（页面和状态）、`演示数据.js`（分类、类型、双语模板、演示记录）。公共 token / 样式 / 壳层来自 `_shared/`，不手改导出附件。本轮把原 HTML 中的消息页面样式归入 `base.css` 的 `.nc-*` 作用域，使母版和资产广场装载同一消息模块时也采用一致布局；没有修改共享 token、壳层 JS、登记表或其他业务模块。

```bash
python3 _shared/export.py --source 面客端/消息中心/消息中心.html --output /tmp/借贷平台-面客消息中心-prototype.html
```

## 共享改动及影响

- `shell.js` 持有面客消息快照、归属过滤与未读计数；顶栏改为铃铛→语言→账号，含最近 10 条快捷面板与账号入口。零未读不画角标，超过 99 显示 99+。管理端入口保持原行为。
- 路由支持查询串，登记表新增两页且不进入主导航；消息两页切换保留列表批次。加入可选模块组合、渲染前后钩子与演示区扩展，使已登记页面复用同一个消息模块；未使用钩子的页面保持原接口。
- `base.css` 增加消息面板/元信息组件，不改 token。视觉取值直接复用：主色 `#1D4ED8`、页面底色 `#F7F8FB`、面客控件 44px、正文 15px、卡片内边距 22px、圆角 12px。
- 母版内容脚本维护在 `_shared/sample-pages.js` 单一源，当前提供广场、控制台及管理端示例；首页不再登记或作为入口。消息附件引用它，未知深链回资产广场。关联对象由样板兼容旧对象地址并进入独立项目页，不代替真实目标业务模块与鉴权集成。
- 母版与已入库资产广场增加同一消息模块的加载，资产广场仅延后统一启动并暴露原页脚回调，原业务内容/筛选/加载规则不改。影响面为全部面客页顶栏；管理端不接入面客消息池。同期新增的登录独立原型未装载消息模块，保留其自有消息演示入口协议；本件回归了登录入口、focus 样板与补全条，未把登录模块的业务流程合并到消息附件。共享变更后旧的离线附件须重新导出。

## 验证与边界

本轮在 Chromium 实际打开导出 HTML，103 项浏览器断言通过，无 JS 异常。375 / 414 / 768 / 769 / 900 / 901 / 1280 / 1440 / 1920 九档检查列表与详情、滚轮只推动整页、桌面吸顶/移动端自然流、详情回顶、返回链接及浏览器前进后退定位；列表与详情均无横向溢出。已查看九档列表截图及桌面/手机详情、中文滚动状态。

专项检查：加载五批 100 条后的浏览器返回、返回筛选、筛选重置批次并刷新保持条件、单条/全部已读（覆盖未加载匹配项）、跨标签已读同步、追加消息和切换语言后保持可见行、有效/未登录/受限/未知深链；母版及资产广场的整页消息列表和详情返回。源码语法与离线导出检查通过。失败重试、过期、三类业务失效和超时等未修改场景沿用首次交付，不计入本轮重测范围。

这是可操作的前端原型。已读、轮询与跨标签同步使用本地演示状态；存储受限时本页仍可操作。真实鉴权/SSO、事件到达 60 秒 SLA、服务端幂等去重与越权防护、刷新不保活及真实后端异常未做集成验收。演示中的受限引导只交接账户必办事项，不实现账户模块。

本期不做：发送/发布消息、订阅偏好、跨平台聚合或跳转、标为未读、删除/归档、完整步骤条、富文本、待办计算。
