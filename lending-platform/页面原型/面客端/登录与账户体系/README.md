# 登录主干、资产方 SSO 与资金方账户 · 页面原型

Stage 4，`existing-system adaptation` / `console`，继承面客端官网型画布。用户 2026-09-18「直接输出页面原型」授权合并 Stage 2–4；公共层补齐及规范修订范围见 WS-348 的 09:03 评论。

基线为 `lending-platform/PRD/v2.0-登录与账户体系/` 中登录主干与资产方 SSO 的六册（`ce68fa8`，与 08:45 附件一致）；入口及资产方卡片文案以本说明末尾的最新反馈为准。默认英文，中英切换；双击源 HTML 从游客壳开始，右上角「登录」进入身份选择。样例身份、协议和响应均为演示数据。

本轮读取底座头为 `c7ef580`，交付前合入并复核 `7ed4d41` 的继承规范更新；原样板最后修改版本为 `d199040`，本次在其上追加 focus。

| 页面组 | shell / archetype | 内容增量 |
| --- | --- | --- |
| P-L02（含首页壳承载） | site / portal，官网首页和顶栏工作区 | 登录 / 入驻入口与游客默认；业务广场正文交接 |
| P-L01 | focus，居中卡片 | 对等双身份路径卡 |
| P-L03、P-L04、P-L10、P-L11、P-L13 | focus，居中卡片 | 中转、错误出口、协议和受限会话 |
| P-L12 | portal，公共卡片 + key/value 详情组 | 只读身份与可写偏好分区 |
| DEMO-L-GATE | portal，工作区内容卡 | 本模块演示页，只有融资入口与判定结果 |

## 维护与导出

- `登录与账户体系.html`：沿用底座容器、仅定义登录域的局部组合样式。
- `登录与账户体系.js`：追加本模块流程页登记、组件内容与交互；共享路由、语言、弹层、Toast 和演示面板宿主。
- 公共层唯一来源为 `../../_shared/`。不手工维护内联副本，不修改 `registry.portal.js` 的正式业务页。

在 `lending-platform/页面原型/` 执行：

```sh
python3 _shared/export.py --source 面客端/登录与账户体系/登录与账户体系.html --output /tmp/借贷平台-登录主干与资产方SSO-页面原型.html
```

生成单文件离线附件；生成件不入库。导出无外部依赖，不发网络请求。

## 评审入口与覆盖

| 覆盖 | 进入方式 | 可检查状态 |
| --- | --- | --- |
| P-L02 游客壳 / P-L01 / C-L01 | 默认游客视图；顶栏登录；演示融资入口触发受限动作 | 同一组对等身份卡、记忆快捷入口、钱包不可用 / 取消、授权中心不可达 |
| P-L03 / P-L04 | 离站弹层「切换身份」；演示工具「主干不可继续」 | 清除身份记忆、返回选择；可重试及游客出口 |
| P-L10 / P-L11 / P-L13 | 默认关闭的演示工具选择授权结果并运行，或离站边界模拟返回 | 回调处理中、首登协议、协议加载 / 保存失败与重试、唯一复选框、正文查看及下载、受限会话路由拦截、建号失败双出口 |
| P-L12 | 已登录账号下拉「账户设置」 | 暂定四项只读、缺失项整块隐藏、加载及重试、语言 / 通知偏好、保存反馈、退出 |
| C-L02 / C-L04 | 硬卡点、常驻条、账户跳链 | 未认证 / 驳回引导、目标平台告知、返回、键盘焦点约束 |
| C-L03 | 模拟资产方未认证；队列工具 | 壳层占位、不堆叠、计数清单、优先级、折叠可见、补齐接替、会话结束重置 |
| F-L31 演示页 | 工具「打开融资硬卡点演示」 | 游客 / 未认证引导、已认证实时复核等待、复核失败重试、旧快照已认证而实时结论失效仍拦截 |
| 回跳与会话可见结果 | 工具选择站内有效回跳 / 当前跨平台兜底；到期 / 上游登出 / 主体不可用 | 重新执行原动作前复核、首页 Toast、原页就地降级、无登录墙、无表单草稿保存 |

硬卡点页面只在本模块登记为 `DEMO-L-GATE`（`#/demo/login/financing`），明确为演示页；未占用 `P-MC-01`，未落入「我的控制台」目录。其他业务导航保留系统位置，交接边界在评审工具中说明，不实现其他模块正文。

## 边界

- `Q-L01` 仍未关闭：账户页四项暂定、已在页面标明，不作为字段验收依据。当前跨平台回跳默认走首页兜底；「有效站内回跳」仅为独立响应场景，不意味着已放行跨平台回跳。
- 资产方离站、钱包连接都只演示交接边界，不调用真实外部系统；不复刻授权中心 / 钱包界面。资金方延伸现已由同目录 `资金方账户.js` 接入；其范围与限制见下方 WS-349 交付说明。
- 本原型覆盖 24 个功能点中适用的页面、组件与用户可见结果。服务端句柄、白名单判定、一次性授权、会话 / 权限强制、实时上游复核、登出通知、审计和告警未实现，也不能用此原型验证。
- 权限矩阵的待填格、业务字段级脱敏梯度、机构审核端、三平台真实域名不在本次范围。其他业务页由对应模块交付。

## 公共层增量

`base.css` 仅追加 focus 和 C-L03 样式；`shell.js` 仅追加 focus 渲染与 C-L03 队列 / 折叠 API；`页面继承参考.md` 增一条居中卡片 archetype；`样板/底座样板.html` 增容器及 `#/sample/focus` 样例；`面客工作区组件规范.md` 仅修订第 3 节：游客按矩阵展示，受限控件仍可引导，不放常驻登录条，补全条仅用于已登录缺失信息。原有 site / portal / app 规则无删除、无改写。

登录模块按 SSO PRD 将 Toast 放在顶部居中；此局部样式不改变其他模块 Toast 位置。弹层沿用公共宿主，键盘约束与焦点归还在模块交互中补齐。

## 实际验证

Chromium 的 file:// 单文件检查通过：73 项交互 / 布局断言；375 / 414 / 768 / 1280 / 1440 / 1920 宽度无内容溢出，主导航窄屏沿用底座横向滚动。另检查 768/769、900/901 弹层边界、键盘限制动作与焦点归还、存储被拒绝、未保存偏好丢弃、有效站内回跳自动重执行。未见脚本错误或网络请求。

共享回归覆盖样板 site / portal / app、追加 focus 及资产广场现有模块；共享 CSS/JS 的 diff 只有新增行。上述为原型浏览器验证，不是服务端安全、SSO 联调或完整 PRD 验收。


## WS-349 · 资金方登录与账户体系

Stage 4 · existing-system adaptation / console（沿用官网型面客壳）。用户评论 `2992cde2-c975-449f-9054-cc78c082ddd0` 授权直接输出原型、合并设计阶段；本次只接入 WS-349。

### 基线与维护

- 04–06 册以 `16e6ec5` 为准，07 册以 `5b91c83` 为准；指定四份附件逐字节与仓库一致。登录主干沿用同目录 WS-348。
- 仓库读取头 `fb89a129ac740585599d320c70a7b7df99870b1c`。保留 WS-347 `d199040` 的视觉与滚动约定，同时读取当前公共层；样板当前最后修改为 `65a22f2`，包含 WS-348 focus 壳和 WS-356 组合接入。
- 只读参考 `cly-V1.0.0 @ ea56882` 的面客组件规范及国际化基线。公共视觉值来自 `_shared/tokens.css`、`base.css`：主色 `#1D4ED8`，页底 `#F7F8FB`，控件 44px / 9px 圆角，卡片 12px / 22px 内边距，正文 15px。未另建 token、导航或公共运行时。
- 本次新增 `资金方账户.js`；同目录 HTML 增加流程组合样式与脚本引用，原 JS 增加资金方分支挂载。四个源文件变化（包括本说明）；公共层和其他模块不改。

| 页面/组件 | 继承与内容增量 | 状态覆盖 |
| --- | --- | --- |
| P-L20 / C-L20、C-L22 | focus 居中卡片，连接后的地址、用途、保管提示、协议和签名 | A–E 五态；拒绝、过期、地址切换、等待与重试 |
| P-L21 | portal 公共卡片、步骤条、辅助摘要 | 邮箱验证、演示机构模版、完整核对、草稿、失败与重复提交防护 |
| P-L22 / C-L21 | portal 详情组、时间线、共享补全条 | 草稿空态、待审核只读、驳回重提、已认证变更降级、历史只读 |
| P-L23 / C-L23、C-L24 | portal 账户区块、锚点、公共账号下拉与弹层 | 联系邮箱再认证、发送/校验异常、取消、语言时区、会话失效 |
| 主干与跨模块接缝 | 保留 WS-348 身份选择、SSO、协议与共享提示条；独立 DEMO-F-GATE 只演示权限/回跳 | 游客与 L0–L3、有效回跳重执行、无效回跳兜底、钱包连接态和会话解耦 |

本期不做：密码或邮箱登录、钱包更换/解绑、多钱包账号、账号恢复、机构模版定稿、运营审核页面与真实审核结论产生、正式协议正文。机构资料模版、上传限制、客服和审核时长仍依赖 WS-358/WS-361；读取 WS-358 最新附件确认其未定边界，演示字段和材料不作为正式采集标准。

### 试用

默认游客首页，右上角登录 → 资金方入口 → 演示连接结果 → 协议勾选 → 演示签名结果 → 机构注册。外部响应弹层明确标为演示，未仿造钱包界面。默认验证码 `123456`；同一流程每次重发递增，最新值可在默认关闭的演示工具查看。注册步骤 2 使用演示模版；可选择任意本地演示文件来观察上传反馈，仅模拟文件名与状态，不上传或保存文件内容。

右下角演示工具可选择下一次连接/签名/邮箱/提交结果，以及草稿、审核中、已驳回、已认证样例。审核返回仅在已有待审核申请时生效。邮箱弹层前选择异常响应；关闭弹层后旧邮箱保持不变。请只输入演示数据：草稿与演示账户存于本地浏览器，工具提供重置；真实持久化和资料隔离需服务端实现。

导出（在本仓库根执行，输出到仓库外）：

```sh
python3 lending-platform/页面原型/_shared/export.py \
  --source lending-platform/页面原型/面客端/登录与账户体系/登录与账户体系.html \
  --output ../借贷平台-资金方登录与账户体系-页面原型.html
```

### 验证与限制

随 issue 附件提供实际 Chromium 浏览器证据、检查结果和限制；原型单文件以 file:// 测试，默认无网络请求。宽度覆盖 375 / 414 / 768 / 769 / 900 / 901 / 1280 / 1440 / 1920，中英双语。原有 SSO 分支保留并做针对性回归；本次不重验其全部历史测试。

所有钱包、签名、nonce、邮箱投递、上传、审核回写、回跳授权、唯一性/限频/权限与会话都只是本地模拟。不能以原型测试代替服务端安全验收、真实钱包兼容性、邮件投递或机构资料正式验收。账户通知为本模块事件样例；完整消息中心仍由对应模块维护。区块浏览器仅在用户主动点击确认后的外链打开，默认不联网。

## 2026-09-20 · 登录入口反馈修订

Stage 4 · existing-system adaptation / console。按用户评论 `b4321266-7ef1-45a1-9a16-8c6d9c47a330` 的两项反馈，沿用 `441ca0e9` 的阶段合并与直接推送授权；本轮读取头为 `eed8881`，样板沿用 `65a22f2`，复用最新公共层及上述 WS-349 页面。

- 公共层仅改 `shell.js` 的游客工具区：保留语言切换，身份入口只留「登录」，取消「申请入驻」；`design-system/面客工作区组件规范.md` 第 2、3 节同步此口径。未改 token、CSS、registry 或其他模块源码。
- 资产方卡片在整页与引导弹层共用同一文案，逐字替换为「我持有资产，想发起融资。将前往资产可信平台登录。」；英文同步为 “I hold assets…”。
- 资金方继续调用 WS-349 的连接、签名与 `/funder/register`、`/funder/status` 页面，无新增页面。仅在资金方已登录且为草稿态时，补全提示和账号菜单提供「申请入驻」。新用户签名验证成功后建号并直接进机构注册；老用户保留账号、草稿及审核状态，已提交/驳回/认证分别查看原状态。账号创建不改变机构审核结果，原有有效回跳优先级不变。
- 修复共享禁用动作守卫更新带来的兼容问题：模块内受限融资按钮通过鼠标或键盘只打开身份/补全引导，不执行业务，不修改公共禁用规则。

### 历次反馈待同步 PRD 位置（集中清单）

均在 `lending-platform/PRD/v2.0-登录与账户体系/`；本轮列出差异，不代改 PRD：

| 文件与位置 | 与本轮反馈的差异 |
| --- | --- |
| `00-登录主干-01-入口与回跳.md` §2.1 `P-L01`、§2.2 最后一行 | 仍把「申请入驻」定义为登录前进入身份选择的另一个标签；应限定为资金方登录后进入既有机构注册流程 |
| 同册 §2.3 卡片 1 中英文说明 | 仍为「我持有应收账款」/ “I hold receivables”；页面按新文案替换 |
| `03-登录主干与资产方SSO接入-流程图与状态机.md` 图 1 | 游客分支仍为右上角「登录」/「申请入驻」双入口 |
| `06-资金方登录与账户体系-验收需求说明.md` §1.3 `AC-L411` | “全链路任一环节”禁止「申请入驻」标签，应澄清只约束登录前链路，避免误禁登录后的申请入口 |
| `05-资金方登录与账户体系-功能需求说明.md` §1.3 `F-L51-1`、§3.5 `F-L61-2`；06 册 `AC-L406`、`AC-L432` | 呈现补充：勾选行改为「我已阅读并同意《平台协议》」，协议名行内预览且不能改变勾选；签名主按钮居中。签名与提交前仍须显式同意，不改变同意门槛 |
| 05 册 §3.4 `F-L60-1`；06 册 §3.4 `AC-L430`～`AC-L431` | 本轮用户授权新增金融机构资料：机构类型、注册地址必填；监管机构、金融牌照编号选填。字段及必填性需同步 WS-358 所有的正式模版。未增机构准入、牌照有效性或审核政策 |
| 05 册 §3.4 `F-L60-2`、§3.5；06 册 §3.4 | 上传交互补充：选中后展示名称、大小、上传状态；支持替换、删除、失败重试，取消替换保留原文件。失败/中断后刷新不能当作上传成功；格式、数量和大小限制仍由正式模版决定 |
| 05 册 §7.2 `F-L73`（偏好区、账号下拉入口）；06 册 `AC-L460`、§6.2 `AC-L455` | 原文未要求保存偏好或页内退出按钮。本轮明确为偏好选择即保存，成功/失败反馈，失败回退，刷新保留；退出仅保留账号菜单。需补交互验收，不是更改退出会话语义 |
| 05 册 §7.2 `C-L23`、§4 `P-L22`；06 册 §4 | 账号菜单原型标签统一「用户信息」：未提交进入既有 `P-L21`，已提交/驳回/认证进入既有 `P-L22`，展示基本资料及认证状态；账户设置仍不放机构资料。此前菜单中的「申请入驻」标签被本轮反馈替换，补全条上的申请入口保留 |
| 05 册 §4 `P-L22`；06 册 §4 | 2026-09-20 用户布局反馈：状态页改为上下分区，移除独立审核进度侧栏，保留审核状态、提交与审核时间。驳回原因和「修改并重新提交」提前至首屏；历史提交仍只读。属于呈现修订，不变更审核或重新提交规则 |

`D-L17` 的连接/签名分离与 `F-L57` 的建号后进入机构注册不冲突，继续沿用。文案放宽不代替业务资产范围的 PRD 决策。

### 跨模块接缝仍未完成

- `_shared/shell.js` 的通用 `signin` 仍是切换资产方演示身份的样例动作；资产广场、借贷广场及独立消息中心尚未装载登录模块。因此共享顶栏外观已经统一，但这些独立业务原型的登录按钮尚不能串起真实的身份选择/资金方登录回跳。
- 本模块 `登录与账户体系.js` 的 `login-handoff` 及主导航捕获仍停在其他模块交接提示；登录结果、机构完备度与原业务动作尚未跨原型共享。资产广场详情的 `deeplink / cta`「申请入驻」及母版首页正文的申请按钮也仍属既有占位入口，需对应模块接入登录态后统一。
- 本附件包含当前 WS-349 既有源码，完整演示模块内新老资金方路径；真实钱包、SSO、审核、后端会话和跨业务模块联调仍不属于此离线原型验证。

### 本轮实际验证

Chromium 以 file:// 打开导出单文件，130 项检查通过：顶部入口、整页/弹层逐字文案、资产方离站提示、连接与取消签名不建号、签名后建号进入机构注册、老资金方草稿/审核中/驳回/认证状态保留与权限区别；375 / 414 / 768 / 769 / 900 / 901 / 1280 / 1440 / 1920 下身份页、签名页、机构注册无页面横向溢出。查看了 375 和 1440 的身份页与机构注册截图。另核对登录源页、资产广场、借贷广场、消息中心和底座样板的游客顶栏。未见脚本异常或远程资源请求；未做真实钱包/邮件/SSO/审核联调，未重验全部历史功能。

## 2026-09-20 · 桌面四项反馈修订

Stage 4 · existing-system adaptation / console，按 `b368a0c5` 用户原文及三张截图执行，沿用阶段合并、直接提交授权；读取与改动基线均为 `83c9b3b`。本轮范围是桌面 Web（1280 / 1440 / 1920），不将上轮移动端结果当成本轮验证。

只改本目录 `资金方账户.js`、`登录与账户体系.html`、本说明。沿用 WS-349 原页面、共享 token、按钮、输入框、提示与卡片，不新增公共组件或页面。

| 反馈 | 本轮结果 |
| --- | --- |
| 协议与签名按钮 | 登录及提交勾选行复用同一行内协议组件；链接与 checkbox 标签独立，预览不代为同意；签名动作组居中 |
| 金融机构信息与上传 | 基本资料两列分组，注册地址跨行；新增机构类型、注册地址（必填），监管机构、金融牌照编号（选填），进入草稿和提交快照。上传区展示文件名、大小和状态，可替换、删除、重试，迟到上传回调不恢复已删除文件 |
| 账户设置 | 去掉「保存偏好」及页内「退出登录」。语言/时区选择即持久化并反馈，存储或模拟保存失败时保留原选项；账号菜单继续可退出 |
| 用户信息 | 四种状态下菜单文案统一；草稿去机构注册，审核中/驳回/已认证去已有资料和认证状态页 |

偏好及文件状态使用浏览器本地存储演示，实际刷新和重新登录后恢复偏好；不伪造后端会话恢复。文件仅保存文件名、大小和模拟上传状态，不保存或上传文件内容。协议正文仍为既有演示内容，不新增法律正文。跨业务模块登录与回跳缺口仍见上节，两轮 PRD 差异已合并到上方集中清单。

实际 Chromium 验证：64 项主流程检查与 11 项边界检查共 75 项通过。覆盖 1280 / 1440 / 1920 的签名居中、机构表单/上传/偏好区无横向溢出；协议鼠标与键盘预览不改变同意；四种用户信息状态路由；新增必填阻止缺项提交、选填为空可提交、资料快照回显；文件替换/删除/失败重试、上传中删除、取消替换、失败后刷新；偏好即时保存、模拟失败和实际拒绝存储时回退、刷新与重登恢复。导出单文件无远程请求、无脚本异常；已查看随交付的四张 1440px 桌面截图。本轮未执行移动端或真实后端联调。

## 2026-09-20 · 注册状态页布局修订

Stage 4 · existing-system adaptation / console，按用户 `8cfea852` 反馈继续修订，读取头 `425401a`，沿用已授权的桌面范围与提交方式。

状态页依次展示状态反馈、提交信息、机构资料，去掉独立审核进度侧栏；提交和审核时间仍可查看。驳回原因与加宽的「修改并重新提交」主按钮集中在首屏，继续进入既有注册流程；审核中不显示修改入口，历史版本只读。仅修改本目录的 HTML、资金方 JS 和本说明，不变更共享层或审核规则。

Chromium 离线原型 49 项检查通过：1280 / 1440 / 1920 的上下布局、首屏主按钮、状态对应动作、键盘进入修改、重新提交回到审核中、资料快照更新、历史版本状态与只读、中英显示、草稿原流程。无页面横向溢出、脚本异常或远程请求；截图仅用于内部视觉核验，本轮只交付自包含 HTML。


## 2026-09-20 · WS-358 审核衔接

机构审核原型复用本模块 P-L21/P-L22/P-L23。新增机构标识类型、字段级驳回原因、提交版本材料快照和本地审核回写接口；审核中的材料写入被拒绝。原 HTML 内样式原样提取到同目录 `登录与账户体系.css`，两端共享引用；独立登录入口保留正常启动，组合入口使用 `CF.deferBoot`。

审核模块的具体范围、演示模版假设与真实服务限制见[机构认证审核说明](../../管理端/资金方机构认证审核/README.md)。原有登录与账户邮箱验证流程保持。
