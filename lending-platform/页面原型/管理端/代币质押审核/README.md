# 代币质押审核 · WS-359

`existing-system adaptation · console`，基于 WS-359 V1.2 两册、WS-351 V1.1 四册（`d58d2e7`）。按用户评论 `3732d02c-d9a8-4cd0-a2e7-d0b4a1e7bb1f` 的“无需等我确认，直接入库”授权合并后续设计阶段。初始共享底座读取于 `4b07c5e`；提交前同步至 `641d4ff`（共享层最近更新为 `6ebc291`），保留面客及运营消息模块的并行成果。

## 打开与源码

直接打开 `代币质押审核.html`，默认进入英文审核队列，右上角可切中文。

- `代币质押审核.html`：沿用管理端 `app` 壳层、共享样式，仅含模块局部组合样式。
- `代币质押审核.js`：队列、独立详情、处置弹窗、状态与校验。
- `演示数据.js`：本地示例记录，无真实企业或业务数据。
- 路由登记于 `../../_shared/registry.admin.js`：`P-O-PR-01` `/ops/pledge-reviews`、`P-O-PR-02` `/ops/pledge-reviews/detail?id=…`。
- 登记表只在入口声明 `data-module="pledge-review"` 时显示本模块导航，避免未装载本模块的底座样板和相邻原型新增空菜单。

继承 `../../_shared/样板/底座样板.html` 的管理端台账和公共详情组件，使用 `tokens.css`、`base.css`、`shell.js` 的导航、语言、格式化、弹层和状态表面。详情独立寻址，保留批次关联；处置限当前笔。没有新增全局样式、第二套 token、登录流程或面客页面。

单文件交付由共享导出器生成，生成件不回存源码目录：

```bash
python3 _shared/export.py --source 管理端/代币质押审核/代币质押审核.html --output /tmp/WS-359-代币质押审核-原型.html
```

## 评审路径

队列默认待审，按提交时间升序；三项统计按笔计数，沿用共享 `PAGE_SIZE=20` 分页。关键词及审核状态常驻，其他条件向下展开，查询后收起并显示已应用条件；支持 Enter 查询、日期范围校验、重置及返回列表保留筛选。排序选择保留提交时间、等待时长、价值，并提供审核截止排序。

列表采用七列：单笔／代币、资产方、项目、价值、审核截止、审核状态、操作。按用户评论 `b7b51ec0-ff9e-480d-b48f-2c663d2aa5e1` 精简多层副行，批次、申请类型、提交时间、等待时长、执行后续仍可在详情查看。

详情以顶部状态／价值／截止摘要、左侧分区和右侧粘性审核区组织。概览展示申请、代币和已形成的结论凭据；关联申请集中同批及跨批记录，所有查看操作位于最右列；操作记录单独成页签。支持键盘方向键／Home／End 切换页签，打开另一笔回到概览。沿用共享英文默认、中英切换、金额及显式时区格式，不另定业务时区。

查看 `DEMO-PR-001`，上传本笔协议后通过；查看 `002` 填原因驳回，`003` 可继续待审；`004` 是同批已通过示例。按本次反馈已移除“选用同批协议”入口与逻辑。不同笔材料和结论独立，审批不触发链上执行。通过后的协议只读可预览、下载。相关申请保留稳定单笔 ID 和明确再申请关联。

右下角默认关闭的演示工具可切权限、加载／空／失败表面、上传失败、提交失败／并发／撤回／结果待确认、附件失败，并推进到审核截止前一分钟或恰到截止。已生效审核不被其他笔超时回退。结果待确认先查询当前结果，避免重复裁定。

清单切换到“全部”可查看执行成功、失败、处理中、结果待核实、差异、复核作废、未发起过期、审核超时和已撤回记录。执行结果和费用均只读，人工通过历史不随执行变化重写。

## 边界

- 所有记录、上传进度、请求反馈、通知事件和权限均为浏览器内存演示，刷新重置。无网络请求、真实鉴权、文件保管、消息投递、签名、转移或后端定时任务；不能据此宣称跨平台运行联验或安全机制已实现。
- 审核期限模拟连续 48 小时。七天入池日期为演示快照／演示运算；正式业务时区尚未裁定，不作为真实截止计算实现。已发起跨七天／项目终态、执行长期未知的终结及差异关闭责任仍未决，本原型不增加相应收口动作。
- 本期排除异常释放、批量通过／驳回、双人复核、转派、风险评分、代币编辑、代签代付、对账更正、清算和协议模板库。正常解除质押及自助提取继续属于 WS-351；面客同步由 WS-364 承接。
- 附件为自包含 HTML，源码持续复用共享层。桌面验收范围为 1280／1440／1920，不声称窄屏支持。截图仅作内部核验，不随交付上传。

## 内部核验

本次为已授权方向内的布局与交互修订，同步共享底座至 `799687e`，只修改本模块 HTML、JS 及本说明。

本轮已用 Chromium 实际打开离线生成件，检查中英 1280／1440／1920 的队列、展开筛选、详情各分区及通过弹窗；检查页面和控件无横向溢出、关联操作右对齐、页签键盘交互、筛选应用／保留／重置、Enter 查询、排序分页、日期错误及无结果。完整走通本笔上传通过、空文件校验、凭据展示、另一笔材料为空、驳回及记录更新，并回归提交失败保留输入、只读／无权限、审核超时和既有审后状态。未接入后端，以上为本地原型交互验证。

此前版本已核验文件类型／10MB／5个上限、驳回10／500字边界、并发及撤回竞合、结果待确认和确认弹窗跨截止提交拒绝；本轮未改变这些校验与竞合逻辑。

检查包含全部明确的只读审后分支；运行无页面脚本错误，无 HTTP(S) 请求。单 HTML 无外部脚本或样式依赖，源码语法和提交差异检查通过。真实跨平台业务、权限服务、文件服务与消息投递未联验。
