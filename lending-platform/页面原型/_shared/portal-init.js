/* Shared local composition; all service responses remain prototype simulations. */
CF.deferBoot = true;
CF.portalConnected = true;
if (!location.hash || location.hash === '#/') location.hash = '#/assets';
