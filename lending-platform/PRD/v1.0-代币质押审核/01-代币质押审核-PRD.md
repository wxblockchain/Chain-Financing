# 代币质押审核 · PRD

2026-09-24｜运营管理平台 · 运营端｜需求评审稿，真实未决项见第 5 章。

<a id="entry"></a>
## 1. 目标与阅读入口

资产方在已通过审核的融资项目内选定代币，**由该代币的持有地址签名，向质押合约发起质押上链**。上链成功才形成一笔待审质押申请：代币此时已在质押合约内，计入总质押额与待审质押额，但**尚不计入有效质押额**。运营人员在运营管理平台逐笔审核，**通过才计入有效质押额**；驳回后由资产方自助解押。审核**不设时效管控**。

本期包含：单笔审核队列与统计筛选、单笔事实与项目/批次上下文、逐笔通过与协议维护、逐笔驳回与唯一结论、待审撤回与解押的只读追踪、质押上链与占用事实的只读追踪、权限留痕与通知触发。相邻模块负责：面客质押发起与解押办理、项目与额度口径（融资需求与代币质押），项目创建审核与合约部署（管理端合约管理，另行立项），面客站内消息的阅读入口与未读语义（面客消息中心），功能权限矩阵与账号状态（运营端账户与登录）。本期不包含第 [1.2 节](#1-2-本期不包含) 各项。

有效依据（用户已确认，2026-09-24 批）：质押上链由资产方以代币持有地址签名发起，gas 自担、失败不退；上链后先不计入有效质押额，须审核通过才计入；**审核无时效管控**；待审期间资产方可自由撤回，自行向质押合约发起申请、自付 gas，合约退回对应地址；审核驳回后资产方自助解押，路径相同；未解押的代币一直留在质押合约内，不允许用于其他项目质押；运营人员不签名，只做业务操作；先建池并通过审核，才能发起质押。沿用本批未变更的既有口径：同批可有不同结论、逐笔独立；异常一律提示联系平台客服人员，不实现异常处置；业务日期统一 UTC、具体时点按用户偏好时区展示；单笔详情以按发生时间倒序的操作记录承载本笔全部事实；通过在本次办理内一次提交；已上传文件须可预览与下载。

阅读顺序：第 2 章角色、对象与共同口径 → 第 3 章完整功能 → 第 4 章同序验收 → 第 5 章依赖、既定限制与待决。

配套（须共同阅读）：[流程图与状态机](02-代币质押审核-流程图与状态机.md)、[消息通知](03-代币质押审核-消息通知.md)。

<a id="reading-sources"></a>
### 1.1 相邻模块与来源

| 业务来源 | 本模块使用范围 |
| --- | --- |
| [融资需求与代币质押](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md)（WS-374） | 项目与质押的面客口径：[项目审核与合约部署合为一步](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#f-ls-22)、[质押上链](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#f-ls-08)与[自助解押](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#withdraw)、单笔申请标识与提交批次、申请类型取值、[三项额度](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#pledge-fields)的权威定义与派生、[本笔当前去向 `PA-15`](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#pa-15)、[质押状态 `PL-06` 与链上执行状态 `PL-07`](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#pl-06)、[异常的客服边界](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#exceptions)。本模块只定义运营端审核规则，不改写面客规则，也不自行定义额度公式 |
| [字段命名与术语对照表](../00-字段命名与术语对照表.md) | R19 的业务名称、英文对照与释义基线。本册全部业务名称取自该表，不自造同义词；更名只改名称与释义，不改编号锚点 |
| [面客消息中心](../v1.0-面客消息中心/01-面客消息中心-PRD.md)（WS-379） | 面客站内信的阅读入口、未读语义、去重与可见时效框架；本模块只定义发什么、何时发、发给谁 |
| [运营端账户与登录](../v1.0-运营端账户与登录/01-运营端账户与登录-PRD.md)（WS-380） | 运营账号来源、账号状态、会话与功能权限矩阵；本模块只消费 `PM-AL-07`、`PM-AL-08`、`PM-AL-12` 三项，不新增授权管理 |
| [资金方机构认证审核](../v2.0-资金方机构认证审核/01-资金方机构认证审核-PRD.md)（WS-381） | 仅作相邻审核模块参照。其"不分权"、只保留最新材料、面客隐藏审核人员等口径是该模块的例外，不推广到本模块 |

<a id="d-pr-39"></a>`D-PR-39`：本册沿用既有稳定编号 `F-PR-01`～`19`、`D-PR-01`～`38`、`RV-01`～`22`、`AC-PR-01`～`71` 及 `AC-PR-N01`～`N08`，不改写其他模块前缀下的编号；引用相邻模块条款时写业务名称并链接到该模块的具体条目。本批按横向要求 R19 只调整业务名称与释义，**不改任何编号锚点**。本批废止的编号集中列在 [5.4 节](#retired)，废止号不再分配新含义；`Q-PR-01`～`03` 仍不复用。

<a id="1-2-本期不包含"></a>
### 1.2 本期不包含

| 编号 | 本期不做 | 归属 / 理由 |
| --- | --- | --- |
| `X-PR-01` | 链上异常的处置系统：工单、异常待办、自动升级、人工改判或补记、强制解押、补偿、代付、换地址、恢复资格 | 异常仅联系平台客服（[`D-PR-38`](#d-pr-38)） |
| `X-PR-02` | 整批一键通过 / 驳回、跨笔批量裁定 | 审核对象为单笔（[`D-PR-29`](#d-pr-29)、[`D-PR-01`](#d-pr-01)） |
| `X-PR-03` | 双人复核、审核与复核分离、转派、审批链 | 权限矩阵未定义复核环节，本期不预置（[`D-PR-05`](#d-pr-05)） |
| `X-PR-04` | 风控评分、风险灯、建议通过 / 驳回、自动筛出不合格资产 | 线下判定、线上留证（[`D-PR-20`](#d-pr-20)） |
| `X-PR-05` | 运营端签名、代签、代付 gas、代资产方发起或撤销链上交易，或修改代币事实与质押 / 执行状态 | 用户已确认运营人员不签名、只做业务操作；链上动作由资产方以持有地址发起（[`D-PR-11`](#d-pr-11)） |
| `X-PR-06` | 对账更正台、清算、协议模板库、抵押物处置 | 无本期消费方 |
| `X-PR-07` | 跨端身份关联互斥（防自审）、运营机构级数据隔离 | 数据可见范围由功能权限决定（[`D-PR-06`](#d-pr-06)） |
| `X-PR-08` | **任何审核时效**：审核截止、倒计时、到期自动失败、催办、超时通知、超时统计 | 用户 2026-09-24 确认审核无时效管控，相关编号见 [5.4 节](#retired) |
| `X-PR-09` | 项目审核与合约部署、平台钱包维护、代币类型与最高质押率 / 项目期限的参数配置 | 项目审核归[融资需求与代币质押 `F-LS-22`](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#f-ls-22)，参数配置归管理端融资参数配置（另行立项）；本模块只读消费其结果 |

## 2. 角色、对象与共同口径

<a id="design-boundary"></a>
本册只定义信息需求与业务行为；页面布局、组件形态、层级与视觉由 Hallmark 设计决定。本册措辞与设计稿冲突时以设计稿为准，业务口径仍以本册为准。

<a id="d-pr-29"></a>
### 2.1 审核对象与角色

`D-PR-29`【逐笔审核的统一来源】：

| 对象 | 含义与范围 |
| --- | --- |
| 提交批次 | 资产方一次质押发起形成的分组；同一资产方、同一融资项目、同一申请类型及发起时间，沿用面客的提交批次编号与关联信息。批次含一笔或多笔 |
| 单笔质押申请 | 批次内**一张代币的一次质押申请**，**以该张代币上链成功为成立前提**。具有稳定单笔标识，关联批次与代币编号；是审核、撤回、协议、原因、解押和通知的最小单位。标识的编码格式不在 PRD 定义 |
| 单笔审核结论 | 每笔至多一个有效人工结论：通过 / 驳回；时间、审核人、协议或原因各自独立。未审与撤回没有伪造的人工结论 |
| 代币在合约内的留存 | 上链成功起，该张代币留在质押合约内并占用，直到资产方自助解押成功、按记录转回质押前原持有地址。留存是链上事实，与审核结论分开记录 |
| 批次进度 | 只做单笔事实的数量汇总，不再有统管全批的通过 / 驳回结论、审核人、结论时间或原因，见 [2.4 节](#states) |

同一批次内一张代币只能出现一笔，仍为整张质押，不拆数量。上链失败不形成申请，也不进入审核队列；该次 gas 已消耗且不退还，由资产方承担。再次质押是新的发起及新的单笔标识，保留与原记录的关联，不复用原结论。同一代币在不同发起中的记录不可仅凭代币编号合并。单笔审核态属于质押业务申请，不写回代币本体。

| 参与方 | 本模块责任 |
| --- | --- |
| 运营人员 | 按功能权限查看全部单笔；在线下判断后，在线逐笔提交证据与结论。**不签名、不代付 gas、不发起或撤销任何链上动作**；不按运营机构隔离 |
| 资产方 | 以代币持有地址签名发起质押上链并自担 gas；查看每笔结论；待审期间可自行向质押合约发起撤回；驳回后自助解押。解押成功前该代币不可用于其他项目质押 |
| 借贷平台 | 拥有申请、质押记录与三项额度的派生；提供审核结果消费、有效质押价值与有效质押额的重算 |
| 代币发行平台 / 质押合约 | 承载链上事实：质押转入、合约内留存与解押退回；按张提供结果，运营审核无代币维护权 |

本期两个信息域：审核队列（`P-O-PR-01`）、单笔审核详情（`P-O-PR-02`，可查看项目与批次上下文）；编号与归属不变，承载形式由设计决定。保留协议上传、历史、通知与链上事实只读追踪；异常仅提示联系平台客服。

<a id="d-pr-05"></a>
### 2.2 权限与账号准入

<a id="d-pr-06"></a>`D-PR-05` / `D-PR-06`：按[运营端账户与登录](../v1.0-运营端账户与登录/01-运营端账户与登录-PRD.md)交付的功能权限执行，本模块不新增授权管理；管理员与被配置业务权限的专员均可按权限办理。两端账号分开不等于已证明跨端同一自然人绝不自审；本期不新增身份关联互斥或双人复核。

| 能力 | 仅有代币质押申请查询（`PM-AL-07`） | 同时有代币质押审核处置（`PM-AL-08`） |
| --- | --- | --- |
| 队列、单笔详情、项目与批次上下文、协议、留痕 | 可查全部授权业务范围，不按机构或本人分配过滤 | 同左 |
| 上传并正式关联协议、通过 / 驳回 | 不显示处置入口，绕过界面仍拒绝 | 仅当前笔仍待审核时可执行 |
| 改结论、改代币、签名、发起或撤销链上动作、手工确认解押 | 不可 | 不可 |
| 运营站内消息 | 另需运营端消息查看（`PM-AL-12`），不由本模块赠予 | 同左；读消息不表示有处置权限 |

只有处置而无查询权限不能进入本流程，联系建设方核对配置。账号待首登改密、锁定、登录失效或当前无权限时，此前已进入的入口提交及下载同样拒绝。已授权但业务终结的对象保留可读原因；无权入口不展示且不泄露记录存在性。

<a id="d-pr-37"></a>
### 2.3 时间、日期与金额口径

`D-PR-37`【统一业务日期与展示】：

- **业务日期边界**：近七个自然日统计、按日期筛选等统一 UTC；日期筛选需让操作者识别其 UTC 口径。
- **展示**：具体时点按用户偏好时区显示并标注。切换显示时区、跨周末或夏令时不改变已发生事实、统计归属或事件先后。
- **无审核时效**：审核不设截止、不设倒计时、不因时间流逝改变任何审核状态；等待时长只作事实陈述，不构成期限（[`N-PR-02`](#n-pr-02)）。
- **金额**：代币价值为 USD 两位小数，gas 与费用用实际发生币种；未知不填 0，不编造数币精度。

<a id="states"></a>
### 2.4 状态、额度与外部口径

`RV-20`【单笔审核轴】枚举，必有，默认待审核：**待审核 / 通过 / 驳回 / 已撤回**（四类）。通过和驳回是人工结论，撤回是资产方在待审期间自行发起链上解押并成功后形成的终结事件。审核轴不因时间流逝自动改变。审核轴与代币在合约内的去向是两条独立的轴：后者取面客[本笔当前去向 `PA-15`](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#pa-15) 的取值，本册只读消费、不另造一套。

| 对象与状态 | 触发及允许行为 |
| --- | --- |
| 单笔待审核 | 上链成功后未终结；有权限可随时裁定，无截止；资产方可自行发起撤回 |
| 单笔通过 | 在通过办理内一次有效提交且协议齐备；该张随即计入有效质押价值并触发有效质押额重算，运营不得二次裁定 |
| 单笔驳回 | 有效提交驳回且原因齐备；保留记录，该张不计入有效质押额，仍留在质押合约内直至资产方自助解押 |
| 单笔已撤回 | 资产方在待审期间自行向质押合约发起并解押成功；无人工结论，平台不收费，gas 由资产方承担 |
| 本笔当前去向：待审核 | 上链成功、尚未裁定；计入待审质押额的待裁定分项 |
| 本笔当前去向：已计入有效质押额 | 审核通过且满足有效性等既有条件；代币继续在合约内 |
| 本笔当前去向：待自助解押 | 审核已驳回或待审已撤销签名意图但尚未发起解押；计入待审质押额的已驳回分项，不可用于其他项目质押 |
| 本笔当前去向：解押处理中 | 资产方已发起解押、尚无确认结果；不得重复发起，不推定成功或失败 |
| 本笔当前去向：已解押退回 | 解押成功、代币按记录转回质押前原持有地址；该张退出总质押额与待审质押额，按当前资格可再质押 |
| 差异 | 并列事实标记，不覆盖人工审核结论；异常仅客服指引，本期没有差异关闭或恢复动作 |

<a id="d-pr-31"></a>`D-PR-31`【批次汇总】：审核进度仅使用"待审核"（全部未处理）、"审核进行中"（仍有待审且至少一笔已终结审核）、"审核已结束"（无待审）。同时列待审、通过、驳回、已撤回**四类**笔数，合计等于本批总笔数；"审核已结束"不等于全部通过。留存摘要另列待自助解押、解押处理中、已解押退回的笔数，差异为可叠加标记、不重复加总。尚无明确解押结果的张不能算入已解押退回。

<a id="quota"></a>**三项额度与审核动作的关系**（名称与公式的权威定义归[融资需求与代币质押](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md)，编号锚点不变，本册只消费、不自行定义）：

| 额度 | 本册消费口径 |
| --- | --- |
| 总质押额（[`FP-10`](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#fp-10)） | 本项目质押合约内全部代币的签发价值合计，**含失效代币与尚未通过审核的代币**；只作展示，不参与准入。上链成功即计入，解押成功即退出 |
| 待审质押额（[`FP-32`](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#fp-32)） | **已在本项目质押合约内、尚未计入有效质押价值**的签发价值合计，同时涵盖：①审核尚未裁定的；②审核已驳回但尚未解押的。两者对额度的影响相同，故合成一个数值；但两者下一步动作不同（待审可等待或撤回，已驳回需解押），**明细必须能区分这两种状态**，不能只给一个合计数 |
| 有效质押额（[`FP-12`](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#fp-12)） | ＝有效质押价值（`FP-11`）× 项目质押率（[`FP-34`](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#fp-34)）。有效质押价值为同时满足审核通过、实际转入成功、发行平台判有效、无对账差异、未撤回预扣的在池价值；底层资产失效的部分不计入 |

审核动作对额度的可观察影响：上链成功 → 计入总质押额与待审质押额，不计入有效质押额；**审核通过 → 该张从待审质押额转出，在同时满足有效性等既有条件时计入有效质押价值，有效质押额按项目质押率随之重算**；审核驳回 → 仍在待审质押额内（已驳回未解押分项），不计入有效质押额；解押成功 → 退出总质押额与待审质押额。运营审核动作只产生结论，不直接改写代币事实、质押状态号或执行状态号；这些由借贷平台与发行平台维护，本册只读消费，其状态号不出现在界面文案。

<a id="chain"></a>**两处链概念分开表述**：本模块涉及的是**质押链**——代币与质押合约所在链，本期仍为 ETH；质押上链、合约内留存与解押退回均发生在质押链，转回地址即该代币质押前的原持有地址，平台不能人工指定其他地址。**结算收款链**（数币收款地址所在链，本期支持 ETH 与 TRON）与本模块无关，审核不读取、不校验、不展示结算收款地址。

状态图见[单笔审核轴](02-代币质押审核-流程图与状态机.md#st-pr-01)与[本笔当前去向与自助解押](02-代币质押审核-流程图与状态机.md#st-pr-02)。

<a id="2-5-共同质量要求"></a>
### 2.5 共同质量要求

| 维度 | 可观察要求 |
| --- | --- |
| 一致性 | 两端同笔结论、原因、时间、留存状态一致；批次进度由同一组单笔事实汇总，不能因重新取数把某笔归给其他批次 |
| 反馈 | 加载、无数据、筛选无结果、读取失败、无权、上传中、提交中、结果不确定分别可辨；失败不丢当前有效输入，跨笔不串材料 |
| 性能 | 常规数据量下队列含统计及详情 2 秒内可交互、筛选翻页 1 秒内返回；单笔数量与测量场景提测前明确，不声称未经实测达标；上传有进度、不冻结其他字段 |
| 展示 | 时间与金额按 [2.3 节](#d-pr-37)；共享国际化与脱敏口径沿用，不以内部状态代码作界面标签 |
| 可访问性 | 状态有文本说明、键盘可用、错误能定位到具体字段；适配的桌面宽度范围与响应式处理由设计决定 |
| 安全 | 无权用户不可读取申请及附件；绕过界面的写操作亦拒绝；结论和留痕不可篡改，无代币编辑、签名或代付能力 |

## 3. 完整功能

<a id="m1"></a>
### 3.1 单笔审核队列（`F-PR-01`～`05`）

前置为正常运营账号且有代币质押申请查询权限。队列中**一条记录对应一笔已上链成功的代币质押申请**，不是一个批次；同批关系可识别。本队列独立，不依附已移交的代币管理，只提供查看，不提供多选裁定。上链失败的发起不产生记录。

| 内容 | 产品规则 |
| --- | --- |
| 默认 | 单笔审核状态＝待审核，上链成功时间升序；同时间按稳定单笔顺序，重新取数不改变相对次序。不做风险优先排序 |
| <a id="d-pr-14"></a>筛选（`F-PR-03` / `D-PR-14`） | 审核状态（全部 / 待审核 / 通过 / 驳回 / 已撤回）、资产方、项目、申请类型（全部 / 首笔质押 / 追加质押）、上链时间范围、关键词（批次编号 / 单笔标识 / 代币编号 / 企业名）。默认除待审核外均为空或全部；组合生效，起止含边界，起点不得晚于终点；关键词忽略首尾空白 |
| 留存情况筛选 | 全部 / 占用中 / 解押处理中 / 已退回 / 解押结果待核实 / 对账差异，命中相应单笔；不是新增人工审核结论 |
| 须可得的信息 | 单笔标识、批次编号、代币编号、资产方、目标项目、申请类型、该张代币价值（USD）、上链时间、已等待时长、审核状态、留存情况、查看。批次合计不得冒充这一张的价值 |
| 三项统计（`F-PR-02`） | 待审核**笔数**、已驳回待解押**笔数**、近 7 个自然日人工审结**笔数**。应用主体 / 项目 / 类型 / 上链时间 / 关键词条件，不应用状态和留存情况筛选；各项按自身状态定义计数并标明范围。人工审结只计通过 / 驳回 |
| 排序与分页（`F-PR-04`） | 上链时间、已等待时长、单张价值可升降序；带总数常规分页。筛选改变回首页、重置回默认。每页条数沿用平台现行取值，不另造值。返回与后退保留筛选、排序、页码；原页为空回最后有效页并说明 |
| 空与失败 | 待审为空、筛选无结果、读取失败分别反馈；失败可重试，不假报 0。无查询权限不显示入口，直接访问亦拒绝 |

申请类型的取值与派生规则由面客侧定义：按发起时本项目是否已有有效质押派生为首笔质押或追加质押，两者入口、链路与审核规则完全相同，仅用于展示与检索。

`F-PR-05`：从一条记录进入详情只进入该笔单笔审核详情；已结论或终结仍可只读回看。批次含 3 笔只审 1 笔后，待审数减少 1，另 2 笔仍在队列。流程见[质押上链、逐笔审核及两端结果](02-代币质押审核-流程图与状态机.md#fl-pr-01)。

<a id="m2"></a>
### 3.2 单笔事实与批次历史（`F-PR-06`～`08`、`17`）

<a id="d-pr-20"></a>`D-PR-20` / `D-PR-21`：线下完成判定，线上维护证据与结论。须中性说明"本处信息供线下判定参考，结论由审核人线下作出后在此维护"；不提供评分、风险灯、建议通过 / 驳回、自动筛出不合格资产。格式必填、权限与状态校验照常生效。

| 区块 / 字段 | 业务含义、类型、必填与校验 |
| --- | --- |
| <a id="rv-19"></a>单笔定位（`RV-19`） | 稳定文本标识，上链成功即有；关联一个批次、一个代币、一家资产方、一个项目；只读，无默认空值。重新质押生成新标识 |
| 项目与批次上下文 | 批次编号、项目编号 / 名称 / 状态、申请类型、上链时间必有，只读；完整批次清单可查，不能修改；本笔清晰定位，不将批次所有代币列为本次裁定对象。项目须为已通过审核并完成合约部署的项目 |
| 项目参数 | 该项目创建时锁定的质押率、项目期限及其起算点，只读事实；运营在本模块不配置、不调整，存量项目不随参数配置变化 |
| 资产方与项目归属 | 企业名称、稳定主体标识；只读，不以名称替代归属判定。缺失标"待核实"，不得猜测，见[`Q-PR-07`](#q-pr-07) |
| 本张代币事实 | 编号、数量及币种、USD 代币价值、底层账期、买方企业、有效 / 已失效状态；读取同一只读代币域，缺失不用 0 或"有效"填充，不二次折算 |
| 质押链上事实 | 质押链（本期 ETH）、本项目质押合约地址（`FP-33`）、代币质押前原持有地址、本笔当前去向（`PA-15`）、上链时点与质押链上凭证（`PL-08`）；只读，凭证展示口径沿只读代币域，平台不做链上核验。gas 由资产方承担，金额与币种如实展示，未提供标"待核实"，不填 0 |
| 审核信息 | 本笔审核状态（`RV-20`）、人工结论（`RV-03`，存在时）、结论时间（`RV-04`）、审核人（`RV-07` / `RV-08`）、协议或驳回原因；不同笔各自展示 |
| 等待与额度归属 | 已等待时长（`N-PR-02`，事实陈述、非期限）；本张当前计入总质押额与待审质押额的哪一分项，或已计入有效质押价值，按 [2.4 节](#quota) 如实说明 |
| 解押信息 | 解押发起时点、结果与凭据（`RV-18`）在本笔操作记录中作为进度呈现，见 [`D-PR-34`](#d-pr-34) 与 [3.6 节](#m6)。尚无结果标未发起 / 处理中 / 暂无结果，不编造失败 |

`F-PR-08`：同项目同资产方的历史按提交批次可回溯，进入后逐笔看代币、状态、审核人、时间、原因全文及关联的再质押。单笔事件按发生时间倒序；没有人工结论的终结显示撤回及发生时间，不冒充人工裁定。摘要可收起，详情原因必须完整。跨项目再质押仅在有明确关联时展示，不凭代币相同推定关联。

<a id="d-pr-34"></a>`D-PR-34`【操作记录与进度的统一来源，覆盖 `F-PR-06`、`08`、`13`、`18`】：单笔审核详情须使申请信息、代币与链上事实、审核结论与凭据、操作记录同处可得。操作记录是本笔全部事件与后续进度的唯一来源，不另设第二套进度来源或重复的明细。关联申请保留原入口；来自旧操作记录入口的访问仍定位到本笔操作记录，不丢失单笔标识，返回时保留队列原有的筛选、分页与所在位置。队列的留存情况筛选、当前状态摘要与操作记录同源。事件与结果须可辨，操作人与时间为附随信息；原因、实际 gas 与凭据须可完整取得。记录按发生时间倒序。

| 记录内容 | 含义与信息要求 |
| --- | --- |
| 事件 / 进度 | 必有文本，区分质押上链、审核通过 / 驳回、撤回，以及解押发起、解押成功 / 失败与差异；只展示本笔事实，不混入同批其他笔 |
| 发生时间 | 已发生事件展示真实时间与时区；按发生时间倒序，同时间保持稳定顺序。时间暂缺标"待核实"，不得用访问时刻代替；未发生的步骤不生成完成记录 |
| 操作主体 | 人工审核事件显示当时审核人；来自资产方的链上动作标"资产方"；来自链上结果回写的标"系统"；未提供主体时标"待核实"，不推定为审核人 |
| 结果与说明 | 该事件的结果、前后状态及原因；原因可展开全文。进行中明确"处理中"及已知事实，未来步骤不预写成功；详情字段按 [3.6 节](#m6) |
| 关联凭据 | 有则提供对应本笔的协议或链上凭据，遵守权限及文件规则；没有则说明暂无，不伪造材料或重复关联其他申请 |
| 记录反馈 | 加载中、暂无记录、读取失败须分别说明；失败可重试且不伪装为空。记录只读，不提供编辑、删除或手工完成进度动作 |

历史事件与当前进度均可查，新增进度不覆盖原审核事实；同一业务事件重复到达只保留一条，不因重新取数重复生成。关联申请历史与本笔操作记录范围分开。流程见[操作记录与后续进度的查看](02-代币质押审核-流程图与状态机.md#fl-pr-04)。

同批不同笔可直接查看或切换；切换前有未提交输入需选择留在当前笔继续编辑或放弃后切换，不提供跨笔自动保存草稿，不能将上一笔原因、协议选择、备注及确认勾选自动带入下一笔。通过 / 驳回办理过程中须始终可得本笔单笔标识、代币、批次和项目。已审笔只读，**不使同批待审笔只读**。

<a id="d-pr-15"></a>`D-PR-15` / `D-PR-16`：额度派生量只读，本模块不提供任何调整入口；审核不读取或修改额度公式本身。代币失效、项目终结显示客观事实，不自动替审核人驳回。**审核通过是该张计入有效质押价值的必要条件，不是充分条件**——是否实际计入仍由借贷平台按有效性等既有排除项判定，本模块如实展示判定结果而不预告。

<a id="m3"></a>
### 3.3 逐笔通过与协议维护（`F-PR-09`、`10`）

<a id="d-pr-01"></a>`D-PR-01`：每次审核针对当前单笔。可以只通过批次内 A、驳回 B、保留 C 待审；不再要求整批同结论。通过 A 不代表批准 B/C，其他笔无需重新发起。本期不提供一次点击为多笔生成结论。

<a id="d-pr-35"></a>`D-PR-35`【通过直接提交】：前置为有查询与处置权限，**当前笔仍待审核**（无截止条件）。线下判定后的通过办理（`C-O-PR-01`）依次为：回显本笔代币、项目、批次与链上事实 → 上传本笔适用协议 → 可填内部备注 → 确认知悉本次结论的业务后果 → 提交。通过在本次办理内完成、不再叠加下一层办理，也没有"核对结论 / 确认审核结论"的后续步骤，不新增第二位审核人。此路径不改变逐笔驳回的原因预览与二次确认。

提交前取消不产生结论，跨笔不携带材料；提交中明确反馈且不可重复点击。材料、权限或状态不满足时就地说明，不产生通过记录；保存失败保留当前笔有效输入可重试。结果不确定先查当前事实，不展示成功或引导重复裁定。成功后结束本次办理并回到本笔详情，结论与操作记录出现本次通过，当前笔只读，其他待审笔仍可办理。

| 编号 | 规则 |
| --- | --- |
| <a id="d-pr-03"></a>`D-PR-03` / `N-PR-03` | 每笔通过必须有 PDF / JPG / PNG 协议，1～5 个，单文件 ≤10 MB；默认未关联任何文件。未上传完或保留文件失败不能通过；错误逐文件指出、可修复、不丢其他输入。只校验规格与有无，不认定内容或法律效力 |
| <a id="d-pr-04"></a>`D-PR-04` / `D-PR-30` | 协议按**本笔结论**关联和锁定。同一线下协议确实适用同批多张时，可逐笔明确选用同一文件，须能从协议内容识别本笔代币；不自动给其余笔挂协议、不自动通过。是否适用由运营线下核对，平台不做内容识别。每笔仍分别确认 |
| 协议生命周期 | 未提交前可增删替换；已通过的本笔关联不可删改。其他笔修改其未提交材料不能影响已通过笔；本笔凭据关联失效只标为"已失效 · 仅存档"，同文件对另一正常笔的关联不跟着失效。再次质押的新笔必须重新明确关联，不自动复用 |
| <a id="d-pr-13"></a>`D-PR-13` | 当前笔通过后，该张在满足有效性等既有条件时计入有效质押价值，并从待审质押额转出，有效质押额随之重算；**没有入池确认环节、没有入池截止、没有需要资产方另行支付的平台费用**。通过 A 的额度效果与 B/C 是否审完无关 |
| <a id="d-pr-16"></a>`D-PR-16` | 审核通过不修改代币事实、不改写质押状态号或执行状态号、不触发任何链上动作；额度重算由借贷平台按既有排除项执行 |

<a id="d-pr-36"></a>`D-PR-36` / `F-PR-10`【已上传文件的信息与动作要求】：本模块协议上传完成项及正式协议、存档文件适用同一套信息与动作要求；具体组件形态由设计决定。

| 要素 / 场景 | 产品行为与边界 |
| --- | --- |
| 文件信息与动作 | 每个文件须可得文件名及当前状态（已上传、已关联本笔结论、已失效 · 仅存档）；可执行的预览与下载须与所属文件一一对应，多文件不串位；完整文件名须可查看 |
| 支持预览 | 已上传完成且具备预览能力的文件显示预览与下载；查看与结束查看后须保留当前申请、所在位置和未提交输入。PDF / JPG / PNG 仍为本模块允许上传类型，通用展示规则不扩展上传白名单 |
| 不支持预览 | 仅保留下载，不显示预览按钮、灰色占位或点击后才告知不支持；其他模块采用通用组件时按其既有文件范围判断 |
| 预览失败 | 对原本支持预览的文件提示本次预览失败，提供下载兜底；区别于已知不支持预览。文件暂不可得或下载失败明确提示并允许重试，不假报无协议或下载成功 |
| 上传中 / 上传失败 | 分别显示进度或错误，不冒充已上传文件，不提供可用的预览与下载；保留既有修复及移除能力。上传完成后才按能力展示文件动作 |
| 编辑与存档 | 未提交材料仍可按原规则移除、替换；正式关联后不可删改。存档标签不等于文件不可读；有访问权限时仍按能力提供预览与下载 |
| 权限与可达性 | 文件访问沿用所属业务权限，下载不绕过权限；动作文字清晰、键盘可达；文件名长度不得使预览与下载不可得 |

正式附件仅授权运营与所属企业资产方可访问，游客、其他企业、资金方不可访问。驳回或撤回导致本次通过未生效时，未提交的文件选择不成为本笔正式协议；同文件已经在其他笔生效的关联不受影响。

| 字段 | 类型与业务校验；全部默认无人工结论 |
| --- | --- |
| `RV-01` 结论记录编号 | 文本，有效人工结论生效时生成，稳定只读；每笔至多一条 |
| `RV-02` 关联提交批次 | 引用，必有、只读；与[`RV-19`](#rv-19)一起定位本次单笔结论，仅批次编号不足以指向结论 |
| `RV-03` 结论 | 枚举，通过 / 驳回；经对应单笔动作生成，一经生效不可修改 |
| `RV-04` 结论时间 | 时间，有效提交时生成，不可手工回填；是该张计入有效质押价值的业务时点依据 |
| `RV-05` 质押协议文件 | 文件集合，通过必填，1～5 个且符合规格；默认空，适用本笔；驳回不产生正式协议 |
| `RV-06` 协议关联标记 | 生效 / 已失效 · 仅存档；本笔有正式协议时才有值，通过时生效，只读；不是法律效力认定 |
| `RV-07` / `RV-08` 审核身份 | 当前运营账号标识及结论时姓名 / 工作邮箱，必有、不可编辑，后续账号资料变化不覆盖历史 |
| `RV-09` 本笔代币价值快照 | USD 金额，结论时取该张业务价值，只读留痕；不取全批合计，不以 0 补缺，不替代额度派生 |
| `RV-10` 内部备注 | 选填文本，默认空，≤200 字；结论前可编辑，之后只读；仅授权运营可见 |

流程与状态见[逐笔审核流程](02-代币质押审核-流程图与状态机.md#fl-pr-01)、[单笔审核轴](02-代币质押审核-流程图与状态机.md#st-pr-01)。

<a id="m4"></a>
### 3.4 逐笔驳回与唯一结论（`F-PR-11`、`12`）

前置与通过相同。本笔"驳回"打开驳回办理（`C-O-PR-02`）：回显本笔代币 → 填原因，可选归类 → 完整预览 → 二次确认对象及原因 → 提交。

<a id="d-pr-02"></a>`D-PR-02` / `D-PR-10`：驳回只结束本笔审核，原因针对本笔；同批 A 通过、B 驳回、C 待审是正常结果。**驳回不自动解押**——该张仍留在质押合约内并占用，由资产方自助解押；驳回本身不产生平台费用，解押 gas 由资产方承担。

| 字段 | 含义、必填、默认与校验 |
| --- | --- |
| `RV-13` 原因说明 | 驳回必填自由文本，去首尾空白后 10～500 字，禁止纯空白或仅标点；默认空。平台不改写措辞或判断真伪 |
| `RV-12` 问题代币标注 | 本笔代币只读回显，取[`RV-19`](#rv-19)所关联代币，不再从全批勾选问题子集；不能借此驳回其他笔 |
| <a id="d-pr-22"></a>`RV-11` 归类标签（`D-PR-22`） | 选填、默认空：代币归属或主体存疑 / 底层应收账款存在问题 / 代币与目标项目不匹配 / 申请材料不完整或不一致 / 其他。仅统计，不影响是否可提交 |
| `RV-14` 驳回原因全文 | `RV-13` 为主体，标签或本笔点名文字可辅助生成、可编辑；最终预览文本原样用于本笔面客原因与通知。选填部分不挤占 10～500 字的说明空间，不截断、不自动套到全批 |

<a id="d-pr-07"></a>`D-PR-07`：同笔并发，只先有效完成的一次裁定生效；后者展示本笔已有结论、时间与审核人，不覆盖。**不同笔并发互不争抢整批结论**，A/B 可分别成功提交。重复同笔同次操作不增加结论、不改时间、不重复通知。结果不确定先显示待确认并可查当前事实，不引导重新制造结论。

<a id="d-pr-12"></a>`D-PR-12`：两端按稳定单笔标识同步该笔结论、时间、原因；批次仅汇总。不因某笔结论而覆盖批次其余笔的待审、驳回或通过结果。已撤回或已有人工结论的笔不能再裁定，已打开的旧办理提交亦拒绝。驳回不触发链上动作；已上链事实与占用只读展示，本期不提供由运营发起的强制解押，不把已通过改成驳回来启动新动作。

<a id="m5"></a>
### 3.5 等待时长、待审撤回与解押（`F-PR-14`～`15`、`F-PR-19`）

<a id="n-pr-02"></a>`N-PR-02`【已等待时长】：待审笔从上链成功起计；不足 1 小时按分钟、不足 24 小时按小时、之后天＋小时。等待时长是**事实陈述，不是期限**：不设截止、不倒计时、不触发任何自动状态变化，也不构成催办依据。人工结论或撤回生效即停止计时。

<a id="d-pr-08"></a>`D-PR-08`【审核无时效管控】：用户 2026-09-24 确认审核不设时限。待审笔可无限期停留在待审核，只能由人工结论或资产方撤回终结；不存在审核截止、到期自动失败、迟到补审拒绝或超时通知。原按 48 小时定义的截止、超时事实与自动终结条款一并废止，见 [5.4 节](#retired)。

<a id="d-pr-23"></a>`D-PR-23`【终结方式收敛】：单笔只有三种终结方式——通过、驳回、资产方撤回。任何时间流逝都不改变审核状态。正在上传、提交尚未生效、线下已决定但未在线提交均不算已审核；已生效撤回后不能补裁定，已有人工结论后撤回不再适用于审核轴。每笔只有一个有效审核结果，不影响同批其他笔。

**待审撤回**：资产方在待审期间可自行向质押合约发起解押申请，自付 gas，合约按记录将该张转回其质押前原持有地址。撤回不经运营审批、不需要运营在本模块执行任何动作；解押成功即本笔审核状态转为已撤回，占用解除。撤回与审核同笔竞合只生效一个结果——若人工结论已先生效，本笔按结论处理，链上解押另按其真实结果记录；其他笔照常。

<a id="d-pr-24"></a>`D-PR-24`【驳回后的自助解押与再质押】：驳回记录保留；该张仍在质押合约内，资产方按与撤回相同的路径自助解押，自付 gas，合约按记录转回质押前原持有地址。**解押成功前该代币不允许用于其他项目质押**（既定业务限制，见 [5.2 节](#limits)）。解押成功后，按最新资格可重新发起质押到同一或其他项目，无冷却；旧笔不恢复、不删除，新笔新标识、新上链时点。其他独立占用、失效或差异仍约束可选性。

例如同批 A 已通过、B 已驳回、C 仍待审：A 的代币按有效质押价值口径计入并继续占用，B 的代币在解押成功前仍占用且计入待审质押额的已驳回分项，C 无限期等待直到被裁定或撤回。零费用的表述只指平台不收费，不代表资产方未承担 gas。图见[单笔审核轴](02-代币质押审核-流程图与状态机.md#st-pr-01)。

<a id="m6"></a>
### 3.6 质押上链事实与解押追踪（`F-PR-18`）

<a id="d-pr-25"></a>`F-PR-18` / `D-PR-25`【只读追踪的范围】：本模块对链上事实只读追踪，不提供任何发起、重试、撤销或代付能力。可追踪的链上事实有两类：**质押上链**（该张进入质押合约，是本笔申请成立的前提）与**自助解押**（撤回或驳回后由资产方自助发起，成功即按记录转回质押前原持有地址）。两类都由资产方以持有地址签名发起、自担 gas，失败不退。

<a id="d-pr-11"></a>`D-PR-11`：本端不提供签名、代签、代付、重发或修改代币事实、质押状态号、执行状态号的能力；运营人员不参与任何链上动作。

质押上链与解押的事实均作为本笔操作记录的进度呈现（[`D-PR-34`](#d-pr-34)）。上链成功即记录"质押上链成功 / 待审核"；实际发生解押动作后追加对应进度，已有审核记录保留。每项进度须可取得下表的时间、原因、gas 与凭据；不另设重复的明细。

| 链上事实字段 | 业务含义及校验 |
| --- | --- |
| 关联 | 单笔标识、批次、项目、代币、质押链、质押合约地址与质押前原持有地址必有、只读；不能仅用批次和一个结果定位 |
| 本笔当前去向 | 待审核 / 已计入有效质押额 / 待自助解押 / 解押处理中 / 已解押退回，取面客 `PA-15` 的取值；实际发起解押后才处理中；无结果不擅填失败。"结果待核实"与对账差异为并列事实提示，不自造终态 |
| 时间 | 上链成功、解押发起、解押完成、发现差异的各自时点及业务时区；没有结果不伪造完成时间 |
| 原因与下一步 | 解押失败或差异有可读原因；处理中不能重复发起；差异保持本张限制并提示联系客服，本端没有处置入口 |
| gas | 由资产方承担，实际是否产生、金额及币种只读；失败不退。未提供写"gas 待核实"，不填 0、不因另一张零 gas 推断全批零 gas。平台不收取质押或解押的服务费、不代付、不垫付、不返还 |
| 凭据 | 成功有可查验凭据，缺失不编造；可复制查验，展示沿共享口径，不以有凭据替代业务确认 |
| `RV-17` 差异事实 | 相关单笔、代币、原记录、迟到或冲突事实与发现时间；无差异不生成；不得关联到同代币的新申请而覆盖旧事实 |
| <a id="rv-18"></a>`RV-18` 解押跟踪 | 本笔自助解押的发起时点、结果、gas 与凭据，只读承接；不提供由运营发起的解押、重试或强制退回。原"异常释放跟踪"含义废止，本编号改指[自助解押](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#withdraw)的只读事实 |

**按张独立**：同批 A/B 的审核结论与解押结果彼此独立；B 解押失败不影响 A 的通过结论与额度归属。只约束相关张，不锁全项目。

<a id="d-pr-26"></a><a id="d-pr-38"></a>`D-PR-26` / `D-PR-38`【链上异常与客服边界】：适用于长期无解押结果、结果与原记录冲突，以及混合结果中仍待核实的笔。两端保留真实单笔、代币、批次 / 项目、本次动作、等待时长、已知 gas 和凭据，告知"结果待核实，请联系平台客服人员"。沿平台已有真实客服联系方式能力提供指引；资料不可得时如实说明联系方式暂不可用，不编造邮箱、电话、核实天数或承诺。客服信息用于联系，不是异常处置入口。本规则与[面客异常口径](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#exceptions)同源。

没有明确解押结果仍为"处理中 / 结果待核实"，不推断失败、资产已转回、gas 为零、可重复发起或可以重新质押；仅相关张受限制，其他明确成功且合格的张正常成立。本期不建立工单、异常待办、升级阈值、客服处理进度、人工改判 / 补记、强制解押、补偿、代付或恢复资格流程；联系客服、查看记录或已读通知均不改变业务状态与限制。

普通晚到且无冲突的真实结果，归本笔正常呈现。迟到结果与原记录冲突时保留原事实并关联旧笔或新申请，不翻转原终态、不自动改回占用中、不双重计入额度，保留相关张的新动作限制并提示联系客服。已有真实差异标记仍可只读查看，不因取消异常处置功能而清掉保护；不生成差异处理任务。

混合结果按张独立：正常明确解押成功的张按当前资格可再质押；未知或冲突张不因同批其他张已结束就自动恢复资格。

<a id="d-pr-27"></a><a id="d-pr-32"></a>`D-PR-27` / `D-PR-32`【占用与解除的语义边界】：

| 场景 | 本期范围及可观察结果 |
| --- | --- |
| 上链成功、等待审核 | 该张占用中，计入总质押额与待审质押额的待裁定分项，不计入有效质押额。无期限，不会因等待而自动失败 |
| 审核通过 | 占用继续（即正常质押占用）；在满足有效性等条件时计入有效质押价值并重算有效质押额。运营不因通过而触发任何链上动作 |
| 审核驳回、尚未解押 | 占用继续，计入待审质押额的已驳回分项，不计入有效质押额；该张不允许用于其他项目质押 |
| 待审撤回或驳回后解押成功 | 代币按记录转回质押前原持有地址，退出总质押额与待审质押额；平台不收费，gas 由资产方承担且不退 |
| 解押失败或长期无结果 | 保持占用与真实事实，按 [`D-PR-38`](#d-pr-38) 提示联系客服；不推定已退回、不自动重发、不解除限制 |
| 链上事实与记录冲突后的强制处置 | **本期不做**（[`X-PR-01`](#1-2-本期不包含)）。不设强制解押入口、待办、自动转回、费用分配或恢复资格流程。已有事实差异只读与不重复操作限制保留 |

流程见[质押上链、逐笔审核及两端结果](02-代币质押审核-流程图与状态机.md#fl-pr-01)、[撤回与驳回后的自助解押](02-代币质押审核-流程图与状态机.md#fl-pr-03)；去向轴见[本笔当前去向与自助解押](02-代币质押审核-流程图与状态机.md#st-pr-02)。

<a id="m7"></a>
### 3.7 留痕与通知触发（`F-PR-13`、`16`）

`F-PR-13`：留痕关联**批次＋单笔＋代币**，包含质押上链、撤回、人工结论、正式协议关联、解押发起与结果、差异事实、重新质押关联；记录事件、时间、主体、前后状态、原因与凭据。人工取当时身份，链上与系统事件不伪造审核人；不可编辑删除，重新质押不覆盖旧笔；不同审核人的结果可在同批并存。记录内容与后续进度统一按 [`D-PR-34`](#d-pr-34)；同一事件不得同时生成重复的操作记录与进度记录。

<a id="d-pr-17"></a>`D-PR-17`～`D-PR-19` / `F-PR-16`：沿用既有面客事件名称和消息端别，**事件归属为单笔，不以批次＋结论类型合并掉第二笔同类结果**；同一笔同一事件重复送达不形成重复消息。页内结果始终保底，投递失败不回滚。通知必须定位本笔代币及所属批次；不等待整批审完才触达。是否聚合呈现由消息模块与设计决定，但每笔内容、原因及可执行动作不能丢失。

| 事件 | 收件方与内容 / 落点 |
| --- | --- |
| 质押审核通过（`financing.pledge.review_approved`） | 面客发起资产方；本笔、代币、项目、通过时间；说明该张已按有效性判定计入有效质押额，无需另行确认入池或支付平台费用 |
| 质押审核驳回（`financing.pledge.review_rejected`） | 同上；明确仅该笔驳回，原因全文原样，其他笔不受影响；说明代币仍在质押合约内，需自助解押后才能再质押，gas 自担 |
| 撤回与解押结果 | 由资产方自行发起，属面客侧动作与结果；本模块不重复发送另一套结果 |
| 运营队列与只读事实 | 审核队列仍逐笔且无时效；解押失败或差异在原笔记录可查，本期不新增异常处置待办、升级或运营广播；详情权限不等于消息收件资格 |

正常新申请不额外全员广播，通过 / 驳回不重复通知刚裁定的运营。内部备注不进入面客原因或通知。<a id="d-pr-28"></a>`D-PR-28`：已读 / 已查看不等于差异已关闭或限制解除。仅资产方本企业可看其材料及链上信息，资金方和游客不能读取审核记录。

通知事件、模板、收件、失效和验收统一见[消息通知第 2～5 节](03-代币质押审核-消息通知.md#notice-rules)及[第 6 节验收](03-代币质押审核-消息通知.md#notice-acceptance)；本模块不另造事件副本。

<a id="acceptance"></a>
## 4. 同序验收

前置：资产方在同一已审核通过的项目内一次发起 A/B/C 三张并全部上链成功；另有同代币新发起记录、不同资产方记录；运营有查询＋处置、仅查询和无权三组身份。审核结论、面客结果、额度归属及逐张解押可分别观察。以下为业务验收要求，未决分支按[第 5 章](#open-items)限制，不以模拟结果冒充已上线能力。

### 4.1 单笔审核队列（对应 [3.1](#m1)）

| 验收 | 场景与预期 |
| --- | --- |
| `AC-PR-01`～`02` 队列按笔生成与统计随实际笔数变化 | 一次发起三张且全部上链成功形成三笔队列记录和待审计数 3；按上链时间升序。上链失败的那张不产生记录。仅通过 A 后待审为 2、近七日人工审结加 1 |
| `AC-PR-03`～`04` 筛选排序生效且统计范围不被状态筛选影响 | 基础条件组合、留存情况筛选及三项排序生效；审核状态只有待审核 / 通过 / 驳回 / 已撤回四个可选值，无"质押失败（审核超时）"；申请类型可按首笔质押 / 追加质押筛选；起止错误就地提示，重置回默认，统计范围不被状态筛选误清零 |
| `AC-PR-05`～`06` 翻页往返保留上下文且失败不假报为空 | 翻页与详情往返保留原筛选、分页与所在位置，从记录进入指定单笔，队列无批量裁定入口；加载失败不显示为零条 |
| `AC-PR-51` 同批已审笔不遮蔽剩余待审笔 | 同批 A 已审、B/C 待审时，B/C 仍可检索和打开办理；查询批次可看全三笔，不把批次剩余待审藏进已审历史 |

### 4.2 单笔事实及历史（对应 [3.2](#m2)）

| 验收 | 场景与预期 |
| --- | --- |
| `AC-PR-07`～`09` 单笔、链上与批次事实只读可回溯 | 当前笔的项目、批次、代币与链上事实（质押链、持有地址、上链时点、凭据、gas）关联清晰、完整清单可回查；代币事实只读、无增删替换，缺失不补 0；历史原因可展开全文，链上与系统事件无伪造审核人 |
| `AC-PR-36`～`39` 线下判定载体无系统建议且校验仍生效 | 线下判定说明存在，无系统建议、评分或风险排序；客观有效性与解押差异可展示；格式、权限与状态校验仍有效；项目质押率与期限为只读事实，本模块无调整入口 |
| `AC-PR-62` 操作记录承载本笔全部事实 | 质押上链、待审、通过、驳回、撤回及解押的操作记录均可在本笔详情取得，与申请信息、代币与链上事实、审核结论同处可得；事件与结果可辨，操作人与时间为附随信息，原因 / gas / 凭据可完整取得，且只有这一套进度来源；按发生时间倒序可查全文、主体及凭据；旧入口能定位本笔操作记录，返回队列保留原筛选、分页与所在位置；加载中、无数据与读取失败可分辨 |
| `AC-PR-52` 跨笔切换不串输入也不回写旧笔 | 从 A 切到 B 时未提交原因 / 材料不串入 B；对同代币新申请的结论不会回写旧笔；同批不同审核人及时间可分别回溯 |

### 4.3 逐笔通过与协议（对应 [3.3](#m3)）

| 验收 | 场景与预期 |
| --- | --- |
| `AC-PR-10`～`13` 通过只结论当前笔且须协议齐备 | 通过 A 必须有适用协议并在本次办理内确认知悉业务后果；在本次办理内一次提交、不再叠加下一层办理，也无核对结论步骤；只有 A 生效，B/C 不必先审完；提交前取消无结论 |
| `AC-PR-14` 协议规格校验的边界值 | 无文件、6 个文件、类型错误、单文件大于 10 MB 或上传未完成不能通过；1～5 个合规文件及恰好 10 MB 可提交；失败可修复、不丢其他输入 |
| `AC-PR-15`～`16` 协议按笔锁定且不跨笔连带 | 通过前可替换，之后不能改本笔关联；同文件明确适用 A/B 时分别选择并确认，B 的关联失效不使 A 关联失效；新笔不自动继承协议 |
| `AC-PR-24` 通过的额度效果可观察且不触发链上动作 | 通过 A 后，A 从待审质押额转出并在满足有效性等条件时计入有效质押价值，有效质押额按该项目锁定质押率重算；总质押额不因通过而变化；通过不修改代币事实、不触发任何链上交易、不产生平台费用 |
| `AC-PR-63` 通过办理一次提交的成功与失败路径 | 合规提交仅一次操作；提交中禁止重复，失败保留本笔输入，结果不确定不假报成功。无协议、上传未完、权限失效或已被裁定 / 撤回均不产生本次通过结论，已有结果不被覆盖；成功回详情且记录一次，同批其他笔不变；驳回流程保持原规则 |
| `AC-PR-64` 文件信息与动作一一对应 | 上传完成文件与正式 / 存档协议均可得文件名、当前状态及可执行动作；支持预览的提供预览与下载，不支持预览的仅下载。长文件名、多文件、中英文下动作仍可得且不串位；查看文件前后不丢编辑内容及所在位置，未提交材料可移除，正式关联不可删改 |
| `AC-PR-65` 预览失败与权限的兜底反馈 | 支持预览但本次失败时有错误说明与下载兜底；文件不可得、下载失败不假报成功；上传中 / 失败不提供可用文件动作。无权限者不能预览或下载；组件复用不放宽本模块 PDF / JPG / PNG、数量及大小限制 |
| `AC-PR-53` 各笔结论彼此独立 | A/B 不同时间通过，各自形成结论、审核人与时间；A 通过不自动给 B 关联协议，也不改变 B 的额度归属 |

### 4.4 逐笔驳回与唯一结论（对应 [3.4](#m4)）

| 验收 | 场景与预期 |
| --- | --- |
| `AC-PR-17`～`20`、`AC-PR-40` 驳回原因校验与只点名本笔 | B 原因空、纯标点或不足 10 / 超过 500 字被阻止，边界 10 / 500 字可通过；归类选填，全文预览与面客原因逐字一致，只点名 B，不给全批套原因；二次确认对象正确 |
| `AC-PR-21`～`23` 同笔并发只生效一次、跨笔并发互不争抢 | 两人同时裁定 B 仅一个生效，后者显示已决结果；两人分别裁定 A/B 可各自成功；重复同笔操作不增加结论、通知，也不改变结论时间 |
| `AC-PR-54` 驳回不自动解押且不影响他笔 | 驳回 B 后 B 的代币仍在质押合约内并占用、计入待审质押额的已驳回分项，平台不收费；运营端没有任何解押或强制退回入口。A 不重新审核，C 仍待审。B 解押成功后方可重新发起质押 |

### 4.5 等待、撤回与解押（对应 [3.5](#m5)）

| 验收 | 场景与预期 |
| --- | --- |
| `AC-PR-28`～`29`、`AC-PR-33` 审核无时效 | C 长期待审：无论经过多久都不出现截止、倒计时、自动失败或超时提示，审核状态始终为待审核，随时仍可正常裁定；已等待时长如实增长且只作事实陈述；无人访问也不会产生任何自动状态变化 |
| `AC-PR-41` 撤回与裁定竞合只生效一个 | 同笔撤回与人工裁定竞合只生效一个结果：人工结论先生效则本笔按结论处理，链上解押另按真实结果记录；撤回先生效则该笔为已撤回且不可补裁定。其他笔不受影响 |
| `AC-PR-42`～`43` 终结后的再质押路径 | 已撤回或已驳回的旧记录可查、不恢复；解押成功后该张按当前资格可立即重新发起质押至同一或另一项目，形成新笔、新标识、新上链时点；解押成功前该张不可用于其他项目质押 |
| `AC-PR-55` 三种结论并存时各自独立 | 批次 A 已通过、B 已驳回、C 待审：三种事实同时可见且互不影响；只重新发起 B 形成新笔，A 不重新审核，C 的审核状态与等待不受影响；四类笔数统计随实际变化 |
| `AC-PR-68` 时点在两端一致且无期限类时间 | 上链、结论与解押时点在两端、记录及通知中指向同一时刻并带时区标注；切换展示时区、跨周末或夏令时不改变任何已发生事实；全流程不出现审核截止、入池截止、倒计时或剩余时长 |
| `AC-PR-69` 统计与筛选的 UTC 口径稳定 | 近七日自然日统计及上链时间区间筛选采用 UTC，切换显示时区不改统计归属、事件先后或审核数量；统计口径仅覆盖待审核、已驳回待解押与近七日人工审结三项，无超时类计数 |

### 4.6 质押上链事实与解押追踪（对应 [3.6](#m6)）

| 验收 | 场景与预期 |
| --- | --- |
| `AC-PR-44`～`45` 只读追踪且按张独立 | 质押上链与解押两类链上事实均可只读追踪，含时点、gas、币种与凭据；运营端无发起、重试、撤销或代付入口。A 解押成功、B 解押失败时各自展示结果与 gas，二者审核结论均不变 |
| `AC-PR-46` 无结果时只给事实与客服指引 | 没有解押结果时显示等待与已知事实，不擅填失败、零 gas 或可重押；提供真实客服指引且不建设异常处置，不设任何自动失败阈值 |
| `AC-PR-47` 冲突迟到结果保留原事实并维持保护 | 迟到结果与原记录冲突时关联正确单笔、原记录保留、不双重计入额度，相关张暂停新动作并提示联系客服；其他张正常。无异常待办、关闭或恢复入口，客服介入不解除保护 |
| `AC-PR-48` 平台侧不产生链上动作与费用 | 全流程中运营端不签名、不代付、不发起或撤销任何链上交易；平台对质押与解押均不收取服务费、不代付、不垫付、不返还；gas 由资产方承担且失败不退 |
| `AC-PR-56` 占用边界按张限定 | B 处理中时不能重复发起解押，而 A 与同项目其他张不被锁定；已驳回未解押的 B 不可用于其他项目质押，解押成功后限制解除 |
| `AC-PR-66` 链上进度在本笔操作记录呈现 | 质押上链、解押发起、解押成功 / 失败与差异均在本笔操作记录中体现进度，可取得时间、原因、实际 gas 与凭据；没有重复的明细来源。上链不生成虚假审核事件，后续结果不覆盖原审核结论，其他笔不混入 |
| `AC-PR-67` 进度重复到达与未知结果的呈现约束 | 进度重复送达或重新取数不重复生成记录；尚无结果不补完成时间、失败或零 gas；普通无冲突的迟到结果归本次动作，冲突另记差异并保留历史，不覆盖新申请或解除限制。查看记录不表示关闭差异，不新增强制解押入口或手工完成进度 |
| `AC-PR-70` 待核实笔只展示事实与联系方式 | 长期未知、冲突或部分待核实笔展示原笔 / 代币、实际动作、已知 gas 与凭据及联系客服说明；不猜结果或 gas、不自动解押 / 重押、不重发交易，不提供工单、异常待办、升级、改判、补偿或恢复入口。已知成功 / 失败张仍按各自真实条件处理 |
| `AC-PR-71` 客服边界不改变业务状态 | 联系客服、读记录不关闭差异、不解除占用；客服联系方式真实可用，缺失不造地址或处理天数，并如实提示。普通无冲突的晚到结果仍归原笔 |

### 4.7 权限、留痕、通知与通用质量（对应 [3.7](#m7)、[2.2](#d-pr-05)、[2.4](#states)、[2.5](#2-5-共同质量要求)）

| 验收 | 场景与预期 |
| --- | --- |
| `AC-PR-25`～`27`、`AC-PR-49` 权限分离与留痕不可篡改 | 所有事件按批次和单笔回溯，不可改删；只查不能审、只有处置不赠查询，账号非正常或无权时旧入口操作和下载拒绝；无代币修改、签名或代付能力 |
| `AC-PR-30` 术语不混淆审核、上链与解押 | 区分审核结论、链上留存与额度归属；不把通过叫"已入池"，不把"审核已结束"叫全部通过，不用"担保"替代质押；三项额度按各自定义陈述，不互相顶替 |
| `AC-PR-31`～`32` 通知按笔触达且不重复 | A/B 分别裁定会分别触达，不因同批同类而漏掉 B；重复 A 事件不重复通知。每笔带自己的原因与后续说明，投递失败不回滚；不等全批审完才通知；无超时类通知 |
| `AC-PR-34`～`35`、`AC-PR-50` 错误可辨、内部信息不外泄 | 错误场景可辨、键盘可达；内部备注不泄露，协议受控；已读不解除差异限制，也不产生异常待办 |
| `AC-PR-57` 四类笔数合计等于总笔数 | 批次审核四类（待审、通过、驳回、已撤回）数量相加等于总笔数；A 通过 B 驳回 C 待审为审核进行中；留存摘要与审核进度分别显示，不跨批统计 |
| `AC-PR-58` 两端同笔口径一致 | 两端按单笔核对原因、协议、时间、审核状态、留存状态与三项额度归属；面客按代币呈现，不以提交分组限制办理。运行联验须基于实现，不能以文档核验替代 |

<a id="open-items"></a>
## 5. 依赖、既定限制与待决

### 5.1 剩余依赖

| 编号 | 当前有效口径 | 剩余影响 |
| --- | --- | --- |
| `Q-PR-04` | 已关闭：审核无时效管控（[`D-PR-08`](#d-pr-08)）；业务日期统一 UTC、按偏好时区展示（[`D-PR-37`](#d-pr-37)） | 仍需真实两端时区与时点联验，不再存在截止类口径 |
| `Q-PR-05A` | 已按 [`D-PR-38`](#d-pr-38) 收口：解押未知、冲突只保留事实与保护并联系客服，本期不实现异常处置，也不设失败阈值 | 真实客服联系方式及真实逐张链上结果能力需具备 |
| `Q-PR-05B` | 强制解押与异常处置移出本期（[`X-PR-01`](#1-2-本期不包含)） | 正常自助解押不受影响，不为本期补设强制流程 |
| `Q-PR-06` | 已按 [`D-PR-38`](#d-pr-38) 及[面客异常口径](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#exceptions)收口：冲突保留真实事实及客服指引 | 非正常情形的处置系统不在本期，不宣称联系客服即处理完毕 |
| <a id="q-pr-07"></a>`Q-PR-07` | 跨平台企业主体稳定对应是能力依赖，不能只用名称匹配；缺失不放行 | 需真实归属及访问范围联验；不借此扩大他人私有资料可见性 |
| `Q-PR-08` | 质押与解押均由资产方以持有地址签名发起、gas 自担，运营不签名 | 真实签名链路、gas 估算与链上回写能力属实现侧依赖，文档确认不代表已接通 |
| `Q-PR-09` | 两项审核消息复用面客既有事件，由本模块唯一发送；超时事件已废止，详见[消息通知](03-代币质押审核-消息通知.md) | 共享消息分类与登记、端到端时效、真实投递待核验 |
| `Q-PR-10` | 沿共享分页及真实单笔规模明确性能测量场景 | 常规验证依赖，不另造业务限额 |
| `Q-PR-11` | 三项额度的权威定义、公式与派生归融资需求与代币质押，本册只消费（[2.4 节](#quota)） | 该册本批定稿后须逐条比对名称、分项与重算触发点；本册不自行定义公式 |
| `Q-PR-12` | 项目审核与合约部署合为一步、结论通过 / 退回，由[融资需求与代币质押 `F-LS-22`](../v1.0-借贷广场-融资需求与代币质押/01-融资需求与代币质押-PRD.md#f-ls-22) 定义；管理端承接审核动作的模块另行立项 | 本模块以"项目已通过审核并完成部署"为发起质押的前置事实；管理端模块立项后须核对入口归属与权限项 |

<a id="limits"></a>
### 5.2 按用户决定保留的业务限制（供研发与测试知悉）

以下为用户明确裁定后的既定结果，如实记录其业务后果，不作为异议保留。

**本模块直接承担的限制：**

1. **驳回或撤回后须资产方自助解押，代币在解押成功前持续占用。** 未解押的代币一直留在质押合约内，不允许用于其他项目质押；平台侧没有任何动作能替资产方解押。若资产方长期不解押，该张将持续占用且持续计入待审质押额的已驳回分项。测试应按此预期设计用例，不将其判为缺陷。
2. **gas 由资产方承担且失败不退。** 质押上链失败不形成申请、不进入审核队列，该次 gas 已消耗；解押失败同样不退还 gas。平台不代付、不垫付、不返还。
3. **审核无时效。** 待审笔可无限期停留在待审核，平台不催办、不自动终结。若运营长期不处理，资产方的唯一出路是自行撤回（自付 gas）。

**本批同时确认、写入以供全局知悉的限制（归属他册，本模块不承担）：**

4. **平台不再采集与校验收款人名称。** 法币与数币账户均取消收款人名称，受益人名称需由业务双方在平台外补充；平台不再具备"收款账户必须属于本企业"的自动校验能力。
5. **报价一经提交，资金方无法撤回。** 取消报价有效期后，退出路径仅为资产方拒绝；资金方提交报价即被锁定至资产方处理为止。
6. **待放款阶段无终止出口。** 待放款的唯一出口是资金方提交放款。若线下协商结果是不放款，平台侧没有任何动作能终结该笔业务，业务将持续停留在待放款，资产方无法另起一轮，**对应质押代币持续被占用**。该后果与本模块的占用口径直接相关：此类占用不因审核结论或时间流逝解除。

### 5.3 跨册待裁定标注（只标注不改写）

1. **`PM-AL-07` / `PM-AL-08` 接缝**：运营端权限矩阵把"代币质押申请查询"与"代币质押审核处置"登记为两项并分开授权，本模块据此区分只查与可办；相邻的资金方机构认证审核采用"不做运营查询/处置分权"。两处口径并存且各自自洽，在哪一册收口请用户裁定，本册未代改他册。本批新增的项目审核、融资参数配置两类权限项归运营端账户与登录登记，本册不代登记。
2. **已登录未选角色**：不影响本模块可见范围。本模块只对完成正常运营登录且具备相应权限的账号开放，面客结果只对发起该笔的资产方账号开放。
3. **游客区间筛选**：登录主干"游客排序筛选只能使用区间"针对未登录浏览；本模块的上链时间、已等待时长、单张价值排序与上链时间区间筛选只对已登录运营人员开放，两者不冲突。
4. **我的控制台**：本模块是运营端审核，不承载登录后的落地与角色判定，不补指向"我的控制台"的说明。
5. **额度名称与编号锚点**：按[字段命名与术语对照表](../00-字段命名与术语对照表.md)落地——`FP-10` 总质押额、`FP-11` 有效质押价值、`FP-12` 有效质押额、`FP-34` 项目质押率，锚点按 R19 硬约束保持不变，仅业务名称与释义调整；待审质押额为本批新增量，编号 `FP-32` 已由融资需求与代币质押分配，本册只引用不改写。本册全部业务名称取自该表，未自造同义词。

<a id="retired"></a>
### 5.4 本批废止编号清单（不静默删除）

审核时效随用户 2026-09-24 裁定整体取消，链路反转后入池确认环节不复存在。以下编号**废止、保留占位、不再分配新含义**；其原有的正文、图示与验收已一并移除或改写。

| 编号 | 原含义 | 废止原因 |
| --- | --- | --- |
| `F-PR-14` | 审核时限计时 | 审核无时效管控 |
| `F-PR-15` | 审核超时自动终结 | 同上 |
| `F-PR-19` | 异常释放 | 此前已停用保号，本批确认不恢复；自助解押改由 [`RV-18`](#rv-18) 承载 |
| `N-PR-01` | 审核时限为提交成功起连续 48 小时 | 审核无时效管控 |
| `RV-15` | 本笔审核截止 | 无截止，字段取消 |
| `RV-16` | 审核超时事实 | 无自动超时终结 |
| `RV-21` | 入池前复核 | 链路反转后无入池确认环节 |
| `RV-22` | 本笔入池截止 | 同上 |
| `D-PR-09` | 入池有效期七个自然日 | 同上 |
| `D-PR-33` | 面客按代币组织与批量确认入池 | 同上；上链发生在审核之前，无待确认集合 |
| `AC-PR-59` | 第 48 小时自动失败的边界验证 | 无时效可验 |
| `AC-PR-60` | 跨提交合格笔一次确认发起 | 无入池确认环节 |
| `AC-PR-61` | 确认前资格变化须再次确认 | 同上 |
| `AC-PR-N02` | 审核超时通知 | 事件废止，见[消息通知](03-代币质押审核-消息通知.md) |

`RV-20` 的"质押失败（审核超时）"取值、`D-PR-31` 五类笔数中的该类、队列筛选与统计中的超时项、面客 `financing.pledge.review_timeout` 事件及模板同批取消；`D-PR-08`、`D-PR-13`、`D-PR-23`、`D-PR-24`、`D-PR-25`、`D-PR-27`、`D-PR-32`、`RV-18`、`F-PR-18`、`AC-PR-24`、`AC-PR-28`～`29`、`AC-PR-33`、`AC-PR-41`～`43`、`AC-PR-54`～`58`、`AC-PR-66`～`68`、`AC-PR-70`～`71` 为**改义保留**，编号不变、锚点不变，内容按本批新链路重写。

### 5.5 后续衔接

开发主读第 3 章完整功能与第 2 章共同口径，测试主读第 4 章同序验收；双方共同阅读[流程图与状态机](02-代币质押审核-流程图与状态机.md)及[消息通知](03-代币质押审核-消息通知.md)。设计取齐三册，重点衔接：上链成功才入队、无时效的待审呈现、四类审核状态、已驳回待解押的占用提示、三项额度归属的只读展示、通过在本次办理内一次提交、已上传文件的预览与下载、无权与失败态。

本模块与融资需求与代币质押须同批评审：质押发起与解押办理、三项额度定义、单笔标识与批次口径均跨两册。真实链上执行、消息投递和权限尚须联验，不以文档一致性代替上线验证。
