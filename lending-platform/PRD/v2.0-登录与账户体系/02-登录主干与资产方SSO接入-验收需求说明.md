# 借贷平台 · 面客端 · 登录主干与资产方 SSO 接入 PRD
## **验收需求说明**

> **本册读者**：测试。
> **章节顺序、功能名称与需求编号与三份《功能需求说明》完全一致**，每条验收都注明回溯位置。
> 本册给的是**验收需求**（角色、前置条件、场景、明确的预期结果），**不是测试执行用例**——具体步骤、数据构造方式与自动化实现由测试自行设计。
> **每条验收点都写成用户或外部可观察的行为**（看到什么、能做什么、数据变没变），**不写协议参数与校验算法层面的检查点**。
> 流程与状态的全景见 [`03-登录主干与资产方SSO接入-流程图与状态机.md`](./03-登录主干与资产方SSO接入-流程图与状态机.md)，走查时可对照图 1～图 4 逐条定位。
> **编号段位**：`AC-L001`～`AC-L199` 对应登录主干两分册（主干 A `00-登录主干-01-入口与回跳.md`、主干 B `00-登录主干-02-引导与权限契约.md`）；`AC-L200`～`AC-L399` 对应 `01-资产方SSO接入.md`。回溯列写「主干 X.Y」「SSO X.Y」，按主册第 7 章的文件清单定位到册。

### 阅读前必须知道的三条

| # | 内容 |
| --- | --- |
| 1 | **`AC-L001`～`AC-L020` 是安全关键项**，建议在任何一条登录链路联调前先过 |
| 2 | 标 **`[待确认 Q-L01]`** 的条目依赖资产可信平台授权中心对外提供信息的形式，**真实联调须等待相应接口能力核实；已确定行为可模拟验证**。上游暂定提供用户 id、钱包地址、邮箱、企业 id 及个人、企业认证基本信息；本人资料及业务必要信息的展示原则已由 P01 确认；实际供给、有效归属及剩余证件脱敏口径需核实。模拟资料只能核验页面行为，不能替代联调验收 |
| 3 | **原 `Q-L02` 已关闭**（两类角色的登录时效各自独立）。第 10 章只验收资产方 SSO 这一侧；资金方钱包登录的时效归 WS-349，**不在本 PRD 的验收范围内**，这是边界不是缺口 |

相关图示：[图 1](./03-登录主干与资产方SSO接入-流程图与状态机.md#flow-1)、[图 2](./03-登录主干与资产方SSO接入-流程图与状态机.md#flow-2)、[图 3](./03-登录主干与资产方SSO接入-流程图与状态机.md#flow-3)、[图 4](./03-登录主干与资产方SSO接入-流程图与状态机.md#flow-4)。

---

# 第一部分 · 登录主干（对应主干 A / 主干 B）

## 1. 回跳 `RT-1`～`RT-5`（`F-L04`～`F-L07`）

**规则摘要**：原操作返回信息不泄露、不被其他会话冒用（`D-L20`、`D-L21`）；站内目标必须满足既定安全边界（`D-L23`～`D-L28`）；跨平台目标必须命中三平台域名精确白名单且对端已确认同等校验（`D-L30`～`D-L32`）；句柄一次性、有有效期、绑定生成会话（`D-L35`、`D-L27`）；任何失败一律静默兜底到默认落地页 + Toast（`RT-5`）。**判定全部在服务端执行，前端校验不构成防线。**

### 1.1 句柄生成、消费与落地

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L001` | 游客 | 资产详情可访问，存在受限原操作 | 登录并完成必要前置条件，当前资格允许 | 回到同一详情并恢复原操作；协议未同意或资金方首次建号时先完成 3.6 规定的引导，不越过门槛 | 主干 3.6 `D-L37` |
| `AC-L002` | 已登录用户 | 句柄的触发能力标识非空 | 登录完成后重新执行被拦下的操作，但该能力在当前档位下仍不可用 | **不报错**，转入受限引导（`C-L02`），并保留原触发能力标识供文案取用 | 主干 3.6 `D-L38` |
| `AC-L003` | 游客 | 主动点击顶部登录，无原受限动作 | 完成登录 | 资产方未同意协议先到 `P-L11`；资金方首次建号先进入机构注册；其他正常登录进入资产广场 | 主干 3.6 |

### 1.2 站内返回的安全结果

| 编号 | 角色 | 前置条件 | 场景（目标值） | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L004` | 任意 | 发起或恢复登录返回 | 目标为未允许的外部网站 | 拒绝该返回目标，回资产广场并给中性提示 | D-L23 |
| `AC-L005` | 任意 | 发起或恢复登录返回 | 目标省略平台信息、企图被解释为外站 | 拒绝，不离开平台 | D-L23 |
| `AC-L006` | 任意 | 发起或恢复登录返回 | 目标包含歧义分隔写法 | 拒绝，不通过自动修正放行 | D-L23 |
| `AC-L007` | 任意 | 发起或恢复登录返回 | 经特殊编码伪装的外站目标 | 拒绝，编码不改变允许范围 | D-L24 |
| `AC-L008` | 任意 | 发起或恢复登录返回 | 需多次解释才看似有效的目标 | 拒绝，不形成跳转绕过 | D-L24 |
| `AC-L009` | 任意 | 发起或恢复登录返回 | 名称类似允许业务域、实际不属于该域 | 拒绝，不误认作允许页面 | D-L25 |
| `AC-L010` | 任意 | 发起或恢复登录返回 | 路径改写后进入管理端或未允许范围 | 拒绝，仍不能进入非面客页面 | D-L24、D-L25 |
| `AC-L011` | 任意 | 发起或恢复登录返回 | 目标夹带用户信息或凭据片段 | 拒绝且可追溯为安全异常，不泄露敏感信息 | D-L26 |
| `AC-L012` | 任意 | 发起或恢复登录返回 | 目标携带登录凭据或另一跳转目标 | 拒绝，不泄露凭据、不成为二次跳转入口 | D-L26 |
| `AC-L013` | 任意 | 发起或恢复登录返回 | 目标为登录、授权中转、首登协议或签名流程自身 | 拒绝，不循环进入登录；正常账户设置不因编号相邻被当成登录流程 | D-L29 |

### 1.3 跨平台白名单

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L014` | 资产方 | 白名单登记了 `asset_trust_platform`，**但对端未确认同等校验** | 发起跨平台回跳 | **不跳转**，按兜底处理（`E-L06`）——这是本期的**实际状态**，不是异常 | `D-L32`、3.4.1 |
| `AC-L015` | 任意 | 白名单中登记了某平台域名 | 目标域名为该域名的**子域、后缀相近域名、带端口或大小写变体** | **一律拒绝**（完整域名精确比对） | `D-L31` |
| `AC-L016` | 任意 | `token_issuance_platform` 登记为「本期不开启」 | 构造以该平台为目标的跨平台回跳 | **拒绝**——登记表里有这一行不等于已放行 | 3.4.1 |

### 1.4 返回信息的有效期与安全

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L017` | 任意 | 完整登录流程 | 查看用户可见及可复制的登录信息 | 不包含可用于冒用登录或转用原返回目标的敏感信息，不因复制链接泄露登录凭据 | `D-L21` |
| `AC-L018` | 任意 | 已成功消费一次的句柄 | 重放同一句柄 | 按失效兜底处理 + **安全留痕** | `D-L35`、`E-L05` |
| `AC-L019` | 任意 | 在会话 A 生成的句柄 | 拿到会话 B 消费 | **拒绝** + 安全留痕 | `D-L27`、`E-L04` |
| `AC-L020` | 任意 | 句柄已超过 `N-L01` | 消费句柄 | 按兜底处理，Toast「链接已失效，已为你回到资产广场」，**不报错、不白屏** | `D-L35`、`E-L02` |

> **兜底表现的统一预期**（适用于 `AC-L004`～`AC-L020` 的全部拒绝场景）：落该身份的默认落地页 + 一条 Toast；Toast **只说「已为你回到资产广场」，不暴露判定失败的原因**；失败原因只进留痕。留痕中**不得出现**回跳句柄、登录过程中产生的任何一次性凭据、钱包签名原文。

---

## 2. 登录入口与路径分流（`F-L01`～`F-L03`）

**规则摘要**：平台默认游客态，可达路由一律直接渲染（`D-L01`）；登录只在「主动点击」与「触发受限操作」两种时机出现（`D-L02`）；类型选择是**路径分流**不是表单切换（`D-L04`）；两张卡片对等、文案两种载体逐字一致（`D-L05`、`D-L06`）；**主按钮按登录方式命名**（`D-L15`）；**点击即进入对应的外部环境，两侧都没有中间页**（`D-L16`），但**连接与签名仍是两次、签名不自动弹**（`D-L17`）；每次明确选择身份，不提供上次身份快捷入口（`D-L08`～`D-L10`）。

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L021` | 游客 | 未登录 | 访问权限矩阵允许游客浏览的内容 | 直接渲染，无自动登录跳转、遮挡弹层或登录墙；回跳允许清单不等同游客访问权限 | `D-L01`、主干 5.3 |
| `AC-L022` | 游客 / 正常登录用户 | 未登录及登录成功后 | 查看顶部身份入口 | 游客仅「登录」，保留语言切换，无「申请入驻」；正常登录后为钱包地址缩写入口。资产方地址缺失兜底及菜单见 `AC-L309`，资金方账户细节归 WS-349 | 主干 2.2、`D-L117` |
| `AC-L023` | 游客 | 未登录 | 全站浏览且不触发任何受限能力 | **不出现任何形式的登录提示** | `D-L02` |
| `AC-L024` | 游客 | 未登录 | 访问白名单外的路由 | 按矩阵 ❌ 语义返回「不可见」，**不跳登录页** | 主干 2.2、5.1 |
| `AC-L025` | 游客 | 在 `P-L01` | 点卡片 1 的主按钮「第三方授权（资产方）」 | 先看到离站告知，确认后到资产可信平台；**中间没有任何本平台页面**；**全程无任何本平台的账号 / 密码 / 验证码输入控件** | `D-L04`、`D-L13`、`D-L16` |
| `AC-L026` | 游客 | 在 `P-L01`，浏览器里有可用钱包 | 点卡片 2 的主按钮「连接钱包（资金方）」 | **钱包弹窗直接弹出**；**不出现邮箱或其他表单输入** | `D-L04`、`D-L16` |
| `AC-L027` | 游客 | 分别查看 `P-L01` 与 `C-L01` | 比对双语卡片 | 两种载体标题、说明、按钮逐字一致；资产方中文为「我持有资产，想发起融资。将前往资产可信平台登录。」；英文采用 I hold assets，与中文含义一致 | `D-L05`、主干 2.3 |
| `AC-L028` | 游客 | 在 `P-L01` | 走查视觉 | 两张卡片视觉权重相同，**无默认选中、无高亮偏向** | `D-L06` |
| `AC-L029` | 任意 | 全站 | 走查 + 全文检索 | **不存在**「输入邮箱后自动判定身份并路由」的任何实现或入口 | `D-L07` |
| `AC-L030` | 资产方 | 完成登录全流程 | 走查资产方链路 | 全链路**不存在任何采集邮箱或其他联系方式的输入框** | `D-L14`、`D-L88` |
| `AC-L031` | 游客 | 曾选择过任一身份 | 重新打开 `P-L01`、`C-L01` | 仅有对等身份卡，无「上次选择」或「直接继续」，无默认选中或自动跳转；本次必须明确点击身份 | `D-L08`、`D-L10` |
| `AC-L032` | 游客 | 浏览器存在旧身份记录（正常、过期、异常值）或刚退出 | 再次选择身份 | 所有情况下均无快捷入口，旧记录不改变当前角色及授权路径，`N-L02` 不再生效 | `D-L09`、`E-L08` |
| `AC-L033` | 游客 | 隐私模式 / 本地存储禁用 | 走完整登录流程 | 身份选择及主流程正常，不出现与缺少身份记忆有关的错误或提示 | `E-L07` |
| `AC-L034` | 游客 | 已选身份但未建立会话 | 选择「切换身份」 | 回 `P-L01`，保留原目标意图与机构草稿，不产生下次快捷登录记录；若实际改选角色则按当前身份重新判定目标；资金方本次连接断开。仅取消离站不按切换身份处理 | `D-L11`、`D-L18`、`D-L36` |
| `AC-L035` | 已登录用户 | 已建立会话 | 走查全站 | **看不到「切换身份」入口**；换身份的唯一路径是退出登录 | `D-L12` |
| `AC-L036` | 游客 | 分别构造：授权中心不可达 / 浏览器里没有可用钱包 / 钱包唤不起来 | 在 `P-L01` 点对应的主按钮 | 三种情况**均停在 `P-L01` 并就地说明**（含如何安装钱包这类可执行的下一步），**另一条路径仍然可点**；**不跳 `P-L04`、不白屏、不进入下游册的任何页面** | `E-L10` |
| `AC-L037` | 游客 | 浏览器里有一个可用钱包 | 点「连接钱包（资金方）」 | **从点击到看见钱包弹窗之间不出现任何本平台页面**，**全程只点一次**。反向走查：这条链路上**不存在第二个「连接钱包」按钮要用户再点一次** | `D-L16` |
| `AC-L038` | 游客 | 已在钱包里确认连接 | 观察后续 | 落到签名确认层（该层归 WS-349）；**签名弹窗没有自动接着弹出**——用户必须显式点一下才触发签名；签名前能看到用途说明、凭据保管须知与协议勾选 | `D-L17` |
| `AC-L039` | 游客 | 钱包连接弹窗已经弹出 | 在钱包弹窗里点取消 | **回到仍然可见的 `P-L01`**，两张卡片照常可点，**不留下空白页、不停在任何中间态** | `E-L11` |
| `AC-L040` | 游客 | 在 `P-L01` 与 `C-L01` 两种载体下 | 走查两张卡片的主按钮文案 | 按钮文案为「第三方授权（资产方）」/「连接钱包（资金方）」，**按方式命名、角色作副标**，中英各验一遍；**不得是中性的「继续」**；两种载体下**逐字一致** | `D-L15`、`D-L05` |
| `AC-L051` | 游客 / 已登录用户 | 无合法原目标且无待完成前置条件 | 打开默认入口、旧首页入口或进入失效回跳兜底 | 到既有资产广场，不展示独立首页；失效回跳有中性提示，正常默认进入不伪报错误 | `D-L19`、主干 3.7 |
| `AC-L052` | 游客 | 从合法资产详情等浏览页发起登录 | 放弃登录并返回 | 恢复原合法页面与浏览上下文；没有原页时回资产广场。不执行原业务动作，不建会话 | `D-L19` |

---

## 3. 受限操作引导组件（`F-L08` / `F-L09`）

**规则摘要**：弹层四段结构固定、文案说清「缺什么 / 补完能做什么 / 去哪补」（`D-L40`～`D-L42`）；弹层不改变任何账户状态（`D-L43`）；主按钮先生成句柄再跳转（`D-L45`）；提示条可折叠不可永久关闭（`D-L46`）；全站同一时刻最多一条常驻提示条（`D-L47`）。

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L041` | 游客 / 资产方 / 资金方 | 分别构造 4.2 表中的各触发情形 | 触发受限能力 | 弹出**正确载体**：游客 → `C-L01`；其余 → `C-L02`（`L1` 情形为纯只读说明） | 主干 4.2 |
| `AC-L042` | 任意 | 触发受限能力 | 走查弹层内容 | 四段结构齐全（缺什么 / 补完能做什么 / 可点去处 / 补充区）；文案中**不出现**「无权限」「请先登录」 | `D-L40`、`D-L41` |
| `AC-L043` | 已登录用户 | 认证引导已展开 | 点击前往认证 | 保留原操作意图，跨平台先展示 `C-L04`；满足跨平台返回条件后才能回原触发点，否则按主干 3.7 回资产广场 | `D-L42`、`D-L45`、`D-L18` |
| `AC-L044` | 资金方 `L1` | 审核中 | 触发受限能力 | 只读说明当前审核状态，显示实际可用辅助信息；不捏造时长或客服渠道，不出现撤回、修改暗示。状态页具体呈现由 WS-349 验收 | `D-L48`、主干 4.2 |
| `AC-L045` | 任意 | 弹层已展开 | 关闭弹层 | **账户状态无任何变化**，回到原页面 | `D-L43` |
| `AC-L046` | 已登录用户 | 存在待补齐事项 | 查看并折叠提示条 | 占据主内容上方位置，不覆盖正文；折叠后仍可见，无永久关闭入口；折叠状态限本次会话。游客不显示此常驻条 | 主干 4.1、4.3 `D-L46` |
| `AC-L047` | 任意 | 存在未补齐项 | 补齐该项 | 提示条**消失**——这是唯一的消失条件 | 主干 4.3 |
| `AC-L048` | 任意 | 构造同时缺多项的状态 | 进入任意页面 | **屏幕上只有一条提示条**，右侧显示「还有 N 项待完成」，点击展开清单；**不堆叠、不并列** | `D-L47` |
| `AC-L049` | 任意 | 多条并存 | 补齐优先级最高的一项 | **下一条立即接替、位置不跳动** | 主干 4.4 |
| `AC-L050` | 任意 | `T3 → T2` 降级发生 | 降级后下次进入 | **发了站内信且进入时有显式说明**；**不得表现为功能凭空消失** | `ST-1`、主干 4.6 |
| `AC-L053` | 游客 / 资产方 | 从整页身份选择、身份弹层、认证引导、提示条或账户页打开离站确认 | 点击返回、关闭或按 Escape | 回原呈现层；不发起授权、不改变账户状态、不清空原上下文，不把取消当切换身份 | `D-L18` |
| `AC-L054` | 键盘用户 | 引导上覆盖离站确认 | 操作并逐层关闭 | 仅最上层可操作，焦点不穿透；关闭后回上层原触发控件。控件已不存在时回该区域可用位置 | `D-L18`、主干 9 |
| `AC-L055` | 已登录用户 | 有未补齐提示且打开引导 | 从引导进入离站确认，再取消 | 原提示与原引导可恢复，最多一层弹层可操作，不出现多条补全条堆叠 | 主干 4.4、`D-L18` |

## 4. 权限矩阵（`F-L10`）

**规则摘要**：四档取值 ✅ / ◐ / ⊘ / ❌，**⊘ 与 ❌ 一律服务端强制**（`D-L50`）；⊘ 的控件仍可点并唤起引导（`D-L58`）；⬜ 与 — 必须可区分（5.2）；行归属按 5.4 判定，**任何需求不得自行加行或加硬卡点**（`D-L56`）。

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L061` | 任意 | 某能力在当前档位为 ⊘ | **绕过界面直接发起请求** | 请求**被拒绝执行**，业务数据未发生任何变化；界面上前端仍画着该按钮 | `D-L50` |
| `AC-L062` | 任意 | 某能力在当前档位为 ❌ | 绕过界面直接发起请求 | 请求**被拒绝执行**，且数据不返回；界面上该入口不出现 | `D-L50` |
| `AC-L063` | 任意 | ⊘ 的控件在界面上 | 键盘聚焦并点击 | 控件**可聚焦、可点击并唤起引导弹层**，读屏可读出受限原因；**不存在点了毫无反应的灰按钮** | `D-L58` |
| `AC-L064` | 评审 / 测试 | 查看矩阵总表 | 走查 | **⬜ 与 — 在渲染上可区分**，且图例随表在位 | 主干 5.2 |
| `AC-L065` | 资金方 `L1` | 处于待审核档 | 尝试编辑机构资料（含绕过界面直接请求） | **一律被拒绝，资料未被修改**——`L1` 是单向锁定态，这一格只能是 ❌ | `D-L63`、主干 5.3 第 12 行 |

---

## 5. 三态与对外契约四字段（`F-L11`）

**规则摘要**：四字段是两类角色共用的唯一状态出口，必须整体取用（`D-L60`、`D-L61`）；完备度只由服务端计算（`D-L64`）；资产方只有 `L0` / `L3`（`D-L62`）；`L1 → L0` 的回边不存在（`D-L63`）；角色由入口决定（`D-L69`）。

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L071` | 展示类功能 | 各档位下分别取一次契约 | 取值检查 | 四字段取值符合 6.2 枚举；`guest` 时 `identity_type` 与 `account_id` **必为 `null`**；`authenticated` 时 `identity_type` **必不为 `null`** | 主干 6.2 |
| `AC-L072` | 展示类功能 | 全代码 / 全文档 | 走查 + 检索 | **不存在**展示类功能直读 `verification_status` 或审核申请单状态做放行判定的实现 | `D-L60` |
| `AC-L073` | 资产方 | 构造 `identity_type = asset_party` 且档位为 `L1` / `L2` 的异常数据 | 消费方判定 | **按 `L0` 保守处理、不放行**，并按异常记录 | `D-L62` |
| `AC-L074` | 资金方 | 处于 `L1` | 尝试任何回到 `L0` 的路径 | **不存在该迁移**；只有审核结论能让它去 `L2` 或 `L3` | `D-L63` |
| `AC-L075` | 任意 | 前端构造伪造的档位值 | 发起受限能力请求 | 服务端**以自己计算的档位为准**，忽略前端传入值 | `D-L64`、`E-L09` |
| `AC-L076` | 任意 | 从 `P-L01` 的某个入口进入 | 完成登录 | `identity_type` **与所点入口一致**；全流程**不出现二次选角色的步骤** | `D-L69` |
| `AC-L077` | 资金方 | 分别构造草稿态 / 待审核 / 已驳回 / 已认证 | 取契约四字段 | 依次对应 `L0` / `L1` / `L2` / `L3`，**与 WS-349 正文使用的中文状态名一一对应且不另发编号** | 主干 6.3 |

---

## 6. 主干范围反向验收

| 编号 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- |
| `AC-L098` | 全文检索三份《功能需求说明》与实现 | **回跳的目标判定、跳转与白名单只有主干第 3 章一处实现**；资产方 SSO 分册与各业务实现均为调用方，**不存在第二套** | `D-L39` |
| `AC-L099` | 走查本 PRD 六份文件 | **不含资金方登录、机构注册、账户体系的功能正文**；出现资金方处**只限于**共用状态语义、矩阵行登记与归属、引导载体及登录后落地的衔接边界——符合本 issue 的产出边界 | 主册 · 2.2 |
| `AC-L100` | 走查本 PRD 六份文件 | **不含**为资产方提供的任何本平台账号 / 密码 / 验证码登录或兜底入口；**不含**资产方邮箱采集入口。资金方邮箱发码前滑块验证的补充要求归 WS-349，不在资产方 SSO 或共用身份选择中增加本地滑块与邮箱验证步骤 | `D-L13`、`D-L14`、主册 2.2 |

---

# 第二部分 · 资产方 SSO 接入（对应 SSO 分册）

## 7. 第三方授权登录的接入规则（`F-L20` / `F-L21`）

**规则摘要**：授权完成后只能回到本平台预先登记的那一个落地位置（`SSO-1`）；一次授权只能换来一次登录，重复使用或被搬到别处使用一律判定失败（`SSO-2`）；本平台必须能确认身份信息来自授权中心、发给本平台、且未过期（`SSO-3`）；用户在授权中心已登录时不应被要求二次输入，不成立时最多回退一次完整登录（`SSO-5`）。

> **本章验的是用户可观察的行为**——什么情况下这次登录被判定为失败、用户看到什么、还能做什么。**不验协议参数与校验算法**，那是技术方案，不在产品验收范围内。
> **三条判定规则任一不通过时，用户侧表现完全一致**（回落游客态 + 中性文案 + 错误码），因此下表每一条的预期结果都包含「与其他失败无法区分」这一层。
> **`SSO-4` 已作废**（原 `AC-L205` 随之移除，编号不再复用），编号保留说明见主册第 6 章。

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L201` | 资产方 | 本平台已登记唯一落地位置 | 构造一个**不是登记的那一个**的落地位置——多一级路径、多一个参数、或大小写不同 | 这次登录**被判定为失败**：不建立登录，用户回落游客态 + 中性文案 + 错误码；**登录状态未发生任何变化** | `SSO-1` |
| `AC-L202` | 资产方 | 已完成一次授权登录 | 把**同一次授权**再用一次 | 第二次**被判定为失败**；**第一次建立的登录不受影响**，用户仍是登录态 | `SSO-2` |
| `AC-L203` | 资产方 | 在浏览器 A 发起并完成授权 | 把**同一次授权**搬到浏览器 B 使用 | 浏览器 B **被判定为失败**，回落游客态；**不会在 B 上登进任何账号** | `SSO-2` |
| `AC-L204` | 资产方 | — | 构造一次**不是来自资产可信平台授权中心**、或**不是发给本平台**的身份信息 | **被判定为失败**（`R3`），回落游客态；**不会凭这份信息登进任何账号**，并**触发运维告警** | `SSO-3`、`D-L91` |
| `AC-L205` | — | — | **（空号）** 原 `SSO-4` 的验收点随该编号作废而移除，**不再复用** | — | 主册 · 6 |
| `AC-L206` | 资产方 | 身份有效期按既有 `N-L24` 兼容约束确认 | 分别返回有效与已失效身份 | 有效且其余条件满足时正常登录；失效时不建立登录、回落游客态。本条不验校验算法 | `SSO-3`、`N-L24` |
| `AC-L207` | 资产方 | 有合法原受限操作，且跨平台返回所需确认已满足 | 完成授权与必需协议同意后返回 | 当前资格满足时回原触发点并恢复操作；返回信息与凭据不向他人泄露。对端确认未满足时按 `AC-L014` 兜底 | 主干 3.2、3.6，SSO 5.3 |
| `AC-L208` | 资产方 | 无可信身份结论 | 仅宣称来自资产可信平台 | 仍判定失败，不建立登录；来源表象不替代身份确认 | `SSO-3`、主干 `D-L28` |
| `AC-L209` `[待确认 Q-L01]` | 资产方 | 用户在授权中心**已处于登录状态** | 走入口 B | **不出现登录表单**，短暂过渡后即回到本平台并完成登录 | `SSO-5` |
| `AC-L210` `[待确认 Q-L01]` | 资产方 | 上游判定本次仍需用户亲自操作 | 走入口 B | 回退为**一次**完整登录（用户看到授权中心登录页一次）；**不出现反复被弹回登录页** | `SSO-5`、`N-L21` |

## 8. 影子账号（`F-L22` / `F-L23`）

**规则摘要**：映射键只能是上游不可变账户标识，**禁止邮箱 / 钱包**（`D-L84`）；只展示当前获准资料，确认删除的旧值不残留，未返回与读取失败不误判为删除（`D-L85`）；快照标注来源（`D-L86`）；快照不作跨角色唯一身份判据（`D-L87`）；不采集资产方邮箱（`D-L88`）。

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L221` | 资产方 | 已建号 | 用户在上游修改联系方式后再次登录 | 本平台仍识别为**同一账号**，**不产生第二个账号**，历史记录不分裂 | `D-L84` |
| `AC-L222` | 资产方 | — | 分别尝试以**邮箱**、**钱包地址**、**企业 id** 作为认人的依据 | 三者**均被拒绝**；邮箱 / 钱包为空的用户可正常建号且不互相合并；**不同上游账户的记录不混用，不以同企业多成员作为本期前提** | `D-L84` |
| `AC-L223` | 资产方 | 已建号 | 尝试修改 `asset_account_id` | **不可变，本平台无任何修改路径** | `D-L84` |
| `AC-L224` | 资产方 | 首次 SSO 成功 | 建号 | 建号发生在 `L-5`；失败时按 `E-L41` 落 `P-L13`，**不静默跳回** | SSO 5.3、`D-L89` |
| `AC-L225` | 资产方 | 上游已删除某快照字段的值 | 再次 SSO 成功回跳 | **上游已确认删除的旧值不在本平台残留**并更新同步时间；未返回字段或读取失败不得误判为已删除，不把失败展示成空数据 | `D-L85` |
| `AC-L226` | 资产方 | 进入账户设置 | 查看身份信息 | 身份分区统一说明来自资产可信平台并由其维护，与可写偏好分区区分；不要求逐字段重复来源说明 | `D-L86`、`D-L114` |
| `AC-L227` | 资产方 + 资金方 | 同一联系方式同时存在于资产方影子账号与资金方账号 | 两边分别登录 | **互不冲突、互不挡号**；不会因快照字段相同而拒绝另一角色登录 | `D-L87` |
| `AC-L228` `[待确认 Q-L01]` | 资产方 | 上游按暂定清单提供邮箱 | 进入账户设置 | 邮箱只读；所在身份分区有上游维护说明与前往修改入口，无输入框或编辑按钮 | `D-L113`、`D-L105` |
| `AC-L229` `[待确认 Q-L01]` | 资产方 | 上游给出用户 id，但未给邮箱、钱包地址或某类基本信息 | 登录并查看账户、企业信息 | 登录正常；可选缺失字段不展示，整个资料组缺失显示相应空态；地址入口缺失显示「我的账户」；不要求补填，通知仍走站内信 | `E-L40`、`D-L117`、`D-L118` |
| `AC-L230` `[待确认 Q-L01]` | 资产方 | 上游给了钱包地址 | 走查资产方全链路与账户设置 | 钱包地址**只读展示**；**全站不出现任何面向资产方的钱包相关操作入口**；资产方的登录方式**仍然只有第三方授权登录一种**；权限矩阵未因此放开任何一行 | `D-L112`、`D-L13` |
| `AC-L231` `[待确认 Q-L01]` | 资产方 | 同一上游账户的当前有效企业关联变化 | 再次登录并查看资料与历史记录 | 仍为同一本平台账户，历史记录不分裂；当前企业资料对应有效归属，待审核新企业不提前生效；不引入本地企业切换或多成员功能 | `D-L84`、`D-L85`、SSO 4.1 |

## 9. SSO 结果、双入口与首登协议（`F-L24`～`F-L26`）

**规则摘要**：三类结果 `R1` / `R2` / `R3`；**失败一律回落游客态，唯一例外是 `E-L41`**（`D-L89`）；用户可见文案中性 + 错误码（`D-L90`）；配置类错误触发运维告警（`D-L91`）；两条入口从 `L-1` 起共用同一套落地链（`D-L96`）；首登未同意协议时为受限会话（`FL-1.1`、`D-L95`）。

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L241` | 资产方 | 构造 `E-L30`～`E-L38` 各分支 | 回调失败 | **均回落游客态 + Toast**，不进错误页 | `D-L89` |
| `AC-L242` | 资产方 | 失败已发生 | 回落后观察落点 | 用户**停在原本要去的页面的游客视图**并可继续浏览，**不被强制送到资产广场、不弹登录页** | `D-L89` |
| `AC-L243` | 资产方 | 失败已发生 | 查看提示文案 | 中性表述 + 一个可复述给客服的错误码；**不同失败原因给用户的说法无法被区分** | `D-L90` |
| `AC-L244` | 运维 | 分别构造：身份信息不是来自授权中心、上游未给出账户标识、两边登记的落地位置不一致 | 触发上述三种情况 | **均触发运维告警**；用户侧行为与其他失败完全一致，不变 | `D-L91` |
| `AC-L245` | 资产方 | 构造 `E-L41` | 影子账号建立失败 | 落 `P-L13`，页面**同时提供「重试」与「以游客身份继续浏览」两个出口**，含可复述错误码 | `D-L89` |
| `AC-L246` | 资产方 | 走查全链路 | 全流程走查 | **不存在只有报错、没有任何可点出口的页面**；除 `P-L13` 外**没有第二个失败页** | `D-L89` |
| `AC-L247` | 资产方 | 从资产可信平台的入口 A 进入 | 回到本平台落地 | 走与入口 B **同一条**处理链，落地到上游指定的那一页 | `D-L96` |
| `AC-L248` | 资产方 | 在本平台选「资产方」 | 跳转前 | 展示 `C-L04` 离站告知，**含目标平台名称**；未确认**不发起跳转** | `D-L92` |
| `AC-L249` | 资产方 / 游客 | 从不同原入口打开离站确认 | 点击返回或按 Escape | 恢复原类型选择页、原引导弹层或原账户页，并恢复焦点；不发起授权，不强制改去整页身份选择 | 主干 2.6 `D-L18`、SSO 5.1 |
| `AC-L250` | 资产方 | 入口 B 已发起授权 | 在授权中心登录页放弃并用浏览器后退返回 | 按「待完成授权」记录恢复类型选择页或原触发点，**不白屏、不报错** | `D-L93`、`E-L46` |
| `AC-L251` | 资产方 | 两条入口各走一遍 | 比对 `L-1`～`L-9` | 每一步行为一致；**实现与文档中只有一套落地逻辑**，差异仅在授权发起方与回跳来源 | `D-L96` |
| `AC-L252` | 资产方 | 本平台已有**另一账号**会话 | 以新账号回跳落地 | **先销毁旧会话再建新会话**，落地后 Toast 告知已切换账号，**不出现串台** | `E-L42` |
| `AC-L261` | 资产方 | 首登且未同意协议 | 尝试访问任意本平台页面与接口 | 会话为受限会话：**除 `P-L11`、协议正文、退出外一律不放行** | `FL-1.1`、`D-L95` |
| `AC-L262` | 资产方 | 在 `P-L11` | 查看、勾选、同意以及加载或保存失败 | 只有一个同意复选框；阅读不代勾，未勾选不可提交。加载失败可重试；保存期间不重复提交，失败后仍受限且可重试；成功后才继续落地 | `FL-1.3` |
| `AC-L263` | 资产方 | 已成功同意协议 | 查看可追溯记录 | 可确认用户、当时同意的协议版本及原文、语种和同意时间；后续协议修改不会覆盖当时同意内容 | `FL-1.4` |
| `AC-L264` | 资产方 | 在 `P-L11` | 选择「退出并以游客身份继续浏览」 | 会话被销毁并回落游客态，**不出现回到 `P-L11` 的死循环** | `FL-1.5` |
| `AC-L265` | 资产方 | 两条入口各走一遍首登 | 比对 | 协议同意步骤**完全一致**，挂在 `L-8` 上而非某条入口特有 | `FL-1.6` |

## 10. 会话与登出（`F-L27` / `F-L28`）

**规则摘要**：资产方 SSO 登录有效期 **1 周（暂定）**、无「记住我」（`S-1`）；退出回落游客态且**不级联登出上游**（`S-2`、`D-L107`）；上游登出必须能同步结束本平台登录，包括告知延迟时的失效兜底（`S-4`、`D-L106`）；**无法确认来源的登出告知一律丢弃**（`E-L48`）；失效表达一律就地降级、不弹登录页（`D-L108`）。

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L271` | 资产方 | 已登录 | 走查界面与登录有效期 | 登录有效期为 `N-L20`（当前暂定 **1 周**）；界面**无「记住我」**。到期后按 `AC-L273` 就地降级，用户可重新登录继续 | `S-1` |
| `AC-L272` | 资产方 | 已登录 | 主动退出 | 回落游客态并可继续浏览，**不落到登录页**；**上游会话不受影响** | `S-2`、`D-L107` |
| `AC-L273` | 资产方 | 会话到期 | 到期后继续操作 | 就地降级为游客视图 + Toast，**不跳转、不弹登录页** | `D-L108` |
| `AC-L274` | 资产方 | 已登录 | 用户在资产可信平台登出，上游随即告知本平台 | 本平台的登录在 `N-L22` 内结束 | `S-4` |
| `AC-L275` | 资产方 | 失效已发生 | 观察页面 | 回落游客态并**给出说明**（「你已在资产可信平台登出……」），页面就地降级为游客视图，**不弹登录页** | `E-L43`、`D-L108` |
| `AC-L276` | 攻击者视角 | 目标用户当前处于登录态 | 向本平台发送一条**无法确认来源**的登出告知 | **该用户的登录不受影响**，仍是登录态；告知被丢弃并触发安全告警 | `E-L48` |
| `AC-L277` | 资产方 | 上游的登出告知**没有送达** | 等到下一个复核点 | 定期复核**补上失效**；登录不会因为告知丢失而长期存活 | `E-L49`、`D-L106` |
| `AC-L278` | 资产方 | 上游失效告知发生延迟或暂不可用 | 在上游退出 | 仍须满足 `S-4` 的失效结果；不能以告知不可用或较长复核间隔视为永久登录，也不能宣称已达到未验证的时限 | `D-L106`、`X-08` |
| `AC-L279` | 资产方 | 会话到期时正在填表 | 到期 | Toast 提示内容未保存，**不暂存草稿** | `S-5`、`E-L44` |
| `AC-L280` | 审计 | 走完整链路 | 检查留痕 | 授权登录的三类结果、建号、快照刷新、协议同意、会话建立与失效原因、登出告知的收发**均有留痕，保留 `N-L26`**；且登录过程中产生的凭据、回跳句柄与签名原文**不入日志** | `S-6` |

> **原 `Q-L02` 已关闭**：两类角色的登录时效**各自独立**（SSO 6.5），本章**只验收资产方 SSO 这一侧**；资金方钱包登录的时效与过期表达归 WS-349，**不在本册验收范围内**。`ST-2`（退出回落游客态）与 `D-L108`（失效后就地降级、不弹登录页）是两类角色共用的主干口径，**正常验收**——「各自独立」只作用于多久过期与怎么表达，不作用于过期之后落在哪。

## 11. `verification_status` 上游只读消费（`F-L29`）

**规则摘要**：权威在上游，本平台**只读、不得回写、不自建实名认证**（`D-L97`）；放行只认「已认证」（`D-L98`）；细分只用于文案分流（`D-L99`）；硬卡点前实时复核（`D-L100`）；降级不得静默（`R-5`、`ST-1`）。

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L281` | 任意 | 全代码 / 全文档 | 走查 + 检索 | 本平台**不存在任何写入、修正或管理 `verification_status` 的路径**；也不存在自建实名认证流程 | `D-L97` |
| `AC-L282` `[待确认 Q-L01]` | 资产方 | 上游提供各类当前有效综合认证结论；包含更新审核中仍有效、驳回后已失效及仅单项审核通过 | 判定放行 | **仅上游确认当前有效已认证时为 `L3`**，其余为 `L0`；不因申请处于审核中自行降级，不因单项通过自行升级；真实接口对应待开发核实 | `D-L98`、SSO 7.1 |
| `AC-L283` | 资产方 | 分别构造「已驳回」与「尚未完成」 | 触发引导 | **文案分流正确**，但**放行结果相同（均为 `L0`，均不放行）** | `D-L99` |
| `AC-L284` | 资产方 | 本平台持有陈旧的「已认证」快照，上游已降级 | 在硬卡点发起请求 | 服务端**实时复核后拒绝**，陈旧快照**不被放行** | `D-L100`、`R-3` |
| `AC-L285` | 资产方 | 用户刚在上游完成认证 | 返回本平台并等待 `N-L23` | 刷新为 `T3`，**无需退出重登**；走入口 A / B 再次进入时立即生效 | `R-1`、`R-2`、`US-L27` |
| `AC-L286` | 资产方 | 上游由已认证降级 | 降级后下次进入 | **按 `EV-L20` 发一条站内信 + 显式说明**；界面从 `T3` 表达切回 `T2` 表达，**不表现为功能凭空消失**；同一降级的刷新不重复提醒，详见第 16 章 | `R-5`、`ST-1` |
| `AC-L287` | 资产方 | 上游返回的取值不在约定枚举内 | 判定 | **按 `L0` 保守处理**并告警，**不猜、不修正、不落自造值** | `E-L39` |
| `AC-L288` | 资产方 | 刷新请求失败 | 会话内进入受限判定点 | **沿用旧快照，不阻塞浏览** | `R-2` |

## 12. 资产方三态、降级表达与硬卡点（`F-L30` / `F-L31`）

**规则摘要**：三态二维判定，不得压成一维（`D-L111`）；降级只有 ⊘ / ❌ / ◐ 三种表达；`T2` 不做全局封锁；**唯一硬卡点是「发起融资申请」**，前端 + 服务端双重校验（8.3、`D-L103`）。

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L291` | 资产方 | 分别构造「会话有效但认证被收回」「认证有效但会话已失效」 | 判定三态 | 两种组合**均被正确判定**，不落进未定义状态 | `D-L111` |
| `AC-L292` | 资产方 | 各态下分别取契约 | 取值检查 | 与 SSO 8.1 的三态取值表**逐格一致**；资产方**不出现 `L1` / `L2`** | SSO 8.1、`D-L62` |
| `AC-L293` | 资产方 `L0` | 已登录未认证 | 浏览全站 | 可正常浏览、进账户设置、看消息中心，**不被全局封锁**；全局展示常驻补全提示条 | SSO 8.2 |
| `AC-L294` | 资产方 `L0` | 置灰能力可见 | 点击 | 显示正确认证引导并保留原操作意图；弹层本身不改变账户状态 | SSO 8.2、`D-L43` |
| `AC-L295` | 资产方 `L0` | 查看引导文案 | 走查 SSO 8.4 各条 | 文案逐条说清缺什么 / 补完能做什么 / 去哪补；**不出现「无权限」「请先登录」** | `D-L40`～`D-L42` |
| `AC-L296` | 资产方 `L0` | — | 点「发起融资申请」 | 前端不放行并唤起引导；**绕过界面直接发起请求时同样被拒绝，融资申请未被创建** | SSO 8.3 |
| `AC-L297` | 资产方 | 走查资产方全链路 | 清点硬卡点 | 「发起融资申请」是资产方链路上**唯一**的硬卡点 | SSO 8.3 |
| `AC-L298` | 任意功能需求 | 走查全部功能文档 | 检索 | **不存在自行新增的第二个硬卡点**；新增均可回溯到主干矩阵的变更记录 | `D-L103`、`D-L56` |
| `AC-L299` | 资产方 | 正在确认融资资格 | 等待期间发生降级、退出、过期、身份变化或离开页面，随后旧成功结果到达 | 不显示当前操作成功、不恢复旧权限、不自动拉回旧页面。降级仍在原页时给认证引导；复核失败可重试；再次进入须重新判断资格 | `D-L116`、`D-L100` |

## 13. 账户菜单、账户设置与企业信息（`F-L32` / `F-L33`）

**规则摘要**：上游给的账户资料四项及个人、企业认证基本信息在本平台**一律只读、不提供任何修改入口，绕过界面的修改请求同样被拒绝**（`D-L104`）；资料仅本人可见，业务必要信息自动获取并遵循该业务既有角色，不扩大为整份基本资料公开（`D-L119`）；只读区块须说明原因、**不放置灰的编辑按钮**（`D-L105`）；跳链跨平台须先离站告知；**不存在联系方式的录入或修改功能**（`D-L14`、`D-L88`）。

| 编号 | 角色 | 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- | --- |
| `AC-L301` | 资产方 | 正常会话进入 `P-L12` | 查看信息与字段 | 账户信息、个人信息、偏好内容可区分，全文可读；具体布局不作为本条验收条件；无资料编辑或灰色编辑按钮，无登录状态分区、页内退出或保存偏好按钮；不展示内部待确认编号 | `D-L114`、`D-L105` |
| `AC-L302` | 资产方 | — | 绕过界面直接发起一次修改 | **请求被拒绝，数据未发生任何变化**；再次进入账户设置看到的仍是原值 | `D-L104` |
| `AC-L303` | 资产方 | 进入 `P-L12` | 点「前往资产可信平台修改」 | 跳转前有离站告知并带回跳；只读区块**有一句话说明为什么不能改** | `D-L105`、`D-L92` |
| `AC-L304` | 资产方 | 进入 `P-L12` | 走查 | **不存在邮箱或其他联系方式的录入 / 修改入口**——邮箱即使由上游给出也只是只读展示；上游没给的那一项区块整体不展示 | `D-L14`、`D-L88`、`D-L113` |
| `AC-L305` | 资产方 | 身份资料加载中或加载失败 | 查看、重试 | 加载中为占位；失败就地提示并可重试，不伪造身份或认证结果；可选字段确实缺失才隐藏对应项 | `D-L114`、`E-L40` |
| `AC-L306` | 资产方 | 正常账户页，语言可编辑 | 改选语言，分别保存成功、失败、重选重试 | 选择立即保存，无保存按钮；处理中锁定该项；成功生效并反馈，刷新、重进、重登均回显；失败选择与实际生效值恢复旧值，显示失败且可再次选择；同值不重复保存 | `D-L115` |
| `AC-L307` | 资产方 | 偏好保存尚未完成 | 从菜单退出、过期或离开页面后收到旧结果 | 不显示迟到成功、不返回旧页面、不恢复旧身份；未成功修改无草稿，失效按 `S-5` 提示；再次进入读取最后成功值，已成功保存不被回滚。退出仅在菜单提供，且不退出上游 | `D-L115`、`D-L117`、`S-2`、`S-5` |
| `AC-L308` | 资金方 / 游客 / 受限会话资产方 | 直接进入资产方账户地址 | 查看路由结果 | 资金方进入自身 `P-L23`，不暴露资产方资料；游客不能读取账户资料；未同意协议的资产方仍受限于 `P-L11` | `D-L114`、`FL-1.1` |
| `AC-L309` | 资产方 | 钱包地址已取得、缺失或身份已变化 | 查看顶部并以悬浮、点击、键盘打开菜单 | 有地址时为前 6＋省略号＋后 4（短值完整），全文可核对；缺失用「我的账户」，不伪造地址；菜单内显示资产方，有「企业信息」且无「机构信息」，仅菜单可退出；地址与当前账户一致 | `D-L117` |
| `AC-L310` `[待确认 Q-L01]` | 资产方 | 上游实际供给与适用模板已核实；本人可见原则已定 | 对照本人当前账户和有效企业资料查看 `P-L12`、`P-L14` | 字段含义、有效归属、适用模板及缺失行为符合 SSO 9.4；待审核企业不替代有效企业；不重复采集、不混用他人资料。证件供给和脱敏未确认部分不判联调通过，也不显示明文 | SSO 9.4、`D-L118`、`D-L119` |
| `AC-L311` | 资产方 | 企业信息有值、可选缺失、整组缺失、读取中、读取失败 | 分别查看并重试 | 有值只读；可选缺失隐藏；整组缺失为有出口的空态；读取中占位；失败提示与重试，不伪造成空数据；所有情况下菜单名称仍为「企业信息」 | `D-L118`、`E-L40` |
| `AC-L312` | 资产方 `L0` / `L3`、游客、资金方、受限会话 | 直接访问 `P-L14` | 查看页面及尝试修改 | 两档资产方仅可查看本人有效归属下已取得资料且修改被拒绝；游客不读资料；资金方回可达资产广场并提示不适用；未同意协议仍到 `P-L11`；信息齐全不放开融资资格 | 主干矩阵 19、`D-L104`、`D-L118` |
| `AC-L313` | 资产方 | 企业页打开离站确认，或读取中退出 / 换身份 | 取消离站、从上游返回、旧读取结果到达 | 取消回当前页并恢复焦点；上游返回刷新当前资料；旧账户与旧企业数据不残留、不因迟到结果重新出现，无本地企业切换或认证提交 | `D-L18`、`D-L85`、`D-L118` |
| `AC-L314` | 资产方 | 个人信息组有值、无值或读取失败 | 查看账户设置与只读边界 | 个人信息只读，有值、空态、失败可重试可区分；证件号码供给与脱敏未确认时不展示真实明文；不出现个人证件影像或企业证明材料上传、预览、下载功能；上游表单必填性不变成本地补填要求 | SSO 9.1、9.4、`D-L104` |
| `AC-L325` | 本人资产方、另一资产方、同钱包资金方、游客、运营人员 | 本人资料已取得，分别处于 `L0`、`L3` | 从入口、直接地址及其他可达方式查看个人/企业基本资料 | 仅本人正常会话可读；同企业或同钱包不扩大本人范围；其他角色及账户不能取得整份资料。有效认证、有效回跳不突破此边界；受限会话继续执行 `P-L11` 限制 | `F-L23` / `F-L32` / `F-L33`、`D-L119`；主干 5.3 |
| `AC-L326` | 当前资产方及业务既有可见角色 | 某业务已有明确必要字段、对象及角色权限；分别有数据、可选缺失、必要信息缺失或失败 | 获取并展示业务所需企业名称、登记信息等；尝试继续依赖该信息的操作 | 自动获取且只读，不要求借贷端手填；仅向该业务已有可见角色展示所需单项，不开放账户资料整组或证件、个人联系方式。可选缺失按空态；必要信息不足或主体不明仅限制相关动作，说明原因并保留返回、适用重试/上游查看；不伪造当前有效信息 | `F-L23` / `F-L33`、SSO 9.4 `D-L119` |
| `AC-L327` | 资产方 | 本模块可选事件清单为空，分别有/无历史通知偏好 | 查看账户偏好并刷新、重新登录 | 不出现可操作通知开关，不产生无效果的保存；展示“认证等关键通知始终接收”。默认开启政策仅对应明确可选事件，不新增事件来填充开关用途；语言偏好仍可即时保存 | `F-L32`、`D-L120`；通知 3.1 |
| `AC-L328` | 资产方 | 无偏好记录、历史开启或历史关闭，分别已有已读/未读消息 | 确认有效认证真实降级，并重复收到同一事件 | 各种偏好情况下均产生应有的 `EV-L20`；既有历史消息不因偏好变化被删除、改变已读态；同一降级不重复、不恢复未读，不追加非关键通知 | `F-L29` / `F-L32`、`D-L120`；通知 3.1、4 |
| `AC-L329` | 资产方 | 同一账户有效企业 A，存在待审企业 B；另一账户 C 可登录 | 资料更新、切换登录账户及旧资料迟到 | 待审 B 不覆盖有效 A，申请进度不替代有效认证；有效关联不明时不得继续依赖该关联的动作。切到 C 后不显示 A 账户个人/企业资料；无本地补填、企业切换或成员管理新能力 | `F-L23`、`D-L84`、`D-L85`、`D-L119` |

## 14. 资产方范围反向验收

| 编号 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- |
| `AC-L398` | 全文检索资产方 SSO 分册与其实现 | **不含**回跳目标的解析或跳转实现、**不含**目标判定规则的第二套实现、**不含**类型选择页与引导组件的定义 | SSO 1.1 |
| `AC-L399` | 全文检索资产方 SSO 分册与其实现 | **不含**任何本平台凭据（密码 / 验证码 / 钱包连接）的资产方实现；**不含**自建实名认证；**不含**对 `verification_status` 的写入路径 | `D-L13`、`D-L97` |

---

## 15. 待确认项对验收的影响

分工以[主册 8.4](./00-登录主干与资产方SSO接入-PRD.md#interface-boundary)为准。开发负责核实真实接口与业务结果的对应关系，测试据此验证身份、有效企业归属、有效认证结论和会话联动；不以技术方案选型作为产品待定项。P01 已确认的本人可见、业务自动获取及既有角色展示可直接验收；实际供给由开发核实，剩余证件脱敏口径不由测试或开发自行补定。所有依赖真实上游能力的条目（包括资料删除、企业归属变化、状态复核和登出联动）均须取得相应对接证据；模拟场景通过只能说明本平台业务行为通过，不能宣称真实联调通过。

| 编号 | 事项 | 对验收的影响 |
| --- | --- | --- |
| **`Q-L01`**（部分业务口径已确认，外部供给未关闭） | P01 已确定展示原则；实际字段供给、有效结论、缺失含义及联动能力由开发核实；证件供给与脱敏仍需落实 | `AC-L209`、`AC-L210`、`AC-L228`～`AC-L231`、`AC-L282`、`AC-L310` 的真实联调须有对应证据；`AC-L325`～`AC-L326`、`AC-L329` 可先用模拟数据核验已定权限、自动取得与主体隔离行为，模拟通过不代表上游接入完成 |
| **~~`Q-L03`~~**（已关闭） | 默认开启，仅控制明确可选事件；本模块只有强制 `EV-L20`，可选清单为空，不交付可操作开关 | 按 `AC-L327`～`AC-L328` 验收本期实际用途和强制通知；不再以政策待定暂停验收，不新增模拟可选事件冒充正式范围 |
| **~~`Q-L02`~~**（**已关闭**） | 两类角色的登录时效**各自独立**，本平台不强求一致 | 第 10 章按此**正常验收**资产方这一侧（`N-L20` 当前暂定 1 周）；资金方钱包登录的时效归 WS-349，**不在本册范围内**——这是边界，不是待确认 |
| — | `asset_trust_platform` 尚未确认同等回跳校验 | `AC-L014` 的**通过判据是「不跳转」**，不是「跳转成功」。对端确认后该条改为验证正常跨平台回跳，届时同步更新本册 |
| — | 站内路径前缀字面串待与页面登记表对齐 | `AC-L009`～`AC-L013` 的**业务安全边界不受影响**；具体路由随页面登记对齐 |


<a id="notification-acceptance"></a>
## 16. 本模块消息通知（`F-L29` / `F-L30`，`EV-L20`）

规则来源：[本模块《消息通知》](./08-登录主干与资产方SSO接入-消息通知.md)。本节只验本模块产生的通知，不代验资金方机构审核或消息中心全部页面。

| 编号 | 角色 / 前置条件 | 场景 | 预期结果 | 回溯 |
| --- | --- | --- | --- | --- |
| `AC-L315` | 资产方已有账户及有效认证 | 上游确认有效综合认证由已认证变为非已认证 | 应用降级并产生一条 `EV-L20` 站内信，保留 `ST-1` 显式说明；通知不依赖再次登录或页面访问 | 通知 3 |
| `AC-L316` | 首次未认证、持续未认证，或更新审核中仍有效认证 | 登录、刷新或读取异常 | 不误发认证失效消息；异常处理及当前权限仍按 SSO 规则，不以通知结果判断权限 | 通知 2、3 |
| `AC-L317` | 同一次降级已有消息，分别已读/未读 | 重复上游告知、多标签刷新、重登、补送 | 仍是一条，已读不恢复未读，不重新置顶或改变原事件时间，不增加未读计数 | 通知 4.1 |
| `AC-L318` | 同一账户降级后已恢复有效认证 | 随后再次发生真实降级 | 产生新的独立消息，旧记录与已读状态保持，不合并吞掉新事件 | 通知 4.3 |
| `AC-L319` | 资产方、同钱包资金方、其他账户和运营人员 | 查看消息、未读数、标记已读及跳转 | 仅被降级的本平台资产方账户可见；其他身份无此消息、不计入其未读，不能修改或读取 | 通知 2、3 |
| `AC-L320` | 资产方账户不在线、通知投递失败或存在历史关闭偏好 | 发生确认降级，再合法登录 | 业务限制及时生效，不等通知成功；最终按同一事件补送且不重复。强制通知不受偏好关闭影响，未登录不可读取 | 通知 3 |
| `AC-L321` | 一条降级消息已存在，当前资格已恢复 | 阅读旧消息并进入账户 | 历史正文不伪装为最新资格；不再执行降级，不删除历史；操作资格按当前真实状态判断 | 通知 4.4 |
| `AC-L322` | 本人消息，分别为有效会话、失效会话和映射缺失 | 点击“查看账户” | 正常到本平台 `P-L12`，不自动出站；失效走主干访问规则，映射缺失保留正文但不显示跳转操作；前往上游仍需既有离站确认 | 通知 4.5～4.6、5.1 |
| `AC-L323` | 中英环境及翻译缺失场景 | 查看消息标题、正文、时间与按钮 | 中英文案齐备、变量集合均为空，正文无完整敏感标识、内部错误或未解析占位；翻译缺失沿用消息框架回落；已读不代表认证已完成 | 通知 3、5.2 |
| `AC-L324` | 普通登录/退出/偏好保存、未处理消息长期存在 | 使用原有功能及反复访问 | 无欢迎信、登录流水、周期催促、额外待办、声音、弹窗或站外渠道；不向资金方或运营人员广播本模块事件 | 通知 2、4 |

模拟数据可验证上述可观察业务结果；真实上游状态、身份关联和投递渠道需对应联调证据；登记差异与真实接入依赖分别见通知 6.1、6.2。`Q-L01` 不因本通知文件交付而关闭；共享登记及消息页面的最终比对由 WS-356 / WS-360 在各功能模块通知完成后处理。
