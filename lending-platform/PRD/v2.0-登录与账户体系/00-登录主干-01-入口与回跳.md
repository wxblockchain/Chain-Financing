# 借贷平台 · 面客端 · 登录主干与资产方 SSO 接入 PRD
## 功能需求说明 · **登录主干 A · 入口与回跳**

> **本册读者**：登录主干与前端壳层的实现方、安全评审。
> **本册装的是「用户怎么进来」和「登录完怎么回去」**——登录入口与路径分流（第 2 章）、回跳（第 3 章），以及这两章的业务字段、可调参数与异常分支（第 7～8 章）。
> 引导组件、权限矩阵、三态契约在**主干 B**；资产方 SSO 链路在**资产方 SSO 分册**；范围、编号段位、角色阅读入口见主册；验收见《验收需求说明》。
> **流程图不在本册**——登录入口分流与回跳的全景图见 [`03-登录主干与资产方SSO接入-流程图与状态机.md`](./03-登录主干与资产方SSO接入-流程图与状态机.md) **图 1**，本册正文只引用图号。
> **本册不写资金方的功能正文**（WS-349）。

> **登录主干分两册交付，按读者切，不按章节序号切**；章节号与全部编号**跨两册连续**，内容不重复、不缺漏。两册各自自解释。

| 册 | 文件 | 装了哪些章节 | 给谁看 |
| --- | --- | --- | --- |
| **主干 A** | `00-登录主干-01-入口与回跳.md` | **1** 定位 ｜ **2** 登录入口与路径分流 ｜ **3** 回跳 ｜ **7** 业务字段与可调参数 ｜ **8** 入口与回跳的异常分支 | **主干实现方与安全评审**。第 3 章是全 PRD 唯一有安全后果的一章 |
| **主干 B** | `00-登录主干-02-引导与权限契约.md` | **4** 受限操作引导组件 ｜ **5** 权限矩阵总表 ｜ **6** 三态与对外契约四字段 ｜ **9** 非功能约束 | **设计与前端；所有展示类功能；所有要往矩阵里填行的需求；WS-349** |

**跨册引用写法**：正文一律写「主干 X.Y」（如「主干 3.3」「主干 6.2」），按上表定位到册；资产方 SSO 分册写「SSO X.Y」。

---

## 1. 本册定位与前提

### 1.1 本册讲什么 / 不讲什么

| 你在找 | 去这里 |
| --- | --- |
| 登录入口在哪、用户怎么选身份、两条路径怎么分流 | **本册第 2 章** |
| 回跳目标怎么判定、句柄怎么流转、跨平台白名单 | **本册第 3 章**（全册唯一有安全后果的一章） |
| 句柄登记项、身份记忆字段、可调参数 | **本册第 7 章** |
| 入口与回跳的异常怎么处理 | **本册第 8 章** |
| 用户被拦下时看到什么、多条提示怎么排 | **主干 B · 第 4 章** |
| 某个能力在某一档下可不可用 | **主干 B · 第 5 章** |
| 该读哪几个字段判断用户是谁 | **主干 B · 第 6 章**（**状态语义的唯一定义处**） |
| 非功能约束（安全、可用性、可访问性……） | **主干 B · 第 9 章** |
| 资产方怎么跳授权中心、影子账号、会话联动失效 | **资产方 SSO 分册** |
| 资金方怎么连钱包、机构注册、账户体系 | **不在本 PRD**——WS-349 |
| 某个字段在脱敏档下显示成什么样 | **不在本 PRD**——由该展示类功能按 `X-L1` 自行定义 |

### 1.2 必须先知道的四条前提（来自主册，不重复定义）

| # | 前提 |
| --- | --- |
| 1 | 平台默认**游客态**，登录是能力升级而不是准入门槛 |
| 2 | **资产方的身份权威在资产可信平台**，本平台不为资产方建立任何本地凭据 |
| 3 | **资金方的身份权威在本平台**，主身份是钱包地址 |
| 4 | 两类角色登录后落到**同一套三态权限矩阵与引导组件**，主干只定义一套状态语义与拦截 / 引导规则 |

### 1.3 用户故事

| 编号 | 故事 | 验收锚点 |
| --- | --- | --- |
| `US-L01` | 作为游客，我打开网址就能看内容，不被强制跳到登录页 | `AC-L021` |
| `US-L02` | 作为游客，我在资产详情页点了「出资」，被引导登录后**回到那条资产并且那一下生效了**，而不是被丢到首页 | `AC-L001` |
| `US-L03` | 作为用户，我在登录时清楚知道自己要选哪种身份、点下去会发生什么，选错了能退回来重选 | `AC-L025`、`AC-L034`、`AC-L040` |
| `US-L04` | 作为被拦下的用户，我看到的提示告诉我**缺什么、去哪补、补完能做什么**，而不是「无权限」 | `AC-L042` |
| `US-L05` | 作为同时缺多项的用户，我不会被几条提示条同时糊在屏幕上 | `AC-L048` |
| `US-L06` | 作为审核中的用户，引导文案不会暗示我可以去改或撤回 | `AC-L044` |
| `US-L07` | 作为业务功能的开发者，我知道该读哪四个字段、该查哪份文档，不必自己解释账户状态 | `AC-L071` |

---

## 2. 登录入口与路径分流（`F-L01`～`F-L03`）

### 2.1 页面与组件清单

| 编号 | 名称 | 形态 | 说明 |
| --- | --- | --- | --- |
| **`P-L01`** | **用户类型选择页** | 整页 | 主动点「登录」/「申请入驻」时进入 |
| **`P-L02`** | 登录入口壳 · 游客态路由承载 | 壳层 | 承载右上角常驻「登录」按钮与游客态可达路由 |
| **`P-L03`** | 身份切换中转 | 整页 | 「切换身份」的返回落点 |
| **`P-L04`** | 主干侧通用失败态 | 整页 | 仅用于主干自身无法继续的情形；链路内的失败仍归各链路（如资产方 SSO 分册的 `P-L13`） |
| **`C-L01`** | 类型选择弹层 | 弹层 | 与 `P-L01` **同一组件的另一种呈现**，内容一字不差 |
| **`C-L02`** | 受限操作引导弹层 | 弹层 | 主干 B · 第 4 章 |
| **`C-L03`** | 常驻补全提示条 | 条 | 主干 B · 第 4 章 |
| **`C-L04`** | 离站告知确认 | 弹层 | 跨平台跳转前 |

### 2.2 游客态默认与登录入口（`F-L01`）

| 编号 | 口径 |
| --- | --- |
| **`D-L01`** | **平台的默认态是游客态。** 任何未登录用户访问游客态可达路由，**一律直接渲染内容——不得跳转到登录页、不得弹出遮挡性的登录弹层、不得用登录墙盖住内容** |
| **`D-L02`** | **登录只在两种时机出现**：① 用户主动点右上角「登录」；② 用户触发了受限操作（主干 4.2 引导）。**除此之外不得出现任何形式的登录提示** |
| — | **右上角常驻「登录」按钮**：全站壳层内固定位置，游客态可见；登录后替换为账号下拉。点击 → `P-L01` |
| — | **游客态路由白名单**：3.3.1 中标 ✅ 的前缀，游客态**全部可直接访问**。白名单外的路由对游客返回「不可见」而不是「跳登录」（矩阵 ❌ 的语义，主干 5.1） |
| — | 「申请入驻」是**同一条链路的另一个入口标签**，不是第二个入口页：与「登录」指向同一个 `P-L01`，差异只在**入口文案**与**建号后的引导** |

### 2.3 用户类型选择与路径分流（`F-L02`）

> **本节是相对基线的实质变更**：基线是「共用表单 + 登录后分流」，本期是**路径分流**——两张卡片点下去进入的是**两条完全不同的流程**，不是同一个表单的两种填法。基线登录页形态需**重画**。

| 编号 | 口径 |
| --- | --- |
| **`D-L03`** | **必须显式选择用户类型，不可跳过、不可由系统推断。** 角色由**用户点击的入口**确定 |
| **`D-L04`** | **类型选择是路径分流，不是表单切换**：选「资产方」→ 进入离站告知后前往**资产可信平台授权中心**；选「资金方」→ 进入**连接钱包并签名**。**两条路径不共用任何输入控件，也不存在「先填再判断身份」的中间态** |
| **`D-L05`** | `P-L01` 与 `C-L01` 是**同一组件的两种呈现**：两张卡片的标题、说明、按钮文案**必须逐字一致**。弹层形态额外带一行上下文说明（主干 4.2） |
| **`D-L06`** | **两张卡片并列、视觉权重相同**。不得把任一方做成默认选中、高亮或更大——本平台两侧是对等的交易参与方，权重差异会被读成平台立场 |
| **`D-L07`**（**否决项，必须显式保留**） | **「输入邮箱后自动判定身份并路由」维持否决，不得实现。** 三条理由：① 系统回答「这是资产方账户」等于确认该邮箱在资产可信平台存在，属账户枚举；② 本期资产方**根本不采集邮箱**，该方案在资产方一侧无输入可依据；③ 该方案在评审中极易被重新提出（看起来省一次点击），因此否决理由必须留在 PRD 里 |
| **`D-L13`** | **本平台不为资产方提供任何自有账号、密码、验证码登录或兜底入口。** 授权中心不可用时资产方无法登录，这是有意的取舍（风险见 SSO 第 11 章），**不得以「加一套本地凭据」缓解** |
| **`D-L14`** | **本平台不向资产方采集邮箱或任何其他联系方式。** 资产方的身份与联系方式均来自上游，本平台侧无录入入口、无修改入口（SSO 第 9 章） |
| **`D-L15`** | **主按钮按「用什么方式登录」命名，角色作副标**：`第三方授权（资产方）` / `连接钱包（资金方）`。这两个按钮**点下去用户立刻离开本平台的界面**——去到资产可信平台，或看到浏览器里的钱包弹窗。**按钮上写的动作必须就是他下一眼看到的那件事**；写成中性的「继续」，用户不知道下一屏为什么是一个钱包弹窗、或者为什么换了一个网站 |
| **`D-L16`** | **点击入口就直接进入对应的外部环境，两侧对称、都没有中间页**：资产方 → 先看到离站告知 `C-L04`，确认后到资产可信平台；资金方 → **直接唤起浏览器里的钱包**。**任一侧都不得插入一个「再点一次同样动作」的本平台页面**——入口按钮写「连接钱包」、下一页主按钮还写「连接钱包」，那一页只让用户多点了一次 |
| **`D-L17`** | **`D-L16` 只合并「连接」这一步，不合并后面的确认。** 资金方侧**连接与签名仍然是两次，签名不自动接着弹**：连接只是让本平台知道用户用的是哪个钱包地址，**不建号、不建会话**，直接弹没有风险；但**签名意味着建号**，在此之前本平台必须先把用途说明、凭据保管须知与协议勾选露给用户看。**签名确认这一层及其失败态归 WS-349**（`P-L20`），本 PRD 到「连接成功」为止，不展开 |

**信息架构（线框级，不是视觉稿）**

```
┌──────────────────────────────────────────────┐
│  平台标识                          [语言切换] │
│                                              │
│              选择你的身份                     │
│         为你展示对应的功能与流程               │
│                                              │
│   ┌────────────────┐   ┌────────────────┐   │
│   │  资产方         │   │  资金方         │   │
│   │  <说明两行>     │   │  <说明两行>     │   │
│   │ [第三方授权]    │   │ [连接钱包]      │   │
│   └────────────────┘   └────────────────┘   │
│        ↓ 点击即离站          ↓ 点击即唤起钱包   │
│    离站告知 → 资产可信平台    钱包弹窗（无中间页）│
│                                              │
│   上次你选择了「资金方」 · [直接继续]          │ ← 仅在有记忆时出现（2.4）
│                                              │
│   以游客身份继续浏览                          │ ← 弱化的文本链接
└──────────────────────────────────────────────┘
```

**卡片文案定稿（中英对照）**

| 位置 | 中文 | English |
| --- | --- | --- |
| 页面标题 | 选择你的身份 | Choose your role |
| 页面副标题 | 为你展示对应的功能与流程 | So we can show you the right features and flows |
| 卡片 1 标题 | 资产方 | Asset party |
| 卡片 1 说明 | 我持有应收账款，想发起融资。**将前往资产可信平台登录。** | I hold receivables and want to raise financing. **You'll sign in on the asset trust platform.** |
| 卡片 2 标题 | 资金方 | Funder |
| 卡片 2 说明 | 我提供资金，想参与出资。**连接钱包并签名即可开始。** | I provide capital and want to invest. **Connect your wallet and sign to get started.** |
| 卡片 1 主按钮 | 第三方授权（资产方） | Authorize with asset trust platform |
| 卡片 2 主按钮 | 连接钱包（资金方） | Connect wallet |
| 记忆行 | 上次你选择了「{身份}」 | Last time you chose "{role}" |
| 记忆行按钮 | 直接继续 | Continue as {role} |
| 弱化出口 | 以游客身份继续浏览 | Keep browsing as a guest |

> **卡片 1「将前往资产可信平台登录」这句是必须的**：资产方点下去会离站，用户应当在点之前就知道。正式的离站告知在 `C-L04`，但文案上先打招呼能显著降低跳出。
> **卡片 2 不提邮箱**：资金方的邮箱在机构注册流程中填写，不是登录凭据；在登录入口提邮箱会让用户以为可以用邮箱登录。
> **两张卡片的主按钮文案不对称是有意的**（`D-L15`）：它们分别说的是两件不同的事，而不是同一个「下一步」。卡片的标题、说明仍然对等，**视觉权重也仍然相同**（`D-L06`）——`D-L15` 改的是按钮上写什么，不是谁更突出。
> **选错类型的代价不对称**：选错「资金方」只是多签一次名；选错「资产方」会**在资产可信平台建出一个空账号**，本平台无法撤销。因此卡片说明是唯一的预防手段，不得简化为一个词。

### 2.4 记忆上次选择（`F-L03`）

| 编号 | 口径 |
| --- | --- |
| **`D-L08`** | **存储位置：浏览器本地存储**——未登录态没有账户可挂载。**存储键 `lp_last_role`**（`lp` = lending platform），**取值只有 `asset_party` / `funder` 两种**，不存任何其他信息 |
| **`D-L09`** | **失效条件**（任一命中即清除）：① 写入后满 `N-L02` = 90 天；② 用户选择了另一种身份（覆盖写入）；③ 用户点了「切换身份」；④ 用户主动清除浏览器数据。**退出登录不清除**——同一台电脑上的同一个人下次多半还是同一种身份 |
| **`D-L10`** | 记忆**只影响「多给一个快捷入口」，不影响默认选中，也不参与任何服务端判定**。有记忆时在两张卡片下方多出一行「上次你选择了 X · 直接继续」；**两张卡片本身仍然对等、仍然都要点一次**。记忆不得演变成自动跳转——那等于绕过了 `D-L03` |

> **为什么记忆不能做成自动跳转**：一台共用电脑上，上一个人是资金方，下一个人打开就被直接送进钱包连接流程——他会以为平台只有这一种身份。

### 2.5 「切换身份」的返回路径

| 场景 | 「切换身份」出现在哪 | 点击后 |
| --- | --- | --- |
| 已选资产方、进到离站告知（`C-L04`）但还没跳走 | 弹层内的次要操作 | 关闭弹层 → 回 `P-L01`（经 `P-L03`） |
| 已点「连接钱包」，**钱包弹窗还开着** | 不提供——此刻界面在钱包那边，本平台没有可见的控件 | 用户在钱包弹窗里取消即可，按 `E-L11` **回到仍然可见的 `P-L01`** |
| **已连接成功、尚未签名** | 签名确认层顶部返回区（该层归 **WS-349**） | 回 `P-L01`，**断开本次钱包连接、不保留任何未完成的中间态**；本 PRD 只定这一条返回口径，该层的其余规格不展开 |
| 从授权中心返回、尚未建立会话 | 落地时 | 回 `P-L01` |
| **已登录成功之后** | **不提供** | 已登录用户要换身份，路径是**退出登录**（`ST-2` 回落游客态）后重新选择 |

| 编号 | 口径 |
| --- | --- |
| **`D-L11`** | **切换身份：保留回跳句柄、清除身份记忆、不清空机构注册草稿。** 保留句柄是因为「用户想去哪」没变、变的只是「以什么身份去」（若身份实际改变则按 `D-L36` 重新生成）；清除记忆是因为用户刚刚用行动否定了上次那个选择；草稿归资金方域自身状态机，与身份选择无关 |
| **`D-L12`** | **已登录用户不得出现「切换身份」入口。** 一个已建立会话的用户点「切换身份」，会同时面临「旧会话怎么办」「已产生的业务数据归谁」两个没有答案的问题 |

---

## 3. 回跳 `RT-1`～`RT-5`（**安全关键章**）

> **唯一实现原则 `D-L39`**：回跳的参数承载、目标判定与跳转**只有本章这一处实现**。资产方链路（SSO 分册）、资金方域（WS-349）、各业务功能一律**调用**，**不得自建**。
> **为什么必须只有一处**：目标判定是**否定式安全控制**——它要挡住的是「所有没被明确允许的目标」。同一条规则写三遍，只要有一遍漏了一种绕过写法（协议相对地址、反斜杠、编码后的 `@`、大小写混淆的域名……），攻击者就从那一处进来。**三处实现的安全性等于其中最弱的那一处，不是三者的平均。**

### 3.1 五条原则（`RT-1`～`RT-5`）

| 编号 | 原则 |
| --- | --- |
| `RT-1` | 回跳目标**不以原始地址的形式在客户端流转** |
| `RT-2` | 回跳目标必须通过**白名单判定**才允许跳转 |
| `RT-3` | 回跳记录与登录流程**同生命周期** |
| `RT-4` | 跨平台回跳须**对端配合并执行同等强度校验** |
| `RT-5` | 目标失效时**静默兜底**，不报错、不白屏 |

### 3.2 回跳句柄的形态（`F-L04`）

| 编号 | 口径 |
| --- | --- |
| **`D-L20`** | 回跳目标**不以原始地址的形式在客户端流转**。主干在生成时把目标登记下来，只把一个**不透明句柄**交给调用方；句柄本身不含目标信息，拿到句柄也推不出目标（`RT-1`） |
| **`D-L21`** | 句柄**只能由服务端保管**：① 本平台内的分流，句柄留在服务端，随本次浏览器会话对应；② 跨平台去授权中心时，句柄**随授权请求在两边后台之间传递**，不经用户可见的地址栏。**任何情况下都不得出现在用户可见的地址栏参数里，也不得存在浏览器上任何页面脚本能读到的位置**——能被复制的东西就会被复制 |
| **`D-L22`** | 句柄**与登录 / 注册流程同生命周期**：流程完成、放弃、或会话销毁时立即作废（`RT-3`） |

**句柄的登记内容（产品口径，不是存储结构）**

| 项 | 说明 | 必填 | 业务意义 |
| --- | --- | --- | --- |
| 目标路径 | 已通过 3.3 判定的**站内路径**（跨平台时为对端标识 + 对端路径） | 是 | 登录完要去哪 |
| 触发能力标识 | 被拦下的那个能力在矩阵里的行标识（主干 5.3） | 否 | 支撑 3.6「重新执行被拦下的操作」；也是引导文案取「缺什么」的依据。用户主动点「登录」而非被受限触发时为空 |
| 目标域标识 | `self`（本平台）或 3.4.1 登记表中的一项 | 是 | 区分站内与跨平台，决定走 3.3 还是 3.4 |
| 生成时间 | — | 是 | 判定有效期 |
| 绑定会话 | 生成时所在的浏览器会话 | 是 | 防止句柄被搬到另一个会话里消费（`D-L27`） |

### 3.3 站内目标判定规则（`RT-2` · `F-L05` · **本章最关键的一节**）

**判定顺序自上而下，任一条不通过即判定失败，直接走 3.7 的失效兜底。不得调换顺序、不得跳过任何一条。**

| # | 编号 | 判定 |
| --- | --- | --- |
| 1 | **`D-L23`** | **只接受站内相对路径**：目标必须以**单个正斜杠 `/`** 开头。以下一律拒绝，**不做任何「修正后再试」的兜底**——带协议头的绝对地址（`http://`、`https://` 及任何 `xxx:` 形式）、**协议相对地址 `//`**、以反斜杠开头或含反斜杠的写法（`\`、`/\`）、以 `.` 或 `..` 开头的相对写法 |
| 2 | **`D-L24`** | **先规范化再判定，且规范化只做一轮**：对目标做一次百分号解码 + 路径归一（消解 `.` 与 `..`）。**若规范化后与规范化前不一致、或一次解码后仍含 `%` 编码序列，一律判定失败**——多轮解码是绕过判定的标准手法，本平台不给第二轮机会 |
| 3 | **`D-L25`** | **路径前缀必须命中允许清单**（3.3.1）。命中判定按**完整路径段**比较，**不做字符串前缀包含判定**：`/assets` 命中 `/assets` 与 `/assets/123`，但**不命中** `/assetsx`、`/assets-admin` |
| 4 | **`D-L26`** | **目标不得含用户信息段或凭据段**：规范化后的路径中不得出现 `@`；查询串中不得出现任何形如令牌 / 验证码 / 签名 / 回跳的参数（清单见 3.3.2）。命中即判定失败并按 `E-L03` 安全留痕 |
| 5 | **`D-L27`** | **句柄必须由生成它的那个会话消费**。跨会话、跨设备消费一律判定失败（`E-L04`） |
| 6 | **`D-L28`** | **`Referer` / `Origin` 不得作为信任依据**，只能用于留痕 |

#### 3.3.1 允许的站内路径前缀清单（`N-L03`）

> 这是**允许清单**，不是禁止清单——**没列进来的一律不允许**。
> 清单**按能力分组维护，不按具体页面维护**：新增一个业务页面若落在已有前缀下，无需改清单；新增一个业务**域**才需要走主干 5.4 同级的变更流程并留痕。
> **前缀字面串须在本平台页面登记表（WS-347 原型底座）落地后做一次对齐**——判定规则不受对齐结果影响，对齐只改清单里的字符串。

| 前缀 | 覆盖 | 允许回跳 | 说明 |
| --- | --- | --- | --- |
| `/`（精确匹配） | 平台首页 | ✅ | 也是站内链路的最终兜底目标（3.7） |
| `/assets` | 已上架代币资产列表与详情 | ✅ | `US-L02` 的主场景 |
| `/financing` | 融资需求与代币质押 | ✅ | 资产方硬卡点的主要来源 |
| `/funding` | 出资 / 授信报价 / 放款 / 还款 | ✅ | 资金方硬卡点的主要来源 |
| `/console` | 我的控制台 | ✅ | 登录后的常用落点 |
| `/onboarding` | 机构注册引导页与审核进度页 | ✅ | 引导弹层主按钮的去向之一 |
| `/account` | 账户设置 | ✅ | 补齐引导的去向 |
| `/messages` | 面客消息中心 | ✅ | 降级通知里的跳链 |
| `/about`、`/faq`、`/agreement` | 平台介绍 / FAQ / 协议 | ✅ | 游客可达 |
| **登录与认证自身的路径** | `P-L01`～`P-L04`、`P-L10`～`P-L13`、钱包连接与签名页 | ❌ **明确禁止** | `D-L29` |
| **其余一切** | — | ❌ | 含管理端路径、内部诊断路径、静态资源路径 |

| 编号 | 口径 |
| --- | --- |
| **`D-L29`** | **回跳目标不得是登录流程自身的任何页面。** 命中即判定失败——否则用户会在「登录 → 回跳到登录页 → 再登录」之间打转 |

#### 3.3.2 目标中不得携带的参数类别（`N-L04`）

**回跳目标里不得夹带下列任何一类参数，命中即判定失败。**

| 类 | 这一类是什么 | 为什么不允许 |
| --- | --- | --- |
| **身份与登录凭据类** | 任何用于表示「这个人已经登录了」的参数 | 一条链接就能把别人的登录状态带走 |
| **一次性授权凭据类** | 任何用于换取一次登录的凭据 | 同上，且这类凭据往往可被立刻使用 |
| **验证码与签名类** | 任何一次性验证码或签名 | 泄露即被冒用 |
| **回跳自身类** | 任何用于指定「下一步去哪」的参数 | **防二次跳转**：允许目标里再带一个回跳，等于把校验过的目标变成一个新的跳板 |

> **具体参数名清单**由实现方按上述四类维护，属 `N-L04` 安全配置；**新增或删除任何一项都必须走主干 5.4 同级的变更流程并留痕**。本 PRD 定义的是「哪几类不允许」，不逐一枚举参数名——枚举会随技术方案变动，类别不会。

### 3.4 跨平台回跳与三平台域名精确白名单（`RT-4` · `F-L06` · `N-L05`）

> **本节是相对基线的实质变更**：基线的对端只有一个、且判定落在「平台内同域 + 一张对端登记表」。三平台独立部署后，**同域假设不再成立**，对端登记表升级为**三平台域名精确白名单**。

| 编号 | 口径 |
| --- | --- |
| **`D-L30`** | **三平台域名精确白名单是一份平台级登记表，不是可由调用方传入的参数**。表中每一项登记：平台标识、站点域名、允许回跳到该平台的路径前缀、以及该平台是否已确认执行同等强度校验。**登记表的唯一来源是 3.4.1**，任何链路不得在运行时临时添加对端 |
| **`D-L31`** | **域名判定按完整域名精确比对**：与登记值逐字符相等才算命中，**不做子域匹配、不做后缀匹配、不接受通配符、不接受端口或大小写变体**。这是「精确白名单」四个字的全部含义 |
| **`D-L32`** | **对端必须做同等强度的校验**（`RT-4`）。对端未确认执行 3.3 同等规则的，**不得开启到该平台的跨平台回跳**——我方判定得再严，对端把用户跳走，钓鱼一样成立 |
| **`D-L33`** | 跨平台回跳**必须显式告知即将离开本平台并展示目标平台名称**，载体为离站告知弹层 `C-L04`，**不用 Toast**——Toast 出现即消失，用户来不及读就已经跳走 |
| **`D-L34`** | 跨平台场景按 `D-L21` 第 ② 种方式携带句柄；**用户回到本平台落地时，必须先确认这次返回确实对应本平台发起的那一次授权、且仍是同一个浏览器会话，再消费句柄**，顺序不得颠倒 |

#### 3.4.1 三平台域名精确白名单登记表

| 平台 | 标识 | 站点域名 | 允许的对端路径前缀 | 本期是否开启 | 对端已确认同等校验 |
| --- | --- | --- | --- | --- | --- |
| **借贷平台（本平台）** | `self` | 本平台面客端域名 | 见 3.3.1 | ✅ | — |
| **资产可信平台** | `asset_trust_platform` | 待登记 | 授权中心登录与授权路径；用户中心（实名认证引导的去向） | ✅ **本期唯一的跨平台回跳对端** | **❌ 未确认** —— 见 `E-L06` 与 `[待确认 Q-L01]` |
| **代币发行平台** | `token_issuance_platform` | 待登记 | — | **❌ 本期不开启** | — |

> **`asset_trust_platform` 一行**：授权中心是新增模块，其对外提供信息的形式与回跳校验强度**均未确认**（`Q-L01`）。按 `D-L32`，在对端书面确认执行 3.3 同等规则之前，**跨平台回跳按 3.7 兜底处理、不放行**（`E-L06`）。这**只影响资产方跨平台引导的体验**——用户去授权中心完成认证后回来落在首页而不是原来那条资产——**不产生安全问题，也不阻塞开发**。
> **`token_issuance_platform` 一行**：面客端在本期没有前往代币发行平台的用户流程，故登记为占位、**不开启**。开启须走主干 5.4 同级的变更流程并留痕，**不得因为「表里已经有这一行」就当作已放行**。
> **两处「待登记」的域名字面串**：随三平台部署域名确定后填入，**判定规则不受影响**。

### 3.5 有效期与作废时机

| 时机 | 处理 | 编号 |
| --- | --- | --- |
| **超过有效期**（`N-L01`，默认 **30 分钟**，可配置） | 句柄失效，按 3.7 兜底（`E-L02`） | **`D-L35`** |
| **被成功消费一次之后** | **立即作废，一次性使用**。重放同一句柄按失效处理（`E-L05`） | **`D-L35`** |
| 登录 / 注册流程完成、用户中途放弃、会话销毁（含退出登录、会话过期、跨平台失效） | 作废 | `D-L22` |
| **用户在流程中改变了身份类型** | **作废并重新生成**——原句柄记录的触发能力可能不再适用于新身份 | **`D-L36`** |

> `N-L01` 取 30 分钟的理由：它要覆盖「被拦下 → 选身份 → 去授权中心登录 / 连钱包签名 → 回来」这一整段。同时不宜长到让一个被遗忘的标签页在半天后仍能跳转。**这是可调参数，不是安全边界**——安全边界是 3.3 的判定规则。

### 3.6 消费与落地

**落地优先级（自上而下，命中即停）——两类角色共用同一张表：**

| 优先级 | 条件 | 落地目标 | 用户看到的表达 |
| --- | --- | --- | --- |
| 1 | 存在**通过判定且未失效**的句柄 | **回到原触发点，并重新执行那个被拦下的操作** | 无中间页、无额外提示 |
| 2 | 本次是**资金方首次签名建号** | 机构注册引导页（`/onboarding`，规格归 WS-349） | 建号确认 + 注册引导 |
| 3 | 本次是**资产方首登且协议未同意** | `P-L11` 首登协议同意（SSO 5.4） | 受限会话 |
| 4 | 其余 | **该身份的默认落地页**（3.7） | 正常进入 |

| 编号 | 口径 |
| --- | --- |
| **`D-L37`** | **优先级 1 是「重新执行被拦下的操作」，不是「回到那个页面让用户自己再点一次」。** 用户在资产详情页点「出资」被引导去登录，登录完回来应当直接进入出资流程——这正是句柄要登记「触发能力标识」的原因（3.2） |
| **`D-L38`** | **重新执行前必须重新做一次权限判定**（主干第 5 章矩阵 + `D-L50`）。登录完成不等于该能力就可用——资金方登录后仍可能因机构资料未提交而在硬卡点上被拦。此时**不是报错，而是转入主干 4.2 的受限引导**，并**保留原句柄的触发能力标识**供文案取用 |

### 3.7 失效兜底（`RT-5` · `F-L07`）

**目标失效、判定不通过、或跨平台对端未确认时，一律落到该身份的默认落地页 + 一条 Toast，不报错、不白屏、不显示技术错误信息。**

| 身份 | 默认落地页 |
| --- | --- |
| 游客 | 平台首页 |
| 资产方 | 平台首页 |
| 资金方 | 平台首页 |

> **Toast 文案口径**：只说「已为你回到首页」这一层，**不暴露判定失败的原因**（如「目标不在白名单内」）——把判定细节讲给用户，等于讲给探测者。失败原因进留痕（3.8），不进界面。

### 3.8 留痕

| 项 | 口径 |
| --- | --- |
| 判定失败 | 记录判定失败的规则编号与**规范化后的**目标，**不记录原始目标的完整内容** |
| 安全留痕 | `E-L03` / `E-L04` / `E-L05` 三类**单独标记为安全事件**，不与普通失败混在一起——这三条是被探测的信号 |
| **不入日志** | 回跳句柄、登录过程中产生的任何一次性凭据、钱包签名原文 **一律不入日志** |

---

## 7. 业务字段与可调参数

> 本章只写**产品要定的业务口径**（取值、枚举、必填、校验、默认、业务意义）。存储结构与索引由开发按本 PRD 自行设计。

### 7.1 回跳句柄登记项的校验口径

| 项 | 校验 | 默认值 |
| --- | --- | --- |
| 目标路径 | 必须通过 3.3 的全部六条规则 | 无默认——不通过即**不生成句柄** |
| 触发能力标识 | 必须是主干 5.3 总表中存在的行标识 | 空（用户主动点「登录」时为空，落地走优先级 4） |
| 目标域标识 | `self` 或 3.4.1 登记表中**已开启且已确认**的一项 | `self` |
| 有效期 | `N-L01` | **30 分钟** |
| 消费次数 | **上限 1 次** | — |

### 7.2 身份记忆

| 字段 | 位置 | 取值 | 必填 | 默认 | 业务意义 |
| --- | --- | --- | --- | --- | --- |
| `lp_last_role` | 浏览器本地存储 | `asset_party` / `funder` | 否 | 无 | 上次选择的身份，**仅用于展示快捷入口**，不参与任何服务端判定（`D-L10`） |
| 写入时间 | 同上 | 时间戳 | 与上者同在 | — | 判定 `N-L02` = 90 天失效 |

### 7.3 可调参数

| 编号 | 参数 | 取值 | 说明 |
| --- | --- | --- | --- |
| `N-L01` | 回跳句柄有效期 | **30 分钟** | 可调；**不是安全边界** |
| `N-L02` | 身份记忆有效期 | **90 天** | 可调 |
| `N-L03` | 允许回跳的站内路径前缀清单 | 见 3.3.1 | **属安全配置**，变更须走主干 5.4 同级流程并留痕 |
| `N-L04` | 目标中禁止出现的查询参数清单 | 见 3.3.2 | 同上 |
| `N-L05` | 三平台域名精确白名单 | 见 3.4.1 | 同上；本期仅 `asset_trust_platform` 一项拟开启，且受 `D-L32` 约束 |

> `N-L03`～`N-L05` 虽然也叫「可调参数」，但**改它们等于改安全边界**，与 `N-L01` / `N-L02` 不是一类。放在同一张表里是因为它们都是配置项，**审批级别不同**。

---

## 8. 入口与回跳的异常分支

| 编号 | 场景 | 处理 | 留痕 |
| --- | --- | --- | --- |
| **`E-L01`** | 目标未通过 3.3 判定 | 按 3.7 兜底 | 记判定失败的规则编号与规范化后的目标 |
| **`E-L02`** | 句柄已过期或已被消费 | 按 3.7 兜底；Toast「链接已失效，已为你回到首页」 | 普通 |
| **`E-L03`** | 目标中含禁止的用户信息段或凭据参数（`D-L26`） | 按 3.7 兜底 | **安全留痕** |
| **`E-L04`** | 句柄被非生成会话消费（`D-L27`） | 按 3.7 兜底 | **安全留痕** |
| **`E-L05`** | 同一句柄重复消费 | 按 3.7 兜底 | **安全留痕** |
| **`E-L06`** | 跨平台对端未确认同等校验（`D-L32`；本期 `asset_trust_platform` 即处于此状态） | 按 3.7 兜底，**不跳转** | 普通 |
| **`E-L07`** | 浏览器本地存储不可用（隐私模式 / 已禁用） | **静默降级**：不展示记忆行，其余一切正常。**不得因此报错或提示用户** | 不留痕 |
| **`E-L08`** | 身份记忆值不是两个合法取值之一（被篡改或版本残留） | 视为无记忆并清除该键 | 不留痕 |
| **`E-L10`** | 用户在 `P-L01` 点了某一侧，但这一侧走不下去：授权中心不可达；**浏览器里没有可用的钱包**；**钱包唤不起来** | **停在 `P-L01` 并就地说明**（如何安装钱包、或改用其他方式），**另一条路径仍然可点**；**不跳 `P-L04`、不白屏、不进入下游册的任何页面** | 普通 |
| **`E-L11`** | 钱包连接弹窗**已经弹出来了**，用户在弹窗里点了取消 | **回到仍然可见的 `P-L01`**，两张卡片照常可点，**不留下一个空白页**。`D-L16` 去掉中间页之后这一侧就没有默认落点了，这条必须显式兜住 | 不留痕 |

---

> **接主干 B**：受限操作引导组件、权限矩阵总表、三态与对外契约四字段、非功能约束，见 [`00-登录主干-02-引导与权限契约.md`](./00-登录主干-02-引导与权限契约.md)。
> **验收**见 [`02-登录主干与资产方SSO接入-验收需求说明.md`](./02-登录主干与资产方SSO接入-验收需求说明.md)。
