# 资产广场 — 流程图与状态机 **V2.2**

> 本册集中本模块的全部流程图（`FL-AM-*`）与状态机（`ST-AM-*`），**开发与测试共同参考**，设计师据此理解页面流与状态呈现。
>
> **图册不另造业务口径**：每张图的完整规则在正文，图只表达角色、流程、分支与状态转换。正文入口 —— 总览与硬口径见 [`v1.0-资产广场-PRD.md`](v1.0-资产广场-PRD.md)，页面与字段见 [`01-功能需求说明.md`](01-功能需求说明.md)，验收见 [`02-验收需求说明.md`](02-验收需求说明.md)。

## 图索引

| 图号 | 名称 | 对象 / 视角 | 关联需求 | 正文出处 |
| --- | --- | --- | --- | --- |
| `FL-AM-01` | 代币从确权到在资产广场可见 | 跨平台数据来路（业务视角） | `F-AM-13`、`D-AM-47`～`D-AM-49`、`D-AM-57`～`D-AM-62` | 主册 7.1、分册 01 第 10 章 |
| `FL-AM-02` | 访客浏览与跳出路径 | 游客 / 登录用户 | `F-AM-01`、`F-AM-06`～`F-AM-10`、`X-AM-10` | 主册 7.2、分册 01 9.1～9.3 |
| `FL-AM-03` | 持有地址表与项目关系浏览【持有地址要求】 | 详情访客 | `F-AM-14`、`D-AM-65`～`D-AM-68` | 分册 01 9.2.4、11.7 |
| `ST-AM-01` | 代币状态轴 | 业务对象：**代币** | `D-AM-11`、`D-AM-16`、`D-AM-58`、`L-AM-07` | 主册 5.4、分册 01 11.2 |
| `ST-AM-02` | 质押状态轴与对外二元映射 | 业务对象：**质押记录** | `D-AM-12`、`D-AM-13`、`F-AM-09`、`L-AM-08` | 主册 5.5～5.6、分册 01 9.2.3、11.5 |
| `ST-AM-03` | 列表页视图状态 | 页面视图 | `D-AM-22`、`F-AM-04`～`F-AM-06`、`E-AM-01`、`E-AM-02` | 分册 01 9.1.3 |

**图例**：实线 = 正常路径；虚线 = 明确不存在的路径或对外不呈现的信息；方括号节点 = 页面；圆角节点 = 平台或业务动作。

---

## `FL-AM-01` 代币从确权到在资产广场可见

关联：`F-AM-13`、`D-AM-47`（本平台只持副本、不做代币权威）、`D-AM-48`（无上架闸门）、`D-AM-57`（新代币同步到即可见、失效当日可见）、`D-AM-58`（失效判定不在本平台）、`D-AM-59`（每日至少核对一次、差异以上游为准）。

```mermaid
flowchart LR
  A1(资产可信平台<br/>应收账款确权 + 上链存证) --> A2(代币发行平台<br/>铸造代币 · 代币事实权威)
  A2 -->|代币含当前唯一链上持有地址及准许公开的业务信息同步过来| B1(借贷平台<br/>持有只读副本)
  A2 -->|代币失效 · 当日同步| B1
  B1 --> V[仅取公开业务集合<br/>个人及企业账户资料不进入广场]
  V --> C1[P-F-AM-01 资产广场列表页]
  V --> C2[P-F-AM-02 代币详情页]
  P1(本平台质押业务<br/>融资事实权威) --> C1
  P1 --> C2
  B1 -.->|每日至少核对一次<br/>代币数量合计（枚） / 代币价值合计 / 逐笔代币状态<br/>差异以上游为准| A2
  B1 -.->|不存在：上架审核 / 运营挑选| GATE["本平台无此环节"]
  C2 -.->|不存在：打开页面时向上游临时取数| A2
```

> 两条标注「不存在」的虚线是显式声明：本平台**没有上架闸门**（`D-AM-48`、`X-AM-11`），页面**不向上游临时取数**（`D-AM-49`、`X-AM-13`）。核对虚线是本平台对自身数据的日常校准，**不是用户可见的页面动作**，核对状态也不对外展示（`D-AM-59`、`D-AM-54`）。

---

## `FL-AM-02` 访客浏览与跳出路径

关联：`F-AM-01`（入口）、`F-AM-06`（可寻址）、`F-AM-08`（自行查验）、`F-AM-09`（所属融资项目）、`F-AM-10`（登录/非登录一致与授权边界 `D-AM-52`）、`X-AM-10`（无反向入口）。

```mermaid
flowchart LR
  E1(顶栏导航 · 资产广场) --> LIST[P-F-AM-01 资产广场列表页]
  E2(外部分享链接 / 搜索引擎结果) --> LIST
  E3(外部分享的详情页链接) --> DETAIL
  LIST -->|点击编号以外的行内区域，含logo| DETAIL[P-F-AM-02 代币详情页]
  LIST -->|点击编号或Enter或Space| COPY[复制完整编号，反馈结果，保留列表视图]
  COPY --> LIST
  DETAIL -->|返回 / 浏览器后退<br/>条件、已加载范围与内嵌滚动位置恢复| LIST
  DETAIL -->|持有表中已质押关系 · 所属项目已发布| LS[借贷广场 · 融资需求详情]
  DETAIL -->|已质押 · 项目为草稿<br/>提示尚未公开 · 不给跳转| DETAIL
  DETAIL -->|区块浏览器 · 新窗口| EXP[[外部区块浏览器]]
  DETAIL -->|页尾 CTA · 用户主动点击| LOGIN(登录 / 注册)
  LS -.->|不新增反向入口| LIST
  LIST -->|顶部登录 · 用户主动点击| LOGIN
  DETAIL -->|顶部登录 · 用户主动点击| LOGIN
  LIST -.->|游客浏览全程不触发授权跳转| LOGIN
```

> 游客与登录用户走**同一条路径、看同一套信息**（`AC-AM-10`）；页尾 CTA 两种身份都展示，文案可不同（`AC-AM-15`）。

---

## `ST-AM-01` 代币状态轴（业务对象：代币）

关联：`D-AM-11`（状态取上游、本平台只呈现）、`D-AM-58`（失效判定不在本平台）、`D-AM-16`（与质押状态正交、不合并）、`L-AM-07`。权威方：代币发行平台。

```mermaid
stateDiagram-v2
  [*] --> 有效: 代币铸造完成并同步到本平台
  有效 --> 失效: 对应应收账款已过到期日，（上游当日判定并同步）
  失效 --> [*]
  有效 --> 已销毁: 本期无来源 · 页面上不会出现
  已销毁 --> [*]
  note right of 失效
    页面展示「失效」并以小字给判定依据
    （对应应收账款的到期日）
    仍计入头部「代币数量」与「代币价值」
    另立「其中失效」数量合计，单位枚
  end note
```

---

## `ST-AM-02` 质押状态轴与对外二元映射（业务对象：质押记录）

关联：`D-AM-12`（质押状态业务定义以本平台为准）、`D-AM-13`（对外收敛为二元）、`D-AM-14`（链上转移过程不对外）、`F-AM-09`、`L-AM-08`。权威方：借贷平台。

```mermaid
stateDiagram-v2
  [*] --> 可质押: 代币在本平台可见且无生效质押
  可质押 --> 已质押: 质押进入某融资项目资产池，且链上转移成功
  已质押 --> 已释放: 本平台确认质押业务已释放
  已释放 --> 可质押: 所属业务确认再次质押资格，本模块不判定
  可质押 --> 可质押: 质押发起但链上转移处理中或失败
  note right of 已质押
    对外展示「已质押」+ 所属融资项目跳转
    项目为草稿时只提示"尚未公开"、不给跳转
  end note
  note left of 可质押
    对外展示「未质押」
    处理中 / 失败 / 已释放一律归入此展示
    业务释放不等于链上地址已转回
  end note
```

> 对外只有 `未质押` / `已质押` 两个取值；链上转移状态、待提取、对账差异、gas、失败原因、撤回预扣**一律不对外**（`D-AM-15`）。一笔代币记录可以同时是「失效 + 已质押」，两个标签各自成列（`D-AM-16`、`AC-AM-43`）。

---

## `ST-AM-03` 列表页视图状态（页面视图）

关联：`D-AM-22`（条件、已加载范围与内嵌滚动位置恢复）、`F-AM-04`～`F-AM-06`、`E-AM-01`（全局空态）、`E-AM-02`（筛选空态）、`E-AM-08`（加载失败）。完整规则见[分册 01 9.1.3](01-功能需求说明.md#913-筛选排序搜索与内嵌滚动加载)，验收见 `AC-AM-18`、`AC-AM-19`、`AC-AM-30`、`AC-AM-47`。

```mermaid
stateDiagram-v2
  [*] --> 首批加载中: 无历史视图进入列表
  [*] --> 视图恢复中: 刷新或打开分享视图
  首批加载中 --> 可继续浏览: 首批最多20条且仍有更多
  首批加载中 --> 全部加载完毕: 首批已包含全部结果
  首批加载中 --> 全局空态: 本平台尚无已同步代币
  首批加载中 --> 筛选空态: 当前筛选或搜索无结果
  首批加载中 --> 首批加载失败: 加载失败
  首批加载失败 --> 首批加载中: 重试
  可继续浏览 --> 追加中: 内嵌列表向下滚动至已加载末端
  追加中 --> 可继续浏览: 追加20条且仍有更多
  追加中 --> 全部加载完毕: 追加剩余记录且无更多
  追加中 --> 追加失败: 保留已有记录与位置
  追加失败 --> 追加中: 用户重试本批
  state 列表浏览 {
    可继续浏览
    追加中
    追加失败
    全部加载完毕
  }
  列表浏览 --> 首批加载中: 筛选或排序或关键词变化，回到列表顶部
  列表浏览 --> 详情页: 点击已加载行编号以外区域
  详情页 --> 视图恢复中: 返回或浏览器后退
  列表浏览 --> 视图恢复中: 刷新或前进后退到对应列表视图
  视图恢复中 --> 列表浏览: 恢复条件和已加载范围，再恢复内嵌滚动位置
  视图恢复中 --> 全局空态: 对应视图为全局空态
  视图恢复中 --> 筛选空态: 对应视图为筛选空态
  筛选空态 --> 首批加载中: 调整条件或重置筛选
  note right of 全部加载完毕
    头部数量始终为筛选搜索全集的枚数合计
    不随追加、地址数或项目数改变
    末批不足20条时按实际余量展示
    显示结束提示，停止追加
  end note
  note right of 视图恢复中
    筛选、排序和关键词支持URL分享
    恢复已加载范围和内嵌滚动位置
    不用页码表示进度
  end note
  note right of 全局空态
    头部三项显示 0、不隐藏整块
    不出现「重置筛选」
  end note
```

---

<a id="fl-am-03"></a>

## `FL-AM-03` 持有地址表与项目关系浏览（持有地址要求）

关联：`F-AM-14`、`D-AM-65`～`D-AM-69`、`E-AM-12`、`AC-AM-68`～`AC-AM-77`。完整规则见分册 01 9.2.4、11.7、12.1a。Q-AM-55 已关闭，Q-AM-56 仅保留事实供给核对依赖，不是让访客选择的页面步骤。

```mermaid
flowchart TD
  A[游客或登录用户打开本代币详情] --> B[取得已同步且准许公开的业务信息]
  B --> L[持有地址模块加载反馈]
  L -->|加载失败| E[失败反馈，可重试]
  E -->|重试| L
  L -->|缺失或空集合或关系冲突| U[持有信息暂不可用，其他可用详情仍展示]
  L -->|本笔唯一当前链上地址与整笔事实可核对| C[五列按指定顺序，仅一个当前地址]
  C --> D[整笔数量与签发价值各一次，不计历史地址]
  D --> R{本笔当前质押业务结果}
  R -->|无生效质押或业务已释放| NP[未质押，项目为横线<br/>地址仍取实际持有位置，不提前改回]
  R -->|已质押，项目为草稿| DR[提示项目尚未公开，不给名称和链接]
  R -->|已质押，项目已公开| P[当前唯一项目名称及既有深链]
  P -->|点击| LS[借贷广场对应详情，目标按当前状态展示]
  C -->|复制地址| CP[复制完整地址并反馈结果]
```

当前唯一地址可以是实际持币合约。业务释放与实际转回是不同事实，不因“未质押”就把地址改回企业；上游提供实际转回后的当前事实时，当前行更新，不保留原地址为第二行。地址不代表账户或企业资料访问权。多当前地址、多有效项目不作为正常路径；事实缺失不当零、不默认未质押。主列表全集数量价值不随地址迁移增减。本流程不产生站内信，见独立通知册；运营察觉数据异常的责任仍保留。



---

> 需求变更时同步更新正文、图示与验收要求；状态名称、转换条件、角色与编号以正文为准。
