# 借贷广场 · 授信报价与接受拒绝 — 功能需求说明 V1.0

开发主读。范围、版本与编号归属见[主册](v1.0-借贷广场-授信报价与接受拒绝-PRD.md)，验证结果见[验收册](02-验收需求说明.md)。本册定义用户行为、业务输入与结果，不定义报文、内部字段或实现机制。跨模块同号条款必须连同来源模块阅读。

<a id="credit"></a>
## 一、授信判定、核定、追加与有效期

关联 `F-LS-20`～`F-LS-23`。入口为详情页 `P-LS-02` 当前“融资报价”环节，授信 `P-LS-04` 是报价弹窗的前置步骤。仅登录资金方本企业员工可做；无独立授信菜单，控制台只读。流程见 [FL-CR-01](v1.0-借贷广场-授信报价与接受拒绝-流程图与状态机.md#fl-cr-01)，对象状态见 [ST-CR-01](v1.0-借贷广场-授信报价与接受拒绝-流程图与状态机.md#st-cr-01)。

### 1.1 四分支与确认

| 进入时结果 | 用户先看到什么 | 确认后的行为 |
| --- | --- | --- |
| 无额度 | 尚未对本资产方核定授信，是否现在核定 | 新建模式，填授信总额与有效期 |
| 生效中但额度不足 | 本次报价金额、当前可用授信、差额，展示“本次报价 − 可用授信 ＝ 差额”；询问是否提额 | 追加模式，填增加额，展示当前额度与调整后额度 |
| 已到期 | 原到期日，询问是否重新核定 | 同一条授信关系重新核定，带出上一版额度供修改，重新填写有效期 |
| 生效中且充足 | 直接进入报价表单 | 不出现授信确认或授信表单 |

`D-CR-46`～`D-CR-50`：前三支先确认、后编辑。确认必须有遮罩，使下方内容不可交互；有确认/取消两个动作，讲清确认后进入额度编辑、取消返回详情页。不能实现为无遮罩的就地替换。同一时刻只有一个弹层，确认、授信表单与报价表单在同一报价流程中切换，不套第二层、不跳离详情页。

`D-CR-49`：在这一步取消，关闭整个报价弹窗，不生成授信记录、融资业务编号、任何金额计量变更或需求锁定，也不留下“曾取消”的业务记录；再次进入按当前事实重新判定。已成功核定授信之后放弃报价按 `D-CR-15` 处理，不能与确认前取消混同。

`D-CR-11`、`D-CR-15`：授信由资金方自行核定，提交即生效，无审批。完成后自动回报价并按 `AC-CR-04` 重验覆盖与授信；若授信仍不足，直接回追加表单并给出新差额，不重复询问授信意图。报价失败或放弃不回滚已生效授信；核定成功本身不产生报价或锁定。

### 1.2 授信业务输入与生命周期

`D-CR-10`：一对“资金方机构 × 资产方企业”只有一条授信关系，适用于该资产方全部项目，不能按项目拆开核定。首次核定生成稳定授信编号；追加、重新核定保留关系和历史。状态只有 `S-CR-1 生效中`、`S-CR-2 已到期`。

| 业务输入 / 展示 | 含义、必填、编辑与校验 |
| --- | --- |
| 授信对象（`CR-03`） | 由进入的项目带出资产方企业，只读且不可另选；资金方为当前企业 |
| 授信额度（`CR-05`） | 必填金额，USD，>0，最多两位小数。首次核定填总额；追加填增加额，必须>0；只允许增加，不提供调低、冻结、解冻。重新核定带出上一版额度，仍遵守不调低及不得低于已有授信已用额的要求 |
| 有效期至（`CR-07`） | 日期。首次及到期重新核定必填，默认核定日+1年；最早核定日+1年、最晚核定日+5年，越界拒绝且告知可选范围，无绕过。重新核定按本次核定日重算范围 |
| 追加时有效期 | 可选修改，不改保持原值；只能保持或后延，不能提前，仍受五年上限约束；追加不强制延至一年以后（`D-CR-20`） |
| 备注（`CR-12`） | 选填文本，最多200字，仅本机构可见；无默认内容 |
| 授信状态与首次核定时间 | 只读。追加不覆盖首次核定时间；核定、追加、重新核定、到期的历史均可追溯 |
| 三项金额与可用授信 | `授信占用额`、`在途报价金额`、两者合计的`授信已用额`，以及当前可用授信。不可编辑，不把授信已用额再作为第四个独立占用量 |

基线未规定首次额度、增加额的默认数值，本册不把原型演示的自动预填数升级成业务规则；须由机构明确提交金额。有效期五年上限已有确认，不再待确认。

`D-CR-13`、`D-CR-14`、`D-CR-42`、`D-CR-43`：授信到期只停止新增报价，既有在途报价与未偿本金按各自生命周期继续存在，不随到期归零；到期后新增报价先重新核定。项目到期不作废授信；授信到期不阻断既有报价的接受、放款与还款。页面提示授信步骤不触链、不产生 gas。

### 1.3 授信校验与异常

`AC-CR-03`：`授信占用额 + 在途报价金额 + 本次报价金额（折 USD） ≤ 授信额度`，全部按同一机构与资产方、跨项目合并。既有各笔按自己的锁定汇率计量。`授信已用额 = 授信占用额 + 在途报价金额`；可用授信显示为 `max(0, 授信额度 − 授信已用额)`。额度不足须在报价填写前给出本次金额、可用授信、差额与提额出路（`AC-CR-06`），不能仅说“额度不足”。

`INV-CR-01`：授信占用额与在途报价金额之和不得超过授信额度；报价只改变对应金额，不扣减授信额度本身（`D-CR-17`）。覆盖与授信是两道独立条件，同时通过才能报价（`D-CR-21`、`AC-CR-22`、L5 `AC-FIN-07`）。覆盖判断依据见[功能二](#quote)，不把覆盖不足导向授信追加。

| 异常编号 | 用户结果与处理 |
| --- | --- |
| `E-CR-01` | 授信成功但报价失败：保留授信，给出报价失败原因，无新融资业务编号；仍可报价时留在表单，状态已变则回详情展示当前状态 |
| `E-CR-02` | 核定后因其他项目报价使可用授信减少：回追加模式，显示最新三个数，两道条件再次满足后方可报价 |
| `E-CR-11` | 授信已用额超过额度：停止该二元组一切新增报价，生成运营待办；不自动改额度、不自动抹掉既有金额 |
| `E-CR-12` | 发现重复授信关系：沿用基线，按最早生效关系计算并告知运营处理，不把多条额度相加放大可用授信 |

`D-CR-22`、`D-CR-23`：资产方可在只读授信视图查看各机构对本企业的当前额度与已用额；机构不可看到其他机构的授信。每次变更须可还原由谁代表哪家企业、何时、因哪个项目改变了多少额度及有效期；内部备注不对资产方披露。

<a id="quote"></a>
## 二、报价、汇率、锁定与七天有效期

关联 `F-LS-24`～`F-LS-26`、`F-LS-31`、`F-LS-34`、`F-LS-36`。流程见 [FL-CR-01](v1.0-借贷广场-授信报价与接受拒绝-流程图与状态机.md#fl-cr-01)、[FL-CR-04](v1.0-借贷广场-授信报价与接受拒绝-流程图与状态机.md#fl-cr-04)。

### 2.1 入口、业务输入与提交

资金方登录且不是本项目资产方；需求处于 L5 下发的“待报价”、无在途融资业务、项目未到期、覆盖条件通过，方可提交新报价。一个需求同一时刻只有一家机构报价，先成功者锁定需求；不提供多家比价或部分融资。授信不足不把报价按钮永久禁用，而是在弹窗内进入功能一。

`P-LS-05` 自上而下只有“授信信息”和“本次报价表单”两段，不设置侧边/顶部操作导航。授信信息显示需求编号、需求金额、质押覆盖状态、本机构可用授信及报价成功后的余量。当前池况沿用 L5 展示，不能恢复常驻融资上限等中间量或报价时池快照。

| 业务输入 / 展示 | 含义、必填、默认与校验 |
| --- | --- |
| 融资金额（`QT-03`） | 只读，等于本轮需求金额，USD；明确“不支持部分融资，金额不可修改” |
| 结算币种（`QT-02`） | 必选 `USD` / `USDT` / `USDC`，只决定结算币种，不改变 USD 记账；基线未指定默认选中项，不预设某币种为已确认选择 |
| 汇率与结算金额（`QT-04`/`QT-05`） | 只读，选择币种后展示当时可获得的上游报价换算依据及金额；提交成功才锁定。展示明确的币种方向，不能只给一个无单位小数 |
| 融资利率（`QT-06`） | 必填年化百分比，`0 < 利率 ≤ 100`，最多两位小数，无默认报价；单复利、计息基数、起息日由还款模块承接，本模块不另定。表单提示“计息规则以双方签署的融资合同为准” |
| 还款类型（`QT-15`） | 业务枚举本期只有“按季付息；到期还本付息”，只读，无选择器；进入商务条款摘要（`D-CR-51`） |
| 最终还款日（`QT-16`） | 日期，只读，恒等于 L5 项目截止日；本期不能修改。跟随项目截止日，不建立独立业务维护入口（`D-CR-52`/`D-CR-53`） |
| 报价有效期与到期时刻（`QT-12`） | 只读168小时，从实际提交成功时起算；提交前的预计到期提示不能当作已经锁定的起点 |

`QT-07`/`QT-08` 保持作废：报价不采集、不展示、不校验资金方收还款账户；基线分册表单中的“还款账户”删除，以主册撤出裁定为准。不能因缺少该账户阻断报价或接受。

`AC-CR-11`、`D-CR-24`：提交前在同一弹窗二次确认，明确提交后不可修改/撤回、会锁定需求、其他机构不能报价、本机构将增加多少 USD 的在途报价金额、七天不处理自动失效并释放、提前解除只能由资产方拒绝。用户显式确认后才提交。

`D-FIN-79`：提交成功同一事件内生成稳定融资业务编号、锁定汇率、开始计时、增加本机构的在途报价金额、业务进入 `S-FD-1 待接受`，向 L5 提供锁定结果；`项目在途金额` 不变。失败不预占编号、不产生新在途报价金额、不锁定需求。覆盖不足被证实的情形还要按2.3使原需求失效，不能误读为“任何失败都不改变需求”。

### 2.2 汇率的唯一来源与可追溯结果

`D-FIN-17`：上游唯一汇率源由**代币发行平台**提供，本平台调用上游，不自算汇率、不建第二套汇率源。按上游汇率折算业务金额不等于本平台自行制定汇率。

`D-FIN-80`、`D-FIN-81`：报价成功时锁定该笔快照，锁定后由借贷平台自持，之后永不重算。四要素**汇率值、生效时间、来源说明、版本标识**必须完整、可追溯；版本标识须能稳定指向这份快照，不能在后续被替换成新行情。同一快照在放款、还款重复引用时，同一原金额须得到相同折算结果。既有每一笔在途报价、每一笔未偿本金各用自己的快照，不用新报价的汇率覆盖旧业务。

`D-FIN-82`：本期需求金额为USD，折USD后不变；结算金额按基线“融资金额（USD）÷汇率值”换算，此式中汇率值的业务方向为“一单位结算币种值多少USD”。若上游展示反向价格，页面必须讲清方向且折算结果一致，不能把乘除方向混用。结算金额随报价固化，放款与还款沿用该笔依据，最新汇率不改变既有债务或授信已用额。

选择币种后的换算展示是提交前预览；最终确认呈现的条款与实际锁定结果须一致。预览后汇率变化时，应展示更新后的结算金额与依据供用户确认，不能静默锁定另一组数。USD也必须有明确的换算依据，不能以“无需换算”为由绕过本轮唯一源要求。

`E-CR-18`：上游取数不可用、超时、缺少四要素或无法完成当场覆盖校验时，拒绝本次提交，清楚提示是“汇率暂不可用”或“暂时无法完成覆盖校验”及稍后重试；不生成编号、不产生在途报价金额、不放行、不退回缓存或历史汇率，也不把“无法判断”当成“已证实覆盖不足”而失效需求。已锁定业务仍使用本平台自持快照，不因上游此刻不可用重算或改写。

`S-FD-2`/`S-FD-11` 终结的报价快照不再用于新增放款/还款换算，历史四要素与当时金额仍可回溯；重新报价使用新编号、新快照与新计时。汇率接口契约即使尚未细化，上述义务不降低为可选项。验收见[验收二](02-验收需求说明.md#quote)。

### 2.3 覆盖终检与并发

`AC-CR-20`～`AC-CR-22`、`D-CR-35`～`D-CR-39`、`D-CR-44`/`D-CR-45`：提交时按当前权威事实重算并执行 [L5 `AC-FIN-26` 与 `INV-FIN-01`](../v1.0-借贷广场-融资需求与代币质押/v1.0-借贷广场-融资需求与代币质押-PRD.md)。沿用 L5 的已乘质押率口径、逐笔累计口径、失效顺序与停止条件，不按原始池值、不省略更早已保留需求项、不用进入页面时的旧数值放行。本期报价承接已发布需求，不重复增加项目在途金额（L5 `AC-FIN-23`）。

失效判据成立时，需求当场失效，本次报价不成立；已有待接受报价同一事件内按功能四终结。提示“该融资需求已失效（池内覆盖不足）”，说明资产方补足覆盖并重新发布后可再次报价，不归咎提交机构。覆盖不足提醒不是失效的前置步骤，没有补足缓冲期。

`E-CR-03`/`AC-CR-13`：两机构同时报价只允许一笔成功；另一笔提示已被报价，无融资编号和新增在途报价金额。`E-CR-04`：若提交时项目已到期、需求已撤下或状态已变，拒绝并展示最新状态与原因，不能保留旧数值诱导继续提交。任何写操作均按当前身份、权限与事实判定，页面按钮不能替代判定。

### 2.4 锁定展示与七天有效期

`D-CR-25`～`D-CR-29`：有效期从平台确认报价成功的时刻起整168小时，精确到秒，不按第七天日末取整，也不从资产方查看或通知到达起算。无延长、续期、宽限。只在 `S-FD-1` 有效，接受或拒绝成功后停止计时；后续待放款等环节不受该七天时限约束。

`D-LS-20`/`D-LS-21`：列表、详情与资产方控制台展示机构全称、锁定起始时间、已锁定时长、剩余有效期与到期时刻。剩余时间通常到小时，不足24小时到分钟并加强提示；时间显式标注时区。锁定信息公开且中性，不贴恶意标签。资产方明确可随时拒绝，或届满自动失效。接受后不再继续显示“等待接受”的倒计时。

无人登录也须届满自动转 `S-FD-11 报价已失效`，与机构在途报价金额全额释放及向 L5 反馈需求解锁同一事件生效；仍有效需求回待报价，项目在途金额不变、质押不释放。处理延迟不延长报价效力，到期后接受不可用。具体并发与两类失效见功能四。

<a id="respond"></a>
## 三、报价确认：账户、合同、接受与拒绝

关联 `F-LS-28`～`F-LS-30`。`P-LS-06` 由本企业资产方在详情页“报价确认”当前环节进入。只处理仍待接受且未届满的本企业报价。流程见 [FL-CR-02](v1.0-借贷广场-授信报价与接受拒绝-流程图与状态机.md#fl-cr-02)。

### 3.1 判断依据与三步进度

先展示机构名称及资质概况、融资业务编号、需求编号、金额与利率、结算币种与金额、锁定汇率、还款类型、最终还款日、报价时间和剩余有效期，以及 L5 当前池况与质押覆盖状态；不展示机构收还款账户、不恢复报价时池快照、不扩写 L5 详情页中间量。

接受三步依次为：填写并确认收款账户 → 查看可复制/打印的商务条款摘要 → 上传线下签署盖章件并勾选条款一致性声明。中断后保留已确认账户和已完成进度，返回可续做，但七天计时不暂停，进度区常驻倒计时（`AC-LS-102`）。三步均属于待接受内的填写进度，不能新增业务状态（`D-FIN-77`）；仅最后“确认接受”成功才进入待放款。

### 3.2 资产方收款账户

`D-FIN-94`/`D-FIN-95`：法币首次为空白，后续预填该企业最近一次成功接受所用账户，可逐项修改；本次成功接受的账户成为下次预填。每笔必须显式确认，默认不勾选；确认针对本次所填内容，修改后不能沿用对旧内容的确认。无账户簿、多账户列表、“设为默认”或独立账户管理入口。

USD 结算使用法币账户，USDT/USDC 使用企业统一数币地址；账户须与结算币种匹配。`D-FIN-85`：数币地址只读，来自企业统一地址，不接受人工指定；地址为空不能确认接受，并引导完成企业地址登记。链按基线 WS-326 `D-LN-60` 固定ETH，只展示、无选择器；本模块不办理链上转账。

`D-FIN-96`～`D-FIN-101`：以下为接受表单业务要求，不是接口字段清单。法币前七项必填、可编辑，均无虚构默认数据；第八组为选填。

| 表单项 | 内容与校验 |
| --- | --- |
| 收款人名称 | 企业户名，须与本企业法定名称或已登记英文名/曾用名一致；比对忽略大小写、空格、常见公司后缀标点与缩写差异 |
| 收款人地址 | 国家/地区、城市和详址，不能缺失 |
| 收款账号 / IBAN | 单项文本，按收款行所在地填账号或IBAN；校验长度与字符格式，不判断账户真实性；具体格式范围未在基线给出，见待承接细项 |
| 收款行名称 | 银行名称，必填文本 |
| 收款行地址 | 至少有城市与国家/地区 |
| SWIFT / BIC | 8位或11位字母数字，格式不符拒绝 |
| 收款行所在国家 / 地区 | 必选地区；与SWIFT国家段不一致时提示但不阻断 |
| 中转行 / 代理行信息 | SWIFT、名称、在中转行账号，整组选填；默认折叠，标注“选填”。说明“若开户行需要通过中转行接收外币汇款，请填写；不确定时请咨询开户行” |

`D-FIN-98`：平台只做规定的格式与户名一致性检查，不核验银行账户真实性、不做银企联验、不判断汇路可达性、不判断是否需要中转行；能力边界须让用户可见，不能显示“账户已验证”。中转行有误本模块不自作真实性判断，相关汇款失败归放款环节。

`E-CR-20`/`E-CR-21`：户名不一致时拒绝且无绕过，显示填写户名与本企业已登记名称两组值，给主体资料补登英文名入口；没有登记英文名时不得猜测对应关系。关联公司名称不是本企业名称。账户为空、币种不匹配与户名不符的反馈应可区分。主体资料能力未就绪会阻断此类接受，不能放松校验掩盖依赖。

### 3.3 手续费、摘要与盖章件

`D-FIN-102`～`D-FIN-105`：跨境汇出、中转、入账手续费由资产方承担，到账金额会少于融资金额；债务本金与额度计量仍按融资金额。必须在确认接受前清楚、不折叠地告知“跨境手续费由您承担；到账金额会少于融资金额，但偿还本金仍按融资金额{X} USD计算”。告知写进摘要并可追溯展示时间，只做告知，不额外增加手续费确认勾选。平台不预知、代收或垫付，不展示预估手续费，本模块不采集实收金额。

`D-FIN-86`～`D-FIN-88`：合同由双方线下拟定与签署，本平台不生成合同、不提供合同模板下载、不依赖模板配置才允许接受。摘要可复制、打印，明确“本摘要不是合同，不产生法律效力”。摘要包含：融资业务编号、需求编号、融资项目编号、双方企业全称、融资金额USD、结算币种与金额、完整汇率快照及生效时间与来源、年化利率、当前抵押物概况、报价时间、还款类型、最终还款日以及手续费与本金告知。数字必须与本笔业务一致，不沿用旧“九项”数量限制漏掉新增内容。

`D-FIN-93`：接受前须上传至少1个线下签署盖章件，PDF/JPG/PNG、单文件≤10MB、最多5个；条款一致性声明默认不勾选，须确认“我确认所上传合同的商务条款与本页摘要一致”。缺少文件、未查看摘要或未确认声明均不可接受。文件失败就地说明格式、大小、数量或上传失败原因，保留其他成功文件（`E-CR-09`）。

`D-FIN-89`～`D-FIN-91`：平台只收取文件并检查格式、大小和数量，不审核真伪及法律效力，界面明确该边界。资金方在放款前核验，有问题时的暂不放款、要求重传或终止由放款模块承接；本模块无“待审核”中间态。盖章件仅向业务相关主体开放，不属于广场公开附件。

### 3.4 接受、拒绝及异常出口

| 动作 / 情形 | 必须成立的结果 |
| --- | --- |
| 确认接受 | 账户已校验并逐笔确认、摘要已查看、盖章件和声明齐备，且报价仍待接受、未届满；业务从 `S-FD-1` 到 `S-FD-3 待放款`，向L5提供接受结果使项目进入融资中、对外进入放款中。保留在途报价金额与项目在途金额，不提前计入授信占用额；报价倒计时结束 |
| 拒绝（`F-LS-30`） | 无需完成账户、摘要、上传步骤。拒绝原因必填自由文本1～200字、无默认值、不分类，对报价机构可见；无冷却、不审核理由。成功后 `S-FD-1 → S-FD-2 已拒绝`，相关额度及需求结果见功能四 |
| 授信已到期（`E-CR-05`） | 存量报价仍可接受，不因机构授信到期阻断；新报价另走重新核定 |
| 项目在报价后到期 | 停止新报价，不提前终结本笔存量报价；仍在168小时内且未终结可继续处理，项目到期后的关闭由L5决定 |
| 当前出现覆盖不足提醒（`E-CR-06`） | 接受本身不增加金额，不另设覆盖准入闸门；接受前呈现当前覆盖状态、缺口与追加指引。追加按L5流程，提交申请不即时补足覆盖 |
| 接受与需求失效同时发生（`E-CR-19`） | 先确认业务是否仍待接受。需求先失效则报价一并失效，不可接受；有效接受先完成则转待放款，不由本模块事后翻回失效，后续放款必须执行L5覆盖要求。不能拿“不重验覆盖”接受已终结报价 |
| 提交时已被接受/拒绝/失效（`E-CR-10`） | 不重复生效；返回详情页说明当前结果，保留可见历史，不白屏、不静默跳首页 |

<a id="lifecycle"></a>
## 四、额度保持释放与需求失效联动

关联 `F-LS-27`、`F-LS-35`；流程见 [FL-CR-03](v1.0-借贷广场-授信报价与接受拒绝-流程图与状态机.md#fl-cr-03)，状态见 [ST-CR-02](v1.0-借贷广场-授信报价与接受拒绝-流程图与状态机.md#st-cr-02)。项目额度定义只引用 [L5 主册5.5](../v1.0-借贷广场-融资需求与代币质押/v1.0-借贷广场-融资需求与代币质押-PRD.md)，本节列事件结果，不重建额度模型。

### 4.1 事件责任与金额结果

`D-CR-16`～`D-CR-18`：报价产生在途报价金额，不产生授信占用额。在途报价金额包含待接受、待放款、待放款确认与放款异议处理中（`S-FD-1`/`3`/`4`/`5`）；放款确认前不能提前移出。已放款未偿本金计入授信占用额，逾期是并行标记，不因标记释放本金。

| 事件 | 机构维度结果 | 项目维度与业务结果 | 执行归属 |
| --- | --- | --- | --- |
| 成功报价 | 在途报价金额增加本次全额 | 项目在途金额不变；业务待接受，项目锁定 | L6报价，L5项目事实 |
| 接受 | 在途报价金额保持，不进授信占用额 | 项目在途金额保持；待放款 | L6接受，L5项目事实 |
| 拒绝 | 本笔在途报价金额全额释放，不进授信占用额 | 已拒绝；有效需求回待报价，项目在途金额不变、质押不释放 | L6拒绝，L5解锁 |
| 七天届满 | 同拒绝 | 报价已失效，原因“有效期届满”；有效需求回待报价，项目在途金额不变、质押不释放 | L6自动失效，L5解锁 |
| 需求因覆盖不足失效 | 其待接受报价在途报价金额全额释放 | 同一事件中报价已失效，原因“所属融资需求失效”；L5释放该需求的项目在途金额并提供终结原因 | L5判需求结果；**L6执行报价状态流转** |
| 放款确认完成 | 全额由在途报价金额转授信占用额 | 同笔全额由项目在途金额转项目融资余额，两维度在同一事件原子转移，无空档或双计 | 放款模块，承接 `AC-FIN-27` 与L5 `AC-FIN-25` |
| 放款异议裁决终止 | 尚在途则全额移出在途报价金额，不进入授信占用额 | 项目侧依L5与放款模块终止口径；不得把释放误记为新本金 | 放款/运营裁决，`AC-FIN-28` |
| 已放款业务裁决终止 | 按裁决终止规则释放本笔剩余未偿本金 | 与项目侧结果同源一致 | 后续模块承接基线终止路径 |
| 还款经机构确认 | 授信占用额按已偿本金逐期递减；结清时本笔剩余本金归零 | 项目融资余额按相同本金变化 | 还款模块 |

`AC-CR-09`：拒绝、报价超时、需求失效（含报价终检发现）、裁决终止、结清五条终结路径全部完成后，**该笔贡献的授信占用额与在途报价金额双双归零**。测试二元组无其他业务时，二元组两个合计量也均归零、可用授信恢复；有其他存量业务时不得误清其他业务金额。

明确不释放：授信额度到期、项目到期但存量业务仍处理、资产方接受、放款异议驳回后回待放款、逾期标记。需求已锁定时资产方不能主动撤下，项目尚有在途/未结清业务也不能通过主动关闭绕开释放条件。项目到期后的重新报价还须满足L5未到期条件，不能把“解锁”解释为允许到期项目重新报价。

### 4.2 需求失效与到期并发

`D-CR-30`/`D-CR-31`：消费 L5 `D-FIN-75` 的需求失效结论时，本模块使仍待接受报价落 `S-FD-11`，机构在途报价金额全额释放；与L5需求终结和项目在途金额释放**同一事件完成**。不得出现“需求已失效，报价还可接受”或“报价已失效，额度未回”的业务中间态。

`D-CR-32`、`D-FIN-92`：系统失效不同于资产方拒绝，不要求填拒绝原因、不计入拒绝率、不给机构负面记录。失效原因只有“有效期届满”与“所属融资需求失效”；前者时间取原到期时刻，后者取需求实际失效事件时刻，不能把尚未到期的需求失效记录到未来。历史报价与编号保留，不能消失。

`D-CR-38`～`D-CR-40`：没有覆盖不足缓冲期；只有报价168小时倒计时。追加质押不能恢复已经失效的报价，需按L5补足覆盖后重新发布，再以新业务重新报价。失效粒度、顺序、停止条件由L5决定，本册不重写。

`E-CR-13`～`E-CR-15`：同笔接受、拒绝与系统失效只允许一个最终结果，平台确认时刻裁定，但必须首先满足未届满条件；在到期时刻及之后不得接受，即使失效处理有延迟。到期前接受成功则不再受报价时限影响。重复触发失效不重复释放、不重复通知；一次事件的金额与状态结果整体一致，不能展示半完成结果。处理异常也不能恢复接受入口或把历史到期时刻往后移动。

`D-FIN-78`：拒绝与报价失效均为融资业务终态，原编号保留不回收。需求仍有效、项目未到期且重新满足报价条件时，任何机构包括原机构均可立即重报，无冷却和次数上限；新编号、新汇率快照、新168小时期限，不是续期。

<a id="presentation"></a>
## 五、页面增量、跳转与通知

关联 `F-LS-31`～`F-LS-33`。业务图见 [ST-CR-03](v1.0-借贷广场-授信报价与接受拒绝-流程图与状态机.md#st-cr-03)。只补L5页面增量，不重写列表、统计区或质押清单。

### 5.1 页面与对外状态

列表卡片与详情融资信息区补公开报价条款、机构名称、报价时间、锁定信息及倒计时；历史报价按已拒绝或已失效展示，并提供对应原因。需求编号沿用L5 `FP-28`，融资业务编号只在成功报价时产生，终态仍可查看。

`D-LS-28`～`D-LS-32`：本模块执行两次向前业务跃迁：报价成功推动“待报价→已报价待确认”；接受成功推动“已报价待确认→放款中”。拒绝和报价届满回退至有效需求的“待报价”；需求失效则消费L5“已失效／已关闭”及终结原因。“已放款”及后续由后续模块承接。**五值定义、派生与下发归L5**，列表、详情与控制台同源，不按内部状态自行拼装。

仅当前环节出可点按钮，按角色限制。游客操作区只有“立即登录”，公开内容继续可见。登录用户不可操作时区别本人需求、已被他人报价、项目已到期、覆盖不足等原因；非当前环节不给可点动作。需求已失效时融资报价与报价确认均不可操作且展示终结原因。

`D-LS-33`：页面呈现用户的金额、差额、时限、结果与可走路径；不展示内部判据公式、条款编号、字段来源、计算机制或规则论证。必须保留三个数的差额算式和锁定倒计时。术语、双语、时区与数字展示沿用L5及本平台面客基线，默认英文，不从原型演示代码复制内部说明文字。

### 5.2 跳转与跨主体隔离

沿用 [L5 `H-01`～`H-05`](../v1.0-借贷广场-融资需求与代币质押/01-功能需求说明.md)。资金方去报价落详情并开报价弹窗，自动带授信判定；资产方去报价确认落所属详情并开接受/拒绝弹窗；历史业务落详情业务区。`D-LS-22`/`D-LS-23`：无单独“去授信”入口或独立授信落地页。

每次落地重新判权、核对对象和当前状态；已终结则落详情并解释，不能打开旧操作层，不报404、不白屏、不静默跳首页。游客看公开内容并登录引导；草稿与无权内容按L5隐藏存在性。控制台不执行授信、报价、接受、拒绝，只提供已有只读视图及按L5约定的业务跳转；从控制台进入保留返回相应位置的入口。授信金额不进入公开报价区或他方通知，盖章件不对游客公开。

### 5.3 五类业务通知

由[本平台面客消息中心](../v1.0-面客消息中心/01-功能需求说明.md)承接；以下定义触发、收件人和用户结果，不定义接口参数、投递或去重机制。事件标识沿用基线，平台与端限定“借贷平台·面客端”，收件人限定相关企业的本平台账号，不向其他机构或其他端广播。

| 事件 | 时点与收件人 | 必须告知的内容 / 落点 |
| --- | --- | --- |
| `financing.quote.submitted` | 成功报价后，资产方 | “收到融资报价”；需求编号、机构、金额、报价到期时刻，前往本笔报价确认 |
| `financing.quote.expiring` | 尚待接受报价到期前24小时，资产方，仅一次 | “融资报价即将到期”；需求编号、机构、准确到期时刻与处理入口，明确不处理将失效 |
| `financing.quote.expired` | 报价终结失效，资产方与资金方各自通知 | “融资报价已失效”；业务编号、失效时间与原因、在途报价金额已释放。超时且需求有效可重报；需求失效须先补足覆盖并重新发布。落历史业务详情 |
| `financing.quote.accepted` | 接受成功，资金方 | “融资报价已接受”；业务编号与接受时间，前往详情查看盖章件并准备放款 |
| `financing.quote.rejected` | 拒绝成功，资金方 | “融资报价已拒绝”；业务编号、拒绝原因、在途报价金额已释放，前往详情 |

`D-LS-24`～`D-LS-27`：无授信核定/追加/到期通知；无“已锁定N天”周期通知，无72小时/1小时多轮提醒。需求失效连带报价失效沿用同一报价失效事件，不另造类型；L5需求失效通知仍按其口径存在，不由本模块代发或取消。同一报价同一事件每位收件人只收到一次，补做处理、重复提交、刷新不得增加第二条；新报价是新业务，不与旧报价互相覆盖。

通知失败不能改变交易结果或暂停计时；页面状态、锁定信息与倒计时始终可见。消息到达后目标状态已变时仍能看事实，操作不可用须清楚解释。分类与双语正式文案沿用消息中心权威登记处；本轮不把旧平台的分类值当作新平台已确认枚举，登记待承接事项见下节。

<a id="dependencies"></a>
## 六、依赖、质量要求与待承接细项

### 6.1 已有质量要求

沿用基线：报价入口授信判定反馈≤1秒，报价提交终检与结果反馈≤2秒，当场覆盖判断目标≤1秒；依赖不可用按 `E-CR-18` 拒绝，不能用旧数值兜底。报价失效效力到168小时即成立；基线失效处理反馈目标≤1分钟、最坏≤5分钟，**不构成接受宽限**，异常延迟按 `E-CR-14` 显示原失效时刻。所有状态与金额结果一致可追溯，不能将处理延迟解释为允许部分结果先展示。

角色、归属和提交校验在平台执行，绕过页面仍不得越权；授信、报价、账户确认、摘要查看、文件上传、声明、接受、拒绝、失效以及相关金额变更均应能按融资业务追溯原因、主体与发生时间。无日志格式或存储要求。时间明确时区，金额带币种，继承L5的双语、响应式、键盘可达及非纯颜色状态表达；不增加本轮原型交付。

### 6.2 依赖与边界

| 依赖 | 必须承接的业务结果 | 未就绪时 |
| --- | --- | --- |
| L5及只读代币域 | 当前有效质押与覆盖判定、需求失效同事件协作、对外五值状态 | 无法完成当前校验则拒绝新增报价；不在L6自判代币失效、重定义PS或补一套额度模型 |
| 代币发行平台汇率能力（`DEP-12`） | 单一来源、快照四要素完整可追溯、同标识重复引用结果一致 | 拒绝新报价，已锁定业务仍自持原快照；契约细化不取消产品要求 |
| 登录与主体资料（`DEP-19`/`DEP-26`） | 可靠企业归属、已登记法定/英文/曾用名、可用的补登入口 | 缺主体归属不得越权；户名未匹配不得绕过接受校验 |
| 企业统一数币地址（`DEP-20`） | 本企业已登记地址与既定ETH链约束 | 空地址不能接受，不允许临时填其他钱包 |
| 放款与还款（`DEP-14`～`DEP-16`） | 放款前合同核验与问题处置、同一汇率快照、两维度原子转移/只出不进、逐期本金递减；还款类型与最终还款日原样承接 | 下游全链路验收未完成前不能宣称融资闭环通过；不在本模块新增放款或还款动作 |
| 面客消息中心（`DEP-17`） | 五类核心通知、时限提醒、业务详情跳转、主体隔离 | 页面持续保底；通知登记与投递仍须提测验证，不因未就绪删除通知 |
| 我的控制台（`DEP-18`） | 同源只读授信与业务结果、锁定时间 | 不另算金额、不在控制台增加本模块写操作 |

### 6.3 尚未给出明确数值的细项

本轮四条裁定均已采用，不重新打开汇率源、PS、代币来源、页面号或交付形式。以下是基线未给完整口径或新平台尚待承接的细项，不阻断PRD交付，**不能被当作已确认规则或实现自行定值**：

| 编号 | 待承接事项 | 当前要求与影响 |
| --- | --- | --- |
| `Q-CR-07` | 授信追加时“五年上限”的核定日，按首次/最近重新核定日还是本次追加日 | 沿用 `D-CR-20` 只能延后、不强制一年、仍有五年上限；基线正文未明确追加时的日期基点，原型演示不能替代裁定。产品负责人补明后完成对应日期边界验收 |
| `Q-CR-08` | 收款账号/IBAN按地区的允许长度与字符范围 | 基线仅要求格式校验，未提供具体集合；沿用必填、户名硬校验及不核验真实性的边界，产品与主体/收款资料承接方在表单验收前补齐，不自行编造统一长度 |
| `Q-CR-09` | 结算币种金额精度及舍入边界 | 已明确USD展示两位、计算不提前舍入与同快照同结果；USD/USDT/USDC最终结算金额如何按各币种保留精度需与放款/还款同源确认，不能由各环节自行舍入 |
| `DEP-26` | 企业英文名/曾用名补登入口在新平台的具体承接 | 业务硬要求不变，正式地址未核实，不虚构页面编号；能力缺失会阻断相应户名的接受 |
| `DEP-17` | 消息分类、正式双语模板与共享登记的适配 | 按消息中心第10章完成，五类业务事件与结果已定；分类值未确认不复制旧平台枚举。本轮仅修改本模块和PRD索引，不代改共享登记处 |

基线未给出的精度或格式不能靠自行拟定技术规则填空，也不能因此免除唯一汇率源与取数失败拒绝的要求。既有残余风险维持：先到先得不能比价；机构报错不能撤回；被拒/失效可重报且无冷却；合同线下核验与账户真实性不在平台能力内；失效无恢复窗口。相关结果在验收册逐项核对。

> [返回主册](v1.0-借贷广场-授信报价与接受拒绝-PRD.md) · [验收需求说明](02-验收需求说明.md) · [流程图与状态机](v1.0-借贷广场-授信报价与接受拒绝-流程图与状态机.md)
