# 借贷广场 · 融资需求与代币质押 — 分册 02：验收需求说明 **V1.4**

> 本册是 [`v1.0-借贷广场-融资需求与代币质押-PRD.md`](v1.0-借贷广场-融资需求与代币质押-PRD.md) 的第 14～19 章，**测试主读**。
>
> | 章 | 内容 |
> | --- | --- |
> | 第 14 章 | 非功能需求 |
> | 第 15 章 | 指标 |
> | 第 16 章 | 依赖与风险 |
> | 第 17 章 | 缺口处置与对上游 / 跨平台的修订请求 |
> | 第 18 章 | **验收标准汇总**（按功能顺序组织，编号与分册 01 一一映射） |
> | 第 19 章 | 待确认项 |
>
> **业务规则不在本册重写**：每组验收在标题给出所属功能点与规则出处（主册第 5 章硬口径、分册 01 第 9～13 章、图册《流程图与状态机》）。本册只给角色、前置条件、场景与预期结果，**不代写测试执行用例、不写测试脚本**。
>
> **通用前置条件**：只读代币域中已有属于测试企业主体的可用代币；测试企业已完成登录；运营端审核结论可按用例给定。**角色**：除特别注明外，**游客、资金方、资产方各跑一遍**。【红线】= 不可削弱；【反向】= 检查"不存在"，构造出即不通过。

---

## 14. 非功能需求

> **全部继承本平台既有基线，不新设一套。**

| 维度 | 要求 |
| --- | --- |
| **性能** | `P-LS-01` 首屏 ≤ 2 秒（首批 20 条）；派生量页面预览 ≤ 200 毫秒；平台权威重算 ≤ 1 秒返回；**跨平台执行为异步，页面须在 1 秒内给出"已发起 · 处理中"的反馈**，不得表现为卡住 |
| **数据时效与跨平台一致性** | 代币事实取只读代币域，准实时；代币状态变更须及时同步并触发受影响项目的重算（`IF-LS-08`）。**页面渲染不回查上游、不读链**；质押事实以本平台为准，**不回读上游的质押状态**（`IF-LS-10`） |
| **时区与时间** | 沿既有国际化时间基线，按用户时区显示并显式标注，**无裸时间**；游客无账户时区时取浏览器时区。项目有效期、入池有效期统一按UTC判定，再按展示时区明确标注；切换时区不改变截止（D-LS-65） |
| **金额** | 金额数值与币种明确可辨；统一 `USD`、展示 2 位小数、计算过程不提前四舍五入；**代币数量、代币价值、应收账款原币金额三者不得相加**；本平台**不二次折算** |
| **安全 · 鉴权** | ① 全部写动作服务端鉴权，可见不可点与隐藏均不构成校验；② 跨主体隔离按企业主体过滤；③ **转出目的地址恒为质押前的原持有地址，不接受人工指定，界面无该入口**；④ **同一次操作不得产生第二次链上转移、不得二次收费**；⑤ 发起链上执行前必须已展示操作内容与费用预估并经确认 |
| **安全 · 不暴露存在性** | 草稿项目深链直达返回"内容不存在或无权访问"，**不暴露该项目是否存在**；他方主体的质押申请与执行过程一律不可见（`D-LS-36`） |
| **一致性** | 任一时刻 `INV-FIN-01` 必须成立；额度的产生、保持、释放原子无空档；同时操作不能造成超额放行或重复占用；**跨平台执行结果按张结算，成功张与失败张互不牵连** |
| **可用性** | 审核 48 小时失败、执行明确失败、执行结果未知及按张混合结果分别可辨；未知不能伪称失败、零费用或资产已返还。审核解除占用不要求提取；本期无异常释放流程 |
| **留痕** | 建池、提交与撤回申请、审核结论回写、复核结论、发起执行、**逐张执行结果与实际费用**、业务释放、额度占用与释放、覆盖不足提醒触发与解除、对账差异消费，每一次变化均留痕（动作人、企业主体、时间、前后状态、关联对象、链上凭证、实际费用） |
| **国际化** | 中英双语、**默认英文**，切换不刷新即完成全部替换（含表头、状态标签、空态、提示与费用文案）；语言、时区、日期、数字、金额、链上凭证的展示口径整段引用本平台国际化基线，**本模块不重新定义** |
| **可访问性** | 状态标签**不以颜色为唯一区分**，必须含文本；键盘可达；窄屏列表转摘要卡，**代币编号、代币价值、代币状态、质押覆盖状态不得被折叠隐藏** |
| **组件与响应式** | 全部取本平台自持的面客端组件规范，**本模块不新设计组件**；两个广场的列表统一为**定高内置滚动区 + 每批 20 条**，窄屏（≤900px）交还整页滚动 |
| **防批量抓取** | 公开页面与其数据接口具备按访问来源的速率限制与异常抓取识别：正常浏览不受影响，超阈值降速并要求人机校验；不提供批量导出 |
| **通知** | 按分册 01 12.2 的事件清单投递，**页面标记始终保底**，推送失败不得导致用户完全不知情；本模块不自定义投递机制；完整事件与模板见[03通知册](03-融资需求与代币质押-消息通知.md)，客服指引不另发站内信 |

---

## 15. 指标

| 编号 | 层级 | 指标 | 口径 |
| --- | --- | --- | --- |
| `M-LS-01` | 北极星 | **建池转化率 / 发布转化率** | 创建草稿 → 成功发布的比例，两段各统计一次，用于判断是否在第二段流失 |
| `M-LS-14` | 北极星 | 逐笔质押链路完成率 | 以单笔申请为分母统计实际成功；驳回、审核超时、未发起过期、复核作废、实际执行失败分开。执行未知不混入失败；批量确认不重新计申请数 |
| `M-LS-15`（**新**） | 驱动 | **按张执行成功率与失败张的再质押率** | 失败张在 7 日内被重新选入任一池的比例；**该值低说明失败张事实上被卡住了**，与 `D-LS-30` 的设计意图相悖 |
| `M-LS-16` | 健康度 | 审核超时比例与执行等待 | 审核 48 小时失败笔数 / 已提交单笔数；执行未知按等待时长观察，本期不把执行等待计为“执行超时失败率”，异常仅提供客服指引 |
| `M-LS-17`（**新**） | 健康度 | **审核周期分布** | 提交 → 结论的时长分布；用于验证 `IF-LS-09` 建议的对外承诺时限是否可承诺 |
| `M-LS-04` | 驱动 | 链上执行成功率 | 入池 / 撤回 / 提取分开统计 |
| `M-LS-05` | 驱动 | 单位融资额的链上成本 | 实际费用合计 ÷ 融资发生额，衡量资产方真实成本负担 |
| `M-LS-02` | 驱动 | 平均池内资产价值 / 平均质押张数 | 按已发布项目统计 |
| `M-LS-03` | 驱动 | 额度利用率 | 项目融资余额合计 ÷ 融资上限合计 |
| `M-LS-06`/`M-LS-07` | 健康度 | 覆盖不足发生率与平均持续时长 / 诱因分布 | 诱因分代币失效、对账差异、其他 |
| `M-LS-08` | 健康度 | 撤回被拒率 | 判断"可撤回上限"是否展示得够清楚 |
| `M-LS-09` | 健康度 | 待提取代币滞留量与时长 | 费用自担下会真实反映"嫌贵不提" |
| `M-LS-10` | 驱动 | 游客 → 注册转化 | 从列表 / 详情的登录引导进入注册的转化 |
| `M-LS-11` | 健康度 | **二次扣费投诉数 / 重复转移发生数** | **应恒为 0** |
| `M-LS-12`/`M-LS-13` | 健康度 | 空池草稿占比 / 需求自动失效率与失效后复发布率 | 前者反映质押链路失败对建池体验的冲击；后者是"判定即失效"实际代价的主要依据 |

---

## 16. 依赖与风险

### 16.1 依赖

| 编号 | 依赖方 | 依赖内容 | 状态 | 不满足的影响 |
| --- | --- | --- | --- | --- |
| `DEP-LS-01` | **代币发行平台** | `X-04`/`X-05`：审核通过后的链上执行与**按张回写**（`IF-LS-01`～`IF-LS-05`） | ⚠️ **契约未定义** | **本模块的质押链路无法闭环**：能提交、能审核，但代币进不了池，池值恒为 0 |
| `DEP-LS-02` | **本平台只读代币域（资产广场）** | 可质押代币的候选来源与代币字段、代币状态跟随（`IF-LS-06`～`IF-LS-08`） | ✅ 已入库 | 无来源即建不了池；归属标识缺失则可质押清单的归属过滤不可靠 |
| `DEP-LS-03` | 代币发行平台 | **代币失效状态的及时同步** | ⚠️ 关键依赖 | 失效同步延迟或缺失，平台会长期按**高估的抵押物**放贷 |
| `DEP-LS-04` | 运营端质押审核 WS-359 | 逐笔结论、协议、原因、时间、48 小时及留痕 | 逐笔审核基线已定；UTC和客服边界由WS-359后续串行轮次同步，真实实现与联验另验 | 缺实现仍只能提交及期限处理，不能以文档交付冒称链路可用 |
| `DEP-LS-05` | 本平台登录与企业主体体系 | 企业主体标识与其在只读代币域快照中的对应口径（`Q-LS-03`） | ⏳ | 归属判定不可靠，最坏情况是把他人的代币列进可质押清单 |
| `DEP-LS-06` | **本平台面客消息中心** | 触发登记与投递（分册 01 12.2、`CR-LS-06`） | ⏳ 已入库、登记待合入 | 审核通过 / 驳回 / 执行失败三类触达不到；**页面标记保底，但资产方可能 7 天后才发现结论失效** |
| `DEP-LS-07` | 本平台我的控制台 | 深链 `H-01`～`H-05` 的消费方 | ⏳ | 不阻塞本模块；本模块是契约的提供方 |
| `DEP-LS-08` | 后台链上持仓对账 | 差异标记（`PL-16`） | ⏳ 后台自动进行 | 本模块只消费标记，不实现对账 |
| `DEP-LS-09` | 本平台国际化基线与面客端组件规范 | 时间 / 金额 / 凭证展示口径、组件与列表形态 | ✅ 已入库 | — |

### 16.2 风险

| 编号 | 风险 | 处理 |
| --- | --- | --- |
| `R-LS-14` | 跨平台按张执行、费用和结果能力尚需落实 | 按 IF-LS-01～05 验真实结果；长期无结果按D-LS-63联系客服；本期不建设异常处置，不沿用审核 48 小时判失败 |
| `R-LS-15` | 结果未确认时错误释放会造成重复质押 | 保留处理中及相关张限制，不以超时证明已回原地址；冲突事实按 E-11 保护。本期不接受“先判失败重押再异常释放”的方案；异常释放移出本期 |
| `R-LS-09` | 重复确认或结果不明时重试可能重复转移、收费 | D-FIN-68 要求同次动作一次结果；确认前变化留在同一容器复核，实际发起后不得重复发起；明确失败后的新动作重新告知费用 |
| `R-LS-13` | **审核让"追加质押救覆盖不足"这条自救路径变慢**：判定即失效已无自救窗口，追加还要先过人工审核，等结论期间那批代币不计入覆盖 | `IF-LS-09` 要求给出可承诺的审核时限并在页面显示预期等待；提醒文案直接说明"追加需先经审核"（`D-LS-39`） |
| `R-LS-12` | **判定即失效 ⇒ 资产方没有自救窗口**：诱因（代币失效）与资产方操作无关，一笔已公开的要约可能在他毫不知情时被作废，在途报价一并作废 | 本期缓解两条：失效**必推**通知；失效后**可立即重新发布**，不设冷却。用 `M-LS-13` 跟踪实际代价 |
| `R-LS-10` | **平台记录与链上失配**：有效质押价值是对外公开的数，一旦与链上事实不符，广场展示的覆盖充足性就是错的 | 收到差异标记后按保守原则处理（不计入、冻结动作、重算并引导联系客服，不新增异常处置队列）；发现失配靠后台对账，不在本模块 |
| `R-LS-16`（**新**） | **两个广场对同一张代币说法不一致**：资产广场的二元展示与本模块的业务判定若各自维护一份数据，会当场自相矛盾 | `D-LS-27`/`D-LS-43` 写死"同一事实只取一处"；`AC-LS-120` 构造同一张代币在两个广场对照的用例 |
| `R-LS-02` | **费用由资产方承担**：批量入池 / 撤回 / 提取有真实成本，费用高企时资产方可能放弃使用或长期不提取 | 批量提交 + 费用前置告知；`M-LS-05`/`M-LS-09` 跟踪 |
| `R-LS-07` | **覆盖不足的主要诱因是代币失效，而平台对失效既无提前量也无处置手段** | 提醒 + 催追加 + `M-LS-07`；处置能力需另行立项（`GAP-LS-03`） |
| `R-LS-08` | **覆盖不足状态公开会对资产方产生真实市场影响** | 限定为中性事实披露；且数据本就公开，隐藏属选择性披露 |
| `R-MC-27`/`R-MC-22` | 质押率硬编码需发版调整；同企业任一员工可动用公司资产并触发费用支出 | 需求方已裁定接受 |

---

## 17. 缺口处置与修订请求

### 17.1 缺口处置

| 编号 | 缺口 | 谁来补 | **本期怎么做** | **不补的后果** |
| --- | --- | --- | --- | --- |
| `GAP-LS-01` | X-04 / X-05 按张执行及结果能力未闭合 | 代币发行平台与跨平台能力负责人 | 按 IF-LS-01～05；正常链上执行不降级放行。审核 48 小时不是执行时限；Q-LS-07/08已按P03收口为联系客服、本期不建设异常处置；真实逐张结果和保护能力仍须联验 | 真实质押及正常解除质押链路须能力完成后联验；不把异常释放纳入本期补齐 |
| `GAP-LS-02` | WS-359 逐笔审核实现与两端联验 | 本平台运营端 | 本轮两端 PRD 已同源；实现须提供逐笔事实。未审满 48 小时自动失败并释放单笔占用 | 审核实现未完成则不能形成通过并入池，自动失败不是可用审核服务的替代 |
| `GAP-LS-03` | **失效代币的处置能力无任何承接** | 需另行立项 | 本期只能"通知 + 催追加"，处置 / 变现 / 清算完全没有模块 | 覆盖不足只能告知、不能挽回；**这是本期明确接受的口子** |
| `GAP-LS-04` | **企业主体标识的跨平台对应口径未定** | 登录体系 + 只读代币域 | 以 `IF-LS-06` 要求快照携带可稳定比对的主体标识；本期不以企业名称作归属判据 | 归属过滤不可靠，最坏情况是把他人的代币列进可质押清单——**这是资金安全问题，不是体验问题** |

### 17.2 修订请求

| 编号 | 提给 | 请求 | 理由 | 本模块的等待姿态 |
| --- | --- | --- | --- | --- |
| `CR-LS-01` | **跨平台契约总册 + 代币发行平台** | 登记 `PS-*` 统一定义（`PS-1` 可质押 / `PS-2` 已质押 / `PS-3` 作废占位 / `PS-4` 已释放，权威方＝借贷平台），废止 `PS-0` | 质押状态的业务权威在本平台，需求方 2026-09-18 已裁定 | **不等。** 本模块已在自己的契约层定死；与资产广场 `CR-AM-01` 是**同一条请求**，不重复发起、不各自表述 |
| `CR-LS-02` | **代币发行平台** | 按 `IF-LS-02` 提供**按张**的执行结果回写（含失败原因与是否已产生费用），而非整单结论 | 没有按张结果，本模块只能整单回滚或整单放行，两者都会伤到资产方的钱 | **等。** 这是 `GAP-LS-01` 的核心内容 |
| `CR-LS-03` | **只读代币域（资产广场）** | 按 `IF-LS-06` 在快照中携带**可稳定比对的资产方企业主体标识**，仅供平台内归属过滤、**不对外展示** | 资产广场本身不做归属过滤，因此其字段全集里没有这个需求；借贷广场必须有 | **等。** 属 `GAP-LS-04` |
| `CR-LS-04` | **借贷平台对外术语表维护方** | 收录分册 01 13.4 的新增对外术语 | 既有规则就是"先收录、再上界面" | **上界面前需要它**；未收录前界面只能用已收录词 |
| `CR-LS-05` | **本平台页面 / 组件编号登记表维护方** | 登记 `P-LS-01`～`P-LS-03` 与 `C-F-LS-01`～`C-F-LS-08` | 不登记会重演撞号 | **不阻塞实现**，入库后回填（`Q-LS-04`） |
| `CR-LS-06` | **面客消息中心（本平台）** | 把分册 01 12.2.2 的有效触发登记（execution_timeout 暂不启用）同步进本平台《面客消息触发登记表》，并给出分类定义表中的对应取值 | 登记框架要求接入方在自己的 PRD 里登记完整、再落共享登记表；未登记不丢消息，但跳转能力受限 | **不阻塞本模块撰写**；**提测前必须完成**登记合入 |

---

## 18. 验收标准汇总

### 18.1 编号、术语与红线（规则见主册 2、5.1、5.7）

| 编号 | 条款 |
| --- | --- |
| `AC-LS-102` | 本 PRD 使用的全部前缀与任何已入库文档**零冲突**；本 PRD 不新增、不改写其他平台前缀下的编号 |
| `AC-LS-103` |【反向】界面文案、URL、对外返回内容与双语文案**不出现** `PS-`/`TS-`/`CT-` 形态的编号；契约层质押状态只出现 `PS-1`/`PS-2`/`PS-4`，**出现 `PS-0` 即不通过** |
| `AC-LS-24` |【反向】界面全文检索：不存在无限定词的"融资余额"；不存在质押率与有效期的可编辑入口；不存在"冻结代币""选择冻结范围""待确认融资金额""有效代币资产"等已作废说法 |
| `AC-LS-70` |【反向】界面上不存在：代币级冻结 / 锁定入口、部分数量质押输入、有效期编辑、质押率编辑、平台代付费用的任何表述、抵押物处置 / 变现入口、**指定转出目的地址的入口** |
| `AC-LS-67` | 覆盖状态与缺口的公开文案为**中性事实描述**；全站不存在"违约""高危""风险企业"等评价性标签，也不存在排行榜或黑名单 |

### 18.2 可质押代币来源与归属（`F-LS-04`；规则见主册 5.2、分册 01 9.4.1）

| 编号 | 场景与预期结果 |
| --- | --- |
| `AC-LS-104` | 上游新同步一张属于本企业的代币，**无需本平台任何操作**即出现在可质押清单；本平台不存在任何上架 / 下架 / 隐藏入口 |
| `AC-LS-11` | 非本企业代币不可见；不合类型或失效不纳入候选。被本方在途申请 / 执行占用的代币可按 E-15 只读提示来源但不可选；本笔确认复核不将自身申请占用误判为他笔占用；已释放待提取不得再次质押 |
| `AC-LS-119`（**新**） | **代币失效判定不自持**：构造只读代币域把某张代币状态推成失效的场景，本平台**在同一次重算内**把它移出有效质押价值并更新统计区；本平台不存在第二套失效判定入口或配置 |
| `AC-LS-121`（**新**） |【红线】构造两家企业各持代币的场景，**任一企业的可质押清单中不得出现另一家的代币**；越权直接调用提交接口一律拒绝 |
| `AC-LS-10` | 同一张代币无法同时出现在两个项目的资产池中；并发提交时**只有一笔成立**（`E-15`） |
| `AC-LS-12` | 质押清单不提供数量输入，粒度为"张"，且支持批量一次提交 |

### 18.3 质押申请与审核结论（`F-LS-21`；规则见分册 01 9.4.2、9.4.3、`ST-LS-03`）

| 编号 | 场景与预期结果 |
| --- | --- |
| `AC-LS-97` | 提交前在**三处**（创建页、追加入口、容器内提交确认步骤）都能看到"要操作两次"的告知 |
| `AC-LS-108` | 容器内提交确认步骤首句即说明"提交的是质押申请，此刻不发生链上操作、不产生任何费用"；**确认步骤内不存在任何支付 / 签名类按钮** |
| `AC-LS-98` | **审核期内统计区不变**：提交前后五个数逐字相同；该批代币不撑融资上限、不撑任何准入 |
| `AC-LS-99` | 同批 A 通过、B 驳回、C 待审可同时呈现；B 原因全文原样、只作用于 B；重提 B 生成新单笔、当前代币行转向新笔，历史仍可查，不重审 A/C |
| `AC-LS-112` | 撤回仅终结被选中的待审笔、解除其申请占用且零费用；同批已审笔保留。撤回与审核只一个有效，截止后不把超时改为撤回 |
| `AC-LS-100`／`AC-LS-101` | 每笔从自己的通过时点起七天；仅未发起到期使其许可失效、零费用；复核不通过只作废当前笔许可、无执行。原人工通过历史保留。已发起跨期按D-LS-63提示联系客服，保持真实状态与限制；不按零费用过期、不验收自动异常处置 |
| `AC-LS-122`（**新**） |【反向】以资金方与游客身份查看同一项目详情页：**看不到**代币审核与确认清单、质押申请编号、质押申请状态、驳回原因、逐张执行结果与费用金额；能看到的只有该代币是否计入覆盖的结果 |

| `AC-LS-130` | 同项目 A/B 来自不同提交，C 来自第三次提交：清单按代币三行，不按提交铺卡；A/B 已通过可一起勾选，C 待审不可选。同代币新提交替换当前行所指单笔，旧记录仍在其历史可查 |
| `AC-LS-131` | A/B 不同审核人、时间、协议及七天截止分别显示；A 先通过可先确认，不等 B/C；批量确认不刷新期限或复制另一张协议；非本企业无权读取协议 |

### 18.4 跨平台执行与按张结算（`F-LS-08`；规则见主册 5.3、分册 01 9.4.4、9.4.5、第 10 章）

| 编号 | 场景与预期结果 |
| --- | --- |
| `AC-LS-105` |【红线】一次申请 3 张、结果 2 成 1 败：2 张进「已质押」并计入统计区；1 张回到可质押清单；页面**逐张**给出结果与费用说明；**不出现整单回滚**，明确失败且无占用的张可重提，未知或差异张不得伪装失败放行 |
| `AC-LS-109` | 逐张结果必须回答四件事：成没成、没成的业务可读原因、**是否已产生费用与金额**、下一步能做什么；**不存在只说"部分成功"或"操作失败"的兜底文案** |
| `AC-LS-110` | 同一次确认内**成功张不因失败张回滚**；构造"2 成 1 败"后立即发布融资需求，校验按**实际成功入池后**的重算结果通过 |
| `AC-LS-113`（**新**） | 失败张在**同一次操作后**即可被选入**另一个**项目并走完全流程 |
| `AC-LS-114`（**新**） |【红线】**中间态不锁整池**：池内有一张处理中时，追加质押、发布需求、关闭项目、对其他代币解除质押**各自按判据照常判定**；被禁用的只有处理中的那一张 |
| `AC-LS-115` | 执行无结果经过 48 小时仍不自动失败、不开放重押，不套审核超时通知；显示已等待及实际已知费用，未知标待核实。长期无结果提示联系客服，无异常处置工单/自动升级/改判；普通晚到且无跨期/冲突结果正常归属本笔 |
| `AC-LS-127` | 提交后第 47 小时 59 分仍待审，第 48 小时自动质押失败（审核超时）、释放申请占用；无须点击或催办，记录保留且正常未执行张可按资格再申请。查看 / 上传 / 编辑不重置，跨周末、时区或夏令时都按连续 48 小时 |
| `AC-LS-128` | 审核失败标明“48 小时未完成审核”；同批已通过或已驳回不被回退，恰好截止及之后的裁定不能补审。审核失败与执行未知、执行实际失败的原因不混用 |
| `AC-LS-129` | 审核超时该笔从未执行、零费用，不出现费用不退或退款提取；已执行的同批其他张仍展示实际费用。与原事实冲突的迟到结果不覆盖旧记录、不重复计覆盖，保护限制沿 E-11；不提供异常释放流程 |
| `AC-LS-116`（**新**） | **费用告知两处齐全**：发起前的内容与费用合并的一次确认里写明承担方＝本企业、平台不收费不代付不返还、**失败也可能已经产生费用且不退回**；失败反馈里再次给出该张**是否已产生费用与金额** |
| `AC-LS-117` | 执行入口不可用或取消确认时不标处理中、不产生执行费用，保留该笔审核和仍有效申请占用及原截止；重新打开只需核对当前事实。未知是否已发起不引导重复执行 |
| `AC-LS-72` | **重复提交只产生一笔链上转移**：连续点击、刷新后重复提交、并发提交同一操作，链上只出现一笔转移，实际费用只记一次 |
| `AC-LS-118`（**新**） | **质押事实的回写方向**：本平台不读取任何来自上游的质押状态；构造上游携带质押状态的场景，本平台**不消费**，页面取值仍以本平台质押记录为准 |
| `AC-LS-13` | 转入失败的代币不计入质押价值且退回可质押；界面可见失败原因、是否已产生费用、重新发起将产生新的费用 |
| `AC-LS-17` | 额度占用 / 释放、提醒触发 / 解除**不产生链上交易**；入池 / 撤回 / 提取**必须**产生链上执行并留存可查验凭证 |

| `AC-LS-132` | 跨提交勾选 A/B 后，原容器一个步骤同时核对项目、代币、逐张费用及按币种合计，一次“确认并发起入池”即提交；没有第二次重复费用确认。取消不执行；空选择、不可选行、不同项目代币不能被带入 |
| `AC-LS-133` | 确认前 B 过期或资格变化，明确指出 B 并更新选择及费用，此时无张被发起；用户在同容器确认更新后的 A 集合才发起，不静默少执行 B。实际发起后 A 成功、B 失败时才按真实结果各自收口 |
| `AC-LS-134` | 正常待审失败的代币保持 / 恢复 PS-1，既不转账退款也不进入 PS-4；未产生费用。真实在池的正常撤回、项目终态释放及提取入口仍存在；异常释放入口、待办、处理进度与费用方案不在本期 |

### 18.5 建池与发布（`F-LS-01`、`F-LS-02`、`F-LS-12`；规则见分册 01 9.1、9.2.2、`FL-LS-02`）

| 编号 | 场景与预期结果 |
| --- | --- |
| `AC-LS-57` | 未勾选任何代币时创建不可提交；创建成功后项目落草稿且**广场不可见、深链不暴露存在性** |
| `AC-LS-58` | **创建不等待审核与链上结果**：提交后页面即返回草稿项目，有效质押价值为 0 且发布入口不可点 |
| `AC-LS-91` | 创建必选**代币类型**，可质押清单按该类型过滤；创建成功**直接落在该项目详情页**，不出现独立发布页与两步进度条 |
| `AC-LS-59` | **发布校验认业务事实**：有效质押价值 = 0 时发布必被拒；至少一张成功入池且需求金额 ≤ 可融金额时发布成功 |
| `AC-LS-60`／`AC-LS-61` | 发布成功后项目在途金额立即等于需求金额、可融金额同步减少；**同一份质押无法连发两笔吃满额度的需求**；报价提交后在途金额不变 |
| `AC-LS-62` | 放款确认完成的同一时刻，金额从在途移出并计入项目融资余额，中间不存在两边都不计的空档 |
| `AC-LS-54`／`AC-LS-55` | 两段式可中断可续做；第二段失败不回滚已完成的质押，**不重复收费** |
| `AC-LS-89` | **不展示 ≠ 不校验**：构造超额发布用例，平台仍按 `INV-FIN-01` 拒绝，并在容器内返回当场重算的可融金额与差额 |

### 18.6 解除质押与释放（`F-LS-06`、`F-LS-17`；规则见分册 01 9.5、`FL-LS-03`）

| 编号 | 场景与预期结果 |
| --- | --- |
| `AC-LS-63` | 构造"撤回后 `INV-FIN-01` 不成立"的用例必被拒绝并给出三个数；构造"额度充足"的用例**即使项目有在途报价也必须放行** |
| `AC-LS-64` | **失效代币撤回不受任何额度限制**，且撤回前后有效质押价值、融资上限、可融金额**三个数完全不变** |
| `AC-LS-36` | 解除质押容器分三区，有效抵押物区常驻显示可撤回上限并实时累计对照 |
| `AC-LS-18` | 撤回链上执行失败后代币回到在池、预扣撤销，**不出现"账面已释放、链上仍在合约"的错配** |
| `AC-LS-68` | 项目关闭 / 结清时业务即时释放，代币置「已释放 · 待提取」；提取由资产方自助发起并自付费用，可批量；未提取代币不计入任何池且不可再质押 |
| `AC-LS-38` | 存在可提取代币时，「解除质押」入口常驻提示张数与金额，并说明需自付费用；**不得让资产方以为代币会自动回到钱包** |
| `AC-LS-65` | **追加质押在项目 `S-FP-1`～`S-FP-4` 任何状态下均可提交**（含有在途报价时）；终态下追加不可点而**解除质押仍可用** |

### 18.7 派生量、覆盖状态与自动失效（`F-LS-07`、`F-LS-16`、`F-LS-19`；规则见主册 5.5、5.6、分册 01 9.6）

| 编号 | 场景与预期结果 |
| --- | --- |
| `AC-LS-66` | 代币失效后：有效质押价值立即扣减、失效代币价值同步增加、派生量重算；跌破提醒线则档位转「覆盖不足」并推送 |
| `AC-LS-39`／`AC-LS-40` | 覆盖不足时报价入口不可点并说明原因；提醒触发与解除自动进行、双方页面一致、留痕可回溯。覆盖持平但已发布申请仍满足原覆盖判据时，不因可融金额为0单独禁止报价，继续按L6授信条件判断 |
| `AC-LS-123`（**新**） | 覆盖不足提醒文案中**必须**出现"追加质押需先经平台审核、提交不即时生效"的说明 |
| `AC-LS-76`～`AC-LS-78` | 失效判据按逐笔口径；**判定即失效、无缓冲**，页面与推送不出现倒计时或"X 天内追加即可保留"；最小必要——只失效最后发布的那一笔；全部失效后仍不成立时**停止动作**、不空转 |
| `AC-LS-79`～`AC-LS-81` | 失效即释放在途金额且不进融资余额；需求失效与其在途报价失效**在同一事件内完成**，查不到"需求已失效、报价仍在途"的中间态；终结原因记为 `自动失效 · 覆盖不足`，**不新增任何项目状态** |
| `AC-LS-83` | 失效与覆盖不足提醒**互不为前置**：一笔需求失效时项目档位可能仍是"正常"；项目落入档③时若已无在途需求则不触发失效动作 |
| `AC-LS-106` | 统计区不受审核期与执行中间态影响（见 18.3、18.4） |

### 18.8 页面、可见度与深链（`F-LS-10`～`F-LS-13`、`F-LS-18`、`F-LS-20`；规则见分册 01 9.2、9.3、第 12 章）

| 编号 | 场景与预期结果 |
| --- | --- |
| `AC-LS-21`～`AC-LS-23` | 游客公开项目和公开记录可见，右侧三个页签保留且操作区统一登录入口，私有授信/账户/协议/凭证不可访问；游客调用写接口一律拒绝且响应不含业务数据；草稿项目不可见且不暴露存在性 |
| `AC-LS-92` | 未登录时操作区**只有一个「立即登录」按钮**；越权直调写接口一律拒绝 |
| `AC-LS-88` | 统计区只呈现 5 个数，其中数量与价值**只算有效代币**；页面无额度尺、无三数等式区、无"额度明细与判定口径"折叠区 |
| `AC-LS-90` | 质押代币清单含代币数量 / 币种、代币价值、有效 / 失效三列，**不含合同号与发票号**，固定每页 5 条且无每页条数切换；融资申请信息清单展示申请编号、金额、当前状态与报价摘要，历史在详情按需展开；放款和还款清单按同一业务关联，空值不能虚构 |
| `AC-LS-107` | 列表页为定高内置滚动区 + 每批 20 条；换排序 / 重置筛选回到第一批并把滚动区带回顶部；加载新一批保住滚动位置；窄屏交还整页滚动 |
| `AC-LS-85` | 右侧操作区只提供当前角色和阶段可办动作；左侧详情只读，无办理或跨业务跳转按钮。办理、必要确认和放弃修改在同一居中弹窗内完成，不叠层；历史不提供已失效动作 |
| `AC-LS-124`（**新**） | `H-03`：额度不足时「解除质押」以**可见不可点 + 原因（含可撤回上限）**呈现，**不得直接隐藏**；游客与他方主体按"不可见"处理 |
| `AC-LS-111` | `action=enter_pool` 对非本方主体只落页面、不打开操作层；目标单笔已失效、已执行或不再可确认时落详情页并说明原因，**不报错、不白屏** |
| `AC-LS-34` | 广场上一行一个项目，整行进入同一项目详情，无操作列；FP-01项目编号、FP-28每轮申请编号分别可辨，不因三类清单而重复生成项目 |
| `AC-LS-25` | 控制台、资产广场与广场读到的同一对象的派生量、覆盖档位、质押状态文案完全一致 |
| `AC-LS-120`（**新**） |【红线】同一张已质押且链上成功的代币：资产广场显示「已质押」并给出所属项目深链，本模块显示在池且计入覆盖；**两处不得出现相反结论** |
| `AC-LS-93` | 融资需求对外状态由平台派生下发，前端不得自行拼装；同一笔需求在列表、清单、控制台三处读到的对外状态必须一致 |
| `AC-LS-95` | `已失效／已关闭` 的标签旁与详情页必须能看到终结原因，不得只给一个合并标签 |

#### 全流程信息与交互验收（F-LS-02/03/11/13/20；D-LS-52～59；图FL-LS-07）

| 验收编号 | 角色、场景与可观察结果 |
| --- | --- |
| AC-L5-UI-01 | 三角色进入公开项目：左侧按质押代币清单/融资申请信息/融资放款信息/还款信息顺序纵向展示，随页面自然滚动；点击四区定位标签只滚动到对应区，不隐藏其他清单；右侧恒定质押/融资/还款三个操作页签。每类清单独立保留筛选与页码，无业务有空态，读取失败有重试，不显示全项目操作日志。游客只有统一登录动作，私有字段不泄露 |
| AC-L5-UI-02 | 本企业资产方成功发布、改额、撤下及再次发布：发布后定位申请行，改额保留编号，再次发布新轮次且历史不覆盖；不得重复质押、收费或自动打开报价。已有在途或项目到期不能借入口变化绕过发布资格 |
| AC-L5-UI-03 | 一轮有拒绝/失效历史与当前有效报价：上方只展示当前有效或已接受一笔，展开旧报价只展示旧条款；没有有效报价时明确空态。授信总/已用是机构对企业当前值，不当成本项目值，不覆盖历史条款 |
| AC-L5-UI-04 | 清单仅打开只读详情抽屉；右侧操作区打开对应对象的居中办理弹窗，详情不提供办理/跨业务跳转或重复快捷入口；返回保留输入，取消不提交、失败可重试，放弃修改就地确认，不叠层 |
| AC-L5-UI-05 | 接受成功即有放款记录行，登记成功才有LN；未确认前显示待放款/待放款确认，资产方确认到账后显示已放款；后续还款、逾期、结清不改变放款行的已放款展示，不提前把待确认业务当还款业务 |
| AC-L5-UI-06 | 项目有多笔成交、多期还款：一行一笔FD业务，按FP/FD/LN任一编号检索同笔，计划一期一行；生成中不冒用预计计划，待确认可进入确认，结清不虚构本期应还。本期选择及金额沿L8 |
| AC-L5-UI-07 | 资产方接受前可查看标“预计·未生效”的计划；报价区查看不打开第二容器。到账确认后只读定稿单版，期次/金额不因查看历史改变，生成中不撤销确认 |
| AC-L5-UI-08 | 当事方核对账户、协议及凭证：放款收款资产方/还款收款资金方可辨；不新增出账账户必填、不默认相等。协议表按文件下载，预览在同容器切换；无权直达或下载仍拒绝，失败不显示下载成功 |
| AC-L5-UI-09 | 多笔待办先明确对象；关闭保持来源筛选、分页、位置与焦点，不自动打开详情或下一业务。控制台/消息按查看或办理目标打开对应容器，状态变化显示本笔当前结果，不跳到新申请 |
| AC-L5-UI-10 | 质押审核清单无操作列，默认提交倒序，协议在对应清单可下载；批量确认不重复下载入口或费用确认，成功发起即关容器并定位质押代币清单看逐张结果，未成功入池不计覆盖 |
| AC-L5-UI-11 / D-LS-60 | 发布参考结算币种未选时拦截，单选/多选及跨法币数币可成功；编辑自动带原值，新轮独立留存。选择USD偏好后机构报价USDT仍按正常资格可提交；金额、占用及实际结算不取偏好集合，历史缺项明确未提供 |
| AC-L5-UI-12 / D-FIN-76 | 需求因覆盖不足失效后恢复资格即可新轮发布，无冷却、不重建项目；新FP-28、旧记录保留，项目有效期不重置。项目到期/终态、仍不足或另一在途需求时不可发布，存量债务不被清除 |
| AC-L5-UI-13 / D-LS-61 | 已有本笔/本企业有效资料自动带入；可编辑项仍需校验，规定的逐笔确认仍由用户完成。缺资料只补必要项，加载失败不填演示数据，不串用其他机构账户、协议或旧转账凭证 |

### 18.9 通知与留痕（分册 01 12.2、第 14 章）

| 编号 | 场景与预期结果 |
| --- | --- |
| `AC-LS-82` | 通知按事件清单投递：**链上成功不推送、可提取不单独推送、到期只推届满前 7 天那一次**；每类事件的页面标记始终存在，推送失败不影响页面可见性 |
| `AC-LS-125`（**新**） | 审核通过通知的文案带**入池截止时间**；驳回通知带**原因原文**；执行失败通知**逐张**说明 |
| `AC-LS-126`（**新**） | **触发登记完整可用**：八类已启用消息各构造一次真实事件；execution_timeout 不当作已启用失败事件——消息可达、跳转落点可打开（目标已失效时按预判给出说明而不是白屏）；**同一项目连续两次进入覆盖不足档产生两条消息**（不同发生轮次分别提醒，`D-LS-44`）；同一批代币的多次执行结果不互相覆盖 |
| `AC-LS-40` | 提醒触发 / 解除、执行结果、费用、审核结论全部留痕且可按项目编号或单笔标识及代币回溯 |

| `AC-LS-135` | 同批 A/B 先后通过均产生各自通知，重复 A 同事件只一次；消息定位相应代币与单笔，展示自己的截止或原因，不自动勾选执行。旧通知指向已终结笔仍可回看，不覆盖新申请 |
| `AC-LS-136` | 两端同一笔结论、协议、原因、时间、48 小时截止一致；48 小时失败仅释放该笔占用。文档校验与实际联验分别记录，不能因 PRD 同步就宣称跨平台执行完成 |

### 18.10 功能点 → 验收映射速查

| 功能点 | 验收编号 |
| --- | --- |
| `F-LS-01`/`F-LS-12` 建池 | `AC-LS-57`、`AC-LS-58`、`AC-LS-91`、`AC-LS-54`/`55` |
| `F-LS-02`/`F-LS-03` 发布与需求生命周期 | `AC-LS-59`～`AC-LS-62`、`AC-LS-89`、`AC-LS-93`、`AC-LS-95` |
| `F-LS-04` 可质押代币来源 | `AC-LS-104`、`AC-LS-11`、`AC-LS-119`、`AC-LS-121`、`AC-LS-10`、`AC-LS-12` |
| `F-LS-05`/`F-LS-21` 申请与审核 | `AC-LS-97`～`AC-LS-101`、`AC-LS-108`、`AC-LS-112`、`AC-LS-122`、`AC-LS-127`、`AC-LS-128`、`AC-LS-130`～`AC-LS-131`、`AC-LS-65` |
| `F-LS-08` 跨平台执行 | `AC-LS-105`、`AC-LS-109`、`AC-LS-110`、`AC-LS-113`～`AC-LS-118`、`AC-LS-129`、`AC-LS-132`～`AC-LS-134`、`AC-LS-72`、`AC-LS-13`、`AC-LS-17` |
| `F-LS-06`/`F-LS-17` 解除质押与提取 | `AC-LS-63`、`AC-LS-64`、`AC-LS-36`、`AC-LS-18`、`AC-LS-68`、`AC-LS-38` |
| `F-LS-07`/`F-LS-16`/`F-LS-19` 派生量、覆盖与失效 | `AC-LS-66`、`AC-LS-39`/`40`、`AC-LS-123`、`AC-LS-76`～`AC-LS-83`、`AC-LS-106` |
| `F-LS-10`/`F-LS-11`/`F-LS-18`/`F-LS-20` 页面与操作区 | `AC-LS-21`～`AC-LS-23`、`AC-LS-92`、`AC-LS-88`、`AC-LS-90`、`AC-LS-107`、`AC-LS-85`、`AC-L5-UI-01`～`10` |
| `F-LS-13` 深链契约 | `AC-LS-124`、`AC-LS-111`、`AC-LS-34`、`AC-LS-25`、`AC-LS-120` |
| `F-LS-14` 费用 | `AC-LS-32`、`AC-LS-33`、`AC-LS-116` |
| 编号与术语 | `AC-LS-102`、`AC-LS-103`、`AC-LS-24`、`AC-LS-70`、`AC-LS-67` |
| 通知与留痕 | `AC-LS-82`、`AC-LS-125`、`AC-LS-126`、`AC-LS-135`～`AC-LS-136`、`AC-LS-40` |

> **承接**：`AC-FIN-01`～`AC-FIN-07`、`AC-FIN-11`～`AC-FIN-26`、`AC-LS-01`～`AC-LS-07`、`AC-LS-27`～`AC-LS-33`、`AC-MC-12`～`AC-MC-14`、`AC-MC-16`、`D-FIN-06`、`D-FIN-14`、`D-LS-04`～`D-LS-06`、`D-LS-09`、`D-MC-105`、`D-MC-110`/`111`。

---

### 18.11 评审决策补充验收

| 编号 | 关联规则 | 验收结果 |
| --- | --- | --- |
| AC-LS-137 | D-LS-62、P02 | 当前唯一链上地址对应整笔数量价值；企业原地址与合约地址不重复计持仓，一个代币不产生两个当前项目。PS-4未提取仍显示真实持有地址；地址/归属不可靠不放行重押 |
| AC-LS-138 | D-LS-62、P01 | 项目及代币业务字段自动只读获取；他人个人、企业账户档案不可因同企业或融资关系查看，不让用户手填对方档案。业务协议/凭证仍按当事方权限，真实缺失不以演示值补齐 |
| AC-LS-139 | D-LS-63、P03 | 长期未知、跨期、冲突或部分结果中的待核实笔提示联系客服；没有异常工单、自动升级、改判、释放或恢复资格入口。未知不当失败、不重复执行，费用未知不当零；正常已知逐张结果独立保留，终态项目不因晚到转入恢复覆盖 |
| AC-LS-140 | D-LS-64、P07 | 终止单笔仅释放本笔对应占用，其他债务/在途继续。已到期项目不恢复新发布/新报价；未到期按全部剩余事实判资格，不自动回募集中或释放整池，只有实际项目终态才触发原业务释放 |
| AC-LS-141 | D-LS-65、P10 | UTC判定项目日期及七天许可；展示时区变更不改截止，审核48小时/报价168小时不延长。USD额度两位展示而计算不提前舍入，链上费用不强套两位；代币失效仍取权威结果，通知使用同一截止 |

## 19. 当前决策与依赖

逐笔审核、代币维度清单、同项目跨提交批量确认、合并内容及费用确认、连续 48 小时未审失败并解除占用均已明确。Q-LS-01 的审核兜底已关闭，不延伸为执行超时裁定；异常释放本期排除，不再要求补齐其流程。P03已确定异常联系客服、本期不实现处置系统，P10已确定UTC，不再把Q-LS-07～09列为等待产品裁定。下表保留真实能力与通知依赖。

| 编号 | 事项与当前推荐 | 影响 |
| --- | --- | --- |
| Q-LS-02 | 本平台单个容器承载内容与费用确认已定；真实签名由哪端承接仍待发行平台能力确认，不增加重复业务确认 | 真实链上交互依赖，不阻塞清单及确认文档 |
| Q-LS-03 | 企业主体跨平台稳定对应，不用名称代替归属 | 上线前落实，未对应不放行越权 |
| Q-LS-04 | 页面与组件正式登记沿既有段位，C-F-LS-05 为代币清单而非批次卡 | 常规登记，不新增页面 |
| Q-LS-05 | 审核连续 48 小时已由用户明确，关闭原处理期限问题，不复用编号 | 查看或上传不重置，不另要求短 SLA |
| Q-LS-06 | 面客八类通知需求的分类及登记，execution_timeout 暂不启用 | 提测前完成正常逐笔通知登记；无异常释放消息 |
| Q-LS-07 | 已按P03关闭本期产品未决：长期未知/冲突联系平台客服，本期不建设核实、升级、改判、异常释放或恢复资格流程 | 未知不判失败、不开放重押，真实逐张结果仍属正常能力依赖；不把客服处理完成视为系统自动恢复 |
| Q-LS-08 | 已按P03关闭本期产品未决：已发起跨入池截止或项目终态等异常仅保留事实、限制并联系客服，不提供自动异常处置 | 不自动释放在途、不假报零费用；项目终态后不增加有效覆盖。WS-359按同一边界承接 |
| Q-LS-09 | P10已确认统一UTC判断业务日期，按用户时区展示并标注；本笔七天期限沿D-LS-65 | 已关闭业务时区选值问题；审核48小时及报价168小时保持连续时长，不因展示时区变化延长 |

本轮阅读入口为L5主册，取齐本模块五册；L6～L8及WS-359按各自最新资料承接，不随本轮重交或修订。重点核对唯一持有地址与资料边界、客服指引与异常处置排除、终止单笔后的项目资格、UTC日期，以及既有四区纵向清单和定位标签、只读详情与独立办理。原型演示不替代裁定，本轮文档交付不代表真实执行或跨平台联验通过。

Q-LS-N01仍见[通知册7.2](03-融资需求与代币质押-消息通知.md)：多成员企业需求/项目通知的具体收件账号须确认，不从企业可操作权限推导全员广播。客服联系方式使用平台现有正式资料；未取得时不能填演示号码或承诺处理时限，实际客服可联系性须由平台承接。

---

> 总览与业务规则：见 [`v1.0-借贷广场-融资需求与代币质押-PRD.md`](v1.0-借贷广场-融资需求与代币质押-PRD.md)。
> 页面与模块详情、跨平台业务要求、数据与字段：见 [`01-功能需求说明.md`](01-功能需求说明.md)。
> 流程图与状态机：见 [`v1.0-借贷广场-融资需求与代币质押-流程图与状态机.md`](v1.0-借贷广场-融资需求与代币质押-流程图与状态机.md)。
