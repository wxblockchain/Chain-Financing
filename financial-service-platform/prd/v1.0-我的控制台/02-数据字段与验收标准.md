# 我的控制台（只读） — 分册 02：数据字段与验收标准 **V3.0**

> 本册是 [`v1.0-我的控制台-PRD.md`](v1.0-我的控制台-PRD.md) 的分册，装 **第 7～11 章**：只读消费字段总表与可见字段矩阵、非功能、指标、依赖与风险、对上下游的请求、**全模块验收标准汇总**。
>
> **读者**：实现方、测试。**编号与主册、分册 01 连续**；同一个编号只在一处定义。
> **V3.0 变更**：① **11.6 整节由「待办带」改写为「行级提醒」**——`AC-MC-28`～`30`、`AC-MC-62`～`65` 逐条改为行级验收，**一条未作废**；② `AC-MC-29` 扩为撤带后的反向清单；③ 7.3 常量表两处译法取 WS-324 分册 7.6 已定词（`Coverage gap`、`Awaiting quote confirmation`），并按对照表原文对齐大小写；④ 10.3 新增第 11 条（对照表中待办三组名已无落点）与第 7 条改写（WS-329 `B-4`/`B-5` 的修订请求）。上游指针跟到 **WS-324 V9.3**、仓库 `9a457a0`。
> **V2.0 变更（留痕）**：字段表按 WS-324 V9.2 / WS-325 V7.2 / WS-326 V3.2 / WS-327 V3.3 更新——环节改名、链收敛 ETH、`LN-15`/`LN-16`、`QT-15`/`QT-16`、`RP-13` 更名；7.3 常量表去掉全部 `[待裁定]` 标记；新增 `AC-MC-77`～`AC-MC-81`。

---

## 7. 数据与字段

### 7.0 本模块**自己拥有**的字段：只有视图状态

| 编号 | 字段 | 类型 | 口径 |
| --- | --- | --- | --- |
| `MC-01` | 当前 tab | 枚举 | 资产方 `代币列表`/`融资项目`/`授信明细`/`融资放款信息`/`还款信息`；资金方 `我的报价`/`授信汇总`/`融资放款信息`/`还款信息`。写入 URL（`D-MC-147`） |
| `MC-02` | 筛选条件 | 结构 | 各 tab 的筛选项（主册 6.5.1）。写入 URL；驱动 `C-MC-04` 条带 |
| `MC-03` | 排序字段与方向 | 结构 | 同上。**精确值排序，不做区间化**（`D-MC-68`） |
| `MC-04` | 页码与每页条数 | 整数 + 枚举 | 每页 20（默认）/ 50 / 100 |

| 编号 | 条款 |
| --- | --- |
| `D-MC-166` | **上表四项是本模块拥有的全部字段，全部是前端视图状态，没有一项是业务数据。** 这是 `AC-MC-11` 在数据层的对应事实：**不新建任何业务实体、不落任何业务库表、不为任何业务字段提供写入路径**。若出现"控制台侧需要存一份 X"的诉求，那是同源红线被破坏的信号，应回上游要接口，**不在控制台建表** |

### 7.1 只读消费字段总表（按 tab）

> 只列**控制台展示的**字段与其**定义方编号**；类型、必填、校验、默认值一律以定义方 PRD 为准，**本册不复述、不改写**。展示口径中的时间、金额、数字、链上地址与哈希、脱敏，整段引用 WS-308 第 4/5/6/8 节。

#### 7.1.1 统计区（`C-MC-01`）

| 指标 | 取数 | 定义方 |
| --- | --- | --- |
| ① 全部代币 数量 / 金额 | 张数；Σ`D-TI-18` | WS-318 |
| ② 已质押代币 数量 / 金额 | `D-TI-24 = 已质押` 的张数与 Σ`D-TI-18`（**账面口径，含失效**） | WS-318 + WS-324 |
| ② 副行 · 其中已失效 | 失效张数与金额（`D-TI-22 = TS-2`；项目维度对应 `FP-19`） | WS-318 / WS-324 |
| ③ 总授信 额度 / 笔数 | Σ`CR-05`；授信记录条数 | WS-325 |
| ④ 已融额度 / 笔数 | 资产方 Σ`FP-13`；资金方 Σ`CR-08`；未结清融资业务笔数 | WS-324 / WS-325 |
| ＋ 剩余可用授信 | Σ`CR-11` | WS-325 |

#### 7.1.2 代币列表 tab

`D-TI-21` 代币编号 ｜ `D-TI-11` 数量 + 代币符号缩写（**WS-318 侧仍为 `[待确认]`，不自造**）｜ `D-TI-18` 代币价值（USD 快照，永不重算）｜ `D-06`/`D-07` 底层应收账款账期 ｜ 买方企业名 ｜ **`D-TI-22` 代币状态**（`TS-1 有效`/`TS-2 失效`）｜ **`D-TI-24` 融资质押状态**（未质押 / 已质押 / **已释放 · 待提取**，后者取 `PL-18`）｜ 所属融资项目 `FP-01`+`FP-02`
**不展示**：合同号、发票号（`D-MC-131`）。

#### 7.1.3 融资项目 tab

**项目级**：`FP-01` ｜ `FP-02` ｜ `FP-29` 代币类型 ｜ `FP-04` 项目状态 ｜ `FP-05` 已到期标记 ｜ `FP-09` 有效期至 ｜ `FP-22` 已收到报价数 ｜ `FP-23` 关闭原因 ｜ **`FP-20` 质押覆盖状态** ｜ **`FP-21` 覆盖缺口 + 需追加资产价值** ｜ `FP-19` 失效代币价值/张数 ｜ `FP-25` 空池标记
**五个数**：质押代币数量 ｜ **`FP-11` 有效质押价值** ｜ `FP-06` 融资需求 ｜ **`FP-13` 项目融资余额** ｜ 质押率常量 80%
**逐轮需求**：**`FP-28` 需求编号** ｜ `FP-07` 币种 ｜ **对外状态（`D-LS-18` 五值，服务端派生下发）** ｜ `FP-27` 终结原因 ｜ `QT-10` 机构名称 ｜ `QT-09` 报价时间 ｜ `FD-13` 已锁定时长 ｜ **`QT-12` 到期时刻 + `QT-13` 剩余有效期**
**不展示**：`FP-12` 融资上限、`FP-14` 项目在途金额、`FP-15` 可融金额、`FP-16` 可撤回上限（主册 6.2.1）。

#### 7.1.4 授信明细 / 授信汇总 tab

`CR-01` ｜ 对方企业主体（`CR-02`/`CR-03`）｜ `CR-04` 额度状态 ｜ `CR-05` 授信额度 ｜ `CR-06` 首次核定时间 ｜ `CR-07` 有效期至 ｜ **`CR-08` 授信占用额** ｜ **`CR-09` 在途报价金额** ｜ **`CR-10` 授信已用额（合称，不作第四个并列量）** ｜ **`CR-11` 可用授信**
**不展示给资产方**：`CR-12` 备注（仅机构本方可见，`D-MC-137` ③）。

#### 7.1.5 融资放款信息 tab

**业务**：`FD-01` ｜ **`FP-28`** ｜ `FD-02` 所属项目 ｜ `FD-03` 业务状态 ｜ `FD-31` 待重传盖章件标记 ｜ `FD-32` 已暂缓放款标记 + 最新原因 ｜ `FD-33` 终止原因 ｜ `FD-34` 终止时间 ｜ **`FD-35` 放款确认时间** ｜ **`FD-36` 确认超期标记 + 超期时刻** ｜ **`FD-37` 放款确认时限至** ｜ **`FD-38` 剩余确认时限** ｜ `FD-39` 到账实收金额（选填留痕，**只展示不校验、不与 `LN-04` 比对**）｜ `FD-09` 盖章件（**只展示有/无 + 上传时间 + 版本数**，`D-MC-129`）
**商务条款**：`QT-02` 结算币种 ｜ `QT-03` 融资金额 ｜ `QT-04` 汇率快照 ｜ `QT-05` 结算金额 ｜ `QT-06` 利率 ｜ **`QT-15` 还款类型（定值只读）** ｜ **`QT-16` 最终还款日（派生只读，＝`FP-09`）**
**放款记录**：`LN-01` ｜ `LN-03` 币种 ｜ `LN-04` 金额 ｜ `LN-05` 发放时间（**旁注：仅作业务参考与对账依据，任何时效与计息都不使用它**）｜ `LN-06` 提交时间 ｜ `LN-07` 凭证（有/无 + 份数）｜ `LN-08` 哈希 + **`LN-09` 链（系统常量 `ETH`、只读、无选择控件）** + **`LN-13` 区块浏览器链接（取 ETH 固定前缀，常驻"平台未核验该交易"）** ｜ `LN-10` 出账收款账户快照（**脱敏**）｜ `LN-11` 放款备注
**还款收款账户（V2.0 新增）**：**`LN-15`**（法币：户名 / 账号 / SWIFT / 开户行 / **中转行**五项）｜ **`LN-16`**（数币：资金方绑定钱包地址，链恒为 `ETH`）——**只读、脱敏、无修改入口**（`D-MC-169`）。

#### 7.1.6 还款信息 tab

**业务级**：`FD-41` 计划定稿时间 ｜ `FD-42` 累计已还本金/利息 ｜ `FD-43` 未偿本金 ｜ **`FD-44` `S-FD-9` 逾期并行标记（最早逾期期次 + 最长逾期天数）** ｜ `FD-45` 结清时间 ｜ **`FD-46` 最终还款日（V2.0：跟随 `QT-16`，不快照）** ｜ **`FD-47` 起息日**
**期次**：`RP-01` ｜ `RP-03` 期次序号 ｜ `RP-04` 应还日（初始版列名「预计还款日」）｜ `RP-05` 计息区间（起/止/天数）｜ `RP-06`/`RP-07`/`RP-08`/`RP-09` 应还本金/利息/合计/结算金额 ｜ `RP-10` 期次状态 ｜ **`RP-11` 逾期标记 + 逾期天数** ｜ **`RP-12` 还款类型（引用 `QT-15`，不另立枚举）** ｜ **`RP-13` 还款性质（V2.0 更名）** ｜ **`RP-14` 机构收款账户（V2.0 改锚 `LN-15`/`LN-16`）** ｜ **`RP-15` 还款入口开启时间** ｜ `RP-16` 计划版本 ｜ `RP-17` 结清时间 ｜ **`RP-18` 计息规则摘要**。**末期行须显式标注"含本金 · 到期还本付息"**。
**还款记录**：`RM-01`～`RM-08` ｜ **`RM-09` 链（系统常量 `ETH`、只读）** ｜ **`RM-10` 收款账户快照（V2.0 改锚 `RP-14`）** ｜ `RM-11`～`RM-14` ｜ **`RM-13` 区块浏览器链接（ETH 固定前缀 + 未核验标注）** ｜ **`RM-15` 还款确认时限至 / `RM-16` 剩余 / `RM-17` 超期标记** ｜ `RM-18` 确认时间。**与 `LN-*` 严格对称，只需一套渲染**（对照表见 WS-327 分册 7.2.1，逐行为真是其 `AC-RP-08`）。

#### 7.1.7 我的报价 tab（资金方）

`FD-01` ｜ **`FP-28`** ｜ 资产方企业名 ｜ `FP-01`/`FP-02` ｜ `QT-02`/`QT-03`/`QT-04`/`QT-05`/`QT-06` ｜ **`QT-15`/`QT-16`（只读）** ｜ `FD-03` 业务状态 ｜ `QT-09` ｜ **`QT-12`/`QT-13`** ｜ `FD-13` 已锁定时长 ｜ `FD-11` 拒绝原因 ｜ **`FD-15` 失效原因** ｜ `FD-17` 失效时间 ｜ `FP-20` 该项目的质押覆盖状态
**不展示**：`QT-07`/`QT-08`（**已作废**，`X-MC-26`）。

### 7.2 可见字段矩阵（承接附册 `A6.2`，**按上游当前入库版本更新**）

> 控制台只展示**归属于我**的记录（服务端归属过滤）。「入口」列＝该状态下是否展示通往广场的深链，**可用性一律由 `available_actions` 决定**（`AC-MC-32`），**承载一律是右侧抽屉 760px**（`D-MC-115`）。
> **相对附册 A6.2 的更新**：① 删去 `S-FD-5`/`S-RP-4` 两行（本期不产生）；② 锚点改为 `?action=`、语义为"落详情页 + 打开抽屉"；③ 增补 `S-FD-11`、`S-CR-*`、对外状态与 `FP-28`；④ **V2.0：环节名改为「报价确认」/「放款确认」，`S-FD-4` 改为 `待放款确认`，`S-FD-10` 按 `Q-LN-02` 已定口径落行**。

| 状态 | tab | 资产方可见字段（增量） | 资金方可见字段（增量） | 入口 |
| --- | --- | --- | --- | --- |
| `S-FP-1 草稿` | 融资项目 | 项目名、代币类型、质押清单、**质押代币价值**、质押覆盖状态、空池标记 | — | ✅ 发布 / 追加质押 / 解除质押 |
| `S-FP-2 募集中` | 融资项目 | + `FP-28`、需求金额/币种、发布时间、有效期至、已收到报价数、**对外状态 `待报价`** | — | ✅ 追加质押 / 解除质押 |
| `S-FD-1 待接受` | 融资项目 / 我的报价 | + `FD-01`、融资币种/金额/利率、汇率快照、**`QT-15`/`QT-16`**、机构名称、报价时间、**剩余有效期倒计时**、**对外状态 `已报价待确认`** | 我的报价：同左 + 已锁定时长 | ✅ **去接受/拒绝报价**（资产方） |
| `S-FD-2 已拒绝` | 融资项目 / 我的报价 | + 拒绝原因、拒绝时间；需求**退回 `待报价`** | + 拒绝原因 | ❌ |
| **`S-FD-11 报价已失效`** | 融资项目 / 我的报价 | + **失效原因 `FD-15`**、失效时间；需求**退回 `待报价`** | 同左（中性表述，**不产生负面记录**） | ❌ |
| `S-FD-3 待放款` | 融资放款信息 | + 我方收款账户（脱敏）、盖章件有/无+版本数、`FD-31`/`FD-32` 标记与原因、**对外状态 `放款中`** | 同左 | ✅ 去放款（资金方）／去重传盖章件（资产方，`FD-31` 为真） |
| **`S-FD-4 待放款确认`** | 融资放款信息 | + 放款币种/金额/发放时间/提交时间/凭证或哈希（链 `ETH`）+ 浏览器链接、放款备注、**还款收款账户 `LN-15`/`LN-16`**、**`FD-37`/`FD-38` 时限与倒计时**、`FD-36` 超期标记 | 同左 | ✅ **去确认收到放款**（资产方） |
| `S-FD-6 还款中` | 还款信息 | + 还款计划全量期次、起息日、累计已还本息、未偿本金、逾期并行标记、**对外状态 `已放款`** | 同左 | ✅ 去还款（资产方）／查看还款计划（双方） |
| `S-RP-1 待还款` | 还款信息 | + 应还日、计息区间、应还本金/利息/合计、**开窗时刻 `RP-15`**、逾期标记与天数、**`RP-12`/`RP-13`** | 同左 | ✅ 去还款（已开窗可点，未开窗 `⊘` + 开启日期） |
| `S-RP-2 待还款确认` | 还款信息 | + 还款币种/金额/还款时间/提交时间/凭证或哈希（链 `ETH`） | 同左 + **`RM-15`/`RM-16` 时限与倒计时**、`RM-17` 超期标记 | ✅ **去确认收到还款**（资金方） |
| `S-RP-3 已结清` | 还款信息 | + 结清时间、该期实际还款记录 | 同左 | ❌ |
| `S-FD-8 已结清` | 融资项目 / 还款信息 | + 结清时间、累计已还本息、全流程留痕；**可提取代币提醒** | 同左 + 授信占用已释放 | ✅ 解除质押（提取，资产方）／查看还款计划 |
| `S-FD-10 已终止` | 融资放款信息 | + 终止原因 `FD-33`、终止时间 `FD-34`。**对外状态：放款前终止 → 退回 `待报价`**（`Q-LN-02` 已关闭），以服务端下发值为准 | 同左 | ❌ |
| `S-FP-5 已关闭` / `S-FP-6 已结清` | 融资项目 | + 关闭原因 `FP-23`、**可提取代币张数与金额** | — | ✅ 解除质押（提取） |
| `S-CR-1 生效中` | 授信明细 / 汇总 | 授信额度、有效期至、`CR-08`～`CR-11` | 同左 + `CR-12` 备注（**仅本方**） | ❌（授信写入口只在报价流程内） |
| `S-CR-2 已到期` | 授信明细 / 汇总 | 同上 + "已到期 · 存量业务照常履约中" | 同左 | ❌ |
| **逾期并行标记** | 还款信息 | + 逾期天数（取 `RP-11`，**不自行推算**）、最早逾期期次、最长逾期天数。**本期无罚息字段** | 同左 | ✅ 去还款（资产方） |

| 编号 | 条款 |
| --- | --- |
| `D-FIN-31`（承接） | 本表字段是**本方完整**口径；广场对游客/他人的可见度是另一回事，以各册公开/不公开标注为准 |
| `D-FIN-32`（承接） | **两张表读同一批状态值**；不得自造聚合态（`AC-MC-14`） |

### 7.3 枚举与常量（**全部引用，本模块不定义**）

> **V2.0：`[待裁定]` 标记已全部去除**——WS-327 V3.3 已按需求方 09-15「其他按照默认取值」把 8 条开放项按默认值定稿。

| 项 | 取值 | 定义方 |
| --- | --- | --- |
| 项目状态 | `S-FP-1 草稿（已建池·未发布）`～`S-FP-6 已结清` | WS-324 |
| **融资需求对外状态** | `待报价` / `已报价待确认` / `放款中` / `已放款` / `已失效／已关闭`（**服务端派生下发**）。**英文整组**：`Awaiting quotes` / **`Awaiting quote confirmation`** / `Disbursing` / `Disbursed` / `Void / closed`。⚠️ 内部态 `待接受 Awaiting response` **不得当对外词用**（V3.0 同步） | WS-324 `D-LS-18` / 分册 7.6 |
| **质押覆盖状态**（`质押覆盖状态 Pledge coverage status`） | `覆盖有余 Ample` / `覆盖持平 At capacity` / `覆盖不足 Insufficient`；**缺口数值 `覆盖缺口 Coverage gap`**；提醒写作「覆盖不足提醒」，其消息英文为 `Coverage shortfall alert`——**两者不再混用**（V3.0 同步，取 WS-324 分册 7.6 已定词） | WS-324 `D-LS-17` / `D-LS-21` |
| 本轮需求终结原因 | `资产方撤下` / `成交` / `项目关闭` / `自动失效 · 覆盖不足` | WS-324 `FP-27` |
| 质押状态 / 链上转移状态 | `PS-1`/`PS-2`/`PS-4`；`CT-0`～`CT-3` | WS-324 |
| 代币状态 / 融资质押状态 | `TS-1 有效`/`TS-2 失效`；`未质押`/`已质押` | WS-318 `D-TI-22`/`D-TI-24` |
| 授信额度状态 | `S-CR-1 生效中` / `S-CR-2 已到期`（**无"冻结""停用"**） | WS-325 |
| 融资业务状态 | `S-FD-1 待接受`/`S-FD-2 已拒绝`/`S-FD-11 报价已失效`/`S-FD-3 待放款`/**`S-FD-4 待放款确认`**/`S-FD-6 还款中`/`S-FD-8 已结清`/`S-FD-10 已终止`；`S-FD-9 逾期` 是**并行标记不是状态**；**`S-FD-5` 本期不产生** | WS-325 / WS-326 |
| 期次状态 | `S-RP-1`/`S-RP-2`/`S-RP-3`；**`S-RP-4` 本期不产生** | WS-327 |
| **还款类型**（`QT-15`） | **单值** `按季付息；到期还本付息`，**只读、不做下拉**；枚举位由上游保留，**本期不为其他取值预留分支** | WS-325 `D-CR-51` |
| **最终还款日**（`QT-16`/`FD-46`） | **派生值**，恒等于项目截止日 `FP-09`，**跟随不快照** | WS-325 `D-CR-52`/`D-CR-53` |
| **还款性质**（`RP-13`） | **两值** `正常还款`/`逾期还款`，系统派生。⚠️ **不得称「还款类型」** | WS-327 `D-RP-50` |
| **链** | **本期只支持 `ETH`，是常量不是选项**：只展示、**不给任何选择控件**；USDT/USDC 即 **ERC-20**；区块浏览器取 **ETH 固定前缀** | WS-326 主册 5.6 `D-LN-60`～`D-LN-63` |
| **还款收款账户** | 法币 `LN-15`（户名/账号/SWIFT/开户行/**中转行**，整组必填，比资产方侧多中转行、**两侧有意不同**）；数币 `LN-16`（资金方绑定钱包，只读）。**放款后本期不可改** | WS-326 `D-LN-64`～`D-LN-66`、`X-LS-39` |
| 报价有效期 / **放款确认时限** / **还款确认时限** | **各为常量 168 小时**，三者**必须分别命名**（`D-MC-121`） | WS-325 / WS-326 / WS-327 |
| **计息口径** | **单利、ACT/360、算头不算尾**；**首期不并、末期并入**；计划表常驻计息规则全文（`RP-18`） | WS-327 `D-RP-17`/`D-RP-18`/`D-RP-14` |
| **还款入口开窗提前量** | **常量 3 个自然日**，开启后保持开启直到该期结清 | WS-327 `D-RP-26` |
| **宽限期 / 罚息** | **不设宽限期**（应还日次日 00:00 判逾期）；**只记逾期天数、不算罚息、不并入应还金额、不触发质押处置** | WS-327 `D-RP-34`/`D-RP-46`～`D-RP-48` |
| 期间长度 | **常量 3 个自然月**，不可配置 | WS-327 `D-RP-15` |
| 质押率 | **常量 80%**，可展示、不得暗示可调 | WS-324 |
| 记账本位币 | `USD` | WS-324 `D-FIN-13` |
| 会话有效期 | **4 小时绝对有效期，无「记住我」** | WS-311 / WS-314 |
| **本期不存在的取值** | 异议相关的一切状态与入口、`S-FD-5`、`S-RP-4`、提前还款、部分还款、罚息、待办的已读/归档、授信冻结/解冻/调低、报价撤回/修改/展期、**`QT-07`/`QT-08`**、**多链**、控制台侧的任何写操作 | — |

---

## 8. 非功能需求

| 项 | 要求 |
| --- | --- |
| **性能** | 控制台首屏（统计区 + 待办带）≤ **2 秒**；单个 tab 首屏（20 条/页）≤ **2 秒**；tab 切换命中已取数据 ≤ **300 毫秒**。**五块数据并行取、互不阻塞** |
| **安全与鉴权** | ① **归属过滤在服务端**，前端不做归属判断；② **跨主体隔离**（机构看不到该资产方在其他机构的授信；资产方看不到机构对其他资产方的授信）；③ **动作鉴权在广场服务端**，按钮可点与否不构成校验；④ 游客与受限会话请求控制台接口一律返回 **401/403 且响应不含业务数据**；⑤ 越权一律落**不存在**语义；⑥ **`D-LS-09`：广场全量公开不取消上述任何一条** |
| **会话与共享设备** | 4 小时绝对有效期；失效**立即清 DOM**（`D-MC-11`）；**不新建轮询、不做保活心跳**（`X-MC-04`）；未读数轮询归 WS-329 且不得续期会话（`D-MC-164`） |
| **一致性** | 控制台与广场读到的同一笔业务完全一致（`AC-MC-12`）；**控制台不参与任何计算**（`X-MC-21`）；同一屏内多处引用同一个量时**取自同一次取数**（`AC-MC-21`） |
| **可用性** | 分块降级（`D-MC-146`）；失败不伪装成空态（`E-MC-01`）；每一条超期待办给出**可复制的客服邮箱与应注明的编号**；空态一律给去处（`D-MC-145`） |
| **兼容性** | 桌面端主流浏览器最近两个大版本；窄屏下统计区与待办带可折行、tab 内容区横向滚动，**状态、金额、时限三类列不得被隐藏** |
| **可访问性** | 状态**不得只用颜色表达**（覆盖不足、逾期、超期三类必须同时有文字标签）；待办带与 tab 可键盘导航；倒计时用 `aria-live` 礼貌级更新，**不抢焦点** |
| **国际化与展示基线** | 默认英文（`D-LS-15`）；语言、时区、日期、数字、金额、链上地址与哈希的展示与脱敏整段引用 WS-308 第 4/5/6/8 节；三个 168 小时与两个日期类时间量按平台统一时区判定并标注 |
| **留痕** | **控制台不产生业务留痕**（它不写业务）。仅按平台既有口径记录访问类日志；**访问日志不得写入任何业务字段值** |

---

## 9. 指标

| 编号 | 指标 | 口径 |
| --- | --- | --- |
| `M-MC-01` | **控制台到广场的转化率** | 从控制台深链进入广场的会话数 ÷ 控制台会话数 |
| `M-MC-02` | **待办点击率（分条）** | 每类待办的点击数 ÷ 展示数。长期为 0 说明文案没说清该做什么，或它本来就不该是待办（`B-2` 的反向检验） |
| `M-MC-03` | **待办平均存活时长（分条）** | 从命中判据到不再命中的时长中位数 |
| `M-MC-04` | **超期待办占比** | （`T-MC-04` + `T-MC-09`）÷ 全部确认类待办。**两个 168 小时时限是否合理的主要依据** |
| `M-MC-05` | **深链落地成功率** | 落到目标抽屉的次数 ÷ 深链点击数 |
| `M-MC-06` | **深链"状态已变"兜底率** | 命中 `H-02` 边界②的次数 ÷ 深链点击数。偏高说明数据新鲜度不够，是调整刷新触发点的依据 |
| `M-MC-07` | **空态 CTA 转化率** | 分 tab 统计 |
| `M-MC-08` | **覆盖不足提醒 → 追加质押的转化** | 衡量提醒文案有没有把代价讲明白（`D-MC-136`） |
| `M-MC-09` | **同企业跨员工的待办承接率** | 由 A 员工触发、B 员工处理的待办占比。**`B-5` 那条设计的价值就在这个数上** |
| `M-MC-10` | **一致性异常事件数** | `E-MC-06` 上报次数，**应恒为 0** |
| `M-MC-11` | **未知动作取值事件数** | `E-MC-04` 上报次数。非 0 说明上游补了动作但控制台未同步（`AC-LS-07` 的落地观测口） |
| `M-MC-12` | 各 tab 的访问分布与停留时长 | 判断默认落地 tab（`D-MC-125`）选得对不对 |

---

## 10. 依赖与风险

### 10.1 依赖

| # | 依赖 | 状态 | 说明 |
| --- | --- | --- | --- |
| `DEP-60` | **WS-324 借贷广场第一段 V9.2**（`FP-*`/`PL-*`/`S-FP-*`、质押覆盖状态、对外状态五值与 6.9.1 映射、`FP-28`、`H-01`～`H-05`、四环节承载 6.9） | ✅ 已入库（`75aadef`） | 本模块最大的上游，**全部引用不改写** |
| `DEP-61` | **WS-325 V7.2**（`CR-*`/`QT-*`/`FD-01`～`FD-21`/`S-CR-*`、**`QT-15`/`QT-16`**、`AC-LS-106`、`D-LS-33`、`quote`/`respond_quote`） | ✅ 已入库 | 授信 tab 与我的报价 tab 的唯一来源。⚠️ `QT-07`/`QT-08` **已作废** |
| `DEP-62` | **WS-326 V3.2**（`LN-01`～`LN-16`/`FD-30`～`FD-39`、`FD-37` 放款确认时限、**链口径 5.6**、**还款收款账户 6.3.4**、三个动作值） | ✅ 已入库 | 融资放款信息 tab；目录已改名 `v1.0-借贷广场-放款与放款确认/` |
| `DEP-63` | **WS-327 V3.3**（`RP-*`/`RM-*`/`FD-40`～`FD-47`、`RM-15`/`RP-15`、`AC-LS-131`～`136`、`AC-RP-23`、**8 条开放项已定稿**、三个动作值） | ✅ 已入库 | 还款信息 tab |
| `DEP-64` | **WS-329 V2.0**（`B-1`～`B-5`、`8.3.4` 轮询归属、铃铛与消息中心壳层组件） | ✅ 已入库 | 6.9 全节引用，**不各写一版**。⚠️ 其对齐基准仍是 WS-324 V9.0（主册 12.3 缺口 5） |
| `DEP-65` | **WS-318**（`D-TI-11`/`D-TI-18`/`D-TI-21`/`D-TI-22`/`D-TI-24`） | ✅ 已定稿 | 代币列表 tab 与统计区①② |
| `DEP-66` | **WS-311 主干三册**（三态与对外契约四字段、权限矩阵、回跳句柄、跨域白名单与 `D-F02` 离站告知） | ✅ 已定稿 | 登录态、深链回跳、跨域待办 |
| `DEP-67` | **WS-311 `UC-01`：输出契约增加 `entity_id`**（P-2） | ⏳ **待执行** | 归属过滤的契约基础。**不阻塞撰写，上线验收前必须完成** |
| `DEP-68` | **WS-316 资金方机构认证审核** | ✅ 已定稿 | `T-MC-14` 的落地页 |
| `DEP-69` | **WS-308『01-国际化基线.md』** | ✅ 已定稿 | 展示与脱敏口径 |
| `DEP-70` | **WS-295 面客认证后 shell** | ✅ 已定稿 | `P-MC-01` 继承它 |
| `DEP-71` | **设计师中英术语对照产出 + WS-324 分册 7.6 对照表** | ⏳ 并行 | 本模块新增术语的英文一律取自它，**不自造**（`D-MC-152`） |
| `DEP-72` | **资产平台侧落地页**（`T-MC-12`/`T-MC-13`） | ⚠️ 受 WS-321 未闭合影响 | 不可达时按 `E-MC-05` 处理，不阻塞其余部分 |
| `DEP-73` | **WS-318 代币符号配置** | ⏳ `[待确认]` | 代币列表"币种"列取值来源，**不自造** |
| `DEP-74`（**新 · V2.0**） | **WS-315 资金方钱包地址绑定**（`D-F36`～`D-F44`，`D-F43` 绑定后不可改） | ✅ 已定稿 | `LN-16` 还款收款地址的唯一来源；控制台**只读展示** |

### 10.2 风险

| 编号 | 风险 | 处理 |
| --- | --- | --- |
| `R-MC-30` | **"待办带有事、铃铛却是 0"会被当 bug 反复上报** | `D-MC-162` 三条落地。**这是设计上正确的，但必须是写明了的正确** |
| `R-MC-31` | **控制台只读引用 ⇒ 任一上游不下发服务端派生量时，控制台"算不出来也不许算"**（`X-MC-21`） | 10.3 逐条列出必须下发的量；`E-MC-02` 定义占位形态。**这是同源红线的必然代价**——前端算一遍就是第二套口径 |
| `R-MC-32` | **待办不落库 ⇒ 没有历史、无法回看"我上周处理了什么"** | `B-3` 的既定取舍。回看历史走消息中心。**不得为此建待办历史表** |
| `R-MC-33` | **`X-2` 不适用于控制台若未被 WS-311 采纳，本模块会被按"违反跨功能约束"验收** | `D-MC-68` 三条理由写死在主册 6.5.3，并在 10.3 向 WS-311 登记 |
| `R-MC-34` | **`UC-01` 未落地期间资金方 L0 的控制台是一张空页** | `D-MC-116` 走空态 + CTA，**不报错** |
| `R-MC-35`（**V2.0 重估**） | **三个「确认」只差一个字，控制台的待办带是它们唯一可能并排出现的地方** | 改名已落地（`D-MC-122`），**但同屏并排的风险不因改名消失**：`AC-MC-36` 要求写全称、禁用无限定词的单字「确认」；`D-MC-152` 要求**英文下三者不共用同一个词** |
| `R-MC-36` | **覆盖不足提醒是事后告知，用户看到它时那笔需求可能已经失效** | WS-324 `R-LS-12`/`R-LS-25` 的既定代价。`D-MC-135`/`D-MC-136` 要求两件事分开呈现并写明出路；**不得做倒计时或"还来得及追加"的暗示** |
| ~~`R-MC-37`~~ | ~~WS-324 的推送位没有 `biz_type` ⇒ 这几类消息发不出去~~ | **V3.0 解除**：`接入指南.md` 3.4 已收录 WS-324 六条 `biz_type`（`9a457a0`），`Q-F-MC-02` 随之关闭。**页面标记不依赖推送这条设计保留**（`D-MC-134`/`D-MC-136`）——它本来就是保底，不因推送恢复而取消 |
| `R-MC-38` | **控制台数据不做实时刷新**（`X-MC-04`），同企业两人同时操作时看到的是上一次取数的结果 | `E-MC-09` + `H-02` 边界②兜底；`M-MC-06` 观测，偏高时调整刷新触发点而不是引入轮询 |
| `R-MC-39`（**新 · V2.0**） | **上游承载形态在 WS-325 与 WS-326/327 之间表述不一致**：WS-325 V7.2 正文仍写"弹窗"（其文档状态明写"承载形态维持弹窗不变"），而其原型 v1.3 已改抽屉、WS-326/327 已按"做事→抽屉 760px"重述并称与 WS-325 同一个数 | **本 PRD 按上游的判据落**（做事类→抽屉），因为判据是 WS-326/327 明文、且 WS-325 原型已按此实现；**已登记为 10.3 第 3 条修订请求**。⚠️ 若需求方裁定 WS-325 那两个承载单元保持弹窗，本册 6.8.2 的第 6、7 行需同步改，**其余不受影响** |
| `R-MC-22`（承接） | 同企业任一员工都能动用公司资产并触发待办 | 需求方已裁定本期不分级并接受 |
| `R-MC-24`（承接） | "广场信息全公开"被误读为"控制台不用做服务端过滤" | `D-LS-09` + 第 8 章鉴权六条 + `AC-MC-03` |
| `R-MC-27`（承接） | 质押率硬编码，调整必须发版 | 控制台只读展示，不得暗示可调 |

### 10.3 对上下游的请求（**本模块不自行修改任何他册**）

| # | 文档 | 请求 | 原因 |
| --- | --- | --- | --- |
| 1 | **WS-311 权限矩阵** | **登记两条偏离，不改矩阵**：① 非登录态下广场操作区只留「立即登录」，与逐动作 `⊘` 语义存在偏离（照 WS-324 `10.5` 第 9 条处置）；② **`X-2` 不适用于我的控制台**，建议在其适用条件中显式排除"本人查看本人数据且无字段级脱敏"的展示功能 | ②不写明会让控制台的精确值排序被误判为违规 |
| 2 | **WS-311 `UC-01`**（P-2） | **输出契约增加 `entity_id`** | 归属过滤的契约基础；**上线验收前必须完成** |
| 3（**新 · V2.0**） | **WS-325 / 总控** | **承载形态表述请统一**：WS-325 V7.2 正文与文档状态仍写"承载维持弹窗"，而其原型 v1.3（`86b9f64`）已改右侧抽屉、WS-326 `D-LN-48` 与 WS-327 `D-RP-71` 均按"做事→抽屉 760px"重述并称与 WS-325 同一个数。**本 PRD 按判据落到抽屉**（`R-MC-39`），请确认后统一 | 三份文档对同一组承载单元给出两种承载，控制台的深链说明只能按其中一种写 |
| 4（**新 · V2.0**） | **WS-324** | **文档状态字段与标题不一致**：主册标题写 `V9.2`、文档状态字段仍写 `V9.1`。**本 PRD 以标题为准**（`V9.2`），请顺手修正，**本模块不代改** | 版本指针被下游逐份引用，两处不一致会让后续模块各取一个 |
| 5 | **WS-324** | ① 请为其分册 8.1 的 **5 个推送位命名 `biz_type`**（WS-329 `Q-F-MC-02` / `PMC-C-05` 已登记，本模块不代取名）；② 建议在分册 6.9.1 的 `已放款` 与 `已失效／已关闭` 两行补一句说明（项目进终态时两行会同时命中，WS-327 已登记同一请求） | ①这 5 类消息发不出去时控制台的页面标记是唯一保底；②控制台按服务端下发值展示，但映射表本身仍会被实现方读到 |
| 6 | **WS-325 / WS-326 / WS-327** | 须下发（控制台**只读引用、不自算**）：**WS-325** —— `CR-08`～`CR-11`、额度与状态与 `CR-07`、**`QT-12` 到期时刻**、`FD-13`、`FD-15`、**`QT-15`/`QT-16`**；**WS-326** —— 放款记录全量字段（含 `LN-15`/`LN-16`）、`FD-31`/`FD-32`/`FD-36` 三个标记、**`FD-37` 到期时刻与 `FD-38`**；**WS-327** —— 还款计划全量期次与还款记录、**逾期标记与天数**、**`RM-15` 到期时刻**、**`RP-15` 开窗时刻**、`RP-12`/`RP-13`/`RP-14` | 控制台侧已按三者 `10.3` 对下游的要求逐条实现：只读不自算、待办区分（放款两类 / 还款四类，超期两类带客服邮箱指引）、时限与开窗取服务端值、**不为 `S-FD-5`/`S-RP-4` 准备 tab/筛选项/文案** |
| 7（**V3.0 重写**） | **WS-329** | ⚠️ **`B-4` / `B-5` 的前提已被 09-16 裁定改变，请按本模块的裁定同步改 `3.3`**（`B-1`～`B-3` 不受影响）：<br>· **`B-4`** 建议改为「**未读数是面客侧唯一的计数角标；业务待办不得并入未读数，接入方也不得另造第二个计数**」，其 **`AC-F933`** 随之从"两个计数分开"改为"**全站只有一个计数角标，且它只计未读消息**"；<br>· **`B-5`** 建议改为「**未读数按 `account_id` 计；接入方页面上的业务数据按 `entity_id` 归属。两者归属维度不同是设计不是缺陷，接入方必须在页面上写明**」，其 **`AC-F937`** 随之改为"**构造同企业跨员工用例，断言业务数据可见而未读数为 0，且页面上存在解释这一点的常驻说明**"。<br>理由与控制台侧落点见分册 01 · 6.9（`D-MC-157`/`D-MC-162`）。<br>其余照旧：① 本模块直接引用 `3.3` 与 `8.3.4`，`PMC-C-02` 已落位（`D-MC-164`）；② 消息 `page_target` 指向控制台时按 `D-MC-147` 构造；③ **其册内两处不一致**（17.4 表内 `Q-F-MC-03` 记"待拍板"、17.7.5 记"已关闭"）。**本模块只给裁定与建议改法，不改 WS-329** | 同一件事不得在两个 issue 里各写一版（`D-F94`）；而 `B-4`/`B-5` 是 WS-328 引用的锚点，**单方面改会把引用方一起带偏** |
| 8 | **接入指南 4.2 段位表** | 申请登记页面与组件段位 **`P-MC` / `C-MC`** 与模块字母 `MC`（占用 `P-MC-01`、`C-MC-01`～`C-MC-05`） | 本模块不自建段位表 |
| 9 | **设计师（原型）** | 请把 `D-MC-152` 列出的控制台新增术语补进中英对照表，**尤其「报价确认 / 放款确认 / 还款确认」英文下不得共用同一个词**、「还款类型 / 还款性质」英文须可区分；**本 PRD 不自造英文** | `D-LS-15` 默认英文；术语一处定义 |
| 11（**新 · V3.0**） | **WS-324 分册 7.6 对照表** | **「待办 To-dos」与待办三组名（去广场办 / 等待中 / 去其他模块）在控制台已无落点**——它们是自 WS-328 原型回填的词，而待办带已于 09-16 撤除。**建议在表中标注"已无落点"而不是删行**（沿用"不回收作废号"的同一精神，将来若恢复可原样取用）。**本模块不改 WS-324** | 对照表已被声明为唯一事实源（`D-LS-21`），留着无落点的词会让新接入方以为控制台还有这块 |
| 10 | **WS-323 附册 `A6.2`** | 建议按本册 7.2 更新：删去 `S-FD-5`/`S-RP-4` 两行、锚点改为 `?action=` 且承载为抽屉、增补 `S-FD-11`/`S-CR-*`/对外状态/`FP-28`、**环节名改为新名、`S-FD-4` 改为 `待放款确认`** | 附册是 A6.2 的定义方，本册只提请求、不自行改附册 |

---

## 11. 验收标准汇总

### 11.1 红线与反向验收（**最先跑这七条**）

| 编号 | 验收标准 |
| --- | --- |
| **`AC-MC-11`** | **控制台零业务写操作，无任何例外。** 全站页面遍历 + 接口清单核对：不存在任何业务提交入口（表单、提交按钮、文件选择器、确认弹窗），也不调用任何业务写接口 |
| **`AC-MC-33`（反向）** | **界面上不存在**：质押 / 解除质押 / 提取的执行控件、发布或改额输入框、授信核定 / 追加 / 调低 / 冻结 / 解冻入口、报价 / 撤回 / 修改入口、接受 / 拒绝的提交控件、放款录入表单、两个确认动作的提交控件、还款录入表单、**"提出异议 / 申诉 / 驳回"任何入口**、待办的"标记已办 / 忽略 / 归档 / 删除 / 催办"控件、有效期或质押率的可编辑控件、"全部结清"按钮、**`LN-15`/`LN-16` 的任何修改入口** |
| **`AC-MC-35`（反向）** | **全文检索**（界面文案、字段名、枚举值、提示语、空态文案）：**不存在旧词根**——只允许 `质押覆盖状态`／`覆盖有余`／`覆盖持平`／`覆盖不足`／`覆盖缺口`／「覆盖不足提醒」；**不存在无限定词的"融资余额"**；不存在"冻结代币""待确认融资金额""有效代币资产"等已作废或已映射的说法 |
| **`AC-MC-36`（反向 · V2.0 重写）** | **环节名全链路清零**：全文检索界面文案、待办文案、按钮名、tab 名、消息标题——**只出现 09-15 裁定的新名「报价确认」「放款确认」「还款确认」**，**已作废的旧展示名零命中**；状态名只出现 `S-FD-4 待放款确认`。**同时不存在无限定词的单字「确认」**，时限一律写全称（「报价有效期」「放款确认时限」「还款确认时限」），**不存在无限定词的"7 天""有效期""倒计时"**。⚠️ 本条**刻意不复写旧名**，否则 grep 自查永远归不了零 |
| **`AC-MC-77`（新 · V2.0，反向）** | **界面不呈现平台内部规则**（`D-MC-167`）：① **不出现任何判据公式**；② **不出现本模块或上游的任何条款/验收编号**——自查方法是按各编号段位的前缀模式在界面文案里检索，命中即不通过（**本条刻意不复写编号样例**）；③ 不出现运行机制的解释性文字。<br>**同时必须留下**：覆盖缺口与需追加资产价值两个数、三个倒计时与锁定信息、额度类与时限类的差额与出路、客服邮箱指引——**构造"连数字一起删"的用例同样判不通过** |
| **`AC-MC-78`（新 · V2.0，反向）** | **链是常量、界面无选择控件**：代币列表、融资放款信息、还款信息三处全文检索——**不存在链的下拉 / 单选 / "默认 ETH 可改"字样**，出现的一律是只读展示的 `ETH`；USDT/USDC 标注为 **ERC-20**；区块浏览器链接取 **ETH 固定前缀**且常驻"平台未核验该交易"。**"链不一致"相关文案与分支一律不得出现**（本期不可达） |
| **`AC-MC-31`** | **会话失效即清 DOM**：构造"停留至会话届满"用例，断言统计数字、各 tab 内容、待办带、展开区**均不在 DOM 中**，菜单节点不渲染，**不存在"已回落游客态但菜单还在"的中间态** |

### 11.2 可见性与鉴权

| 编号 | 验收标准 |
| --- | --- |
| `AC-MC-01`～`AC-MC-06`（承接） | 游客态菜单节点**不渲染**；游客请求控制台接口返回 401/403 且**响应不含业务数据**；会话失效后 DOM 不残留业务数据 |
| `AC-MC-39` | **归属过滤在服务端**：用他人的项目编号直调控制台接口一律拒绝且**不暴露对象是否存在**；前端不做归属判断 |
| `AC-MC-40` | **跨主体隔离**：机构侧断言看不到该资产方在其他机构的授信额度与占用；资产方侧断言看不到 `CR-12` |
| `AC-MC-41` | **受限会话与游客同处置**：菜单不渲染、深链不放行、走首登协议同意 |
| `AC-MC-42` | **游客深链直达控制台**：走回跳句柄 → 类型选择页 → 登录后**原路返回原来那一屏**（含 tab 与筛选）；**不给 404、不白屏、不静默跳首页**；URL 中不出现 `redirect`/`return_to`/`next`/`target` |

### 11.3 统计区

| 编号 | 验收标准 |
| --- | --- |
| `AC-MC-21` | 每组指标的数量与金额**同源于一次取数**；②的失效副行与②本体同源 |
| `AC-MC-43` | **①②呈现为包含关系**：②在①的从属版式内（或"其中"表达），**不存在两个同级并排卡片** |
| `AC-MC-44` | **②不等于有效质押价值**：构造"池内有失效代币"用例，断言②为账面口径（含失效）且②下存在"其中 N 张已失效、合计 X USD **不计入覆盖**"副行；并断言 ②金额 − 失效金额 = Σ有效质押价值 |
| `AC-MC-45` | **四个中间量不上页面**：全站检索断言不展示 `融资上限`/`可融金额`/`项目在途金额`/`可撤回上限`，且不存在额度尺 / 进度条式额度可视化 / 「额度明细与判定口径」折叠区 |
| `AC-MC-46` | **剩余可用授信旁常驻**"实际可融金额以发布需求时服务端当场重算的结果为准"，页面上**不给出任何"你还能融 X"的数字** |
| `AC-MC-47` | **`entity_id` 为 `null` 时**：走整页空态、**不报错、不白屏**，统计区**不渲染一排 0** |

### 11.4 五个 tab

| 编号 | 验收标准 |
| --- | --- |
| `AC-MC-48` | **代币两根轴分列且不合并**：断言"代币状态"与"融资质押状态"是两列、可同时取值；失效行标注"不计入覆盖"；**不存在合同号与发票号两列** |
| `AC-MC-49` | **可提取代币提醒**：构造"项目结清后有 3 张未提取"，断言顶部出现"您有 3 张（合计 X USD）可提取，需自付 gas，可批量一次提完"，**不存在任何暗示代币会自动回到钱包的表述、不存在倒计时** |
| `AC-MC-76` | **覆盖不足提醒**：构造 `融资上限 < 项目融资余额` 用例，断言项目行出现提醒且**同时给出覆盖缺口与需追加资产价值两个数**、文案为中性事实、带「去追加质押」与失效代币清单入口；**断言不存在任何倒计时、进度条或"还来得及追加"的表述**；断言资金方侧同样可见 |
| `AC-MC-50` | **档位与失效不混讲**：构造"需求已失效、但项目档位仍是 `覆盖有余`"用例，断言项目行显示 `覆盖有余`、逐轮需求行显示 `已失效／已关闭` + `自动失效 · 覆盖不足`，且**页面上不存在"覆盖不足导致需求失效"这类因果表述** |
| `AC-MC-51` | **对外状态只取服务端派生值**：断言前端不存在由 `S-FP-*`/`S-FD-*` 拼装对外状态的逻辑；同一笔需求在广场列表、广场详情、控制台三处一致；控制台**不把对外五值做成筛选标签** |
| `AC-MC-52` | **`已失效／已关闭` 旁必须能看到终结原因**（`FP-27`），不得只给合并标签 |
| `AC-MC-53` | **授信不自算**：断言可用授信直接取 `CR-11`，不存在 `CR-05 − CR-08` 这类本地计算；`CR-10` 呈现为"合计"而非第四个并列量 |
| `AC-MC-54` | **`S-CR-2 已到期` 呈现为中性**（"已到期 · 存量业务照常履约中"），**不用告警色、不标"异常"**；其名下在途报价与未偿本金照常展示 |
| `AC-MC-55` | **利息与逾期天数不重算**：断言显示值与服务端 `RP-07`/`RP-11` **逐位相同**，前端不存在计算逻辑 |
| `AC-MC-56` | **开窗由服务端判定**：未到 `RP-15` 的期次入口可见但不可点并给出开启日期；**逾期期次入口一直可点** |
| `AC-MC-57` | **不为本期不产生的状态准备任何东西**：全站检索断言不存在 `S-FD-5`/`S-RP-4` 的 tab、筛选项、状态文案，也不存在"提前还款""部分还款""罚息"的字段与文案 |
| `AC-MC-58` | 区块浏览器链接旁常驻"平台未核验该交易，链接仅供自行查验"；**不存在**"已核验 / 已确认上链 / 交易有效 / 平台已审核"任何字样 |
| **`AC-MC-79`（新 · V2.0）** | **三个只读字段按定值呈现**：`QT-15` 恒为 `按季付息；到期还本付息`、`QT-16` 与 `FD-46` 恒等于项目截止日 `FP-09`，**界面上都没有可编辑控件、选择器或日期选择器**；三者出现在我的报价 tab、放款 tab 商务条款与还款 tab。**另断言 `FD-46` 跟随项目截止日而非快照** |
| **`AC-MC-80`（新 · V2.0）** | **两个账户字段与术语撞名**：① **还款收款账户**在放款 tab 只读展示——法币断言**五项齐全（含中转行）**、数币断言取绑定钱包地址，**两者均无修改入口**；② 还款 tab 的 `RP-14`/`RM-10` 断言取自 `LN-15`/`LN-16`，**全站不存在对 `QT-07`/`QT-08` 的任何引用**；③ **全站检索 `RP-13` 一律标为「还款性质」，不出现"还款类型"指代它的情形** |
| `AC-MC-22`（反向 · 代币列表） | 本 tab **不出现**：质押 / 解除质押 / 提取的**执行**控件（只有跳广场的深链）、数量输入框、代币级冻结或锁定入口 |
| `AC-MC-23`（反向 · 融资项目） | 本 tab **不出现**：有效期编辑入口、质押率编辑入口、需求金额的直接编辑控件（改额在广场抽屉内）、"提出异议"入口 |
| `AC-MC-24`（反向 · 授信） | 本 tab **不出现**：核定 / 追加 / 调低 / 冻结 / 解冻 / 审批入口，也**不出现任何"去授信"按钮**（授信无独立待办来源、不占独立锚点，`D-LS-22`/`D-LS-23`） |
| `AC-MC-25`（反向 · 融资放款信息） | 本 tab **不出现**：放款录入表单、确认到账的提交控件、"提出异议"入口、重传盖章件的文件选择器、**`LN-15`/`LN-16` 的编辑控件**——五者都是广场抽屉内的动作，控制台只出深链按钮 |
| `AC-MC-26`（反向 · 还款信息） | 本 tab **不出现**：还款录入表单、确认收到还款的提交控件、"全部结清"按钮、跨期合并提交、金额编辑控件、罚息字段 |
| `AC-MC-27`（反向 · 我的报价） | 本 tab **不出现**：撤回报价、修改报价、展期 / 续期 / 延长报价有效期的任何入口（本期均不存在，`X-LS-20`/`X-LS-24`） |

### 11.5 倒计时与时限

| 编号 | 验收标准 |
| --- | --- |
| `AC-MC-59` | **倒计时取服务端到期时刻**：断言三类倒计时（`QT-12`/`FD-37`/`RM-15`）与服务端下发的绝对时刻一致；**把客户端系统时间调偏 2 小时后倒计时显示不变**；跨时区展示按 WS-308 标注 |
| `AC-MC-60` | **归零后不再倒数**：改为展示超期标记与**已超期 N 天**，N 取服务端超期时刻回算、不取任务执行时刻 |
| `AC-MC-61` | **超期表述可验证**：文案是「已超过放款确认时限」/「已超过还款确认时限」，**不含"已失效""已逾期"**；超期后确认入口照常可用、状态与金额未变 |

### 11.6 行级提醒（**V3.0 整节改写，七条逐条落到行上，无一作废**）

| 编号 | 验收标准 |
| --- | --- |
| `AC-MC-30`（改写） | **14 类判据逐条可构造、逐条可命中**，且命中后**在正确的那一行**呈现（对照分册 01 · 6.7.2 的落点表逐行断言）；`T-MC-04`/`T-MC-09` 的行展开区**必须带可复制的客服邮箱**且注明对应编号（`FP-28` + `FD-01` / `RP-01`） |
| `AC-MC-28`（改写） | 在广场办掉后返回，**该行的状态列与时限列自动变化**（不再命中判据的行不再呈现提醒），**无需任何"标记已办"操作**，也不留"已办"记录 |
| **`AC-MC-29`（改写 · 反向，撤带后的完整清单）** | 全站检索断言**不存在**：① **待办带、折叠区、浮层、红点、第六个 tab、tab 上的数字角标、统计区里的"待处理 N 件"**（`X-MC-27`/`D-MC-123`）；② 标记已办 / 忽略 / 归档 / 删除 / 催办控件；③ "提出异议"入口与"异议处理中"两类判据；④ **把"待办"用作功能区名字的列标题、区块标题或筛选项**（`D-MC-174`） |
| `AC-MC-62`（改写） | **落点分布可验证**：`T-MC-01`～`T-MC-10` 共 **10 类**的按钮落在广场深链上、`T-MC-11` **1 类**落在控制台本页行内且无动作、`T-MC-12`～`T-MC-14` **3 类**落在其他模块（控制台不设入口）；**合计 14 类** |
| `AC-MC-63`（改写） | **`T-MC-11` 所在行无动作按钮**：我的报价 tab 的 `S-FD-1` 行状态列为「待接受」、时限列给剩余报价有效期，**行上没有任何可点动作**，也**没有"催办"按钮** |
| `AC-MC-64`（改写） | **跨域只剩一处且照常走离站告知**：代币列表空态 CTA 指向资产平台时展示 `D-F02` 组件；断言控制台**不自建跨域跳转解析**，且 **`T-MC-12`/`T-MC-13` 在控制台不再单独设入口**（`D-MC-172`） |
| **`AC-MC-65`（改写）** | **同类多笔各自成行、紧迫的排在前**：构造"3 笔待我确认收到放款、剩余时限各不相同"的用例，断言**三行各自呈现**（不聚合、不折叠），且**默认排序把剩余时限最短的排在最前**；再构造含超期行的用例，断言**超期行排在全部未超期行之前**（`D-MC-156`/`D-MC-173`） |
| **`AC-MC-82`（新 · V3.0）** | **每张表都有承担紧迫度的列**：断言五（四）个 tab 的列表**都有状态列**；融资项目（逐轮需求）、融资放款信息、还款信息、我的报价**四处还必须有时限列**，其取值由服务端下发的绝对到期时刻渲染（把客户端时间调偏 2 小时后显示不变）。**缺任一列即判不通过**——那等于把撤带做成了能力删除（`D-MC-173`） |

### 11.7 快捷按钮与深链

| 编号 | 验收标准 |
| --- | --- |
| `AC-MC-32` | **按钮由 `available_actions` 驱动**：断言前端不存在"按状态推断是否显示按钮"的逻辑；三态（不返回 / 可点 / `⊘`+原因）均可构造；**额度不足时"解除质押"以 `⊘` + 原因（含可撤回上限）出现而不是消失** |
| **`AC-MC-66`（V2.0 重写）** | **落详情页 + 开抽屉**：逐条构造分册 01 · 6.8.2 每个锚点，断言落地结果是"该融资项目详情页 + 对应**右侧抽屉 760px** 已打开"（还款类再断言定位第③段并选中正确期次），**关闭抽屉后停留在详情页**；**断言控制台没有任何深链直接打开提示类居中弹窗** |
| `AC-MC-67` | **不新开跳转约定**：控制台发起的全部跳转 URL 的 `action` 取值**都能在 WS-324～327 的登记表里找到**；未登记取值按 `E-MC-04` 忽略并上报 |
| `AC-MC-38` | **状态已变兜底**：构造"点击时业务已变"用例，断言落详情页 + Toast，**不报 404、不白屏、不静默跳首页**；返回后该条待办已消失 |
| `AC-MC-34` | **`H-05` 闭环**：落回控制台时 tab、筛选、排序、页码与出发时一致，目标行在可视区域内 |
| `AC-MC-68` | **草稿与越权**：他人的草稿项目深链一律返回"内容不存在或无权访问"，**不暴露存在性** |

### 11.8 视图状态、空态与降级

| 编号 | 验收标准 |
| --- | --- |
| `AC-MC-69` | **URL 视图状态**：切 tab / 筛选 / 排序 / 翻页后刷新，视图**完整恢复**；把 URL 发给同主体的另一个员工，打开后看到同一屏 |
| `AC-MC-70` | **过滤条带**：任何带过滤的落地都显示「已按 X 过滤 / 清除」条带，点「清除」回到无过滤视图 |
| `AC-MC-71` | **分块降级**：构造"还款模块超时"用例，断言还款信息 tab 就地展示可重试错误态，**统计区与其余 tab 照常可用、整页不 500**，**失败不被伪装成空态** |
| `AC-MC-72` | **空态给去处**：六种空态（主册 6.4）逐条构造，文案与 CTA 与表一致；**资产方授信为空时不给 CTA** |
| `AC-MC-37` | **控制台不轮询**：断言页面停留期间**不存在**由控制台发起的周期性业务数据请求；刷新只有三个触发点（进入页面、从广场返回、手动刷新） |

### 11.9 与消息中心的边界、语言

| 编号 | 验收标准 |
| --- | --- |
| `AC-MC-73` | **两个计数分开**：待办计数与铃铛未读数**分别展示、分别计数**，**不合并成一个红点**；待办计数不可清除 |
| `AC-MC-74` | **`B-5` 被写明**：构造"员工甲发起、员工乙登录"用例，断言乙看得到该企业的待办、铃铛未读数为 0，**且待办带上存在解释这一点的常驻说明**；断言待办**未按 `account_id` 过滤** |
| `AC-MC-75` | **控制台不碰消息**：断言控制台内**不存在**消息列表、未读角标、已读逻辑与"全部已读"控件 |
| **`AC-MC-81`（新 · V2.0）** | **中英一致性**：切到英文后，① `D-MC-152` 列出的新增术语全部有译文、**无中文残留**；② **「报价确认 / 放款确认 / 还款确认」三者英文互不相同**；③ **「还款类型」与「还款性质」英文可区分**；④ 三个时限的英文各自带限定词、**不共用一个泛称**；**⑤（V3.0）两处译法可验**：覆盖缺口的**数值**一律 `Coverage gap`（全站不出现 `Coverage shortfall` 指代数值，该词只允许出现在「覆盖不足提醒」这条消息的英文 `Coverage shortfall alert` 里）；对外状态「已报价待确认」一律 `Awaiting quote confirmation`，**全站不出现用 `Awaiting response` 当对外状态词的情形** |

### 11.10 承接

`AC-MC-10`（自洽性校验在服务端、控制台只展示不调平）、`AC-MC-12`～`AC-MC-14`（同源三条）、`AC-MC-16`（同企业员工不分级）、`AC-LS-97`／`AC-LS-106`／`AC-LS-126`／`AC-LS-127`／`AC-LS-130`／`AC-LS-131`～`AC-LS-136`／`AC-RP-08`／`AC-RP-23`（上游对控制台的数据回流与一致性要求）、`D-LS-09`、`D-MC-11`、`D-MC-68`、`D-FIN-31`／`D-FIN-32`。

---

*本分册与主文档、分册 01 同属一次交付，编号连续，不可单独作为实现依据。本模块不定义任何业务字段与枚举，全部引用上游；7.1 各表若与定义方 PRD 出现差异，一律以定义方为准。*
